# dependencies
node_modules/

# build output
dist/

# vite dep-optimizer cache
.vite/

# TypeScript build cache
*.tsbuildinfo

# environment files
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# private working files
_private/

# 로컬 실행이 만드는 첨부 저장소 (ATTACHMENT_DIR 기본값)
apps/api/data/

# 유료 화면 모듈이 놓이는 자리 (vite 별칭 `@pro` — open-core-boundary.md §5).
# **공개 저장소에 절대 들어가면 안 된다.** 유료판을 이 작업 사본에서
# 빌드했을 때 `git add -A` 한 번으로 통째로 공개되는 것을 막는다.
packages/pro-ui/

# npm pack 산출물 — 시험에서 유료 모듈·파서를 심을 때 만든다 (2026-09-21).
# 빌드 부산물이라 저장소에 들어가면 안 된다 (두 번 실수로 커밋에 딸려 왔다).
*.tgz
