# CLAUDE.md — 이 저장소에서 일하는 방식

> AI(Claude Code)가 이 저장소에서 작업할 때 **항상** 지키는 협업 규칙.
> 코드 규칙은 `docs/05-implementation/coding-conventions.md`,
> 테스트는 `docs/05-implementation/test-catalog.md` 를 따른다.

## 1. 언어

사용자와의 모든 대화·PR 제목·PR 본문·커밋 메시지는 **한국어**로 쓴다.

## 2. 작업 흐름 — 나는 PR 까지, 병합은 사용자가

1. 브랜치를 만들어 작업하고 커밋·푸시한다.
2. **PR 을 만든다** (사용자가 따로 요청하지 않아도 만든다).
3. 사용자가 병합한다. "N 병합완료" 를 받으면 `main` 을 당겨 받고
   로컬 브랜치를 정리한 뒤 다음 작업으로 넘어간다.

### 2.1 브랜치는 **쌓지 않는다** (2026-08-11)

새 작업은 **항상 최신 `origin/main` 에서** 시작한다. 병합을 기다리는
브랜치 위에 새 브랜치를 만들면, 앞 PR 이 **squash** 로 병합되는 순간
같은 내용이 커밋만 다르게 겹쳐 **충돌**한다 (#255 에서 실제로 겪었다).

앞 PR 과 같은 파일을 고쳐야 하면 병합될 때까지 기다리거나, 겹치지 않는
자리만 손댄다.

### 2.3 ★ 작업 시작 전 **기준이 최신인지 확인한다** (2026-08-15)

컨테이너가 재시작되면 작업 사본이 **옛 커밋으로 되돌아가 있을 수 있다.**
그 상태에서 브랜치를 만들면, 그 뒤 병합된 것들을 **통째로 되돌리는 PR** 이
된다. 브랜치를 만들기 전에 **항상** 확인한다.

```bash
git fetch origin
git log --oneline -1                 # 지금 내 기준
git log --oneline origin/main -1     # 진짜 최신
git rev-list --count origin/main ^HEAD   # 0 이 아니면 뒤처져 있다
```

뒤처져 있으면 `git checkout -B <브랜치> origin/main` 으로 다시 잡고
편집을 **처음부터 다시** 한다 (낡은 파일 위에서 고친 것은 버린다).

(2026-08-15: 재시작 뒤 18커밋 뒤처진 사본에서 커밋·푸시했다. 병합했다면
#256~#274 가 되돌아갈 뻔했다. 문서 파일 하나가 옛 내용이라는 것을 우연히
알아채 발견했다 — **눈으로 알아채는 데 기대면 안 된다.**)

### 2.2 ★ 리베이스로 커밋을 **버릴 때는 내용을 확인한다** (2026-08-11)

`git rebase --onto origin/main <드롭할커밋>` 처럼 "이미 병합됐으니
버려도 된다"고 판단할 때는, **그 커밋의 내용이 정말 `main` 에
있는지 문자열로 확인**한 뒤 버린다.

```bash
git log --oneline origin/main..HEAD          # 남길 것
grep -c "그 커밋이 넣은 고유 문장" <파일>     # main 에 실제로 있나 (0 이면 없다!)
```

느슨한 패턴(`grep -c "A\|B\|C"`)으로 세면 **다른 항목이 맞아 통과**한다.
그 커밋만 넣은 **고유한 문장 하나**로 세어야 한다.
(#254 병합 뒤 문서 커밋 2개를 이렇게 잃었다 — 나중에 cherry-pick 으로
되살렸다.)

## 3. ★ 병합 준비가 되면 **항상 이 형식으로 알린다**

PR 을 만든 직후, 답변의 **맨 첫 두 줄**을 이 형식으로 시작한다.

```
PR #221 준비 완료 — 이 문구 확인 후 병합해 주세요.
병합 후 재배포 범위: 프런트엔드만입니다 (API·DB 변경 없음).
```

### 3.1 재배포 범위 — 무엇을 적나

바꾼 파일로 판정한다. **추측하지 말고 `git show --stat` 으로 확인**한다.

| 바뀐 곳 | 적는 문장 |
|---|---|
| `apps/frontend/` 만 | `프런트엔드만입니다 (API·DB 변경 없음).` |
| `apps/api/` 만 | `API 만입니다 (프런트엔드·DB 변경 없음).` |
| 둘 다 | `프런트엔드 + API 입니다 (DB 변경 없음).` |
| `docs/` 만 | `없습니다 — 문서만 바뀌었습니다.` |
| 스키마(`schema.sql`) 포함 | 위에 더해 **`+ 델타 SQL 적용 필요`** 를 붙이고, 바로 아래에 붙여넣을 스크립트를 준다 |

### 3.2 함께 적는 것

- **델타 SQL 이 있으면** 서버에 붙여넣을 스크립트를 그 자리에 준다
  (두 번 실행해도 안전하게 · 검증 출력 포함). 적용 전에도 앱이 죽지
  않는지(컬럼 유무를 보고 빼는지) 함께 밝힌다.
- **재기동이 필요하면** 무엇을 어디서 재기동하는지 적는다. 자동 배포로
  충분하면 "재기동 불필요"라고 분명히 적는다.
- **확인 방법**은 *어디서 · 무엇을 입력/클릭하는지*까지 적는다.
  "브라우저로 열어 본다" 같은 뭉뚱그린 안내는 쓰지 않는다
  (2026-08-09 지적 — `dev-server-runbook.md §6.4` 참조).

## 4. 마무리에 항상 하는 것

- 관련 **문서 갱신** (기능 문서 · api-spec · runbook 등)
- **`docs/05-implementation/test-catalog.md`** 에 e2e 항목과 갱신 이력 추가
  (e2e 스크립트 자체는 커밋하지 않는다 — 카탈로그가 산출물이다)
- **검증 결과를 숫자로** 밝힌다 (예: `e2e135 13항목 PASS`). 고친 것이
  실제로 효과가 있는지 **되돌려 깨지는 것까지** 확인하면 함께 적는다.

## 5. 정직하게 보고한다

- 재현하지 못한 것은 "재현하지 못했다"고 적는다.
- **내가 만든 문제와 원래 있던 문제를 구분해서** 밝힌다.
- 서버 설정처럼 **코드로 고칠 수 없는 것**은 그렇다고 말하고, 고칠 자리를
  짚는다. 고칠 수 없는 곳을 고치라고 안내하지 않는다.
