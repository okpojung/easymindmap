# 백로그 (To Do)

> 결정됐지만 아직 구현하지 않은 항목의 공식 목록. 항목을 시작할 때
> 이 파일에서 상태를 갱신하고, 완료되면 해당 기능 문서로 옮긴다.

| # | 항목 | 배경/근거 | 규모 | 상태 |
|---|---|---|---|---|
| B1 | **수식(KaTeX) 렌더** — 노드·노트에서 `$…$` 수식 표시 | markmap 파리티 점검(2026-07)에서 유일한 미지원 항목. 에디터 SVG + HTML 뷰어 양쪽 통합 필요 | 중 | **보류 — 오픈 후 고객 의견 수렴** (2026-07-31 결정: 수식 수요가 많지 않을 것으로 예상, 정식 오픈 후 요구가 확인되면 착수) |
| B2 | **리치 노드 콘텐츠** — 노트 4종(문단·코드·표·체크)을 노드 본문으로 + MD 불러오기 배치 옵션(기본 노드) + A4 분량 제한 | [rich-node-content.md](../04-extensions/rich-node-content.md) | P1~P4 | **완료** (2026-07, PR#124~#137 — 상세는 rich-node-content.md) |
| B3 | 프론트 번들 코드 스플리팅 — 첫 화면에 필요 없는 코드(AI 패널·내보내기·파서·다이얼로그 등)를 지연 로딩 청크로 분리해 초기 로딩 단축 | vite 빌드 경고(청크 512KB — gzip 166KB라 체감 급하지 않음). 순수 성능 작업, 기능 변화 없음 | 소 | 보류 — **정식 오픈(서버 배포) 준비 단계에 진행 권장** (2026-07-31) |
| B4 | e2e26 기대값 현행화 | 샘플 맵의 노드별 레이아웃 오버라이드로 접기 토글이 '아래'에 오는 것이 정상이 됨 — "전부 좌/우" 기대가 낡음 (2026-07 확인, 제품 결함 아님) | 소 | **완료** (2026-07-31 — "모두 접기 후 에디터↔뷰어 칩 방향 분포 파리티"로 재작성, test-catalog e2e26) |
| B5 | Actions 러너 Node 20 deprecation 경고 정리 (checkout/setup-node v5) | CI 로그 노란 경고 — 기능 영향 없음 | 소 | 보류 |
| B6 | 편집 중 인라인 마크 미리보기 | 노드 편집(textarea)에서는 `==`·백틱 마커가 원문으로만 보여 강조 적용 여부를 커밋 전에 알기 어려움(다크 하이라이트 이슈 조사 중 확인, 2026-07). 스타일드 오버레이 에디터 필요 — 중규모 | 중 | To Do |
| B7 | '맵 닫기' 메뉴 (서버 연결 시) | 새 맵 패널에 닫기 항목 추가 — **닫기 = 모든 내용을 자동 저장하고 닫기 → 문서 목록**으로 (2026-07 사용자 확정). 서버 연결 전 로컬 전용에서는 '새 맵 만들기'가 사실상 닫기 역할이라 보류. 첫 실행 샘플 맵도 정식 버전에서는 문서 목록/빈 시작으로 대체 | 중 | 서버 연결 시 (2026-07-31 재확인) |
| B8 | **히스토리(저장 버전 이력)** — 세션을 닫고 저장할 때마다 저장일시별 버전 생성, 복원은 새 맵(제목_history_YYMMDD) | 12-history-undo-redo·13-version-history 확정 설계 (2026-07-31 사용자 결정). 백엔드 문서 스냅샷 API 활용 | 중 | 서버 연결 시 |
