# 백로그 (To Do)

> 결정됐지만 아직 구현하지 않은 항목의 공식 목록. 항목을 시작할 때
> 이 파일에서 상태를 갱신하고, 완료되면 해당 기능 문서로 옮긴다.

| # | 항목 | 배경/근거 | 규모 | 상태 |
|---|---|---|---|---|
| B1 | **수식(KaTeX) 렌더** — 노드·노트에서 `$…$` 수식 표시 | markmap 파리티 점검(2026-07)에서 유일한 미지원 항목. 에디터 SVG + HTML 뷰어 양쪽 통합 필요 | 중 | **보류 — 오픈 후 고객 의견 수렴** (2026-07-31 결정: 수식 수요가 많지 않을 것으로 예상, 정식 오픈 후 요구가 확인되면 착수) |
| B2 | **리치 노드 콘텐츠** — 노트 4종(문단·코드·표·체크)을 노드 본문으로 + MD 불러오기 배치 옵션(기본 노드) + A4 분량 제한 | [rich-node-content.md](../04-extensions/rich-node-content.md) | P1~P4 | **완료** (2026-07, PR#124~#137 — 상세는 rich-node-content.md) |
| B3 | 프론트 번들 코드 스플리팅 — 첫 화면에 필요 없는 코드(AI 패널·내보내기·파서·다이얼로그 등)를 지연 로딩 청크로 분리해 초기 로딩 단축 | vite 빌드 경고(청크 512KB — gzip 166KB라 체감 급하지 않음). 순수 성능 작업, 기능 변화 없음 | 소 | 보류 — **정식 오픈(서버 배포) 준비 단계에 진행 권장** (2026-07-31) |
| B4 | e2e26 기대값 현행화 | 샘플 맵의 노드별 레이아웃 오버라이드로 접기 토글이 '아래'에 오는 것이 정상이 됨 — "전부 좌/우" 기대가 낡음 (2026-07 확인, 제품 결함 아님) | 소 | **완료** (2026-07-31 — "모두 접기 후 에디터↔뷰어 칩 방향 분포 파리티"로 재작성, test-catalog e2e26) |
| B5 | Actions 러너 Node 20 deprecation 경고 정리 (checkout/setup-node v5) | CI 로그 노란 경고 — 기능 영향 없음 | 소 | 보류 |
| B6 | 편집 중 인라인 마크 미리보기 | 노드 편집(textarea)에서는 `==`·백틱 마커가 원문으로만 보여 강조 적용 여부를 커밋 전에 알기 어려움(다크 하이라이트 이슈 조사 중 확인, 2026-07). 스타일드 오버레이 에디터 필요 — 중규모 | 중 | **완료** (2026-07-31 — EditPreviewBackdrop, 02-node-editing §18, e2e72) |
| B7 | '맵 닫기' 메뉴 (서버 연결 시) | 새 맵 패널에 닫기 항목 추가 — **닫기 = 모든 내용을 자동 저장하고 닫기 → 문서 목록**으로 (2026-07 사용자 확정). 서버 연결 전 로컬 전용에서는 '새 맵 만들기'가 사실상 닫기 역할이라 보류. 첫 실행 샘플 맵도 정식 버전에서는 문서 목록/빈 시작으로 대체 | 중 | **완료** (2026-08-02 — 저장 후 문서 목록, ☁ 서버 맵 불러오기 활성화, e2e79). 같은 날 로그인 UI 정리로 **닫기 버튼이 상단 툴바 ✕ 로 이동**, 이후 미저장 맵은 경고 후 선택. 예고했던 "첫 실행 샘플 맵 → 문서 목록/빈 시작"도 완료(인증 켜진 배포에서 샘플 맵 미주입) — [auth-session-ui.md](../04-extensions/auth-session-ui.md) |
| B8 | **히스토리(저장 버전 이력)** — 세션을 닫고 저장할 때마다 저장일시별 버전 생성, 복원은 새 맵(제목_history_YYMMDD) | 12-history-undo-redo·13-version-history 확정 설계 (2026-07-31 사용자 결정). 백엔드 문서 스냅샷 API 활용 | 중 | **완료** (2026-08-02 — map_document_versions + keepVersion 저장, 히스토리 패널에서 복원, e2e80). 복원은 같은 날 **브라우저 새 탭('새 탭으로')** 으로 바뀜 — 현재 편집 중인 맵을 밀어내지 않는다 |
| B9 | **이미지·첨부의 서버 저장 + MD 변환 시 서버 경로** — 지금은 이미지·작은 첨부를 data URL(mime)로 파일에 내장하는데, 서버 저장 도입 후에는 서버에 올리고 MD 내보내기가 서버 URL을 쓰도록 | 2026-07-31 사용자 요청. **설계 확정 2026-08-02 — [attachment-storage.md](../04-extensions/attachment-storage.md)**: provider 추상화(server 기본·요금제 차감 / folder 내 드라이브·차감 없음 / embed 하위호환 / 향후 gdrive·webdav), 첨부는 참조만 저장(스냅샷 비대·히스토리 증폭 해소), 브라우저는 경로 문자열 접근 불가 → 폴더 선택(File System Access, Chrome·Edge) 방식 | 대 | **P1 완료 (2026-08-02) · §12 청크 업로드 완료 (2026-08-06)** — 남은 것은 **P2 내 드라이브 · P3 스냅샷 경량화 · P4 클라우드 API**(설계만). C5(새로고침 후 이어받기)는 **하지 않기로 했다** — 2026-08-15 사용자 결정, attachment-storage.md §12.9 |
| B10 | **다국어(i18n) — 최소 한국어+영어** — 문자열 사전(t() 헬퍼) + 언어 선택(브라우저 감지→로컬 설정→로그인 프로필 순), 뷰어 내보내기 현지화, 개인설정 연동. 단계·주의사항: [i18n.md](../04-extensions/i18n.md) | 2026-07-31 사용자 결정 — 글로벌 사용 대비, 로그인 개인설정에서 언어 선택. E2E가 한국어 라벨 셀렉터에 의존하므로 data-testid 이행 병행 필요 | 대 | P1~P2는 오픈 준비 시(B3과 함께 권장) · P3은 서버 로그인 시 |
| B14 | **외부 공개(오픈 테스트) 준비** — ① 레이트 리밋(`@nestjs/throttler`) ② 가입 통제(초대 코드 또는 이메일 확인 필수화) ③ **dev 가 아닌 별도 인스턴스** ④ DB 백업 주기 ⑤ 오류·용량 모니터링 ⑥ 개인정보 처리방침(이메일 수집) | 2026-08-07 사용자 결정 — *"MVP 개발이 완료되면 외부 사람들이 접속해 테스트할 수 있어야 한다"*. **현재 전수 확인 결과**: 인증·격리(JWT 가드·소유자 검증·RLS)와 용량 남용(Free 10MB · Basic 10GB)은 **이미 막혀 있다**. 구멍은 **레이트 리밋(코드에 구현 없음 — `.env.example` 에 `API_RATE_LIMIT_*` 만 있다)** 과 **가입 통제(GoTrue 기본 = 누구나 가입)** 둘이다. ③은 dev 가 재배포·스키마 변경으로 계속 부수는 곳이라 테스터 데이터가 섞이면 마음껏 실험할 수 없기 때문 — dev 는 이미 Coolify prod parity 라 같은 절차로 하나 더 세우면 된다. 인프라 쪽 전제(브라우저가 여는 호스트에 Access List 금지)는 [infra-architecture.md §7.3·§7.6·§7.7](../90-architecture/infra-architecture.md) | 중 | **①·② 완료 · ③~⑥ 남음** (2026-08-15 현행화). ①레이트 리밋 완료(2026-08-08 — [rate-limit.md](../05-implementation/rate-limit.md), e2e130). ②가입 통제는 **가입 이메일 인증**으로 해결됐다(2026-08-09 — 6자리 코드, [auth-session-ui.md](../04-extensions/auth-session-ui.md) §11). **남은 것 넷**: ③별도 인스턴스(운영 VM — [admin-console.md](../04-extensions/admin-console.md) §7 과 같은 전제) ④DB 백업 — **스크립트 완료 2026-08-28, 서버에 거는 것은 남음**. [`scripts/emm-db-backup.sh`](../../scripts/emm-db-backup.sh) 은 `pg_dumpall` 로 전체를 담고 **담은 뒤 확인한다** — gzip 온전성 · `gotrue` 가 파일 안에 실제로 있는지 · 푼 크기. 하나라도 어긋나면 파일을 남기지 않고 실패로 끝내고 관리자에게 메일. 가짜 docker 로 24항목 통과([test](../../scripts/emm-db-backup.test.sh)). 설치는 §2.1 의 `curl` 한 줄 — **문서에서 복사해야 했던 것이 12일 동안 안 걸린 이유였다.** `gotrue` 가 별도 DB 라 Coolify 기본 백업으로는 로그인 계정이 빠진다 ⑤오류·용량 모니터링 — **헬스체크 감시 완료**(2026-08-15, `scripts/health-watch.sh` + 런북 §2.2): `/v1/health` 를 5분마다 보고 **상태가 바뀔 때만** `ADMIN_EMAILS` 로 메일. **서버가 통째로 죽는 경우는 못 잡는다** — 외부 감시는 쓰지 않기로 했다(사용자 결정). 오류율·용량 감시는 아직 ⑥개인정보 처리방침 — **정책 결정 3건 완료 (2026-09-02)**: 만 14세 미만은 가입 불가이며 **가입 화면 확인란까지 구현했다**(§9 — 처리방침에만 적으면 제품이 지키지 않는 약속이 된다) · 저장 버전 이력 보유 기간은 [13a-version-retention.md](../03-editor-core/history/13a-version-retention.md) 의 요금제별 7일/90일/1년(정리 로직 구현은 아직) · 휴대폰번호는 필드를 두고 처리방침에 용도를 명시했다(다만 아직 그 용도로 쓰이는 곳이 없다 — 공개까지 안 쓰면 빼는 편이 낫다). **남은 것은 순수 데이터와 검토**: 사업자 정보 · 보호책임자 · 시행일 · 메일 발송 위탁처(관리자 콘솔에서 확인) · **법률 검토**. **오픈을 막고 있는 것은 결제(PG)가 아니라 이쪽이다** |
| B13 | **맵 제목 유니크 인덱스 승격** — 같은 폴더 같은 이름 금지를 DB 제약으로 | 2026-08-02 문서함에서 **API 검사**로 구현했다. 유니크 인덱스는 이미 중복 제목이 남아 있는 DB 에서 생성이 실패해 스키마 적용 전체를 멈춘다. 기존 데이터 정리(중복 제목 리네임) 후 `CREATE UNIQUE INDEX … (owner_id, COALESCE(folder_id,…), lower(btrim(title)))` 로 올린다 | 소 | 데이터 정리 후 |
| B12 | **앱 내부 탭(멀티 편집창)** — 한 창에서 여러 맵을 탭으로 | 2026-08-02 탭 모델 결정에서 **브라우저 새 탭**(`?map=<id>`)을 채택했다. 앱 내부 탭은 documentStore·undo 히스토리를 맵마다 다중화해야 하고 이미지 data URL 때문에 메모리가 누적된다. 브라우저 탭으로 부족하다는 사용자 요구가 확인되면 착수 — 그때 동시 편집 개수 상한도 함께 정한다 | 대 | 보류 — 요구 확인 시 |
| B11 | **DB 마이그레이션 도구 도입** — 버전 관리 + 적용 이력 추적(node-pg-migrate 등) | 현재는 schema.sql 단일 기준 + 멱등 재적용(`npm run db:apply`) + 헬스체크의 스키마 검증으로 충분하다(테이블 추가만 있음). **기존 컬럼 변경·삭제가 필요해지는 시점**에 도입 — 그때는 재적용만으로 해결되지 않고 데이터 보존이 걸린다. 2026-08-02 서버 세션 제안 | 중 | 컬럼 변경이 필요해질 때 |
| B17 | **MD 내보내기가 루트 노드 사진을 빠뜨린다** | 2026-08-16 B16 ② 검증 중 발견(별건). `serialize.ts` 의 `buildEmmBody` 는 루트의 **텍스트·노트만** 쓰고 `root.image`·`root.images` 를 내보내지 않는다 — 루트에 붙인 사진이 MD 로 나가면 **사라진다**(HTML 내보내기는 정상). 고치려면 직렬화가 바뀌므로 **불러오기 왕복까지 함께 확인**해야 한다 | 소 | **완료 2026-08-18** — e2e161 11항목 + 기존 적합성 12/12 무회귀. 파서도 함께 고쳤다(내보내기만 고치면 돌아올 때 **사진 문법이 담긴 루트 노트**가 된다) |
| B16 | **협업 선행 조건 2건 (공개 코어)** — 협업을 시작하기 전에 코어에서 끝내야 하는 것 | 2026-08-16 `27-sync-model.md` §10 에서 갈라낸 항목. ①**`kind='collab'` 이면 편집 잠금을 비켜 준다** — `map_edit_locks` 가 지금 `maps.kind` 를 보지 않아, 이대로면 **둘째 사람이 읽기 전용**이 된다. 작다 ②**이미지를 문서 밖으로** — 클립보드 사진이 `image.src` 에 data URL 로 들어가 문서가 수 MB 가 된다. CRDT 에 넣으면 그 바이트가 업데이트 로그에 **영원히** 남는다. 역호환(옛 문서는 그대로 읽히고 다음 저장에서 올라간다)이 핵심이고 **일괄 마이그레이션은 하지 않는다**(되돌릴 수 없다). **둘 다 협업이 없어도 값어치가 있다** — ②는 단독 사용자의 자동저장·버전 히스토리도 가볍게 만든다 | ①소 ②대 | **완료 (2026-08-18)** — ① 편집 잠금 비켜 주기(e2e157 18항목) ② 사진을 문서 밖으로(슬라이스 4단계 · e2e158 17 + e2e159 22 + e2e160 20항목). 검증에서 **65,941 → 741 바이트**. 정리(GC)는 **우리가 옮긴 사진만 · 히스토리까지 훑어 · 하루 지난 것만** 지운다 |
| B15 | **오픈코어 경계 — 유료 기능은 private 저장소에서** | 2026-08-15 사용자 결정. 저장소는 **공개(Apache-2.0) 그대로**, 협업 등 유료 기능의 **구현만** `okpojung/easymindmap-pro`(private). **설계 문서는 공개에 그대로 둔다**(로드맵이 보이는 편이 낫다). 법적으로는 나눌 필요가 없다 — Apache-2.0 은 독점 파생을 허용한다. 나누는 이유는 **git 저장소가 통째로 public/private 이라, 한 번 실수로 커밋되면 되돌릴 수 없기 때문**이다. 경계와 확장점 설계: [open-core-boundary.md](../04-extensions/open-core-boundary.md) | 중 | **경계 재정의 (2026-08-16)** — 유료 범위가 협업만이 아니라 **MVP 이외 제품 기능 전부**로 정해졌다(협업·실시간 번역·플러그인 등). private 저장소 생성·경계 장치 완료. **서버 확장점 완료 (2026-08-16)** — 하나의 이음매(`@easymindmap/pro` · `GET /v1/features`)로 일반화, e2e155 24항목. 유료 기능이 늘어도 공개 코어에는 커밋이 생기지 않는다. **화면 자리까지 완료** (e2e156 17항목) — 좌측 레일의 협업 자리가 `/v1/features` 를 물어 서버가 준 이유를 그대로 보여 준다. private 패키지도 라이선스 판정까지 완료(pro-e2e1 20항목). 남은 것: **파는 기능의 구현**(협업·번역 …)과 유료판 Dockerfile — 둘 다 private 저장소 몫 |
| B19 | **스키마 정비 — 협업 전 마지막 코어 작업** ([schema-overhaul-plan.md](../90-architecture/schema-overhaul-plan.md)) | 2026-09-02 계획. **A** `maps.owner_id` CASCADE → RESTRICT + 탈퇴 흐름(협업맵 개설자는 409 로 막고 목록을 준다) · **B** 버전 보관 컬럼(`plan_quotas.version_days/max_pinned`, `map_document_versions.pinned/label/pinned_by/pinned_at` + 부분 인덱스) · **C** `map_ownership_transfers` 표 · **D** 첨부 저장 경로를 NFS 로 | A 중 · B·C 소 · D 운영 | **A·B·C 완료 2026-09-04** (e2e197 27항목 · smoke 40/40 · 델타 `apps/api/database/deltas/2026-09-04-schema-overhaul-abc.sql`). **남은 것**: ⑴ **D** — NFS 마운트 + `STORAGE_LOCAL_DIR` + rsync, 코드 변경 없음(사용자가 서버에서) ⑵ **`version-prune` 워커 + 영구보관 UI** — 컬럼만 있다 ⑶ **소유권 이전 API·UI** — 표만 있다. 탈퇴가 막힌 개설자가 쓸 출구라 ⑶이 먼저다. **설계 완료 2026-09-05** — [29-invite-and-ownership-transfer.md](../04-extensions/collaboration/29-invite-and-ownership-transfer.md) (초대장·나가기까지 한 문서, 사용자 결정 4건). 구현 순서 **P0 ✅ 2026-09-05(e2e200)** → P1(유료: 이전) → P2(유료: 초대장) → P3(코어: 협업맵 첨부 주인 = 맵 주인) |
| B18 | **백업이 멈춘 것을 알아채기** — cron 이 조용히 사라지거나 서버가 부팅 뒤 cron 을 잃으면 아무 일도 일어나지 않는다 | 2026-08-28, B14 ④ 작업 중 확인. 백업 스크립트는 **실패하면** 메일을 보내지만 **아예 돌지 않으면** 말이 없다 — 멈춘 cron 은 자기가 멈췄다고 말해 줄 수 없다. 성공할 때마다 `/var/lib/emm-backup/last-success` 에 시각을 남겨 두므로 **감시할 재료는 이미 있다**. 5분마다 도는 `health-watch.sh` 가 이 파일을 함께 보는 것이 가장 싸다(메일 배관을 이미 갖고 있다). 다만 그쪽은 e2e153 이 지키는데 이 저장소에서 그 테스트를 돌릴 수 없어, **검증된 경로를 검증되지 않은 변경으로 바꾸는 것**이 된다 — 두 스크립트를 함께 시험할 수 있게 된 다음에 한다. 근본적으로는 외부 감시의 영역이고, 외부 감시는 쓰지 않기로 했다(2026-08-15) | 소 | 대기 — health-watch 를 시험할 수 있게 된 뒤 |

## 하지 않기로 한 것 (2026-08-15 사용자 결정)

목록에 다시 올라와 같은 논의를 반복하지 않도록 **이유까지** 적어 둔다.

| 항목 | 왜 하지 않나 |
|---|---|
| **C5 — 청크 업로드 새로고침 후 이어받기** | 브라우저는 새로고침 뒤 **그 파일을 다시 읽을 수 없다**(`File` 핸들이 새로고침을 넘기지 못한다). 그래서 "자동 이어받기"가 아니라 **사용자가 같은 파일을 다시 골라 주면 남은 조각만 올리는** 것이 된다. 이미 조각 재시도가 붙어 있어 실효가 좁다 — 서버 쪽(상태 조회·멱등 PUT·24시간 세션)은 이미 다 되어 있으므로, 필요해지면 프런트만 붙이면 된다 |
| **캔버스 전역 드롭 — 빈 캔버스에 떨어뜨리면 새 노드** | 필요 없다고 판단(2026-08-15 사용자) |
| **되돌리기 스택 IndexedDB 영구 보관** | ⑴ **2026-07-31 결정과 어긋난다** — 되돌리기는 "이 편집 세션 한정"으로 정했다([12-history-undo-redo.md](../03-editor-core/history/12-history-undo-redo.md)) ⑵ 그 용도는 **저장 버전 이력**이 이미 덮는다 — 둘 다 두면 사용자가 어느 쪽을 써야 할지 알 수 없다 ⑶ 지금 99단계가 싼 이유는 스냅샷이 **메모리에서 구조를 공유**하기 때문인데 **직렬화하면 그 공유가 깨져** 전체 문서 99벌이 된다. 문서에는 이미지가 data URL 로 들어 있어 브라우저 저장소를 금방 밀어낸다 |
