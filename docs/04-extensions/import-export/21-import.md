# 21. Import
## IMPORT

* 문서 버전: v1.0
* 작성일: 2026-04-16
* 참조: `docs/01-product/functional-spec.md`, `docs/04-extensions/integrations/30-obsidian-integration.md`

---

### 1. 기능 목적

* 외부 Markdown 파일을 **마인드맵 노드 트리로 변환하여 가져오는** 기능
* Obsidian, Notion, VS Code 등에서 작성한 아웃라인 문서를 즉시 마인드맵화
* 기존 텍스트 기반 계획서·절차서를 마인드맵으로 빠르게 전환

---

### 2. 기능 범위

* 포함:
  * Markdown 파일 가져오기 (IMPORT-01)
  * 아웃라인 모드 파싱 (헤딩 기반 계층 구조)
  * 문서 모드 파싱 (단락/섹션 기반)
  * 가져오기 미리보기

* 제외:
  * HTML 가져오기 (후순위)
  * PDF 가져오기 (후순위)
  * CSV/Excel 가져오기 (후순위)
  * 실시간 파일 동기화 (→ `30-obsidian-integration.md`)

---

### 3. 세부 기능 목록

| 기능ID      | 기능명              | 설명                              | 주요 동작       |
| --------- | ---------------- | ------------------------------- | ----------- |
| IMPORT-01 | Import Markdown  | Markdown 파일을 노드 트리로 변환하여 가져오기   | 파일 선택 → 변환  |
| IMPORT-02 | Outline Mode     | # 헤딩 계층 구조 기반 파싱                | 헤딩 → 노드 깊이  |
| IMPORT-03 | Document Mode    | 헤딩 없는 단락 문서를 섹션별로 파싱            | 단락 → 리프 노드  |
| IMPORT-04 | Import Preview   | 변환 결과 미리보기 후 확정                 | 트리 미리보기 표시  |

---

### 4. 기능 정의 (What)

#### 4.1 파싱 모드 선택

| 모드          | 적용 조건                     | 설명                   |
| ----------- | ------------------------- | -------------------- |
| Outline Mode | `#`, `##`, `###` 헤딩 3개 이상 | 헤딩 레벨을 노드 깊이로 변환     |
| Document Mode | 헤딩 없거나 1~2개              | 단락/줄바꿈 기반 리프 노드 생성   |

#### 4.2 Outline Mode 변환 규칙

```text
# Root 제목         → Root 노드 (depth 0)
## 섹션 A           → 자식 노드 (depth 1)
### 항목 A-1        → 자식 노드 (depth 2)
### 항목 A-2        → 자식 노드 (depth 2)
## 섹션 B           → 자식 노드 (depth 1)

헤딩 사이 paragraph → 상위 노드의 note로 저장
코드 블록           → 상위 노드의 note 코드 블록으로 저장
인라인 태그 [tag]    → 해당 노드에 태그 자동 할당 (태그 존재 시)
```

#### 4.3 Document Mode 변환 규칙

```text
빈 줄 구분 단락 → 각각 독립 자식 노드
파일명          → Root 노드 text
```

#### 4.4 가져오기 미리보기 UI

```text
┌─────────────────────────────────────────────────────┐
│  가져오기 미리보기                          [✕]       │
├─────────────────────────────────────────────────────┤
│  파일: linux-setup.md                                │
│  모드: Outline Mode    [변경]                         │
│  노드 수: 24개                                        │
│                                                     │
│  📁 Linux 서버 구축                                   │
│    ├── 📄 패키지 관리                                 │
│    │     ├── 📄 APT 업데이트                           │
│    │     └── 📄 Nginx 설치                            │
│    └── 📄 보안 설정                                   │
│          └── 📄 방화벽 설정                            │
│                                                     │
│  [ 취소 ]          [ 새 맵으로 가져오기 ] [ 현재 맵에 추가 ] │
└─────────────────────────────────────────────────────┘
```

---

### 5. 동작 방식 (How)

#### 5.1 사용자 동작

* 상단 메뉴 > `가져오기` > `Markdown 파일`
* 파일 선택 다이얼로그 → `.md` 파일 선택 (또는 드래그 앤 드롭)
* 파싱 모드 자동 선택 (헤딩 구조 감지)
* 미리보기 화면에서 노드 트리 구조 확인
* `새 맵으로 가져오기` 또는 `현재 맵에 추가` 선택 후 확정

#### 5.2 시스템 처리 흐름

```
파일 선택 (File input / Drag & Drop)
    │
    ▼
클라이언트 파싱 (Markdown → AST)
    │
    ▼
파싱 모드 자동 감지 (헤딩 수 기준)
    │
    ▼
AST → NodeTree 변환
    │
    ▼
미리보기 렌더링
    │
    ▼ (사용자 확정)
POST /maps/{mapId}/import { nodes: NodeTree }
  또는
POST /maps { title, nodes: NodeTree }  ← 새 맵 생성
    │
    ▼
Document Store 반영 + Auto Save
```

---

### 6. 규칙 (Rule)

* 지원 파일 형식: `.md`, `.markdown`
* 파일 최대 크기: 5MB
* 노드 최대 생성 수: 500개 (1회 가져오기 기준)
* 헤딩 최대 깊이: H6 (`######`) → depth 5 (이하 flat 처리)
* 중복 태그 [tag]는 자동 매칭 (기존 태그가 없으면 신규 생성)

---

### 7. 예외 / 경계 (Edge Case)

* **지원하지 않는 파일 형식**: `.md`, `.markdown` 외 파일 선택 시 오류 안내
* **파일 크기 초과**: 5MB 초과 시 오류 메시지
* **노드 수 초과**: 500개 초과 경고 → 분할 가져오기 안내
* **빈 파일**: "가져올 내용이 없습니다" 안내 메시지
* **파싱 실패**: 원본 텍스트 전체를 단일 노드 note에 저장

---

### 8. 권한 규칙

| 역할      | 가져오기 |
| ------- | ----- |
| creator | ✅     |
| editor  | ✅     |
| viewer  | ❌     |

---

### 9. DB 영향

* `nodes` — 가져온 노드 트리 INSERT
* `node_notes` — 헤딩 사이 단락, 코드 블록 저장
* `tags` — 신규 태그 자동 생성 (인라인 [tag] 가져오기 시)

---

### 10. API 영향

* `POST /maps/{mapId}/import` — 현재 맵에 노드 추가
* `POST /maps` — 새 맵 생성 (가져오기)

---

### 11. 연관 기능

* EXPORT (`20-export.md`)
* OBSIDIAN_INTEGRATION (`30-obsidian-integration.md`)

---

### 12. 구현 우선순위

#### MVP
* IMPORT-01 Markdown 파일 가져오기
* IMPORT-02 Outline Mode 파싱
* IMPORT-04 미리보기

#### 2단계
* IMPORT-03 Document Mode 파싱
* 인라인 태그 자동 할당
* 드래그 앤 드롭 파일 선택
