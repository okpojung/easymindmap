# 리치 노드 콘텐츠 — 노트 4종을 노드 본문으로 (설계 제안)

> 상태: **구현 진행 중** — P1(노드 코드 블록) 완료(2026-07), P2~P4 예정.
> 발의: "인라인 코드처럼, 노트의 4가지(문단·코드·표·체크)를 노드에 넣을
> 수 있나? MD 불러올 때 노트/노드 배치 옵션(기본 노드) + A4 분량 제한."

## 1. 현재 노드가 이미 담을 수 있는 것

| 콘텐츠 | 노드 본문 | 비고 |
|---|---|---|
| 여러 줄 텍스트 | ✅ | 자동 줄바꿈 + 수동 \n |
| 인라인 강조 | ✅ | 굵게·기울임·취소선·밑줄·하이라이트·**코드**(2026-07) |
| 사진 | ✅ | 텍스트 중간 인라인 포함, data URL 내장 |
| **표** | ✅ | Markdown 표를 노드 안에 격자로 렌더(mdTable) |
| 코드 블록(여러 줄) | ❌ | 노트로만 |
| 체크박스 | ❌ | 노트로만 |
| 문단(장문) | △ | 텍스트로 가능하나 붙여넣기 시 정책에 따라 노트로 감 |

→ 격차는 **코드 블록**과 **체크박스** 두 가지다. 표는 이미 되고,
문단은 "배치 정책"의 문제다.

## 2. 제안 설계

### 2.1 노드 본문 블록 (에디터·뷰어 파리티)

- **코드 블록**: 노드 텍스트 안 ``` 펜스 구간을 모노스페이스 + 회색
  패널로 렌더(표(mdTable)와 같은 방식의 블록 레이아웃).
- **체크박스 줄**: 노드 텍스트의 `- [x] …` 줄을 체크박스 글리프로 렌더,
  클릭 시 토글(에디터) / 뷰어는 읽기 전용.
- 기존 표 렌더(mdTable) 프레임을 재사용해 "노드 내 블록" 공통 레이아웃
  으로 일반화한다.

### 2.2 MD 불러오기 배치 옵션

불러오기 대화상자에 선택 추가 (기억됨):

| 옵션 | 동작 |
|---|---|
| **노드로 (기본)** | 문단·코드·표·체크를 해당 위치의 **노드 본문**에 넣는다 |
| 노트로 | 현재와 동일 — 문단=문단 노트, ```=코드 노트, 표=표 노트, `- [x]`=체크 노트 |

파서에는 `blockPlacement: 'node' | 'note'` 옵션으로 전달(기본 `node`).
EMM 왕복: 노드 본문 블록은 원문 그대로 직렬화되므로 손실 없음.

### 2.3 노드 분량 제한 — "A4 한 장"

노드가 문서화되는 것을 막는 안전선. 근사 기준:

- 텍스트: **2,500자** (A4 1장 분량 근사) — 이미지 1장 = 600자로 환산
- 초과 시: 넘치는 블록부터 **자동으로 노트로 이동** + "A4 분량을 넘어
  노트로 옮겼습니다" 안내 (데이터는 잃지 않는다)
- 편집 중 실시간 카운트는 두지 않는다(소음) — 불러오기·붙여넣기 시점만 적용

## 3. 구현 단계 (승인 시)

| 단계 | 내용 | 규모 |
|---|---|---|
| P1 | ✅ **완료** — 노드 코드 블록 렌더(에디터+뷰어). `mdCode.ts`(parse/layout, mdTable 방식 미러) + sizeNodeForText 통합(코드 구간 줄바꿈 제외·패널 크기 반영) + NodeRenderer 패널 + 뷰어 파리티(parseMdCode JS). **보완(2026-07)**: 패널 헤더 = 언어 라벨 + ⧉ 복사(→복사됨 ✓, 노트 코드와 동일 구성, 에디터·뷰어 모두 클릭 복사) · 하이라이트/코드 밴드 **불투명(1.0)** — 다크 카드색과 섞여 탁해지던 시인성 문제 해결. 검증 e2e56(확장) · **보완 2차(2026-07)**: 스마트 Enter(열린 ``` 펜스 안=줄바꿈, 밖=편집 완료) + 툴바 `{ }` 코드 블록 삽입 버튼 — Enter=커밋 규칙 때문에 여러 줄 펜스를 손으로 칠 수 없던 문제 해소(e2e57, 실키보드 재현) · **보완 3차(2026-07)**: 코드 패널 **언어 라벨 클릭→변경**(prompt, 여는 펜스 줄 교체) + 하이라이트 다크 잔여 표면 수정(칸반 컬럼 헤더 인라인 마크 렌더 누락, 미니 툴바 H 버튼 자체 글자색) — e2e58 | 중 |
| P2 | 노드 체크박스 렌더 + 클릭 토글 | 중 |
| P3 | 불러오기 배치 옵션(기본 노드) + A4 제한·오버플로 이동 | 중 |
| P4 | 아웃라인·칸반·검색에서 노드 블록 표시 정합 | 소~중 |

각 단계는 독립 PR로, EMM 왕복·뷰어 파리티 E2E와 함께 진행.

## 4. 미결정 사항 (구현 전 확인)

1. A4 환산 기준(2,500자·이미지 600자)이 체감과 맞는지 — P3에서 실측 조정
2. 붙여넣기(웹 기사)의 기존 "무조건 노드" 정책과 옵션의 관계 —
   붙여넣기는 현행 유지, 이 옵션은 **MD 불러오기 전용**으로 시작

관련: `emm-spec.md` §3.5~3.6, `../00-project-overview/backlog.md`(수식 등).
