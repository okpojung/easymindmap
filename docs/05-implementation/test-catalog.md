# 테스트 카탈로그 (MVS E2E)

> **이 문서의 역할**: MVS(에디터 프런트엔드) 개발 과정에서 각 PR마다
> 작성·실행한 Playwright E2E 테스트를 **기능 영역별로** 정리한 회귀
> 검증 목록이다. "무엇이 이미 검증되어 있는가"를 한눈에 보고, 회귀
> 실행 시 어떤 테스트를 돌려야 하는지 고르는 기준표다.
>
> **갱신 규칙 (중요)**: 코드가 업데이트되어 **새 E2E를 추가하거나 기존
> 테스트를 바꿀 때마다 이 문서도 함께 갱신**한다. 각 배치 작업의
> 커밋에 테스트 파일과 이 표의 변경을 같이 담는다. (feature 문서의
> "검증: E2E eNN" 문구와 짝을 이룬다.)

---

## 1. 개요

- **도구**: Playwright(Chromium) — 실제 브라우저에서 앱을 구동하고
  DOM·SVG 좌표를 실측하는 통합 테스트. 단위 테스트가 아니라 **사용자
  시나리오 재현** 방식이다.
- **실행 환경**: 개발 서버(`apps/frontend`, Vite) + 헤드리스 Chromium.
  테스트 스크립트는 세션 스크래치패드에 `eNN.cjs`로 보관한다(저장소에
  커밋하지 않는 개발 자산 — 산출물은 이 카탈로그와 feature 문서의 검증
  문구).
- **검증 데이터**: 합성 입력 + **로컬 전용 실제 파일**(was.md,
  ubuntu-wp.html 352노드 WordPress 가이드, ChatGPT 대화 내보내기 등)로
  실사용 시나리오를 재현한다. 실제 파일은 **저장소에 커밋하지 않는다**
  — 저장소의 적합성 코퍼스(packages/emm-parser/conformance)는 전량
  합성본이다(2026-08-04 실문서 3종 합성 교체).
- **판정 방식**: 각 테스트는 `PASS/FAIL` 체크를 출력하고 하나라도
  실패하면 비정상 종료한다. 좌표·색상·개수를 **실측**해 비교한다
  (스냅샷 이미지가 아니라 계산값 단언).

---

## 2. 영역별 테스트 목록

각 행: **테스트 ID · 대상 기능 · 핵심 검증 항목 · 관련 feature 문서**.

### 2.1 노드 편집·인디케이터·부분 강조

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e18 | 부분 강조(마크) | 하이라이트 인접 tspan 글자 겹침 0(실측)·툴바 토글 해제·툴바 버튼 ≥26px | 03-node-indicator |
| e2e28 | 사진 노드 드래그 | 네이티브 드래그 고스트(주황 창) 차단 + 이동은 정상 | 02-node-editing |
| e2e35 | 마크가 줄바꿈에 걸침 | 강조 구간 밀림 버그 수정(에디터+뷰어) | 06-node-rendering |
| e2e51 | 형광펜 다크 시인성 | ==하이라이트== 구간 글자 = 진한 고정색(#1F1B16)·노란 띠 유지(에디터·뷰어 라이트/다크 동일) | 06-node-rendering |

### 2.2 붙여넣기·기사 사진

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e38 | 기사 붙여넣기 사진 위치 | 이미지가 문단 사이 원위치 보존·지연로딩 자리표시자(1px gif→data-src) 복원 | 04-node-content |
| e2e39 | ThinkWise식 붙여넣기 | 선택+붙여넣기 = 하위 노드에 모두, 노트 분리 없음·클립 배지·편집 중 붙여넣기 | 02-node-editing |
| e2e40 | 노드 텍스트 중간 인라인 사진 | 사진이 텍스트 줄 사이 원문 위치·노드 높이 확대·개별 제거·뷰어 파리티 | 04-node-content |
| e2e41 | 기사 사진 다운로드 내장 | CORS 허용=data URL 내장 / 차단=원본 URL 폴백, 원문 위치 보존 | 04-node-content |

### 2.3 캔버스·레이아웃·선택

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e19 | Pan·러버밴드·방사형 이동 | Pan 모드 분리·다중선택 일괄스타일·좌우 이동·맞춤 +버튼 | 10-canvas / 11-selection |
| e2e20 | 방사형·양쪽 반대쪽 이동 | 빈 공간 드롭 = 새 1레벨 브랜치, 같은쪽 드롭은 취소 | 08-layout |
| e2e21 | 좌우 이동 안내·레벨 표기 | "왼쪽 이동" 배지·중심=1레벨 표기·드롭존 동기 회귀 | 08-layout |
| e2e22 | 반대쪽 형제 드롭존 | 상/하 드롭 = 그쪽 side로 이동(우↔좌, 3레벨→좌 형제) | 08-layout |
| e2e23 | 도형·테두리·시간배치 | 도형 4종(화살◀▶·원통·별)·테두리 이중선·시간배치 레이아웃 | 05-node-style / 08-layout |
| e2e30 | 맵설정·최소줌·표 마크 | 노트 인라인 강조 팝업·194노드 전체 맞춤·표 셀 마크·레벨별 맞춤/도형·우클릭 임시 Pan·최소줌 2% | 10-canvas |
| e2e42 | 방사형·양쪽 배치 버그 | MD 불러오기 후 좌·우 모두 배치·한쪽 레이아웃 종전대로·저장 side 존중 | 08-layout |

### 2.4 칸반

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e24 | 칸반 편집 | 카드 드래그(서브트리 포함)·강조 툴바·사진 붙여넣기·아웃라인 분할·카드 추가 | 09-kanban |

### 2.5 접기/펼치기·접힘 칩

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e26 | 뷰어 접기 토글 위치 | ①진행트리 = 토글 전부 아래 ②기본 맵(노드별 오버라이드 포함): **'모두 접기' 후 에디터 칩 방향 분포 = 뷰어 칩 방향 분포**(최근접 노드 rect 기준 1:1 파리티 — 뷰어 칩은 별도 칩 레이어 g.mm-toggle, open 토글 제외) + 좌·우 존재 + 유효 방향만. B4 현행화(2026-07-31): 옛 "전부 좌/우" 고정 기대를 파리티 기반으로 교체 — 샘플 맵이 바뀌어도 유효 | 03-node-indicator |
| e2e45 | 접기 3건 | 토글 전부 하단(트리·오른쪽)·모두 접기/펴기(2레벨)·뷰어 재배치 | 03-node-indicator |
| e2e46 | 접힘 칩·줌 | 칩 자리 확보·숨은 노드 수 표시(에디터·뷰어)·최상위 레이어·줌 5%·직접 입력·1:1 | 03-node-indicator / 10-canvas |
| e2e46b | 칩 다른 레이아웃 | 방사형·오른쪽/트리·아래 칩 숫자·겹침·최상위 | 03-node-indicator |
| e2e49 | 방사형·양쪽 모두 접기 백지 | layoutBlock depth 누락 수정·JS 오류 0·좌우 분할 유지 | 20-export |
| e2e49b | 뷰어 레이아웃 전수 스윕 | 8개 레이아웃 × 전 조작(접기/펴기·토글·fit·검색·다크) pageerror 0 | 20-export |

### 2.6 내보내기·불러오기 (MD/HTML/ZIP)

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e25 | 내보내기/불러오기 왕복 | HTML 메타데이터 복원·MD 본문 수정 반영·일반 MD 파싱·사진 있으면 ZIP | 20-export / 21-import |
| e2e27 | 첨부 왕복 | 첨부 드롭→MD=ZIP·작은 첨부 인라인(data URL)·ZIP 직접 불러오기 복원 | 20-export |
| e2e29 | 실파일 완전 변환 | 실사용 보고서형 MD(로컬 전용, 저장소 미포함): 절 순서·전체 견출·머리말→루트 노트·문단/표/인용/코드·넘침 0 | 21-import |
| e2e31 | 다중 파일 변환 | 5개 파일 공통 불변식(변환 성공·링크 원문·표 마크 없음)·개별 규칙 | 21-import |
| e2e32 | 구조 규칙·템플릿·굽기 | was.md 절/자식/불릿·새 맵 메뉴·라이브러리 템플릿·레벨 레이아웃 복원·뷰어 정렬 | 21-import / 22(구 번호) |
| e2e47 | 기본 템플릿 | 트리-진행트리맵 골격·MD 메타데이터 레벨별 layoutType·목록 첫 항목·시작 | 21-import |

### 2.7 HTML 뷰어 파리티·인터랙션

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e33 | 툴팁·다크·뷰어 인터랙션 | 커스텀 툴팁(위쪽)·다크 토글/유지·뷰어 Pan/⌖ 중앙/(+)(−)/커스텀 툴팁 | 20-export |
| e2e34 | 노트 폰트·팝업 크기 | 노트 폰트 기본 13pt·팝업 자동 크기(최대 화면 1/2×1/2)·뷰어 반영 | 20-export |
| e2e36 | 뷰어 파리티 8건 | 줌 바·선택 점선 오버레이·⌖ Focus·노트 15pt·노드 색·다크 노트 | 20-export |
| e2e37 | 100% 파리티·검색 | 100% 노드 폭 일치·노트 글자 상속·에디터/뷰어 검색 강조·중앙 이동·조상 펼침 | 16-search / 20-export |
| e2e48 | 로고·검색·줌 7건 | 로고 배지·파비콘·100 확대경 아이콘·다크 강조 글자·검색 중앙 100%·fit 파리티·키보드 5% | 20-export / 16-search |

### 2.8 검색·아웃라인

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e13 | 아웃라인 표시·노트창 | 아웃라인 여러 줄·사진 표시·노트 입력창 15행·svg user-select none | 10-canvas |
| e2e14 | 아웃라인/편집 안정화 | 편집 textarea 드래그 고스트 차단·부분 선택 안정화·여러 줄 H 감싸기·아웃라인 강조 (2026-07-31 셀렉터 현행화: 편집창=body 포털 `textarea`, 미니 툴바=`[data-testid=mark-toolbar]` 7버튼) | 02-node-editing |
| e2e50 | 아웃라인 ↔ 맵 동기 | 호버 버튼 인디케이터 밀림 0·아웃라인 클릭=맵 중앙+검색식 강조(라이트/다크)·맵 클릭=행 자동 스크롤 | 10-canvas |
| e2e53 | 아웃라인 모드/맵 모드 전환 | 에디터 전체 토글·분할 중 비활성 / 뷰어 분할·전체 아웃라인·행 클릭=중앙+강조·JS 오류 0. 아이콘은 이모지→인라인 SVG(목록/마인드맵) 재디자인 후 육안 시인성 확인(라이트·다크·뷰어) | 10-canvas / 20-export |
| e2e54 | markmap 파리티(코드·체크) | `- [x]`→체크 노트(자식 노드 아님)·MD 내보내기 `- [x]`/`- [ ]`/백틱 보존(왕복)·인라인 코드 모노+회색 밴드(에디터·뷰어)·미니 툴바 `{ }` 통일(`<>` 제거 확인)·뷰어 JS 오류 0. | emm-spec §3.5~3.6 |
| e2e55 | 노드 전체 강조 밑줄 | 스타일 탭 밑줄 토글 → 에디터 노드 전체 underline 렌더·뷰어 파리티(st.underline 베이크)·JS 오류 0 | 05-node-style §15 |
| e2e57 | P1 실사용 재현(키보드) | { } 버튼 코드블록 삽입·**스마트 Enter**(열린 펜스 안=줄바꿈, 닫힌 뒤=커밋)·H 버튼→다크 쨍한 밴드 실측+캡처 — page.fill이 놓친 Enter=커밋 함정을 실키보드로 재현·수정 검증 | rich-node-content P1 |
| e2e56 | 리치 노드 P1(노드 코드 블록) | 노드 텍스트 속 ``` 펜스 → 회색 패널+모노 줄(마커 숨김·일반 텍스트 유지·박스가 패널 포함)·뷰어 파리티·JS 오류 0 | rich-node-content P1 |
| e2e58 | 하이라이트 다크 잔여 표면 + 언어 라벨 | 미니 툴바 H 버튼 자체가 다크에서 진한 글자(#1F1B16)·칸반 **컬럼 헤더/카드** ==형광== 노란 배경+진한 글자·코드 패널 언어 라벨(✎) 클릭→**팝업 편집기**(언어·코드 프리필)→언어 교체(bash→python) | rich-node-content P1 |
| e2e59 | P1 사용성(순서·팝업·배지) | 코드 블록 **앞/뒤 텍스트 원문 순서** 렌더(위 텍스트=패널 위, 아래=패널 아래 — 에디터+뷰어 실측)·{ } 팝업 삽입 후 편집 유지(e2e57 [A]와 세트)·검색 결과 **종류 배지**([노드]/[태그]/[노트]/[링크] 제목 앞 색 칩 — 에디터 다크 실측+뷰어)·뷰어 JS 오류 0. (e2e37 [S2]의 sub 꼬리표 기대값도 배지 기준으로 갱신) | rich-node-content P1, user-guide 03·06 |
| e2e60 | 리치 노드 P2(체크박스) | 노드 텍스트 `- [ ]`/`- [x]` 줄 → 체크박스 글리프(마커 숨김·항목 텍스트 유지)·**글리프 클릭 = 원문 토글**(재클릭 복귀)·미니 툴바 ☑ 버튼(- [ ] 삽입/해제)·뷰어 파리티(pos.ck 베이크, 읽기 전용)·뷰어 JS 오류 0 | rich-node-content P2 |
| e2e62 | 새 맵 메뉴 정리(3단 아코디언) | 상위 3개 메뉴(새 맵/서버/Local)·**Local 하위는 접힘 기본, 클릭 시 트리 펼침**(MD→배치 옵션(중복 없음 1개)→HTML→ZIP)·하단 '등록된 템플릿에서 시작' 제거·**새 맵 직후 템플릿 선택 = 골격 시작**(제목=중심 주제)·건너뛰기 = 기본 골격. (파일 불러오기를 쓰는 전 테스트에 "Local 펼침 보장" 스니펫 일괄 반영, e2e37도 '노트로' 명시) | 21-import §MVS, user-guide 02 |
| e2e78 | Phase 3 Supabase Auth 풀스택 | 구성: 로컬 PG16(:55432, 스키마 4종) + API :3001(`AUTH_MODE=supabase`) + **mock GoTrue :9999**(같은 JWT_SECRET로 실 HS256 발급 — 가입/로그인/refresh/로그아웃) + vite :5198(`VITE_SUPABASE_URL` 주입). ①비로그인 ☁=로그인 폼(저장 숨김) ②오답 비밀번호 오류 ③가입 즉시 로그인+☁ 저장(실 JWT→API→DB) ④새로고침 세션 유지(persist)+목록 열기 ⑤로그아웃 복귀+타 계정 목록 분리. **API 단위 8단언**(auth-api-test.mjs): 무토큰/위조 서명/만료/aud 불일치=401, 유효 토큰 목록·생성, JIT public.users 생성, 사용자별 분리. dev 모드 회귀 e2e-cloud/cloud2 무변화(공유 dev 사용자 잔여 맵 = 테스트 격리 이슈로 DB 비우고 단독 재확인) | backend-phase1.md Phase 3 |
| e2e77 | 표 ⧉ 스트립(겹침 해소) | ①에디터 노드 표: ⧉가 표 **바깥 오른쪽 위 스트립**(MD_TABLE_COPY_STRIP=13)에 — 마지막 열 머리글("남은 NVMe")과 bbox 겹침 없음 실측 + 클릭 복사(TSV)·복사됨 ✓ 유지 ②HTML 뷰어 동일 파리티+JS 오류 0 ③아웃라인/칸반 HTML 표: 래퍼 paddingTop 스트립(버튼 bottom ≤ 표 top) ④노트 팝업: 표=paddingTop 20 스트립, 문단=paddingRight 30px 예약(computed style) | 04-node-content §14.6, user-guide 03 |
| e2e76 | MD 블록 분리 변환 | ①markmap.md '노드로' 불러오기 → Blocks 아래 **코드·표·이미지가 각각의 자식 노드 3개**로 분리(제목 노드에 뭉치지 않음 — data-node-id 상이 실측, markmap 파리티) ②내보내기(ZIP)→ZIP 재불러오기(메타 enrich): 노드 수 21=21·분리 구조·내장 이미지 유지(블록-노드는 `표`/`코드` 라벨 견출로 나가고 같은 키로 복원) ③표 바로 뒤 인용문(※ 첨부)은 표 노드 본문에 이어 붙음(lastBlockNode). 원칙: MD 독립 블록=분리, 기사 붙여넣기(텍스트 중간)=한 덩어리 | markdown-export.md §1.0, user-guide 08 |
| e2e75 | MD 왕복(블록·이미지 URL) | ①'노드로' 불러오기→MD 내보내기: 노드 안 표=**파이프 표(구분선 포함)**·코드=펜스·체크=- [x]로 여러 줄 저장(한 줄 뭉개기 없음, splitNodeBody), 표 뒤 텍스트(※첨부)는 표 뒤 인용문(>) — 원문 순서 indexOf 실측, 링크=🔗 [라벨](url) ②내보낸 MD 재불러오기(메타 enrich): 노드 수 동일+표 격자 유지+잉여 🔗 자식 노드 0(🔗/이미지 문단 폴딩) ③'노트로' 불러오기→내보내기: 노트 4종도 MD 블록으로 ④![](이미지 URL) → **다운로드+data URL 내장**(page.route로 PNG 서빙)+"사진 1개 내장" 안내 ⑤404 URL → 이미지 제거+링크(🔗) 폴백+"링크로 대체" 안내. emm-parser 적합성 코퍼스 12/12(🔗 폴딩으로 bodyRT=원본 노드 수 일치, 스냅숏 갱신) | markdown-export.md §1.7·§2, user-guide 08 |
| e2e74 | 표 원문 위치·문단 ⧉·아이콘 통일 | ①노드: 앞 텍스트 < 표 < 뒤 텍스트(※첨부) **y 실측**(표 뒤 텍스트가 표 위로 올라가던 문제 수정 — mdTableAt 앵커링) ②뷰어 동일 순서 파리티+JS 오류 0(뷰어 parseMdTable에 before/after 추가) ③노트 문단 팝업 ⧉(머리말→루트 문단 노트 경유)=텍스트 복사+복사됨 ✓ ④**복사 아이콘 통일**: 노드 코드/표·아웃라인 코드/표 전부 '⧉'만(호버 툴팁). e2e56·63·73의 옛 '⧉ 복사' 라벨 기대도 현행화 | 04-node-content §14.6, user-guide 03·05 |
| e2e73 | 표 복사(⧉)·HTML 표 붙여넣기 | ①노드 표 ⧉ → **클립보드 text/html(`<table>` + 인라인 테두리 스타일 — 웹 에디터에 테두리째 붙음)+text/plain(TSV) 동시** 실측(clipboard-read 권한)+복사됨 ✓ ②아웃라인 표 ⧉ 동일 ③노트 표 팝업 ⧉('노트로' 불러온 ⊞ 인디케이터 경유) ④웹 표(text/html) Ctrl+V → 하위 노드 = 파이프 MD 표 격자(마커 숨김)+⧉ ⑤**MD 내보내기 = 파이프 표 형식 저장** ⑥편집 중 붙여넣기도 파이프 변환(textarea.value 실측) ⑦뷰어 노드 표 ⧉ 클릭 피드백+JS 오류 0. 함정: MD 내보내기 버튼 title은 "내보내기 (Markdown…" | 04-node-content §14.6, user-guide 03 |
| e2e72 | 편집 중 마크 라이브 미리보기 (B6) | 편집 진입=미리보기 레이어+textarea 글자 투명(캐럿만)·`==형광==`/`**굵게**`/백틱 타이핑 즉시 노란 띠·가짜 굵게(text-shadow)·회색 띠·마커 흐림(0.35)·**미리보기 텍스트=원문 1:1(글자 수 불변)**·미리보기↔textarea 메트릭 완전 동일(캐럿 정렬 전제)·커밋 후 기존 렌더(마커 숨김)·코드 펜스 줄 흐림+블록 안 띠·스마트 Enter 불변. 함정: SVG 코드 패널 공백은 NBSP일 수 있어 공백 유연 매칭 | 02-node-editing §18, user-guide 03 |
| e2e71 | 색상 슬라이더 드래그 성능 | 피커 드래그 재현(15ms 간격 input 41발+change): ①반영 100ms 스로틀 — MutationObserver 실측 41입력→~7회 반영 ②마지막 값 반드시 반영 ③**드래그 전체 = undo 1단계**(첫 변경만 기록, 이후 setHistoryPaused — 되돌리기 1번에 드래그 전 색) ④단발 설정(input+change 1회, 기존 테스트 패턴) 동작 불변 | 05-node-style §4 |
| e2e70 | 템플릿 적용 = 지정 노드 색 보존 | 사용자 재현 절차 그대로: 칸반에서 카드 3개에 서로 다른 채움 지정 → 템플릿 라이브러리 '트리-진행트리맵' **적용** → ①칠한 3개 색 유지(최빈값과 다른 색=강조 보존) ②칠하지 않은 노드는 템플릿 레벨 스타일로 교체(갈아입히기 유지) ③되돌리기 1번=적용 취소. 함정: "첫 노드=레벨 대표" 휴리스틱은 처음 칠한 노드가 대표가 되어 보존 실패 — 최빈값(다수결)으로 판정 | 05-node-style §4, user-guide 02 |
| e2e69 | 색상 전 경로·팝업 위치·첨부 열기 | 2026-07-31 4차: ①색상 매트릭스 — 라이트 채움/테두리/글자 즉시 반영·**하이라이트 + 지정 글자색 = 지정색 우선(버그 수정)**·다크 왕복 불변·루트 동일·뷰어 tspan 파리티+JS 오류 0 ②칸반 채움 지정 → 진행트리 전환 = **색 유지** (보고 재현 방어 — 스토어·레이아웃 경로 모두 이상 없음 실측) ③링크 19개: 팝업이 **노드 오른쪽**(left ≥ 노드 right) + 19항목 + **호버 강조**(배경·700·↗, 비호버와 대비) ④첨부 팝업 동일 + **txt = blob 새 탭**(MIME 보정, 본문 실측) ⑤**docx = 즉시 다운로드**. 함정: '문서 선택'은 button이 아니라 label, 넓은 노드는 fit 후 좌표 클릭 | 05-node-style §4, 04-node-content §14.5, user-guide 05 |
| e2e68 | 칸반 Del·러버밴드·카드 색 + 되돌리기/히스토리 분리 | 2026-07-31 3차: ①칸반 카드 클릭+Del=서브트리 삭제(undo 복구) ②빈 영역 드래그=러버밴드 다중 선택(강조 표시)→스타일 탭 "N개 일괄" + 채움 일괄=카드 배경 반영→Del=**일괄 삭제 undo 1단계**(deleteNodesBulk) ③카드 색: 어두운 채움→밝은 고정 글자·테두리·글자색 지정 우선 (맵 styledText 규칙) ④**되돌리기/히스토리 분리**: undo=세션 한정 99단계(카운터 두 자리), 히스토리 패널=가짜 목록 없이 안내(서버 저장 연결 시 저장일시별 버전·새 맵 복귀 규칙 예고). 함정: 인스펙터 탭이 직전 '레이아웃'을 유지하므로 색 입력 전 '스타일' 탭 클릭 필요 | 08-layout §7.6, 12-history-undo-redo, 13-version-history, user-guide 04·09·10 |
| e2e67 | 칸반 스크롤·커스텀 색 다크·undo 카운터 | 2026-07-31 2차 3건: ①커스텀 색 노드 = 라이트/다크 전환에 글자색 불변 — 밝은 채움→#1F1B16·어두운 채움→#F8F5EF 고정, 글자색 지정 존중, 기본 노드는 테마 따라 변경(대조군), **뷰어 다크 토글 파리티** ②undo 카운터(`undo-depth`): 0→-1→-2→redo -1→새 편집 0 ③칸반 코드/표 스크롤 영역(`data-html-scroll`) 드래그 = 카드 이동 없음 + **보드 dragStart 차단**(텍스트 선택 후 네이티브 드래그가 포인터 이벤트를 가로채 다음 카드 드래그가 먹통 되던 실버그 — probe로 격리) + 일반 영역 카드 드래그 정상 | 05-node-style §4, 08-layout §7.6, 12-history-undo-redo, user-guide 01·04·09 |
| e2e66 | 칸반 폭·undo 레이아웃·뷰포트 보존 | 2026-07-31 보고 3건: ①큰 MD→칸반 전환 시 카드(넓은 표·코드·긴 URL)가 **컬럼 폭(300px)을 넘지 않음** + 표/코드는 카드 안 가로 스크롤 실측 ②칸반 전환→**되돌리기 = 이전 레이아웃(맵)까지 복원**(history `{map, layout}` 엔트리) + 되돌리기 2=샘플 맵, 다시 실행 2=칸반 복귀 ③맵 전체 맞추기→100% 보기→아웃라인 전체 왕복 = **줌·위치 그대로**(Canvas 재마운트 fit 재실행 가드 — 칸반 왕복·undo 복귀 포함) | 08-layout §7.6, 12-history-undo-redo, user-guide 04 |
| e2e65 | 아웃라인 열기 = 보던 위치 | 아웃라인 분할/전체 진입 시 **선택 노드(있으면) 또는 맵 화면 중앙 최근접 노드**의 행으로 스크롤 (was.md 110노드 — 검색 중앙 이동 후 선택 해제 → 분할/전체 열기 → 해당 행 가시 + 첫 행은 지나감 실측. 전체 모드는 맵 DOM이 사라지므로 토글 시점 캡처 검증) | 10-canvas, user-guide 04 |
| e2e64 | 레이아웃 탭 선택 없음 = 맵 전체 | 선택 해제 상태에서 레이아웃 버튼 **전부 활성** + "선택한 노드가 없어 맵 전체에 적용됩니다" 안내 + 클릭 = 전체 맵 레이아웃 변경(상태바 실측)·하위 노드 선택 시 메인 전용 비활성 기존 동작 유지 | 08-layout, user-guide 04 |
| e2e63 | 리치 노드 P4(표면 정합) | 아웃라인 행·칸반 카드/헤더: 체크 글리프(**클릭=원문 토글, 맵 동기**)·코드 모노 패널(**⧉ 복사→복사됨 ✓** + **언어 라벨(✎)→팝업 편집기** — 맵 패널과 동일 헤더)·표 격자·마커 원문 숨김 / 검색(에디터·뷰어)·뷰어 아웃라인·인스펙터 요약: ☑/☐·⧉코드·⊞표 접기 표시·뷰어 JS 오류 0. (e2e13의 낡은 셀렉터(첫 svg·svg textarea·옛 툴바 타이틀·붙여넣기 정책)도 현행 UI 기준으로 갱신) | rich-node-content P4 |
| e2e61 | 리치 노드 P3(배치 옵션·A4) | 새 맵 패널 "MD 블록 배치" 라디오(기본 노드·기억)·**노드 배치**: 인용문·코드 패널·표 격자·체크 글리프가 노드 본문에·**A4 초과** 블록 → 노트 이동 + 안내 문구·**노트 배치**: 기존 방식(노드 블록 0). (e2e29/31/54는 '노트로'를 명시 설정해 노트 규칙을 계속 검증) | rich-node-content P3, markdown-export §1.0 |

### 2.9 AI 생성

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e43 | AI 생성 플로우 | 툴바→AI 탭·키 미등록 비활성·EMM 템플릿 내장·(모킹) 응답→맵·기록·401 오류·영속 | 18-ai |
| e2e44 | AI 설정 개선 | 우선순위·자동 해석·모델 선택/직접 입력·프롬프트 v3·마이그레이션·**Gemini 실시간 모델 목록 조회**·키 발급 도움말 | 18-ai |
| e2e52 | AI 노드 확장(프로젝트 워크스페이스) | 선택 노드 확장 — 맥락에 루트 지침+직계 조상 경로(옆가지 제외)·@소스 노드 참고·cache_control 마킹·답변 하위 노드 삽입·전달범위 도움말 | ai-project-workspace |

### 2.10 서버 연결 (백엔드)

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| smoke(api) | 백엔드 맵·노드·autosave·문서 | CI backend 잡에서 임시 Postgres로 매 PR 실행 — 맵 CRUD·노드 CRUD·ltree 이동·순환 차단·autosave(버전·멱등)·문서 스냅샷 손실 없는 왕복·소유권 격리·404 | backend-phase1 |
| e2e-cloud | 클라우드 저장/열기(풀스택) | 프론트 ☁ 메뉴 → 루트 편집→저장(토스트)→새로고침 리셋→목록→열기→마커 복원(실제 백엔드+DB 왕복)·JS 오류 0 | backend-phase1 |
| e2e-cloud2 | 클라우드 사용성(풀스택) | 자동 저장이 수동 저장 없이 서버 반영(디바운스)·목록 이름변경 반영·삭제 후 목록 비움·JS 오류 0 | backend-phase1 |

### 2.11 서버 저장·문서함·세션

> 백엔드(로컬 PG + API)와 함께 도는 프런트 E2E — 서버 저장·히스토리·
> 문서함·로그인/세션·편집 잠금 계열 (e2e79~e2e94).

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e79 | B7 맵 닫기 + 서버 맵 불러오기 | 구성: 로컬 PG16 + API :3000(dev 모드) + vite :5199. ①'☁ 서버 맵 불러오기'가 활성(disabled·"서버 연결 후" 배지 없음) ②저장된 맵에서 **상단 툴바 ✕ 맵 닫기**(2026-08-02 좌측 패널에서 이동) → 캔버스가 '문서 없음' 1노드로 비고 문서 목록 모달 자동 표시 + 서버에 저장됨(목록 1건) ③목록에서 다시 열면 닫기 직전 내용 복원(중심 주제 표식) ④**새 페이지 격리** — 아직 저장한 적 없는 맵도 **확인 대화상자 없이** 저장하고 닫힘(목록 2건) ⑤닫기 후 Ctrl+Z 로 노드 수 완전 복구(30=30). 함정: 맵 제목(map.title)은 중심 주제와 별개라 목록에서 표식으로 못 찾는다 → 열어서 내용으로 검증 · 모달/패널이 캔버스 클릭을 가로막아 시나리오는 새 페이지로 분리 | backlog B7, user-guide 08 |
| e2e80 | B8 히스토리(저장 버전 이력) | 구성: 로컬 PG16(map_document_versions) + API :3000 + vite :5199. ①클라우드 미연결 → 히스토리 패널이 안내만(목록 없음) ②☁ 저장 → v1 1건(저장일시·제목·KB 표시) ③내용 변경 후 재저장 → 2건, **최신이 맨 위** ④**편집만 하고 자동저장 대기(3s) → 버전 안 늘어남**(용량 정책 검증) ⑤v1 '**새 탭으로**' → `제목_history_YYMMDD_HHMM` 새 맵이 만들어져 **브라우저 새 탭(`?map=<id>`)** 에서 열리고 내용이 v1 시점, **현재 탭의 맵은 최신 그대로**, 목록에 원본+복원본 2건 (2026-08-02 탭 모델로 갱신 — 안내 문구는 3초 뒤 사라지므로 새 탭 로딩 **전에** 읽어야 한다). 별도 **API 7단언**(version-api-test.mjs): keepVersion 없으면 0건 · 있으면 version 1→2 · 목록은 doc 미포함 메타만 · 버전별 doc 정확 · 현재 문서는 마지막 저장분 · 없는 버전 404 · 타 사용자 접근 404 | 13-version-history §0, backlog B8 |
| e2e81 | 로그인 UI 정리(게이트·계정 메뉴·탭 모델) | 구성: 로컬 PG16 + API :3000(dev, `CORS_ORIGIN=http://localhost:5199`) + vite **:5199(인증 꺼짐)** / **:5198(`VITE_SUPABASE_URL` 주입 = 인증 켜짐)**. ①미로그인 → 소개(슬로건)+로그인만, `map-save`·캔버스 **없음**(완전 차단) ②SNS 제공자 자리 3종(준비 중) ③개발 모드는 게이트 없이 에디터 ④상단 = ☁ 저장 + ✕ 맵 닫기, 옛 `cloud-menu` 제거 ⑤아바타 메뉴(개인설정·프로필·구독) ⑥좌측 '서버 맵 불러오기' 하나(`cloud-open` 중복 제거) ⑦맵 닫기 = 저장 후 '문서 없음' + 내 문서 목록 ⑧빈 상태에서는 **현재 탭**에 열림 ⑨히스토리 복원 = 새 탭 ⑩편집 중 다른 맵 = **새 탭(`?map=<id>`)**, 현재 탭 유지 ⑪`?map=` 탭이 그 문서를 로드 ⑫이미 이 탭에서 편집 중인 맵은 새 탭 안 만듦. 함정: `window.open(...,'noopener')` 는 **항상 null** 을 돌려줘 팝업 차단으로 오인 → noopener 없이 열고 `w.opener=null` · `CORS_ORIGIN` 은 **콤마 목록을 못 받는다**(단일 출처만) | auth-session-ui.md, user-guide 01·08 |
| e2e82 | 문서함(폴더·저장 규칙·문서 브라우저·맵 유형) | 구성: 로컬 PG16(map_folders + maps.folder_id/kind) + API :3000(dev) + vite :5199. ①새 맵은 제목 입력칸 없이 '새 맵'으로 시작 ②첫 ☁ 저장 → **폴더·이름·유형 대화상자** ③대화상자에서 새 폴더 생성 + 협업맵으로 저장 ④같은 폴더 같은 이름 재시도 → **"이미 있습니다" 안내 + 저장 안 됨**(서버 409 메시지 그대로) ⑤목록은 팝업이 아니라 **편집 영역 문서함**(옛 `cloud-list-modal` 없음) ⑥폴더 진입 시 그 폴더 맵만 + breadcrumb · **이름/수정일 오름·내림 정렬**(같은 열 재클릭) ⑦유형 배지 👤/👥 표시·토글 ⑧서버 맵을 열어 중심 주제를 바꾼 뒤 저장 → **대화상자 없이 열었던 이름 그대로**(중심 주제 변경이 맵 이름을 바꾸지 않음) ⑨미저장 맵 닫기 → **"저장되지 않았습니다" 경고** → 저장 없이 닫기 ⑩저장된 맵 닫기 → 경고 없이 저장 후 문서함. 함정: **새 맵·파일 불러오기가 서버 링크를 끊지 않으면** 새 문서의 자동저장이 직전 서버 맵을 덮어쓴다(실측 → `detachFromServer()`) · 편집 중인 탭에서는 다른 맵이 새 탭으로 열리므로 이 탭 검증 전에 먼저 닫아야 한다 | document-library.md, user-guide 02·08 |
| e2e83 | 스키마 미적용 배포의 진단 가능성 | 구성: **문서함 도입 이전 스키마** DB(oldschema — schema 적용 후 map_folders DROP + maps.folder_id/kind DROP 로 재현) + API :3010 + vite :5197. ①`/v1/health` 가 `missingTables:[map_folders]`·`missingColumns:[maps.folder_id,maps.kind]` 로 원인을 짚는다 ②`/v1/folders` 가 500 "Internal server error" 가 아니라 **503 + `npm run db:apply` 안내** ③문서함 화면 상단에 그 안내가 **빨간 배너**로 그대로 보인다. 배경: 2026-08-02 dev 배포에서 스키마 적용을 빠뜨려 화면에 "Internal server error" 만 떴다 — 코드는 자동 배포·스키마는 수동인 구조의 대표 사고 | document-library.md, dev-server-runbook §0 |
| e2e84 | 로그인 후 빈 화면 + 폴더 이동 선택창 | 구성: API :3000(dev) + vite :5199(인증 꺼짐) / :5198(인증 켜짐, `emm.auth` 에 가짜 세션 주입해 게이트 통과). ①인증 켜진 배포에서 로그인 상태로 부팅 → **캔버스 노드 0개**(샘플 맵 없음) + 문서함 자동 표시 ②그 상태에서 '새 맵 만들기' → 문서함 닫히고 편집 화면 ③문서함 📂 = `window.prompt` 가 **아니라** 폴더 선택 창(클릭 가능한 목록, dialog 이벤트 발생 없음) ④폴더를 골라 '여기로 옮기기' → 홈에서 사라지고 그 폴더에 나타남. 배경: 로그인하면 샘플 맵이 떠 헷갈린다 · 폴더 이동이 번호 입력이라 고를 수 없다(2026-08-02 사용자 지적) | document-library.md §5·5-1 |
| e2e85 | dev 실사용 보고 4건(빈 상태·유형 제거·레이아웃 왕복·재로그인 리셋) | 구성: API :3000(dev, **CORS_ORIGIN 콤마 목록** `5199,5198`) + vite :5198(가짜 세션). ①빈 상태 ☁ 저장 → 대화상자 없이 "열려 있는 맵이 없습니다" ①b저장 대화상자에 **맵 유형 선택 없음**(협업맵은 초대·승인으로 전환) ②맵 닫기로 undo 이력이 남은 **빈 문서**에서 새 맵 → "'문서 없음'을 닫을까요?" 확인 없음 ③**레이아웃 왕복** — 레일 '레이아웃' 탭에서 트리·아래로 바꿔 저장 → 재부팅 → 서버에서 열면 복원(스냅샷 v2 editor. 옛 't' Tweaks 패널은 삭제됨) ④로그아웃→재로그인 → 문서함 '편집 중' 배지 없음 + 맵 정상 열림(세션 전환 리셋: 문서·링크·undo) ④b편집 중 맵 클릭 = 거부가 아니라 편집 화면 복귀 ⑤유형 배지 SPAN(표시 전용). 함정: v1 스냅샷은 editor 가 없어 레이아웃을 복원할 수 없다(현재 값 유지) · CORS_ORIGIN 이 단일 출처면 두 번째 프런트가 전부 차단된다(이 테스트에서 실측 → 콤마 목록 지원 추가) | document-library.md §3·5-0·6 |
| e2e86 | UI 4건 + 에디터-뷰어 링크/첨부 파리티 | ①**Tweaks 시안 패널 삭제** — 어느 빌드에서도 t 키에 반응 없음(코드째 제거) ②문서함 맵 행 아이콘 = 앱 SVG(🗺 이모지는 윈도에서 깨져 교체) ③접힌 레일에서 **나브·속성 아이콘 어느 쪽을 눌러도** 패널 자동 펼침(이중 토글 버그 수정 — 펼침은 store 의 setNavTab/setInspectorTab 이 담당) ④뷰어 파리티: 마커 호버 툴팁 = 에디터와 동일(복수 = `링크/첨부파일/멀티미디어 N개`, 단수 = 이름/URL), 복수 링크·첨부·미디어 클릭 = 우상단 상세 패널이 아니라 **마커 옆 선택 팝업**(#mm-chooser — 호버 강조·Esc/바깥 클릭 닫기). 루트 노드의 링크·첨부를 써서 **루트 콘텐츠가 뷰어 DATA 에서 통째로 빠지던 버그**와 **루트 첨부가 ZIP files/ 에 패키징되지 않던 버그**도 함께 검증. 함정: 전역 커스텀 툴팁이 **호버 중인 요소의 title 속성을 떼어** 둔다 — title 로 상태를 판정하려면 먼저 마우스를 치워야 한다 · 첨부가 있으면 내보내기는 ZIP(unzip 후 안의 .html 검사) | 20-export.md·10-canvas.md §29 |
| e2e87 | 첨부 원본 생존성 + 내보내기 안내 (dev 보고: ZIP이 아닌 html만 생성) | 원인 = 첨부가 blob: URL(브라우저 세션 한정) → 저장 후 다시 연 맵은 원본 사망 → 내보내기 fetch 전부 실패 → **묵묵히 html 폴백**. ①≤2MB 첨부는 data URL 내장(attachmentFile.ts) — 저장→**새로고침**→다시 열기 후 내보내기 = ZIP + files/ 내용 원본 일치 ②그 뷰어의 첨부 마커 tip = 파일명 ③>2MB(3MB로 실측)는 **로그인 안 한 시안 빌드(:5199)에서** 같은 세션 = ZIP, 재열기 후 = HTML + 툴바 토스트 '원본을 찾을 수 없어 HTML만' (로그인 상태는 서버 업로드로 유지 — e2e88) ④그 HTML 뷰어에서 첨부 = 죽은 blob 링크가 아니라 '(파일 없음)' 비활성 행. 함정: 새 맵을 만들기 전에 열려 있는 맵을 먼저 닫아야 확인 게이트에 안 걸린다 · file:// 로 열 파일은 .html 확장자여야 한다(.bin은 다운로드됨) | 20-export.md 첨부 원본 생존성 |
| e2e88 | B9 첨부 저장소 (방식 A: 로컬 디스크 드라이버) + 쿼터 | 구성: API :3000 에 `STORAGE_LOCAL_DIR` 지정 + :5198(가짜 세션 — dev API 는 토큰 무시). ①로그인 상태 3MB 첨부 = **서버 업로드** (스토리지 `u/<uid>/<id>` 파일 + attachments 행 + 인디케이터) ②저장→새로고침→다시 열기→내보내기 = ZIP 에 3MB 원본 포함(서버 저장소라 세션 종료에도 생존) ③계정 메뉴에 저장 용량(문서+첨부 합산/한도) 표시 ④psql 로 quota_bytes 축소 → 업로드 413 거부 + 첨부 탭 '저장 용량 한도를 초과' 안내 + 파일 수 불변 ⑤첨부 ✕ 삭제 = attachments 행 + 스토리지 파일 동시 삭제 ⑥**맵당 내장 합계 상한 10MB** — 1.9MB×6 첨부 시 5개 내장 + 6번째부터 서버(DB 행 1·스토리지 1) ⑦단수 첨부 클릭 = 토큰 부착 blob 새 탭(직행 URL 401 방지). 함정: 다시 연 맵은 기본 탭이 '스타일'이라 첨부 입력이 없다(노드 선택 + 링크·첨부 탭 재진입 필요) · 잔재 첨부가 쿼터 표시 검증을 흔든다(시작 시 attachments 비우기) | document-library.md 델타 SQL·dev-server-runbook §1.5 |
| e2e89 | UI·히스토리 5건 (2026-08-03) | ①새 맵 패널 '새 맵 만들기' **단일 버튼**(이중 트리 제거) — 클릭 즉시 시작+템플릿 선택 ②로그인 상태 좌측 '서버 맵 불러오기' 없음 + 상단 breadcrumb **'내 문서' 클릭 = 문서함**(crumb-docs) ③**다른 이름으로 저장**(map-save-as) — 기본값 '<이름> 사본', 원본 유지+새 맵 생성+탭 전환, 같은 폴더 같은 이름 409 안내 ④히스토리 버전 상세(history-detail) = 레이아웃·노드수·문서/첨부 크기. 함정: SaveMapDialog 는 Esc 로 닫히지 않는다(취소 버튼 클릭) | 13-version-history.md·document-library.md |
| e2e90 | 템플릿 선택 중 메뉴 숨김 + 무변경 저장 스킵 + 첨부 개수 표시 (2026-08-03 2차) | ①템플릿 선택 카드(tpl-choose)가 떠 있는 동안 아래 메뉴('새 맵 만들기'·'Local 파일 불러오기') **전부 숨김**, 건너뛰기 후 복귀 ②서버맵을 **조회만 하고 맵 닫기** → 히스토리 버전 생성 없음 + `maps.updated_at` 도 그대로(문서함 정렬 불변) ③편집(Tab 자식 추가) 후 닫기 = 버전 +1 ④무변경 상태 명시적 ☁ 저장 = 토스트 "변경된 내용이 없어 그대로 두었습니다" + 버전 그대로 ⑤히스토리 상세에 **첨부 N개 · 크기**(내장 data URL 환산 + 서버 합). 근본 원인: 새 맵은 edgeType·children 기본값 없이 저장 → 재열기(로드 정규화) 후 같은 내용인데 jsonb 가 달라짐 → buildSnapshot 이 로드와 같은 정규화를 거치도록 수정. 함정: 서버 비교는 **마지막 버전과도** 해야 한다(레거시 맵은 버전 0개 → 첫 버전은 남김) | 13-version-history.md §무변경 저장 |
| e2e91 | 웹 AI 클립보드 왕복 (방법 A, 2026-08-03) | 구성: vite :5199 + Playwright `grantPermissions(clipboard-read/write)`. ①키 미등록 = **웹 AI 모드 기본**(모드 스위치 web/api) ①b새 맵 템플릿 카드 "AI로 초안 만들기" → AI 탭 웹 모드 ②[① 프롬프트 복사] = EMM 규칙+주제+"코드블록 하나로만" 지시 + 복사됨 플래시 ③잡담+```emm 코드블록``` 답변 paste → **즉시 맵 생성**(코드펜스 추출, 4노드·notice) ④잡담만 paste = 실패 안내 + 재요청 문구 복사 ③b**~~~ 물결 펜스**(안에 ``` 코드 펜스 포함 — Copilot GPT 형식) 정상 추출 ③c바깥 ``` 가 안쪽 ``` 에 조기 잘리는 답변 = 펜스 제거 폴백(answerFromPaste)으로 복구 ⑤선택 노드 확장 프롬프트 복사([확장할 노드] 포함) + 답변 [선택 노드에 삽입] = 하위 +2 ⑤c**실사용 재현(2026-08-04)**: 렌더 드래그 복사 답변(전체 맵 펜스 없음 + 작은 ```text 예시 블록만 펜스 → 최장 블록 추출이 예시 조각을 집음) = 삽입도 후보 체인(mapSourceCandidates)으로 성공 ⑥API 키 모드 회귀. 출력 지시는 **~~~ 물결 펜스 강제**(렌더돼 나오는 답변 방지). ③d**템플릿 v4(2026-08-04)**: ###### 6레벨 헤딩 = 깊이 6 맵 + `>` 인용문 = **노드 본문 블록으로 캔버스 표시**(AI 경로 blockPlacement 'node' — 코드 블록은 자식 노드로 분리돼 [3b] 기대 노드 수 3→4 현행화). 함정: 답변 paste 는 합성 이벤트(값 주입 native setter+input 이벤트, 그 뒤 ClipboardEvent) — Ctrl+V 는 headless 포커스에 따라 흔들린다 · 캔버스 텍스트 검사는 `svg g[data-node-id]` 로(첫 svg 는 툴바 아이콘) · AI 레일 아이콘 title 은 'AI 생성'이 아니라 **'AI'** · strict:false 라 유니언 내로잉은 `'reason' in res` 방식 | web-ai-clipboard.md·18-ai.md |
| e2e92 | 로그인 UX 4건 + Guest 체험 (2026-08-04) | 구성: :5198, **깨끗한 컨텍스트(=시크릿창)** 와 가짜 세션 컨텍스트 2개. ①SNS = **카카오→네이버→Google**(Apple 없음) + Guest 버튼 ②Guest 진입 = 에디터(샘플 맵)+좌측 **새 맵 패널**+'서버 맵 불러오기' 없음 ③'내 문서' = **GuestBrowserNotice 안내 화면**(빈 목록 X)+가입 유도+계속 체험 ④☁ 저장 = "가입 후 가능" 토스트 ⑤첨부 = 안내만(파일 입력 없음) ⑥AI = 웹 AI 전용(모드 스위치·AI 설정 없음) ⑦계정 메뉴 'Guest 체험 중'+[가입하고 시작하기]→로그인 화면 ⑧**로그인 첫 화면 = 문서함+좌측 새 맵 패널**(스타일 탭 아님 — 시크릿창 재현). 로고: 초기 화면 72px+워드마크 40px, Mark/Down 글자 14(세 도안 공통) | auth-session-ui.md §8·9 |
| e2e93 | 맵 단일 세션 편집 잠금 (2026-08-04) | 구성: :5198 가짜 세션 컨텍스트 2개(A·B — dev API 는 토큰 무시라 같은 사용자). ①A 저장 = map_edit_locks 1행 ②B 같은 맵 열기 = **🔒 읽기 전용**(readonly-badge 배너 상시 + 토스트 + 서버 링크 없음 → saveAs 숨김) ③읽기 전용 ☁ 저장 = 새 맵(사본) 저장 대화상자 ④다른 세션 키의 직접 PUT = **409** "다른 세션(브라우저)에서 편집 중" ⑤a읽기 전용 맵 닫기 = 저장 없이 닫힘 안내 ⑤A 닫기(잠금 해제, 0행) → B 재열기 = 편집권 획득(배너 없음·saveAs 표시). 함정: B 탭에 읽기 전용 문서가 열려 있으면 canReuseThisTab 이 거짓 → 재열기가 **새 탭 경로**로 빠진다(먼저 map-close) · 하트비트 25s/TTL 60s 는 시간상 e2e 미검증(수동·코드 리뷰) | auth-session-ui.md §10 |
| e2e94 | 기본 기능 5건 (2026-08-04 실사용 보고) | :5199. ①새 맵 기본 골격 = **11노드**(주제1·2: 하위주제+내용2, 주제3: 하위주제 — 사용자 지정 이미지, documentStore.newMap·트리-진행트리·진행트리-트리 3곳 축소) ②**노드 복사**(Ctrl+C, 신규 nodeClipboard 내부 클립보드·시스템에는 토큰만) → 다른 노드 선택 → Ctrl+V = 서브트리가 그 노드의 **하위로**(15노드·토큰 텍스트 미유출 — 이전엔 복사 미구현이라 텍스트 경로로 잡탕이 붙었다) ③칸반 **컬럼 헤더 클릭 = 선택**(신규 — 선택 불가라 Del 도 불가했다) → Del = 컬럼 삭제 ④칸반 우상단 **보기 도구**(CanvasFloatingToolbar kanban 모드: 선택 카드 스크롤·보드 처음·전체화면 / Pan·접기 숨김) ⑤Kanban 템플릿 = **주제1~4 × (하위주제+내용2)**(구 백로그→완료). ⑥**칸반 모드에서도** 카드 복사(Ctrl+C) → 컬럼 선택 → 붙여넣기 = 서브트리가 하위로(+3 카드, 2026-08-04 2차 — KanbanBoard 에 같은 내부 클립보드 배선). 함정: 노드가 선택돼 있으면 레이아웃 패널이 서브트리 모드라 Kanban(맵 전용) 클릭이 무시된다 — Esc 로 선택 해제 후 전환 · Ctrl+C/V 는 Playwright 실키(clipboard 권한) | 10-canvas.md·nodeClipboard.ts |
| e2e123 | 실사용 9건 — 로그인 문구·문서함 머리글/검색·상태바·템플릿 이름·시간배치(중앙노드) (2026-08-07) | 구성: PG(TCP)+API :3000, vite :5198(로그인 게이트)/:5199. ①로그인 화면에서 "계정별로 분리" 안내가 사라졌다(당연한 말은 안 쓴다) ④좌하단 상태바에 **가짜 노드수·depth·좌표가 없다** — 예전 값은 하드코딩이라 화면이 바뀌어도 그대로였다("정보가 반영되지 않음" 보고의 진짜 원인) ⑤"Supabase 동기화됨"도 없다(저장 상태는 상단 배지가 사실대로 말한다) ⑥대신 **이 맵의 무게**(문서 크기·첨부 수·첨부 용량) ⑥b**편집하면 문서 크기가 따라 움직인다**(가짜 값이 아님을 실증 — 4000자 붙이면 3.0KB→6.9KB) ⑦새 맵 템플릿 카드에 'AI로 초안 만들기'가 없다(AI 진입점은 AI 탭 한 곳) ⑧템플릿 이름 = **방사형 양쪽 / 시간배치 (타임라인) / 계층형 오른쪽** ⑧b시간배치 템플릿은 **레이아웃도 `timeline`** ⑨`timeline-center` = 주제가 루트 **좌·우로 갈리고** 시간축이 관통한다(실좌표) ②머리글 = `폴더/파일명` + **같은 줄 오른쪽**에 맵/폴더 수량 ②b숫자 합계 4종은 **열 이름 위** ②c제목 열은 1줄(nowrap) ③검색창 = 이름 포함만 남는다 ③b합계·수량도 검색 결과 기준으로 다시 센다. 함정: 템플릿 카드에 가려면 **왼쪽 레일 `새 맵` 탭 → `새 맵 만들기` → `현재 맵 닫고 계속`** 3단계를 실제로 밟아야 한다(한 번에 뜨지 않는다) · 템플릿 버튼에는 testid 가 없어 `[data-testid="tpl-choose"] button b` 로 읽는다 · **뒷정리 필수** — 이 테스트가 만든 맵을 남기면 e2e96 의 노드수 정렬 데이터셋이 흔들린다 | 08-layout.md §22.1·08-내보내기-불러오기.md |
| e2e187 | **HTML 뷰어 — 접힌 채 내보낸 진행트리 맵을 펼치면 맵이 사라졌다** (2026-09-04 사용자 보고) | 구성: 진짜 Chromium 이 dev 서버 모듈 `buildStandaloneHtml` 로 **1단계 11개가 접힌 진행트리 맵**(총 95노드 · 코드·표·체크 포함)을 내보내 `file://` 로 연다. **8항목**. ①초기 화면 1단계 12개 · NaN 없음 ①b**숨은 노드까지 전부 `pos` 가 있다** ②★ 모두 펼치기 → **95개 전부** 그려진다 ②b★ NaN 좌표 없음 ②c콘솔 오류 없음 ③모두 접기 → 12개 ③b★ 1단계 +N 칩 하나를 누르면 그 아래만 펼쳐지고 NaN 없음 ④★ **숨은 노드 83개의 `pos` 를 지운 채(옛 파일 모양) 펼쳐도** 뷰어가 스스로 재서 95개를 그린다. **되돌려 깨뜨리기** — 수정 전 같은 스크립트에서 모두 펼치기 뒤 **노드 2개만 남고** `path d` 에 `V NaN H NaN`, `rect x` 에 `NaN` 콘솔 오류 — 사용자 화면(백지)과 같은 모양. **왜 고쳤나** — 레이아웃 엔진이 접힌 노드의 자식을 배치에서 빼서 그 아래 노드에 `pos` 가 없었고, 뷰어 `reflowFixed.prep` 이 `pos` 있는 자식만 따라 들어가 크기 없는 블록을 더했다. 내보내기는 전부 펼친 사본을 한 번 더 배치해 빠진 크기를 채우고, 뷰어는 `pos` 없는 자식을 `measure` 로 스스로 잰다. 함정 1가지 — **위치가 아니라 크기가 전부다**: 숨은 노드의 x·y 는 뷰어가 다시 계산하므로 펼친 사본의 좌표를 그대로 써도 화면이 어긋나지 않는다 | 20-export.md(내보내기 방식 표) |
| e2e186 | **HTML 뷰어 아웃라인 양방향 동기 — 맵 노드 선택 → 아웃라인 행으로 이동** (2026-09-04 사용자 보고) | 구성: 진짜 Chromium 이 dev 서버 모듈 `buildStandaloneHtml` 로 **가지 60개 맵**(아웃라인이 넘치도록)을 내보내 `file://` 로 연다. **11항목**. ①아웃라인이 넘친다 ②아래쪽 행(b50)은 처음엔 안 보인다 ③맵의 '가지 50' 노드 클릭 ④★ 그 행이 `sel` 로 강조된다 ⑤★ 아웃라인이 그 행으로 스크롤돼 보인다(scrollTop 2131) ⑥다른 노드(가지 3)를 고르면 위로 돌아온다 ⑥b이전 행 강조는 지워진다 ⑦★ **선택이 그대로면 접기/펴기로 다시 그려져도 스크롤을 끌고 가지 않는다**(0 → 0) ⑧아웃라인 행 클릭 = `on`+`sel` 한 행만 ⑨맵에서 그 노드가 노란 강조(반대 방향 유지) ⑩빈 캔버스 클릭으로 선택을 풀면 아웃라인 강조도 사라진다. **왜 고쳤나** — 뷰어 `buildOutline` 이 검색 강조(`SEARCHHIT`)만 보고 맵 선택(`SEL`)은 보지 않았고, 스크롤은 아예 없었다. `render()` 끝의 `syncOutline` 에서 선택 행을 표시하고, 선택 id 가 바뀐 때만 `scrollIntoView` 한다. 함정 2가지 — ⑴**매 재구축마다 스크롤하면 안 된다**(접기 한 번에 화면이 튄다) — 마지막으로 스크롤한 id 를 기억한다 ⑵Playwright 의 `click()` 은 대상을 화면에 보이게 **먼저 스크롤**하므로 ⑦ 같은 검사에서는 DOM 이벤트로 눌러야 한다(테스트 함정) | 20-export.md(아웃라인 분할) |
| e2e185 | **HTML 뷰어 아웃라인이 코드·표·체크를 에디터와 같이 그린다 + `?map=` 안내 자동 해제** (2026-09-04 사용자 보고) | 구성: 진짜 Chromium 이 dev 서버 모듈 `buildStandaloneHtml` 을 불러 **실제 내보내기 HTML** 을 만들고 `file://` 로 연다 + 가짜 GoTrue + 진짜 API·PG(⑬). **15항목**. ①HTML 생성(233KB) ②◫ 아웃라인 페인 ③★ 코드가 '⧉코드' 칩이 아니라 **실제 코드 줄**로 보인다 ④★ 코드 패널 = 언어 라벨(`bash`) + ⧉ 복사 + 모노 줄 2개(`.mm-ol-code`) ⑤★ 표가 '⊞표' 칩이 아니라 **격자 표**(th 2·td 4) ⑥셀 내용이 원문 그대로 ⑦★ `- [x]`/`- [ ]` = 체크 글리프 2개(완료 1) + `**굵게**` 마커 숨김 ⑧굵게가 `font-weight:700` ⑨코드 앞·뒤 텍스트가 **원문 순서** ⑩행 클릭 = 노드 강조(기존 동작 유지) ⑪전체 아웃라인 모드에서도 표 ⑫**검색 결과 한 줄 요약은 여전히 ⧉코드 로 접힌다**(그 규칙의 자리는 거기다) ⑬a·b·c★ 없는 id 로 `?map=` 을 열어 안내+문서함이 뜬 뒤 **문서함에서 맵을 열면 안내가 사라지고** 맵이 캔버스에 보인다. **왜 고쳤나** — 뷰어 `buildOutline` 이 검색 결과용 `flattenText`(접기)로 행 본문을 그려, 에디터 아웃라인(`NodeRichText`)과 달랐다. 뷰어에 있던 노트 패널 DOM(`.mm-code`·`.mm-table`)과 `parseMdCode`/`parseMdTable`/`parseInlineSegs` 를 그대로 써서 `richTextEl` 을 만들었다(`parseMdCode` 에 `after` 추가). 안내는 `urlMapErr` 상태가 한 번 서면 지워지는 길이 없었다 → 문서가 비어 있지 않게 되면 거둔다. 함정 2가지 — ⑴**VIEWER_JS 는 `String.raw` 템플릿이라 주석에도 백틱 3개를 쓸 수 없다**(템플릿이 끝난다 — tsc 가 잡았다) ⑵Playwright 의 `innerText` 는 코드 줄의 NBSP 를 그대로 주고 SVG `<text>` 에는 아예 없다(`textContent` 로 읽어야 한다) — 테스트 쪽 함정이지 제품 함정이 아니다 | 20-export.md(아웃라인 분할) · 14-save.md §7.12 |
| e2e184 | **토큰 갱신이 열린 문서를 지웠다 — 로그인 전환은 계정으로 판정 + 빈 자리표시 저장 거절 + `?map=` 탈출** (2026-09-04 사용자 보고) | 구성: 진짜 Chromium + vite dev :5199(`VITE_SUPABASE_URL`=**가짜 GoTrue :54321**, `expires_in=30` 이라 만료 60초 전 규칙에 걸려 **모든 API 호출 직전에 토큰 갱신**이 일어난다 — 실사용의 "로그인 1시간 뒤" 를 매 호출로 압축) + 진짜 API :3001(`AUTH_MODE=dev`) + 진짜 PostgreSQL 16 :5433. **23항목**. ①로그인 → 문서함 ②새 맵 11노드 ③a☁ 저장 대화상자를 여는 사이(폴더 조회) **갱신이 실제로 일어났다**(가짜 GoTrue 카운터) ③b★ 그래도 캔버스가 '문서 없음' 으로 안 바뀐다 ③c★ 문서함이 멋대로 안 열린다 ④a·b·c★ 저장된 서버 문서에 **가지 3개**·중심 주제가 '문서 없음' 이 아니다 ⑤a·b★ 연결된 맵의 재저장(갱신 동반) 뒤에도 문서·`cloudMapId` 가 그대로(다른 이름 저장 버튼 유지) ⑥a~f★ 없는 id 로 `?map=` 을 열면 **서버가 말한 이유**가 붙은 안내 + **문서함이 곧바로 열린다** · 빈 문서에서 '맵 닫기' 도 문서함으로 · 안내 ✕ 로 닫힘 ⑦a·b★ '문서 없음' 자리표시에서 `saveNewMap` 은 거절되고 서버에 맵이 안 생긴다 ⑧a·b·c `PUT …/document` 를 한 번 끊으면(`page.route`) `saveNewMap` 이 실패로 끝나고 **유령 맵(문서 없는 맵)이 문서함에 남지 않으며** 화면 문서는 그대로 ⑨회귀 — 로그아웃 → 재로그인 뒤에는 이전 문서가 남지 않는다. **되돌려 깨뜨리기** — 수정 전 같은 스크립트에서 ③b·③c·④b·④c·⑤a·⑤b·⑥b·⑥c **8항목 FAIL**(캔버스 '문서 없음' · 서버 문서 `branches=0, root='문서 없음'` · 문서함이 뒤에서 열림 · 연결 끊김) — 사용자 보고와 같은 모양이다. **왜 고쳤나** — `EditorPage` 의 로그인 전환 리셋이 `[session, guest]` 를 의존성으로 써서, `getFreshAccessToken` 이 만드는 **새 세션 객체**를 계정 전환으로 오인했다(`clearCurrentMap`). 함정 2가지 — ⑴**`session` 객체를 의존성으로 쓰지 않는다** — `userId` 가 계정의 정체다. 로그아웃(`null`)은 그대로 잡힌다 ⑵**서버의 빈 문서 가드(§7.2)는 새 맵을 못 막는다** — 덮을 것이 없기 때문이다. 그래서 `saveNewMap` 이 보내기 직전에 자리표시를 본다 | 14-save.md §7.12 · auth-session-ui.md §4-1 · document-library.md §5-0 |
| e2e183 | **EMM 선언의 레벨별 레이아웃이 그림에 반영된다 + levelLayouts 색인 통일** (2026-09-03 사용자 조사) | 구성: 진짜 Chromium 이 **dev 서버가 서빙하는 실제 모듈**을 `import('/src/utils/…')` 로 불러 호출한다(번들이 아니라 소스 경로 그대로라 `import.meta.env` 가 살아 있다) + `resolveDeclaration` 단위 14항목. **9항목**. ①`template: progtree-tree` 를 불러오면 **맵 전체 `process-tree-right` + depth 1~4 노드 전부 `tree-right`** ②`levels: {2: {layout: kanban}}` 도 같다 ③**선언 없는 .md 는 그대로** — `settings` null · 노드 `layoutType` 전부 null ④`settings.levelLayouts` 의 `[1]` 이 "2레벨"이다(설정 패널 `MapSettingsPanel:272` 가 읽는 칸) ⑤`applyLevelLayout(4, …)` 이 **depth 4 와 5 둘 다** 바꾼다("그 이상 전부") ⑥`radial` 이면 `edgeType`=`curve-line`, 아니면 `tree-line` ⑦`null` 이면 `layoutType`·`edgeType` 이 함께 해제된다 ⑧**선언에 없는 레벨의 기존 오버라이드는 남는다** — depth 2 의 `timeline` 이 2레벨만 선언한 뒤에도 그대로다(빈 칸은 "지워라"가 아니라 "말한 적 없다") ⑨단위 14항목 전부 통과, `levelShapes[2]`·`levelLayouts[2]` 가 **같은 레벨을 가리킨다**. **되돌려 깨뜨리기** — 수정 전에는 같은 파일에서 노드 `layoutType` 이 **전부 null**(그림에 아무 반영 없음)이고 `settings.levelLayouts` 가 `[null,null,'tree-right',…]` 로 **한 칸 밀려** 있었다. **왜 고쳤나** — 레이아웃 엔진은 `settings` 를 읽지 않는다(`apps/frontend/src/layout/` 전체에서 참조 0건). 노드 `layoutType` 이 단일 진실인데 불러오기는 `settings` 만 합치고 있었다. 함정 2가지 — ⑴**엔진이 `settings` 를 읽게 만들면 안 된다.** 그러면 노드와 `settings` 중 어느 쪽이 이기는지 또 정해야 한다 ⑵**순회 규칙을 두 벌로 만들면 안 된다.** 설정 패널과 불러오기가 각자 순회하면 "4레벨=그 이상 전부"·`edgeType` 같은 세부가 언젠가 어긋난다 — `utils/levelLayouts.ts` 한 벌로 모았다 | 13-version-history.md 아님 · emm-spec.md · markdown-export.md |
| e2e182 | **에디터 실사용 3건 — 새 노드 점·좁은 툴바·'저장 안 됨' 문구** (2026-09-02 사용자 보고) | 구성: 진짜 Chromium + 진짜 dev 서버(:5199) 를 **되돌려 깨뜨리는 것까지** 확인. **11항목**. ①**새 형제 노드에 '•' 가 안 붙는다** — `Shift+Space` 로 1레벨 형제를 만들고 SVG 텍스트에서 `'•'` 개수를 센다(0개). **되돌리면 1개가 다시 생긴다**(수정 전 실측) ②**사용자가 넣은 아이콘은 그대로다** — 샘플 맵의 🧱·✨·👥·🚀·📊·⚠️ 6개가 살아 있다(일괄 제거를 하지 않았다는 증거) ③~⑥**창 폭 1400·1100·900·700px 에서 배지가 세로로 안 쌓인다** — 배지 실측 높이 25·25·14·14px, `white-space: nowrap`. **되돌리면 900·700px 에서 배지 높이가 240px** 이 된다(52px 툴바 안에서 글자가 한 줄에 하나씩 쌓인 것) ⑦**같은 폭에서 'AI 생성'·'공유' 버튼도 안 쌓인다** — 수정 전 47~65px·52px 였다 ⑧**툴바 높이는 어느 폭에서나 52px** (캔버스 레이아웃 전제를 안 건드렸다) ⑨**1100px 에서 배지 문구가 짧아진다**('새 문서') ⑩**900px 이하에서는 점만 남고 버튼은 아이콘만** — 저장·맵 닫기·공유·AI 네 버튼이 **모두 그대로 있고 title 도 살아 있다**(숨긴 것은 라벨 텍스트뿐) ⑪**새 맵 배지가 중립색** — `rgb(107,99,88)` = `t.textMuted`. **되돌리면 `rgb(202,138,4)`**(경고색)이다. **왜 고쳤나** — ⓐ `makeBranch` 가 `icon: node.icon ?? '•'` 로 정규화 기본값을 채워, 사용자가 지정한 적 없는 점이 화면에 나왔다 ⓑ 툴바에 `white-space` 지정이 없어 좁히면 글자가 세로로 쌓였다 ⓒ `unsaved` 는 실패가 아니라 '아직 서버에 없다' 는 **정상 초기 상태**인데(툴팁이 처음부터 그렇게 적고 있었다) 경고색 '저장 안 됨' 이라, 새 맵을 만들자마자 뭔가 실패한 것처럼 보였다. 함정 2가지 — ⑴**기존 맵의 '•' 는 데이터로 박혀 있어 코드로는 안 사라진다.** 사용자가 일부러 넣은 '•' 와 구분할 수 없어 **일괄 제거 마이그레이션을 만들지 않았다** ⑵**폭 판정 기준을 둘로 두면 안 된다** — 미디어쿼리(뷰포트)와 ResizeObserver(요소)를 섞으면 분할 보기에서 어긋난다. 툴바 자신의 폭 하나만 잰다 | 13-version-history.md · ui-ux-spec.md |
| e2e186 | **AI 지시문이 점검표를 체크리스트로 만들게 한다** (2026-09-04 사용자 보고) | 구성: `packages/emm-parser` 직접 호출(진짜 파서) + 프런트엔드 타입체크·빌드. **파서 5 + 프롬프트 5**. **증상** — AI 로 만든 맵의 '최종 점검표'가 **표**로 나오고 확인 열에 `□` 글자만 있어 **누를 수 없었다**. **원인은 지시문이었다** — `emmSystemPrompt.ts` 규칙 14 가 `→ 최종 점검표(표)` 라고 못 박고 있었다. **파서 실측 5** — ①표 셀에 `- [ ]` 를 넣으면 `table` 블록의 **본문 텍스트**로만 남는다(체크 정규식 `/^[ \t]*[-*+]\s+\[([ xX])\]/` 가 **줄 시작**을 요구한다 — 셀은 `\|` 로 시작한다) ②`☐` 문자도 마찬가지 ③`- [ ]` 로 쓰면 `checklist` 블록 + `checked` 플래그 ④**★ 노드 배치(`blockPlacement:'node'` — AI 생성이 실제로 쓰는 값)에서도** 노드 본문에 `- [ ]` 줄이 남아 `mdCheck.ts` 가 체크박스 글리프로 그리고 **클릭 토글된다** ⑤표는 노드 배치에서도 그냥 텍스트다. **즉 체크리스트로 쓰면 노트·노드 두 배치 모두 눌린다.** **프롬프트 5** — ⑥규칙 8-1(체크리스트) 신설 ⑦규칙 7 에 '체크 목록은 표로 쓰지 마라' ⑧규칙 14 를 `최종 점검표(- [ ] 체크리스트)` 로 ⑨`tech`·`wbs` 유형별 지시에도 보강 ⑩**★ 마이그레이션** — 직전 기본값(v4.2)을 `EMM_SYSTEM_PROMPT_PREVIOUS` 에 넣었다. 빠뜨리면 **기본값을 쓰던 사용자가 옛 지시문에 갇힌다**(`aiSettingsStore.ts` 의 migrate 가 PREVIOUS 와 일치할 때만 갱신한다). 현행이 PREVIOUS 에 없는 것도 확인했다(무한 갱신 방지). 함정 1가지 — **PREVIOUS 안의 옛 보관본은 고치면 안 된다.** `최종 점검표(표)` 가 5건 남아 있는데 그것이 옛 기본값을 알아보는 지문이다 — 고치면 마이그레이션이 아무도 못 알아본다 **★ 실사용 확인 (2026-09-04, 사용자)** — 지시문을 고친 뒤 AI 생성으로 **체크리스트가 실제로 나오는 것**을 확인했다. 고치기 전에는 같은 자리가 표 + `□` 글자였다. 지시문 한 줄(`최종 점검표(표)`)이 결과를 정하고 있었다는 뜻이다. | emm-prompt-templates.md · easymindmap-copilot-gpt.md |
| e2e181 | **헬스체크가 배포 반영을 말한다 — 커밋 SHA + 런타임 버전** (2026-09-02 사용자 요청) | 구성: 진짜 PostgreSQL 16 + 빌드 산출물 `dist/main.js` 를 환경변수만 바꿔 **네 번** 기동. **6항목**. ①`SOURCE_COMMIT` 을 주면 `commit`(12자)과 `runtime{node,nestjs,express}` 가 실려 온다 ②**커밋 환경변수가 하나도 없으면 `commit` 필드가 통째로 빠진다**(빈 문자열이 아니다 — 없는 것과 모르는 것을 섞지 않는다) ③폴백 이름 `GIT_SHA` 도 읽는다 ④**40자 SHA 가 12자로 잘린다**(사람이 눈으로 대조하는 값이다) ⑤`SOURCE_COMMIT` 이 `GIT_SHA` 를 이긴다(우선순위) ⑥**회귀** — 스모크 33/33, CI 가 쓰는 `grep '"schema":"ok"'` 도 그대로 통과(필드를 더해도 기존 검사가 안 깨진다). **왜 만들었나** — 그전 응답은 `{status,db,schema,time}` 뿐이라 **옛 코드가 돌고 있어도 똑같았다**. NestJS 11 이관(#347) 뒤 헬스체크만 보고 '서버에 반영됐다'고 판단했는데, 그것이 근거가 못 된다는 것을 사용자 지적으로 알았다(`schema:ok` 는 DB 이야기다). 이제 `runtime` 만 봐도 새 빌드인지 알 수 있다. **다만 `commit` 은 빌드한 저장소의 SHA 다** — 개발 서버는 유료판 private 저장소에서 빌드되므로 공개 저장소의 `git log` 와 대조하면 언제나 어긋난다(2026-09-02 실측: 서버가 `44d642ca5537` 을 보고했고 공개 저장소에 없는 커밋이었다). 런북에 그렇게 적었다. 함정 1가지 — **버전을 정적 import 로 읽으면 안 된다**: 패키지가 `exports` 로 `package.json` 을 막아 두면(`file-type` 이 실제로 그렇다) 헬스체크가 통째로 죽는다. `require` 를 `try/catch` 로 감싸 **모르면 그 필드만 뺀다** | api-spec.md §20 · dev-server-runbook.md §0 ①b |
| e2e180 | **NestJS 10 → 11 · Express 4 → 5 이관** (2026-09-02) | 구성: **진짜 PostgreSQL 16**(로컬 초기화 후 스키마 4벌 로드, 28표) + 빌드 산출물 `dist/main.js` 직접 기동 + 진짜 HTTP·multipart. **부팅 2** — ①`AUTH_MODE=dev` 로 기동해 `/v1/health` 가 `{db:up, schema:ok}` ②**★ `AUTH_MODE=supabase` + `--no-experimental-require-module`** 로 기동해 토큰 없는 `/v1/maps` 가 401. **스모크 33/33** — 맵 CRUD · 노드 이동(ltree) · 순환 차단 · autosave 3종(신규·중복 patchId 409·버전 충돌 409) · 문서 스냅샷 왕복 · from-url 차단 9종 · cascade 삭제 · 사용자 격리. **vault 단위 전부 통과**. **레이트리밋 3** — `RATE_LIMIT_MAX=5` 로 띄워 ③5회까지 200 ④6회째부터 429 ⑤**커스텀 한국어 메시지와 `retryAfterSec` 이 살아 있다**(`RateLimitGuard extends ThrottlerGuard` 가 11 에서도 동작). **첨부 5** — ⑥한글 파일명 업로드 후 DB `name` 이 `한글 파일명 테스트.txt` (multer 2.2.0 도 `originalname` 을 latin1 로 넘긴다 — `attachments.service.ts:124` 의 복원 전제 유지) ⑦다운로드 200 + RFC5987 파일명 + 내용 일치 ⑧**★ 청크 업로드 3단(start → part PUT → complete)이 112바이트를 무손실 왕복** — `application/octet-stream` 본문을 body-parser 2 가 삼키지 않는다(삼켰으면 **0바이트 조각이 조용히 쌓인다**). **★ 잡은 회귀 1가지** — `@nestjs/config@12` 는 `"type": "module"` 인 **ESM 전용**이라 CJS 빌드가 `require()` 로 못 읽는다(`ERR_REQUIRE_ESM`). **dev 모드 기동과 스모크는 전부 통과했고 supabase 모드 부팅 스모크만 잡았다** — Node 22 는 기본값이 `require(ESM)` 을 허용하지만 컨테이너의 Node 20 은 아니라, 그대로 배포하면 터진다. `^4.0.4`(CJS, lodash 4.18.1 이라 취약하지도 않다)로 되돌려 해결. 함정 2가지 — ⑴**스모크는 멱등하지 않다.** 연달아 두 번 돌리면 '같은 이름 맵이 이미 있습니다'(409)로 6건이 깨진다 — DB 를 새로 만들고 한 번만 돌려야 한다 ⑵**`@nestjs/throttler` 6.5.0 이 NestJS 12 를 peer 로 선언하지 않는다**(`^11.0.0` 까지). 그래서 12 로 가면 `npm ci` 가 ERESOLVE 로 막힌다 — 11.2.3 이 취약점(`@nestjs/core <=11.1.17`)을 이미 해소하므로 11 로 멈췄다 **★ 브라우저 실사용 확인 (2026-09-04, 사용자)** — dev 서버에 배포된 상태에서 ①맵을 저장하고 ②노드에 **한글 이름 파일**(`[2025]서버구성-Hnglife.md`)을 첨부해 **파일명이 화면에 온전히 표시되는 것**까지 봤다. multer 2.2.0 의 latin1 전제와 Express 5 라우팅이 실제 화면에서 도는 것을 확인한 것이라, 이 이관에서 **로컬 검증만으로는 덮지 못하던 마지막 자리**가 닫혔다. 같은 날 `/v1/health` 가 `runtime{nestjs:11.2.3, express:5.2.1}` 을 보고해 **서버 반영도 함께 확정**됐다(e2e181). | backend-architecture.md · rate-limit.md · attachment-storage.md |
| e2e179 | **vault 미러가 실제로 파일을 만든다** (2026-08-23, vault 미러 슬라이스 3) | 구성: **진짜 PostgreSQL + 진짜 파일시스템 + 진짜 EMM 직렬화기**(퍼블리시 전이라 타르볼을 `--no-save` 로 넣어 검증) + 순수 단위. **경로 계획 22 + 실물 30 = 52항목**. **경로 계획 22** — 폴더 트리가 그대로 경로가 된다 · 홈은 루트 · **폴더 이름도 겹친다**(`map_folders` 에 unique 가 없다. 먼저 만든 것이 이름을 지키고 나중 것이 id 를 단다) · 부모가 다르면 같은 이름 그대로 · **대소문자만 달라도 가른다** · **부모가 없어져도 맵을 버리지 않는다**(홈으로 올린다) · **폴더가 고리를 이뤄도 멈춘다** · **★ 서로 다른 유령 폴더에서 둘 다 홈으로 올라와도 안 겹친다**(마지막 판정을 `folderId` 가 아니라 **디렉터리 경로**로 해야 잡힌다) · **★ 입력 순서가 달라도 같은 답**. **실물 30** — ①직렬화기를 안 꽂으면 **꺼져 있고 폴더조차 안 건드린다** ②꽂으면 `연구/RAG 증분 인덱싱.md` 가 폴더 트리 그대로 생기고 내용이 **진짜 EMM**(`# 중심 주제` · `## 가지 하나` · 메타 주석) ③`vault_files` 에 경로와 64자 해시가 기록된다 ④**안 바뀌었으면 `mtime` 도 안 건드린다**(다시 쓰면 Obsidian·Git 이 '바뀐 파일' 로 본다) ⑤**제목을 바꾸면 옛 파일을 휴지통으로 보내고** 새 자리에 쓴다(유령 파일이 안 쌓인다) ⑥**★ 사용자가 고친 파일은 덮지 않고** `.conflict.md` 로 비킨다 ⑦**★ 충돌 때는 `vault_files` 를 갱신하지 않는다** — 갱신하면 다음 번에 '우리가 쓴 그대로' 로 보여 **그 편집을 덮는다** ⑧그래서 다시 돌려도 또 비켜 쓴다(`.conflict-1.md`) ⑨**맵을 지우면 파일도 휴지통으로**(지우지 않는다) ⑩남의 맵·없는 맵은 안 건드린다. **디바운스 6 (§6)** — 같은 맵을 세 번 예약해도 **대기가 하나로 합쳐진다** · 다른 맵은 따로 · 아직 아무것도 안 썼다 · 종료하면 정리된다 · **`VAULT_DIR` 이 없으면 예약조차 안 한다** · 기동 로그가 '꺼짐/대기/켜짐' 을 말한다. **회귀** — API 를 `VAULT_DIR` 을 켠 채 띄워 저장해도 **아무것도 안 쓰고**(직렬화기 미연결) 저장은 200, 백엔드 스모크 전체 통과. 함정 2가지 — ⑴**큐가 없다.** 설계 §6 이 '`export`·`ai-generate` 큐 옆에 붙인다' 고 했지만 BullMQ·Redis·워커가 저장소에 하나도 없다. vault 는 셀프호스트 전용이라 API 가 한 벌만 돌아 **맵 id 별 타이머 하나**로 갈음했다 ⑵**충돌 때 기록을 갱신하면 안 된다.** 무심코 갱신하면 다음 저장에서 사용자의 편집을 조용히 덮는다 — 전용 항목(⑦)으로 박았다 | vault-mirror.md §3 · §6 · §8 |
| e2e177 | **vault 에 안전하게 쓴다 — 남의 파일을 덮지 않는다** (2026-08-21, vault 미러 슬라이스 2) | 구성: `apps/api` 빌드 산출물 직접 호출(`npm run test:vault`) + **진짜 파일시스템**(tmpdir 에 vault 를 만들고 심볼릭 링크까지 건다). **49항목**. **마커 10 (§7 ①)** — 마커 없는 폴더는 **쓰기도 휴지통도 거절** · 마커와 README 를 만든다 · 지워진 README 를 되살린다(안내가 사라지면 첫 겹이 무력해진다) · **★ 비어 있지 않은 폴더는 vault 로 안 받는다**(그 폴더의 파일은 그대로, 마커도 안 만든다 — 잘못 지정한 경로는 되돌릴 수 없다). **★ 경로 탈출 9** (설계에 없던 위험) — `..` · 중간에 섞인 `..` · 절대 경로 · 빈 경로를 거절하고, **폴더가 vault 밖을 가리키는 심볼릭 링크면 거절**한다(경로 문자열만 봐서는 절대 못 잡는다). 링크 너머에 파일이 안 생긴 것까지 확인. **쓰기 7** — 새로 쓰기 · 하위 폴더 생성 · 해시 반환 · **내용이 같으면 아예 안 건드린다**(다시 쓰면 `mtime` 이 바뀌어 Obsidian·Git 이 '바뀐 파일' 로 본다) · 우리가 쓴 그대로면 다시 쓴다 · **임시 파일이 안 남는다**(원자적 쓰기). **★ 충돌 8 (§7 ③)** — 사용자가 Obsidian 으로 고친 파일은 **덮지 않고** `.conflict.md` 로 비켜 쓴다 · **사용자가 고친 내용이 그대로 남는다** · **지난 충돌본도 안 덮고** `.conflict-1.md` 로 번호를 올린다 · **우리가 쓴 적 없는 자리(`prevHash=null`)에 남의 파일이 있으면 그것도 안 덮는다**. **휴지통 6 (§3.3)** — 지우지 않고 `.trash/` 로 옮긴다 · 내용이 살아 있다 · **휴지통 안의 같은 이름도 안 덮는다** · 없는 파일은 `null`. **이동 9 (§3.2)** — 새 자리에 쓰고 옛 파일은 휴지통으로 · **대소문자만 바뀌어도 된다**(`rename` 이었으면 일부 파일시스템에서 실패한다) · **★ 새 자리가 충돌이면 옛 파일을 건드리지 않는다**(옛 자리까지 치우면 사용자의 편집을 찾을 길이 없다). 함정 2가지 — ⑴**심볼릭 링크는 `path.resolve` 로 못 잡는다.** 이미 있는 조상 디렉터리를 `realpath` 로 풀어야 보인다 ⑵**원자적 쓰기를 안 하면 없는 충돌이 생긴다** — 반쯤 쓰인 `.md` 가 남으면 다음 해시 비교가 그것을 '사용자가 고쳤다' 로 읽는다 | vault-mirror.md §7 · §3.2 · §3.3 |
| e2e176 | **vault 파일 이름 규칙 — 디스크에 쓰기 전에** (2026-08-21, vault 미러 슬라이스 1) | 구성: `apps/api` 빌드 산출물을 직접 호출(`npm run test:vault`) + **진짜 ext4 에 파일 생성**. **56항목 + 실파일 12/12**. 순수 함수만이라 서버·DB·브라우저가 필요 없다. **금지문자 4** — `/ \ : * ? " < > |` 전부 `-` 로 · **경로 구분자가 남으면 하위 폴더가 생겨 버린다** · 줄바꿈·탭은 공백으로(붙여넣은 제목에 실제로 들어온다) · 연속 공백은 하나로. **Windows 예약어 6** — `CON`·소문자 `con`·`COM1`·`LPT9` 는 뒤에 `_`, **`COM0`·`CONSOLE` 은 예약이 아니다**. **앞뒤 다듬기 4** — 앞뒤 공백·끝의 점·점과 공백이 섞여 끝날 때·**가운데 점은 그대로**(`v1.2.3`). **★ 앞의 점 5** (설계에 없던 위험) — 폴더가 `.attachments`·`.trash` 가 되면 **vault 가 자기 폴더를 사용자 폴더로 덮어쓴다**. 앞의 점을 `_` 로 바꾸고, 우리가 쓰는 이름 셋 중 어느 것도 정규화 결과로 나올 수 없음을 확인. **255바이트 6** — **글자 수가 아니라 바이트다**(한글 3바이트, 84자면 상한) · `.md` 를 포함해 딱 255 · 몸통 252 · 3바이트 한글이 안 깨진다 · 이모지를 반으로 안 자른다 · **자른 끝이 공백이면 다시 다듬는다**(Windows 거부). **빈 제목 6** — 빈 문자열·공백만·점만 → `Untitled-<id 8자>` · 금지문자만이면 이름이 남는다(`---`). **NFC 3** — macOS 가 주는 NFD(`ㄱ`+`ㅏ`, 6바이트)가 NFC(`가`, 3바이트)와 **같은 파일 이름**이 된다. **중복 8** — 먼저 만든 맵이 원래 이름을 지키고 나중 것이 `-<id 8자>` · **★ 입력 순서가 달라도 답이 같다**(안 그러면 자동저장마다 두 파일이 이름을 바꿔 가며 지워졌다 다시 쓰인다) · 시각이 같으면 id 순 · **★ 대소문자만 다른 제목도 겹치는 것으로 본다**(macOS·Windows 에서만 덮어쓰기가 나면 못 잡는다). **첨부 해시 8** — `<sha256 앞 16자>.<확장자>` · 소문자 · 확장자 없으면 `bin` · **★ 경로가 섞인 이름(`../../etc/passwd.png`)에서 확장자만** · 이중 확장자는 마지막만 · 같은 사진은 같은 이름 · **해시가 모자라면 던진다**(조용히 뭉개지 않는다). **상대 경로 4** — 폴더 트리 그대로 · 홈은 루트 · **구분자는 항상 `/`**(DB 에 들어가는 값이라 서버 OS 가 바뀌어도 같아야 한다). **끝으로 실제 ext4 에 12개 이름을 전부 파일로 만들어 봤다** — 규칙이 맞는지는 파일시스템이 답한다. 함정 2가지 — ⑴**`unhide` 를 끝점 제거보다 먼저 돌리면 `...` 이 `___.md` 가 된다**(빈 제목으로 안 떨어져 `Untitled-<id>` 를 못 만든다). 순서를 뒤집어 고쳤다 ⑵**255바이트를 몸통에만 적용하면 안 된다** — `.md` 까지 합쳐 255다. 처음 기대값을 252로 잡았다가 시험에서 잡혔다 | vault-mirror.md §3.1 · §4 |
| e2e175 | **노트 HTML 속 사진도 문서 밖으로** (2026-08-20, B16 ② 슬라이스 6 · 28번 §3.5 셋째 예외 제거) | 구성: 로컬 PG(:5433) + API :3001 + vite :5173 + Playwright + **CORS 헤더를 주지 않는 남의 사이트**(192.0.2.2:8099) + emm-parser 직접 호출. **파서 33 + 브라우저 13 + 서버 8 = 54항목**(요약표에는 굵은 것 21). **쓰는 쪽 3** — ①`embedRichHtmlImages` 가 노트 HTML 에 **서버 주소**를 넣는다(base64 아님) ②본문·`alt`·`loading` 은 그대로 ③**우리 저장소 사진은 다시 안 받는다**(안 거르면 붙일 때마다 한 벌씩 쌓인다). **화면 3** — ④노트 주소에 토큰이 붙는다 ⑤그 주소로 200 ⑥**토큰 없이는 못 그린다**(인증이 실제로 걸려 있다는 확인). **게스트 1** — ⑦서버가 없으면 노트 HTML 을 손대지 않는다(동작 변화 없음). **내보내기 6** — ⑧HTML 결과물에 서버 주소 없음 ⑨노트 사진이 data URL 로 들어갔다 ⑩못 받은 사진 0(MD·HTML) ⑪**API 를 정말 죽이고**(health 실패 확인) ⑫**외부 요청을 전부 막은 채** `file://` 로 열어 노트 사진이 **120×80 으로 그려진다** ⑬결과물에 서버 주소 없음. **서버 8** — ⑭저장하면 노트 HTML 속 data URL 이 `/v1/attachments/` 로 바뀐다 ⑮본문·`alt` 보존 ⑯첨부 1개 ⑰이름이 내용 해시 꼴 ⑱다시 저장해도 안 늘어난다 ⑲**★ 하루 지난 사진도 노트가 쓰면 GC 가 안 지운다** ⑳노트에서 빼면 그때 지워진다. **파서 단위 33** (`test/note-images.test.ts`, `npm run test:unit`) — 따옴표 3종·대문자·자기닫음·여러 장·중복·엔티티 왕복·못 바꾼 것 보존·참조 보존. 함정 3가지 — ⑴**정규식 `\\bsrc\\s*=` 는 `data-src=` 에도 걸린다**(`-`와 `s` 사이가 단어 경계) — 지연 로딩 자리표시자를 진짜 사진으로 착각한다 ⑵**`<img[^>]*?src=` 는 속성값 안의 `>` 에서 끊긴다**(`<img alt="a>b" src="x">`; 직렬화는 속성값의 `>` 를 이스케이프하지 않는다) — 둘 다 **조용히 안 바뀐다**로 끝나 파일은 정상 생성되고 그 사진만 깨진다. 그래서 정규식을 버리고 따옴표를 지키는 **속성 스캐너**를 썼다 ⑶**GC 참조 세기를 빠뜨리면 데이터가 없어진다** — 노트 HTML 은 주소가 **여럿**이라 첫 하나만 보는 `take()` 에 넘기면 두 번째부터 안 세고, 하루 뒤 **보이는 사진이 지워진다** | content-permanence.md §7.1 · 28-sync-prework-plan.md §3.5 |
| e2e174 | **문서에 사진 바이트가 남지 않는다 — 메타 주석·불러오기·작은 스크린샷** (2026-08-20, B16 ② D-5·D-6) | 구성: 로컬 PG(:5433) + API :3000(AUTH_MODE=dev) + vite 2벌(**게스트 빌드**와 **로그인 빌드**=가짜 세션 주입) + Playwright(Chromium) + emm-parser 직접 호출. **게스트 20 + 로그인 21 + 파서 10 = 51항목**. **메타 주석 6 (D-5)** — ①본문이 `files/img-1.png` ②**메타를 디코드해도 `data:image` 가 없다** ③메타가 본문과 **같은 경로**를 가리킨다 ④**같은 사진은 한 벌만** 담긴다(예전엔 노드마다 한 벌) ⑤서로 다른 사진은 2벌(`img-1.png`·`img-2.gif`) ⑥외부 https URL 은 그대로. **불러오기 6 (D-6)** — ⑦게스트는 지금처럼 data URL(동작 변화 없음) ⑧**로그인은 서버 첨부 주소로 되올라간다** ⑨문서에 `data:` 없음 ⑩`files/` 경로가 문서에 남지 않는다 ⑪되이은 사진 수 ⑫폴백으로 떨어진 사진 0장. **뒷문 3** — ⑬`resolveRemoteImages` 가 **우리 저장소 사진을 건드리지 않는다**(그것도 `http` 라 거르지 않으면 불러올 때마다 data URL 로 도로 넣는다) ⑭내장 카운트 0 ⑮`relinkImages` 단독 동작. **작은 스크린샷 3** — ⑯**2MB 미만 스크린샷도 `/v1/attachments/` 로 들어간다**(`preferServer`) ⑰토큰이 문서에 저장되지 않는다 ⑱크기 실측. **첨부 회귀 2** — ⑲첨부(≤2MB)는 **여전히 문서에 내장** ⑳첨부(>2MB, 로그인)는 **여전히 서버로**. **의도된 예외 4** — ㉑HTML 은 **파일 하나**(폴더가 되지 않았다) ㉒**서버 사진이 HTML 안에 인라인**(서버 꺼도 보인다) ㉓HTML 에 서버 주소 없음 ㉔게스트 HTML 도 인라인. **서버 4** — ㉕`from-url` 성공(9,208바이트 PNG) ㉖**리다이렉트를 따라간다** ㉗차단 9항목 여전히 400 ㉘**연결까지 가는가**(아래 함정 ⑵). 함정 3가지 — ⑴**`.md` 를 눈으로 보고 판정하면 안 된다.** 메타 주석 자체가 base64 라 안에 든 data URL 이 `base64` 라는 글자로 **보이지 않는다** — D-5 를 되돌려도 텍스트 검사는 통과한다. **디코드해서** 봐야 한다 ⑵**`from-url` 성공 경로가 전부 죽어 있었다**(#316 병합분) — `pinnedLookup` 이 Node 규약을 어겨(`net` 은 `{all:true}` 로 부르고 **배열**을 기대한다) 모든 다운로드가 `Invalid IP address: undefined`. **차단 시험으로는 안 걸린다**(막는 길은 연결 전에 끝난다) — 스모크에 한 줄 박았다 ⑶**작은 스크린샷은 `attachmentUrlForFile` 을 그냥 부르면 문서에 남는다**(≤2MB 내장 규칙). 그건 첨부에 맞는 규칙이라, 판정을 두 벌로 만들지 않고 **`preferServer` 옵션**으로 갈랐다 | content-permanence.md §7.1 · 28-sync-prework-plan.md §3.4~3.6 |
| e2e173 | **붙여넣은 사진이 문서 밖으로 나간다 — 쓰는 쪽** (2026-08-20, B16 ② 슬라이스 5 · 28번 §3.4 D-1~D-4) | 구성: 진짜 PostgreSQL 16 + API :3001 + vite :5173 + Playwright + **CORS 헤더를 주지 않는 남의 사이트**(192.0.2.2:8099) + 진짜 PNG 2종(124B · **5.6MB 노이즈**). **전 76항목** — SSRF 35 · 브라우저 26 · 오프라인 8 · 노트/기사 7. **SSRF 35** — 과제가 지정한 다섯(`127.0.0.1:3000` · `localhost:5432` · `169.254.169.254` · `file:///etc/passwd` · **공개 200 → 302 → `http://10.0.0.1/`**)이 전부 거절되고, 덤으로 gopher·data·ftp·10/8·172.16/12·192.168/16·`::1`·`fd00::/7`·`0.0.0.0/8`·CGNAT·**`::ffff:127.0.0.1`**·**DNS 가 127.0.0.1 을 주는 공개 도메인**(A 레코드 우회)·리다이렉트 3회 상한·`text/html`·`image/svg+xml`·404·**Content-Length 없이 흘리는 대량 응답**·선언 크기 초과·잘린 응답·**끝나지 않는 응답(8초 컷)**. **브라우저 26** — ①작은 스크린샷은 **첨부와 같은 판정으로 data URL**(기준을 바꾸지 않았다) ②**5.6MB 스크린샷은 `/v1/attachments/`** ③`blob:` 이 아니다 ④브라우저 fetch 가 **실제로 CORS 로 막히는** 것을 확인한 뒤 ⑤같은 사진이 **서버 경유로 성공** ⑥원본 URL 이 문서에 안 남는다 ⑦크기 실측(1400×1400 · 120×80) ⑧새로 올린 사진을 **붙인 직후 200 으로 받아 `<img>` 로 그린다** ⑨MD·HTML 내보내기에 **서버 주소가 없다** ⑩남의 사이트 URL 은 그대로 ⑪못 받은 것 0 ⑫옛 data URL 문서는 그대로 ⑬게스트는 **data URL·서버 요청 0건** ⑭게스트 CORS 차단 시 원본 URL 폴백 ⑮**SSRF 로 거절당해도 노드에 빈 자리가 안 남고 이유까지 알린다**. **오프라인 8** — 사진 두 장을 서버로 올린 맵을 HTML 로 내보낸 뒤 **API 를 정말 죽이고**(health 실패 확인) `file://` 로 열되 **모든 외부 요청을 route.abort 로 막고** ⑯사진 2/2 가 `naturalWidth > 0` 으로 **실제로 그려졌다**(스크린샷 확인) — content-permanence.md §7.1 의 핵심 약속. **노트/기사 7** — ⑰리치 노트 HTML 은 서버가 받아 오되 **data URL 로 둔다**(서버 주소를 넣으면 내보내기가 노트를 안 훑어 깨진다) ⑱기사 붙여넣기의 노드 사진은 서버 주소 + 원문 위치 유지. **첨부 무회귀** — 백엔드 스모크 전체 통과 + 업로드/다운로드/Range 206/쿼터/삭제 204/삭제 후 404/청크 세션 시작. 함정 3가지 — ⑴`192.0.0.0/24` 를 두 옥텟만 보고 막으면 **192.0.0.0/16 이 통째로** 막힌다(멀쩡한 공개 대역) ⑵URL 파서가 `[::ffff:127.0.0.1]` 을 **`::ffff:7f00:1` 로 정규화**하므로 점 표기만 보고 벗기면 IPv4 검사를 통째로 건너뛴다 ⑶전체 시간 상한을 **플래그로만** 두면 아무것도 안 끊긴다 — 열린 요청을 들고 있다가 `destroy` 해야 한다 | content-permanence.md §7.1 · collaboration/28-sync-prework-plan.md §3 |
| e2e172 | **배포 준비 — 파서를 밖에서 쓸 수 있게, 종료 때 편집을 잃지 않게** (2026-08-18) | 구성: **진짜 npm 설치**(`npm pack` → 타르볼 설치)로 유료 모듈을 코어 API 에 심고, 진짜 PostgreSQL + 진짜 WebSocket 클라이언트. **파서 패키징 8** — 빈 디렉터리에 `npm install` 한 뒤 ①**CJS `require` 로 28개 내보내기** ②**ESM `import`** ③하위 경로 `@easymindmap/emm-parser/tree-rules` (CJS·ESM 둘 다) ④타입 파일 존재 ⑤`parseEmm` → `serializeEmm` 왕복이 제목·가지를 지킨다 ⑥적합성 12/12 ⑦프런트엔드 빌드 회귀 없음(여전히 `@emm` 소스 별칭을 본다) ⑧`dist/cjs/package.json` 이 `{"type":"commonjs"}`. **종료 3** — 협업 편집을 보낸 **0.6초 뒤** SIGTERM 을 줘도 ①**마지막 편집이 DB 에 저장되고** ②프로세스가 **2.1초** 만에 끝나며 ③클라이언트는 닫힘코드 **1001** 을 받아 재접속 백오프로 돌아온다. **되돌려 깨뜨리기 3** — ⑴`enableShutdownHooks()` 가 없으면 종료 훅이 **한 번도 안 불린다** ⑵훅을 `onApplicationShutdown` 단계에 두면 **20초를 기다려도 저장이 시작되지 않는다**(열린 WebSocket 이 HTTP 서버 close 를 붙잡는다 — 컨테이너는 보통 10초 뒤 SIGKILL 이므로 **재배포마다 조용히 유실**) ⑶`--no-save` 로 따로 설치하면 다음 `npm install` 이 **앞서 심은 패키지를 지운다**(한 번에 설치해야 한다). **검증이 설계를 고쳤다** — 파서 `main` 이 TS 소스라 Node 가 못 읽었고, ESM 산출물은 상대 경로에 확장자가 없어 `ERR_MODULE_NOT_FOUND` 였다(번들러가 가려 주던 것이 밖에서 드러났다) | open-core-boundary.md §7 |
| e2e171 | **초대 화면과 “공유받은 맵” 목록** (2026-08-18, 협업 6단계) | 구성: **진짜 API :3000(유료 모듈 심음) + 진짜 PostgreSQL + vite :5199 + Playwright 2탭**(주인 A · 초대받는 B). **유료 21항목 + 공개판 7항목**. **공유 버튼 3** — 버튼이 있고, 맵이 열려 있으면 눌리고, **대화상자가 실제로 열린다**(예전에는 `onClick` 이 없어 **눌러도 아무 일도 없었다**). **초대 6** — 처음엔 아무도 없고 · **가입 안 한 주소는 이유를 그대로 말하고**(“먼저 가입한 뒤 다시 초대해 주세요”) · 초대되면 목록에 뜨고 · DB 에 참가자가 생기고 · **단독맵이 협업맵으로 올라간다**(이게 없으면 초대는 됐는데 둘이 열면 한쪽이 읽기 전용이 된다). **권한 2** — **주인이 아닌 사람의 초대는 404** 이고 그 시도로 참가자가 늘지 않는다(참가자가 참가자를 늘리면 한 번 공유한 맵이 주인 모르게 퍼진다). **받는 쪽 5** — 문서함에 **“공유받은 맵” 구역**이 생기고 · 그 맵이 목록에 있고 · **주인이 누구인지** 알 수 있고 · **주인만 할 수 있는 버튼(이름 변경·이동·삭제)은 보이지 않고** · 상세 정보는 되고 · **열려서 편집까지 된다**. **내보내기 3** — 목록에서 사라지고 · DB 에서도 빠지고 · **맵은 협업맵으로 남는다**(되돌리면 열려 있던 사람이 갑자기 읽기 전용이 된다). **공개판 7** — 공유 버튼은 살아 있고 대화상자도 열리지만 **초대 입력이 없다**(행을 만들 길이 없다) · 왜 못 쓰는지 서버 문장 그대로 · JS 오류 0. **되돌려 깨뜨리기 2** — 주인 확인을 빼면 **남의 맵에 초대가 201 로 성공**하고, 공유 맵의 버튼 숨김을 빼면 **지울 수 없는 버튼이 다시 보인다**. **검증이 코드를 고쳤다** — 공유 목록이 늘 비어 있었다. `u.email` 로 읽었는데 이메일은 `auth.users` 에 있어 질의가 통째로 실패했고, **오류를 삼키는 catch** 가 그것을 “공유 없음”으로 보여 주고 있었다. `to_regclass` 로 **표만 확인**하고 나머지 오류는 드러나게 바꿨다. 함정 1가지 — 개발 모드에서 프런트는 `x-user-id` 를 보내지 않아 **모든 탭이 같은 사용자**가 된다(검증 하네스가 헤더를 붙여 두 사람을 갈랐다) | api-spec §13 · 27-sync-model.md §14.4 |
| e2e169 | **협업 프런트엔드 자리 — 저장은 하나가 책임진다** (2026-08-18) | 구성: **진짜 API :3000 + 진짜 PostgreSQL + vite :5199 + Playwright**, 그리고 **가짜 유료 UI**(`packages/pro-ui`, 커밋하지 않음)를 끼운 빌드와 **공개판(스텁)** 빌드를 각각 돌렸다. **유료 UI 18항목 + 공개판 13항목**. **단독맵 2** — 예전 그대로 "미저장 편집 N개". **협업맵 5** — 배지가 **협업 중 — 자동 반영** · 편집해도 그대로 · **통째 저장(PUT) 0건** · 여러 번 고쳐도 0건 · **탭 전환·창 닫기에도 0건**(평소 곧바로 올리는 안전 3시점인데도). **밖에서 온 변경 5** — 화면에 반영되고 · PUT 을 부르지 않고 · **Ctrl+Z 한 번에 남의 글자가 아니라 내 편집이 돌아온다**(원격 3회를 넣고 확인 — 한 번만 넣으면 한 칸 되돌려도 결과가 같아 보여 아무것도 증명하지 못한다). **자리 4** — 세션·접속자·커서가 뜨고 **커서 자리에 화면 기하가 그대로 넘어간다**. **공개판(스텁) 13** — 셋 다 **아무것도 그리지 않고**(정상 동작) **협업맵도 예전대로 미저장으로 세고 탭 전환에서 저장된다**(유료 모듈이 없다고 편집이 사라지면 안 된다). **되돌려 깨뜨리기 3** — ①자동저장 가드를 빼면 탭 전환에서 **PUT 2건**이 나가고 ②배지 파생을 빼면 **"저장됨"으로 덮이고**(맵 여는 경로가 뒤늦게 쓴다 — 이게 실제 버그였다) ③되돌리기 제외를 빼면 **Ctrl+Z 가 남의 글자 사이를 걷는다**. 함정 2가지 — **Enter 가 커밋, Escape 는 취소**다(Escape 로 검증해 "편집이 안 세어진다"는 잘못된 결론을 한 번 냈다) · 맵이 열려 있으면 문서함에서 여는 다른 맵은 **새 탭**으로 열리므로 검증은 `?map=<id>` 주소로 연다 | open-core-boundary.md §5.3·§5.4 |
| e2e168 | **참가자 표 델타 스크립트** (2026-08-18) | 구성: **가짜 `docker`** 를 PATH 에 심어 rbooster + easymindmap 이 같은 호스트인 상황을 재현 + 진짜 PostgreSQL. **4항목**. ①`bash -n` 문법 ②**남의 앱(rb-api-…)을 건너뛰고 우리 표가 있는 DB 를 고른다** ③**두 번 실행해도 같다**(멱등) ④검증 출력(표 이름·참가자 수). 적용 전에도 앱은 돈다 — **소유자만** 맵을 연다(예전과 같다) | runbook §1.5-0-G |
| e2e167 | **공유받은 사람이 맵을 연다 — 맵 참가자 권한** (2026-08-18, 협업 4단계) | 구성: **진짜 API :3000 + 진짜 PostgreSQL** + 실제 HTTP(소유자·참가자·제3자 3인). **34항목**. **참가자 전 3** — 남은 문서·맵 모두 404, 소유자는 200. **editor 5** — 문서·맵·버전 목록을 열고 **저장까지 되며** 그 내용이 실제로 들어간다. **viewer 4** — 열리지만 **저장은 403** + "읽기 권한만 있습니다" + **문서는 그대로**. **제3자 2** — 읽기·쓰기 모두 404(없는 맵과 구분하지 않는다 — 구분하면 남의 맵 id 존재를 알려 주는 셈이다). **소유자만 3** — 참가자는 이름 변경·삭제 불가, 맵은 살아 있다. **첨부 8** — **공유받은 맵의 그림이 실제로 열리고**(이게 없으면 맵은 보이는데 그림이 다 깨진 채로 열린다 — 사용자에겐 그냥 고장이다) 내용이 같고 제3자는 못 열고 **참가자는 남의 파일을 못 지우며**, **참가자가 올린 그림을 소유자가 본다**. **델타 미적용 서버 7** — 표를 지우고 ①**앱이 살아 있다** ②헬스가 `map_members` 를 이름으로 알려 준다 ③소유자는 읽기·쓰기 그대로 ④**참가자였던 사람은 못 연다**(안전한 쪽으로 물러난다) ⑤첨부도 같은 규칙. **뜨겁게 되살아나기 1** — 델타를 적용하자 **재기동 없이 50초 만에** 공유가 살아났다. **검증이 코드를 고쳤다** — 처음엔 `42P01` 오류 코드를 보고 물러나려 했는데, 코어의 `DatabaseService` 가 그 코드를 **503 으로 바꿔서** 올려 주기 때문에 아무것도 못 잡고 **델타 미적용 서버에서 맵 열기가 통째로 503** 이 됐다. `to_regclass` 로 **존재 여부를 직접 묻도록** 바꿨다 | 27-sync-model.md §14 |
| e2e166 | **런북이 남의 앱 DB 를 고르지 않는다** (2026-08-18 실사용에서 또 깨졌다) | 구성: **가짜 `docker`** 를 PATH 에 심어 이 서버 상황(rbooster + easymindmap 이 같은 호스트)을 재현. **3항목**. ①탐색 스크립트가 `bash -n` 문법 통과 ②`rb-api-…`(rbooster)를 **건너뛴다** ③`map_documents` 가 있는 앱을 고른다. **무슨 일이 있었나** — 사용자가 델타 SQL 을 돌렸는데 `grep -i api` 가 **rbooster 의 API** 를 물어 **남의 DB 에 `ALTER TABLE` 을 실행**했다. `relation "public.plan_quotas" does not exist` 로 끊겨 아무것도 바뀌지 않았지만, **표 이름이 겹쳤다면 남의 앱 DB 를 고쳤을 것**이다. 런북은 이미 "이미지 이름으로 찾지 마라"(§1.5-0-A, e2e151)를 적어 뒀는데 **어느 컨테이너의 `DATABASE_URL` 인지를 이름·포트로 고르는 구멍**이 남아 있었다 — 8곳이 `grep '3000/tcp'` 를 쓰고 있었고 이 서버에서는 rbooster 도 그 포트를 연다. **전부 "우리 표가 있는지 직접 확인"으로 바꿨다**(추측이 한 겹도 남지 않는다) | runbook §1.5-0-A |
| e2e165 | **요금제 구독 요금을 관리자 화면에서 정한다** (2026-08-18, 사용자 요청) | 구성: PG + API :3001(관리자 표를 직접 서명해 발급) + vite :5173 + Playwright. **35항목**. **설정 목록 4** — 요금 항목 4개 · 편집 열림 · 단위 `원/월` · **용량 항목도 그대로**(회귀 없음). **값 바꾸기 4** — 저장 · DB 반영 · 화면 반영 · **0(무료)도 된다**. **막아야 할 값 8** — **1~999 거절**(PG 최소 금액) · 거절된 값은 DB 미반영 · 음수 · 소수 · 없는 요금제 · 상한 초과 · **관리자 표 없이는 401** · 변경이 로그에 남는다. **델타 미적용 서버 7** — 컬럼을 지우고 ①**용량 편집이 그대로 살아 있다** ②요금은 '조회 실패' ③편집이 열리지 않는다 ④무엇을 하라고 알려 준다 ⑤저장은 503 + 안내 ⑥**헬스가 degraded 로 바뀌고 `plan_quotas.price_krw` 를 이름으로 알려 준다** ⑦적용하면 되돌아온다. **화면 9** — 입력칸 4개 · 용량 4개 유지 · 서버 값 표시 · **1~999 는 저장 버튼이 잠기고 왜 안 되는지 말한다** · 0 은 열리고 뜻을 알려 준다 · 저장 결과 표시 · JS 오류 없음. 검증에서 잡은 것 2가지 — ⑴처음엔 요금을 **용량과 같은 쿼리**로 읽었는데, 컬럼이 없는 서버에서 쿼리 전체가 실패해 **용량 편집까지 사라졌다**(새로 더한 것 때문에 되던 것이 사라지면 안 된다 — 쿼리를 나눴다) ⑵저장 실패 메시지가 "`/v1/health` 의 missingColumns 를 보라"고 안내하는데 **그 목록에 이 컬럼이 없었다** — 아무것도 알려주지 않는 곳을 가리키는 안내라 `REQUIRED_COLUMNS` 에 넣었다 | api-spec 22j |
| e2e164 | **밖에서 들어오는 결제 통지가 레이트 리밋에 막히지 않는다** (2026-08-18, PG 준비) | 구성: API :3001 을 `RATE_LIMIT_MAX=3` 으로 띄우고 임시 라우트 `/v1/pg/probe`(검증용, 커밋하지 않음)로 확인. **8항목**. ①form-urlencoded 본문이 들어온다 ②한글 값 온전 ③JSON 회귀 없음 ④일반 경로는 3번 뒤 429 ⑤**통지 경로는 12번을 쳐도 안 막힌다** ⑥헬스체크도 여전히 안 막힌다. **되돌려 깨뜨리기 2** — ⑦예외를 빼면 통지 경로도 429 로 막힌다(고침이 실제로 일한다) ⑧**파서를 빼도 폼 본문은 그대로 들어온다** ← 이것이 내 전제를 뒤집었다. PG 재전송이 429 를 받으면 PG 는 실패로 보고 재전송을 소진한다 — **돈은 빠져나갔는데 원장은 미결제**로 남는 조용한 사고다. 헬스체크와 같은 이유로 비켜 준다: **정작 중요할 때 막히면 안 되는 길** | 유료 저장소 pg-ciderpay.md §2 |
| e2e163 | **트리 모양 규칙 — 한 벌만 둔다** (2026-08-18, 협업 준비) | 구성: emm-parser 직접 호출(tsx). **11항목** + 적합성 **12/12 무회귀**. 같은 판정이 세 군데(정규화 SQL · 협업 CRDT · 화면 드래그)에서 필요해 **순수 함수로 공개 패키지에** 두었다 — 규칙이 두 벌이 되면 어느 쪽이 맞는지 아무도 모르게 된다. ①뿌리로 올리기는 안전 ②형제 밑은 안전 ③자기 밑은 막는다 ④**자손 밑은 막는다** ⑤제자리는 안전 ⑥모르는 노드에서 끊기면 통과 ⑦**이미 순환이 들어간 망가진 데이터에서도 멈춘다**(상한을 두고, 판정 못 하면 **막는 쪽**을 고른다) ⑧부모가 사라진 노드를 찾는다 ⑨**그 자손도 함께** ⑩멀쩡한 노드는 안 담는다 ⑪**순환에 갇혀 뿌리에 못 닿는 노드도 고아로 본다**(화면에서 사라지는 것은 부모가 없는 것과 같다). 고아는 **지우지 않고 뿌리로 올린다** — 사용자가 방금 쓴 것을 우리가 지우면 안 된다 | 27-sync-model.md §4 |
| e2e162 | **Yjs 실측 — 노드 1만 개에서 쓸 만한가** (2026-08-18, 협업 착수 전) | 구성: Node + yjs 13.6.32 단독 스크립트(앱 없이). 설계 문서(`27-sync-model.md` §12)가 **"구현 전에 재 봐야 한다"** 고 적어 둔 것을 잰 것이다. 노드 하나 = `Y.Map` + `Y.Text`. **잰 값** — 10,000노드에서 서버 메모리 **21.2MB** · 인코딩 1,495KB(우리 JSON 의 **1.28배**) · 물질화 **15ms** · 다시 열기 167ms · **한 글자 편집 전파 24바이트** · 1만 번 편집 뒤 **1.07배** · **노드 절반 삭제 시 0.56배로 줄어듦**(Yjs 기본 GC 가 실제로 돈다). 200/1,000노드도 함께 재 선형(노드당 **약 2.1KB**)임을 확인. **결론** — 설계를 바꿀 필요는 없다. 다만 **새 제약**이 드러났다: 서버 메모리가 사람 수가 아니라 **열려 있는 큰 맵 수**에 비례한다(1만 노드 맵 100개 ≈ 2GB). §5 의 "아무도 없으면 메모리에서 내린다"가 **선택이 아니라 필수**가 됐고, 인스턴스당 동시 맵 상한과 **거절 이유를 말하는 것**을 구현 항목으로 추가했다. **재지 않은 것을 적어 뒀다** — awareness 오버헤드, 브라우저 쪽 메모리, 접속자 20명 상한의 근거 | 27-sync-model.md §13 |
| e2e161 | **루트 노드 사진이 MD 로 나가고 돌아온다** (2026-08-18, B17) | 구성: emm-parser 직접 호출(tsx). **11항목** + 기존 적합성 **12/12 무회귀**. ①루트 사진(data URL)이 `files/img-1.png` 로 담긴다 ②본문에 사진 줄이 들어간다 ③**루트 노트로 새지 않는다** ④**가지가 늘지 않는다**(사진이 노드가 되지 않는다) ⑤메타데이터가 루트 사진을 들고 있다 ⑥외부 URL 은 ZIP 에 담지 않는다 ⑦⑧외부 URL 은 **본문으로 왕복**한다 ⑨루트 노트가 함께 있어도 섞이지 않는다 ⑩⑪사진이 없으면 아무것도 달라지지 않는다. **직렬화만 고치면 안 됐다** — 파서는 첫 견출 앞의 문단을 **루트 노트**로 담으므로, 내보내기만 고치면 돌아올 때 **사진 문법이 그대로 적힌 루트 노트**가 된다. 파서에 "첫 견출 전의 사진만 있는 문단 → 루트 사진" 갈래를 함께 넣었다. 함정 1가지 — `files/` 상대 경로는 **본문이 아니라 메타데이터·ZIP 이 복원한다**(가지 노드도 같은 규칙). 이걸 모르고 본문 왕복을 기대하면 **멀쩡한 동작을 실패로 읽는다** | emm-spec.md · markdown-export.md |
| e2e160 | **안 쓰는 사진만 지운다** (2026-08-18, B16 ② 슬라이스 4) | 구성: PG + API :3001. **20항목**. **유예기간 2** — ①방금 만든 사진은 **안 지운다**(사진을 지우고 자동저장이 돈 직후 Ctrl+Z 를 누르면 다시 필요해진다) ②로그도 안 찍힌다. **히스토리 보호 4** — ③하루가 지나도 **버전이 쓰는 사진은 살아 있다** ④그래서 아무것도 안 지운다 ⑤버전을 지우면 **그때 비로소 정리된다** ⑥남은 것은 문서가 쓰는 사진. **무변경 저장 3** — ⑦⑧⑨문서가 그대로인 저장에서도 정리한다(**바뀐 저장에서만 정리하면 정리할 기회가 영영 안 온다** — 사진을 지운 저장은 한 번뿐이고 그때는 유예기간 안이다) + 지운 개수·용량을 로그에 남긴다. **안전장치 4** — ⑩안 쓰는 **사용자 파일은 살아 있다**(우리 이름 꼴이 아니다) ⑪쓰는 사진은 **9일이 지나도** 남는다 ⑫사용자 파일도 남는다 ⑬**이름을 흉내 낸 파일(`img-0123456789abcdef.png`)은 지워진다 — 알려진 한계로 기록**. **되돌리기 시나리오 3** — ⑭사진 2장 ⑮**지운 직후에도 파일이 살아 있다** ⑯되돌리면 **다시 올리지 않고 같은 파일을 쓴다**(내용 해시). **버전 보호 재확인 4** — ⑰⑱⑲⑳. 함정 2가지 — ⑴`keepVersion:true` 로 저장한 픽스처는 **히스토리 버전이 사진을 붙들어** 안 지워진다. 처음엔 GC 버그로 오해했는데 **코드가 옳았다** ⑵옛 첨부 id 를 물고 있는 픽스처를 재사용하면 GC 가 "안 쓰는 사진"으로 판정한다 — **검증 문서는 매번 현재 DB 에서 떠야 한다** | content-permanence.md §7.1 |
| e2e159 | **저장하면 사진이 저장소로 옮겨간다** (2026-08-16, B16 ② 슬라이스 3) | 구성: PG + API :3001 + vite :5173 + Playwright. **22항목**. 24KB 짜리 진짜 PNG 를 만들어 data URL 로 넣은 문서(**65,941 바이트**)를 저장. **이관 7** — ①문서가 **741 바이트**로 줄었다 ②첨부는 **1개**(같은 사진을 두 노드가 써도 하나) ③이름이 내용 해시(`img-<16자>.png`) ④상대 경로로 바뀐다 ⑤두 노드가 **같은 주소**를 가리킨다 ⑥**8KB 아래 작은 사진은 그대로 data URL** ⑦**남의 사이트 URL 은 손대지 않는다**. **멱등 4** — ⑧같은 data URL 문서를 다시 저장해도 **첨부가 늘지 않는다**(화면은 저장 뒤에도 메모리에 data URL 을 들고 있어 자동저장마다 또 보낸다 — 알아보지 못하면 **저장할 때마다 사진이 하나씩 늘어 용량을 갉아먹는다**) ⑨로그가 `이미 있던 것 1장` ⑩⑪이미 옮겨진 문서를 다시 저장해도 그대로. **왕복 6** — ⑫서버가 상대 경로를 저장했다 ⑬내보내기가 그것을 되받아 담는다 ⑭**결과물에 서버 경로가 남지 않는다** ⑮못 담은 것 0 ⑯**화면이 API 절대 주소로 풀어서** 그린다(상대 경로를 그대로 두면 **프런트 주소로 풀려 404**) ⑰요청 200. **되돌려 깨뜨리기 5** — 문턱을 999MB 로 올려 이관을 끄면 ⑱문서가 그대로 크고 ⑲첨부가 안 생긴다. 되돌리고 나서 ⑳**옛 문서(data URL)를 그대로 읽어 준다** ㉑**다음 저장에서 옮겨진다**(일괄 마이그레이션 없이) ㉒그때 문서가 작아진다. 함정 1가지 — `POST /maps` 응답의 키는 `id` 가 아니라 **`mapId`** 다 | content-permanence.md §7.1 |
| e2e158 | **사진이 서버 저장소에 있어도 내보내기·화면이 온전한가** (2026-08-16, B16 ② 슬라이스 1·2) | 구성: PG + API :3001 + 진짜 1×1 PNG 를 첨부로 업로드 + vite :5173 + Playwright. **17항목**. **내보내기 10** — ①MD 가 ZIP 으로 나오고 사진 2장(서버1+data1)이 `files/` 에 담긴다 ②**서버 URL 이 결과물에 남지 않는다** ③**남의 사이트 URL 은 그대로 남는다**(우리 것만 손댄다) ④못 담은 것 0 ⑤HTML 은 첨부가 없으니 단일 `.html` ⑥**서버 URL 없음** ⑦사진이 data URL 로 들어 있다 ⑧못 담은 것 0 ⑨**없는 첨부 id 를 가리키는 사진은 지우지 않고 원래 URL 로 남는다** ⑩그 수를 `external` 로 알린다. **화면 3** — ⑪캔버스가 서버 사진을 그린다 ⑫사진 요청이 실제로 나갔다 ⑬200. **주소 판정 4** — ⑭우리 저장소 URL 을 알아본다 ⑮남의 사이트는 아니다 ⑯data URL 도 아니다 ⑰**토큰이 붙어 있어도 알아본다**. 함정 2가지 — ⑴HTML 내보내기는 **첨부가 없으면 일찍 반환**하는 길이 있어, 사진 되돌리기를 그 위에 두지 않으면 **사진만 있는 맵이 서버 URL 로 나간다** ⑵직렬화는 `png·jpg·gif·webp` 만 ZIP 에 담는다. 그 밖의 형식을 data URL 로 바꿔 놓으면 **알아보지도 못하고 http URL 도 아니라서 사진이 통째로 빠진다** — 못 담는 형식은 원래 URL 로 둔다 | content-permanence.md §7.1 |
| e2e157 | **협업맵은 편집 잠금을 비켜 간다** (2026-08-16, B16 ①) | 구성: PG + API :3001 + 같은 맵을 두 세션(A·B)으로. **18항목**. **단독맵 5**(지금 동작이 안 깨졌는지) — ①A 가 열면 `acquired` ②B 가 열면 `busy` ③**잠금 주인 A 의 저장은 통과** ④B 의 저장은 `편집 중인 맵` 409 ⑤B 하트비트 `held:false`. **협업맵 10** — ⑥⑦A·B 둘 다 `editLock` 을 **아예 받지 않는다**(`acquired` 라고 답하면 잠근 적 없이 잠갔다고 말하는 것이다) ⑧`kind` 는 알려 준다 ⑨⑩**A·B 저장이 둘 다 200** ⑪⑫**A·B 하트비트가 둘 다 `held:true`** — 이걸 빠뜨리면 25초마다 프런트가 편집권 상실로 읽어 **맵과의 연결을 끊고** 편집이 조용히 로컬 초안으로 빠진다 ⑬해제 ok ⑭**잠금 표에 행 자체가 없다** ⑮단독맵은 행이 있다. **되돌려 깨뜨리기 3** — `kind` 를 `solo` 로 바꾸면 ⑯`acquired` ⑰`busy` ⑱저장이 다시 막힌다. 함정 2가지 — ⑴`PUT /document` 는 `editSession` 을 **본문**으로 받는다(쿼리가 아니다). 쿼리에 넣으면 세션 없는 저장이 되어 잠금 주인마저 409 가 난다 ⑵두 맵에 **같은 제목**을 주면 제목 중복 409 가 나는데, 잠금 409 와 **상태 코드가 같아** 잠금이 깨진 것처럼 보인다 — 409 는 코드가 아니라 **문장으로** 판정해야 한다 | 27-sync-model.md §7 |
| e2e156 | **협업 자리 — 화면이 서버에 물어본다** (2026-08-16, B15) | 구성: PG + API :3001 + vite :5173 + Playwright. **17항목**. **꺼짐 11** — ①좌측 레일에 `협업` 자리가 있다 ②**가짜 배지가 붙은 옛 버튼이 사라졌다**(읽지 않은 메시지를 뜻하는 빨간 점이 붙어 있었는데 누르면 아무 일도 없었다 — **오지도 않은 메시지를 왔다고 말하는 표시**였다) ③**열기 전에는 서버를 부르지 않는다**(게으르게) ④누르면 `/v1/features` 를 부른다 ⑤기능 이름이 **서버 것**으로 나온다(`실시간 협업`) ⑥`준비 중` 배지 ⑦**서버가 준 이유가 그대로** 보인다 ⑧머리글 부제 ⑨**다시 열어도 또 부르지 않는다**(모듈 수준 캐시) ⑩자바스크립트 오류 없음 ⑪실패한 요청은 외부 폰트 CDN 뿐. **켜짐 3** — 진짜 유료 패키지에 협업만 산 라이선스를 넣고 ⑫**"준비 중"이라고 말하지 않는다** ⑬`이 화면 모듈이 설치돼 있지 않습니다` ⑭빌드 안내를 준다(서버는 켜졌는데 UI 가 없는 상태 — 조용히 빈 화면을 주면 배포에서 UI 를 빠뜨린 것을 아무도 못 찾는다). **서버가 죽었을 때 3** — API 를 내리고 ⑮**"유료라서 안 된다"고 하지 않는다**(백엔드가 잠깐 죽은 것을 "안 샀다"로 보여 주면 **있지도 않은 결제를 의심하게 된다**) ⑯`기능 정보를 불러오지 못했습니다` ⑰다음에 무엇을 할지 알려 준다. 함정 1가지 — 콘솔의 `Failed to load resource` 는 **주소를 담지 않는다.** 문자열로 거르면 무엇이 실패했는지 모른 채 통과시키게 되므로 `requestfailed` 로 **URL 을 모아** 판정해야 한다 | open-core-boundary.md §5 |
| e2e155 | **유료 확장점 일반화** — 이음매 하나로 갈리는가 (2026-08-16, B15) | 구성: PG + API :3001. **28항목**(드리프트 4항목은 68차 추가). **스텁 10** — ①기동 로그 `유료 모듈: 없음(스텁) — MVP 공개판입니다` ②**유료 모듈 없이도 헬스 ok** ③`GET /v1/features` 가 **로그인 없이 200** ④`installed:false` ⑤**목록이 비어 있지 않다(6개)** — 비면 화면이 "그런 기능이 원래 없다"로 읽고 자리조차 안 그린다 ⑥전부 꺼짐 ⑦전부 이유가 있다 ⑧이름이 온다(`실시간 협업`) ⑨**옛 `/v1/collab/status` 는 404**(이음매가 실제로 옮겨졌다) ⑩헬스가 유료 표를 요구하지 않는다. **구현 9** — `node_modules/@easymindmap/pro` 에 가짜 유료 패키지를 만들어 ⑪로그가 `설치됨 · 켜진 기능: collab, translate` ⑫`installed:true` ⑬구현이 준 목록 6개가 그대로 ⑭**켜진 것 2개**(라이선스 판정이 유료 쪽에서 돈다) ⑮켜진 것은 `reason:null` ⑯**안 산 기능은 다른 이유**를 준다(`이 계약에 포함되지 않은…`) ⑰**유료 모듈이 자기 경로를 가져왔다**(`/v1/collab/ping` → `from:@easymindmap/pro`) ⑱그 경로가 200 ⑲헬스 ok. **되돌리기 5** — 패키지를 지우면 ⑳로그가 다시 `없음(스텁)` ㉑`installed:false` ㉒**유료 모듈이 가져왔던 경로가 사라진다(404)** ㉓목록은 그대로 6개(자리는 남는다) ㉔헬스 ok. **드리프트 4** — 모듈 dist 를 손으로 고쳐 오래된 판을 흉내내 ㉕**모듈이 모르는 기능도 목록에 남고** ㉖그 이유가 다른 것과 구분되며(`모듈이 코어보다 오래된 판일 수 있습니다`) ㉗**코어가 모르는 id 도 감추지 않고 뒤에 붙고** ㉘총 7개가 된다. 함정 1가지 — 가짜 패키지는 **순수 JS** 라 데코레이터를 못 쓴다. `Module({...})(ProModule)` 처럼 **함수 호출로** 적용해야 한다(그냥 `@Module` 로 쓰면 `require` 가 문법 오류로 던지고, try/catch 가 그걸 삼켜 **"패키지가 없다"로 조용히 오인**된다) | open-core-boundary.md §4 |
| e2e154 | **협업 확장점** — 스텁과 구현이 자리를 바꾸는가 (2026-08-15, B15) | 구성: PG + API :3001. **15항목**. 스텁일 때 — ①기동 로그가 `협업 모듈: 없음(스텁)` ②**유료 모듈 없이도 헬스가 ok**(없는 것이 공개판의 정상 동작이다) ③`enabled:false` ④**왜 꺼졌는지 말한다**(유료 기능) ⑤기능 목록 비어 있음 ⑥`status` 는 **로그인 없이 200** ⑦참가는 **404 로 거절**(조용히 성공하지 않는다) ⑧같은 이유를 본문에 그대로 ⑨토큰 없이는 401(가드 살아 있음) ⑩**헬스체크가 협업 표를 요구하지 않는다**(공개판이 영원히 degraded 가 되지 않게). **구현을 꽂았을 때** — `node_modules/@easymindmap/collab` 에 가짜 유료 패키지를 만들어 ⑪로그가 `설치됨` 으로 바뀌고 ⑫`enabled:true` ⑬구현이 알린 기능 목록이 그대로 오고 ⑭참가가 **200 으로 성공**하며 ⑮구현이 만든 세션(endpoint·token)이 그대로 온다. 그리고 **패키지를 지우면 다시 스텁으로 돌아간다**(enabled:false · join 404 · 로그 `없음(스텁)`) — 양방향을 다 봤다. 함정 1가지 — NestJS 의 POST 기본 응답은 **201** 이다. 이 저장소 관례(`login/start` 등)대로 `@HttpCode(200)` 을 붙였다 | open-core-boundary.md §4 |
| e2e153 | **헬스체크 감시** — 나빠지면 메일 (2026-08-15 사용자 요청, B14 ⑤) | 구성: **상태를 마음대로 바꾸는 가짜 헬스 엔드포인트 :3999** + **가짜 SMTP :2525**(curl 이 실제로 보낸 것을 파일로 받는다). **18항목**. ①처음 도는데 정상이면 **메일을 보내지 않는다** ②상태는 기록한다 ③정상→degraded 에 보낸다 ④수신자 둘이 모두 RCPT 에 실린다 ⑤본문에 **원인**(`missingTables`)이 담긴다 ⑥제목이 MIME 인코딩(`=?UTF-8?B?`) ⑦해독하면 한글이 온전하다 ⑧⑨**같은 상태가 이어지면 몇 번을 돌려도 다시 보내지 않는다**(알림 피로) ⑩하루가 지나면 한 번 더 보내고 ⑪제목이 `계속됨` 으로 구분된다 ⑫degraded→down 은 곧바로 보낸다 ⑬down 으로 기록 ⑭안내가 "Coolify 배포 로그를 보라"로 바뀐다 ⑮**포트가 안 열린 주소도 down 으로 잡는다** ⑯복구도 알린다 ⑰제목이 `복구됨` ⑱복구 뒤 조용해진다. **실제로 잡은 버그** — 첫 실행에서 정상인데도 `복구됨` 메일을 보냈다(이전 상태가 없어 "바뀐 것"으로 봤다). 설치하자마자 뜬금없는 복구 메일이 온다. 첫 실행 + 정상이면 조용하도록 고쳤다. 함정 1가지 — 메일 헤더는 **CRLF** 라 `base64 -d` 앞에 `tr -d` 로 `\r` 을 빼지 않으면 제목 해독이 빈 문자열이 되고 **단언이 조용히 통과**한다 | runbook §2.2 |
| e2e152 | **접속한 곳 칸은 늘 있다 · 연이은 두 로그인** (2026-08-15 사용자 지적) | 구성: PG(앱+gotrue) + API :3001 + 가짜 GoTrue 로그인 :9999 + vite :5173. **8 + 서버 3**. ①**8초 간격으로 진짜 로그인 폼을 두 번 눌러** 2건이 각각 기록되는 것을 확인 (중복 방지 창을 60초 → 5초로 줄인 결과 — 60초일 때는 뒤엣것을 잃었다) ②기록이 있는 회원에 `접속한 곳` 칸 ③**기록이 없는 회원에도 칸이 있다** ④**두 표의 칸 구성이 완전히 같다**(`시각·사건·접속한 곳`) ⑤값이 있는 쪽엔 IP·기기 ⑥없는 쪽은 `—` ⑦없는 쪽은 이유를 함께 적는다 ⑧콘솔 오류 없음. 서버 — ⑨1회차 `recorded:true` ⑩**2초 뒤는 false**(같은 요청 중복은 여전히 막는다) ⑪**6초 뒤는 true**(진짜 두 번째 로그인은 기록된다). 고친 이유 — 값이 하나도 없으면 칸을 숨기던 것은 GoTrue 가 IP 를 **영원히** 안 주던 때의 판단이다. 이제 값이 채워지므로 숨기면 **회원마다 표의 칸 수가 달라져** 관리자가 두 회원을 나란히 볼 때 같은 화면을 다른 것으로 읽게 된다 | admin-console.md §3.1 |
| e2e151 | **런북 DB 탐색 · 사설 사슬 안내** (2026-08-14 실사용에서 깨졌다) | 구성: API :3001. **3항목**(서버 명령은 실사용 로그로 검증). `/v1/health/ip` 의 `hint` 를 세 가지 사슬로 확인 — ①사용자가 실제로 겪은 `192.168.0.1, 192.168.0.74`(전부 사설·2단계)에 **"TRUST_PROXY 를 바꿔도 달라지지 않는다 + 휴대폰 LTE 로 갈라 보라"** 가 나온다 ②공인 IP 가 섞이면 `OK — 공인 IP 입니다` ③사슬이 하나뿐인 사설은 기존 `lostUpstream` 판정이 그대로 산다. 고친 이유 — 전에는 사슬이 **하나일 때만** 원인을 짚고, 둘 이상이면 "TRUST_PROXY 조정이 필요합니다"라고만 말해 **고칠 수 없는 자리를 고치라고 안내**하고 있었다. 런북 쪽 — 델타 SQL 스크립트가 **이미지 이름으로** DB 컨테이너를 찾고 `-U postgres` 를 고정하던 것을, **API 의 `DATABASE_URL` 에서 컨테이너·계정·DB 를 읽도록** 전부 바꿨다(§1.5-0-A). 실사용에서 앱 DB 이미지가 **ID(`33f923b05f64`)로만 보여** 걸리지 않았고, 대신 다른 앱 DB 가 잡혀 `role "postgres" does not exist` 로 끊겼다 | runbook §1.5-0-A · §6.4 |
| e2e150 | **접속한 곳이 안 보일 때 무엇을 하면 되는지 말하는가** (2026-08-14 사용자 지적) | 구성: PG(앱+gotrue) + API :3001 + 가짜 GoTrue 로그인 :9999 + vite :5173. **14항목**. ①표를 **지우면** `ipSource:no-table` ②그래도 **목록은 그대로 보인다**(로그인 이력은 IP 와 무관하다) ③화면이 `data-why="no-table"` 로 갈라 말하고 ④**델타 SQL 위치(§1.5-0-F)까지 짚는다** ⑤표를 되살리면 `no-records` ⑥**재기동 없이** 알아본다(표 유무를 매번 확인한다 — 캐시하면 적용하고도 계속 "없다"고 답한다) ⑦⑧화면이 "다시 로그인하면 그때부터 보입니다"라고 말한다 ⑨**안내대로 진짜 로그인하니 기록이 생겼다**(안내가 거짓말이 아닌지 확인) ⑩`ipSource:ok` 로 바뀐다 ⑪접속한 곳 칸이 돌아온다 ⑫방금 줄에 `127.0.0.1 · Linux · HeadlessChrome 141` ⑬못 맺은 줄이 남으면 **그것만 따로** 설명한다 ⑭"표가 없다/다시 로그인하라" 안내는 사라진다. 함정 1가지 — **e2e 토큰을 파일에 남겨 쓰면 2시간 뒤 401 로 죽는다**(실제로 겪었다). 매 실행마다 새로 발급한다 | admin-console.md §3.1 |
| e2e149 | **로그인 접속 IP 를 우리가 남긴다** (2026-08-14 사용자 요청) | 구성: PG(앱+gotrue) + API :3001 + **가짜 GoTrue 로그인 서버 :9999**(진짜 로그인 흐름을 지나야 `authStore` 가 기록을 부르는지 볼 수 있다) + vite :5173. **서버 17 + 화면 12**. 서버 — ①기록했다고 답한다 ②**서버가 본 IP**(`X-Forwarded-For: 203.0.113.9`)가 들어간다 ③기기도 ④**본문에 `ip` 를 실으면 400 으로 거절**(전역 ValidationPipe) ⑤60초 안 두 번째는 `recorded:false` ⑥위조한 1.1.1.1 은 **한 줄도 안 들어갔다** ⑦줄이 늘지 않았다 ⑧토큰 없이 401 ⑨⑩**1분 간격 로그인 둘에 서로 다른 IP 가 정확히**(일대일 매칭) ⑪기기 정보도 함께 ⑫**로그아웃 줄에는 붙지 않는다** ⑬**기록 이전 로그인은 null** ⑭시각이 10분 어긋나면 붙지 않는다 ⑮관리자 조회에도 실린다 ⑯260건을 넣어도 **200건만 남는다** ⑰계정을 지우면 함께 사라진다(CASCADE). 화면 — ⑱로그인 전에는 기록이 없다 ⑲**진짜 로그인 폼으로 로그인하면 `POST /account/login-event` 가 딱 1회 나간다** ⑳DB 에 `127.0.0.1|Linux|HeadlessChrome 141` 한 줄 ㉑IP 는 본문이 아니라 서버가 본 값 ㉒브라우저도 알아냈다 ㉓**새로고침은 기록을 늘리지 않는다** ㉔이력에 `접속한 곳` 칸이 생긴다 ㉕방금 로그인 줄에 IP ㉖기기도 함께 ㉗**30일 전 로그인은 `—`** ㉘왜 비었는지 밝힌다 ㉙콘솔 오류 없음. 함정 3가지 — ⑴**`INET` 컬럼은 `::text` 가 `/32` 를 붙인다** — 화면에 `203.0.113.9/32` 로 새어 나갔다. 기존 `map_document_versions.client_ip` 와 같은 `VARCHAR(45)` 로 맞췄다 ⑵**가짜 GoTrue 의 경로는 `/auth/v1/token`** 이다(`/token` 으로 받으면 로그인이 조용히 실패한다) ⑶psql 에 여러 줄 SQL 을 `JSON.stringify` 로 넘기면 **`\n` 이 글자 그대로** 들어간다 — 공백으로 눌러야 한다 | admin-console.md §3.1 · runbook §1.5-0-F |
| e2e148 | **회원관리 가로 스크롤 · 이력 건수 규칙 · IP 없음** (2026-08-14 사용자 지적) | 구성: PG + **GoTrue DB 에 세 사람을 심었다** — IP 있는 사람(5건) · **IP 가 빈 문자열인 사람**(8건, 운영과 같은 상태) · **141건 쌓인 사람** + API :3001 + vite :5173. **17항목**. 가로 — ①②1920·1600px 에서 표가 그릇을 넘지 않는다(1522/1522) ③1280px 로 좁혀도 넘치지 않는다(1242/1242) — 표가 스스로 줄어든다. IP — ④IP 가 하나도 없으면 **칸(th)이 아예 없다** ⑤대신 "GoTrue 가 남기지 않는다"고 말한다 ⑥**줄줄이 `—` 가 보이지 않는다**(0개) ⑦IP 가 있는 사람은 **칸이 그대로 나오고** 값(203.0.113.9)이 보인다 ⑧그때는 "IP 없음" 안내가 뜨지 않는다. 건수 규칙 — ⑨8건뿐이면 `기록 전체 8건입니다` ⑩141건이면 목록은 50건이고 ⑪⑫`이 목록은 최근 50건까지만 보여 줍니다 (전체 로그인 141회)`. ⑬⑭사용자 창(아바타 → 로그인 기록)도 **같은 문구·같은 규칙**. ⑮콘솔 오류 없음. **되돌려 깨뜨리기** — 곁들이 정보를 **다시 같은 줄에 붙이면**(`display:inline` + `nowrap`) 표가 **1424px** 로 부풀어 1242px 그릇을 넘는다 = 사용자가 본 가로 스크롤바. 함정 2가지 — ⑴**측정 대상을 잘못 잡으면 늘 통과한다**: 처음에 `admin-members > div` 의 마지막 자식을 쟀는데 그건 표가 아니라 **표 아래 안내 문구**였다. `data-testid="admin-members-table-wrap"` 을 붙여 다시 쟀다 ⑵**폭을 넓힌 것이 고친 대목이 아니다**: 1280 → 1560 은 넓은 화면에서 덜 빽빽하려는 것이고, 좁은 창에서도 안 넘치는 이유는 **줄바꿈**이다 — 되돌려 깨뜨려 구분했다 | admin-console.md §3.1 |
| e2e147 | **관리자 콘솔이 스스로 구르는가** — 설정관리 아래가 잘렸다 (2026-08-14 사용자 지적) | 구성: PG + API :3001 + vite :5173, **1440×800**(사용자와 같은 노트북 높이대). **14항목**. ①내용이 3436px 로 화면(800px)보다 길다 — 잘릴 수 있는 상황이 맞다 ②콘솔 바깥이 `overflow-y: auto` ③처음엔 마지막 묶음(기능 스위치)이 화면 밖 ④실제로 굴러간다(`scrollTop=2636`) ⑤**끝까지 굴리면 마지막 묶음이 화면 안(y=514)** ⑥굴려도 탭 머리글이 붙어 있다(y=10, sticky) ⑦회원관리도 같은 그릇 ⑧낮은 창(1200×260)에서 로그인 카드가 화면보다 길다 ⑨**위쪽이 잘리지 않는다**(y=20 — `align-items:center` 를 쓰면 음수가 된다) ⑩굴리면 [다음] 버튼에 닿는다(y=153) ⑪⑫**에디터 쪽은 그대로** — `body` 가 여전히 `overflow:hidden` 이고 페이지가 통째로 구르지 않는다. **되돌려 깨뜨리기** — 고치기 전 모양(`minHeight:100vh` · overflow 지정 없음)으로 되돌려 **휠을 12번 굴려도** 마지막 묶음이 y=3150(화면 800px)에 남아 있는 것을 재현하고, 고친 뒤 같은 휠 동작으로 y=514 에 들어오는 것을 확인했다. 함정 1가지 — **`scrollTop` 을 프로그램으로 넣으면 재현되지 않는다**: `overflow:hidden` 인 body 도 스크립트로는 움직인다. 사용자가 겪은 것은 "휠이 안 먹는다"이므로 **실제 휠 이벤트**로 봐야 한다 | admin-console.md §1 |
| e2e146 | **설정관리 편집** — DB 값만 열고 나머지는 잠근다 (2026-08-14 사용자 결정) | 구성: PG + API :3001(supabase) + vite :5173. **서버 22 + 화면 24**. 서버 — ①요금제 항목에만 `editable` 이 실린다 ②basic 이 10240 MB ③범위(1~1048576)를 **서버가 준다** ④**환경변수·코드 항목에는 `editable` 이 하나도 없다** ⑤편집 가능한 것은 요금제 4개뿐 ⑥응답이 옛 값·새 값을 함께 준다 ⑦`usersUpdated:2, usersKept:1` ⑧표가 20 GB ⑨`updated_by` 에 관리자 이메일 ⑩**함수가 새 값을 준다** ⑪기존 회원 2명이 따라왔다 ⑫**특별 계약은 50 GB 그대로** ⑬요금제를 바꾼 회원이 트리거로 새 한도를 받는다 ⑭0·1048577·1.5·없는 요금제·문자열 **5가지 모두 400** ⑮관리자 표 없이 401 ⑯막힌 뒤에도 값은 그대로 ⑰**`schema.sql` 을 다시 적용해도 되돌아가지 않는다**. 화면 — ⑱편집 가능한 줄 4개 · 잠긴 줄 41개 ⑲입력칸도 4개뿐 ⑳`ADMIN_EMAILS` 줄에 입력칸 없음 ㉑`CODE_TTL_MIN` 줄에도 없음 ㉒안내가 "DB 값뿐"이라고 밝힌다 ㉓왜 환경변수를 막았는지도 적는다 ㉔10240 MB 옆에 `= 10 GB` ㉕안 바꿨으면 [저장] 잠김 ㉖0 도 잠김 ㉗범위를 알려 준다 ㉘너무 큰 값도 잠김 ㉙바꾸면 열린다 ㉚**한 번 묻는다**(`10 GB → 20 GB`) ㉛특별 계약을 그대로 둔다고 밝힌다 ㉜`회원 2명 반영` ㉝`1명은 특별 계약이라 그대로` ㉞입력칸이 새 값으로 남는다 ㉟DB 표·회원·특별 계약 3가지 확인 ㊱새로고침해도 20480 ㊲콘솔 오류 없음. **되돌려 깨뜨리기 3가지** — ⑴씨앗을 `ON CONFLICT DO UPDATE` 로 넣어 보니 **관리자 수정이 10 GB 로 되돌아갔다** (그래서 `DO NOTHING`) ⑵`UPDATE users … WHERE plan='basic'` 로 전원을 덮어 보니 **특별 계약 50 GB 가 20 GB 로 사라졌다**(그래서 옛 한도와 같은 사람만 옮긴다) ⑶**델타 적용 전 상태를 재현**해(옛 함수 + 표 삭제) 열어 보니 입력칸이 **0개**, 값은 그대로 10240 MB, 안내는 "델타 SQL 을 적용하면…", 저장 시도는 503, `/v1/health` 는 `degraded · missingTables:[plan_quotas]`. 함정 1가지 — **`where`·`note` 는 화면이 그대로 찍는다**: 마크다운 `**강조**` 를 넣었더니 별표가 그대로 보였다 | admin-console.md §3.3 · runbook §1.5-0-E |
| e2e145 | **관리자 도메인 분기 + 환경 배지** — 운영 `admin.easymindmap.org` (2026-08-14 사용자 결정) | 구성: vite :5173 **만**(API 불필요 — 로그인 화면까지는 서버를 부르지 않는다) + `/etc/hosts` 에 `admin.localhost`·`web.localhost` 를 심어 **진짜 호스트명으로** 연다. **주소 분기 10 + 배지 4 + 되돌려 깨뜨리기 1**. 주소 — ①`admin.localhost/` = 콘솔 ②에디터 문구(`내 문서`)가 없다 ③콘솔 오류 없음 ④`admin.localhost/anything/deep?x=1` 도 콘솔(**경로와 무관**) ⑤`admin.localhost/?map=abc` 로도 **에디터가 열리지 않는다**(IP 제한을 우회할 통로를 두지 않는다) ⑥`localhost/admin` 은 지금 그대로 콘솔 ⑦끝의 `/` 도 콘솔 ⑧`localhost/` = 에디터 ⑨**`/administrator` 는 콘솔이 아니다** ⑩**`web.localhost` 도 아니다**. 배지 — ⑪로그인 화면에도 있다 ⑫⑬⑭`VITE_API_URL` 을 바꿔 가며 세 번 띄워 `api-dev.mindmap.ai.kr`→**개발**(파랑 `rgb(227,240,255)`) · `api.easymindmap.org`→**운영**(빨강 `rgb(253,232,232)`) · 없음→**로컬**(회색)이고 툴팁이 `API: <호스트>` 인 것을 확인. **되돌려 깨뜨리기** — 같은 화면에 일부러 `운영`을 기대시켜 **FAIL 이 나는 것**까지 봤다(단언이 살아 있다는 증거). 함정 2가지 — ⑴**호스트명 검증은 `localhost` 로 못 한다**: `/etc/hosts` 에 심어 실제 이름으로 열어야 `startsWith('admin.')` 가 검증된다 ⑵`pgrep -f "vite --port"` 로 죽이면 **내 셸이 같이 죽는다**(exit 144) — PID 를 따로 얻어 죽인다 | admin-console.md §1 · §7 |
| e2e144 | **로그인 이력 전체** — 횟수·시각·IP 목록 (2026-08-14 사용자 요청) | 구성: 앱 PG + **GoTrue DB(`gotrue`) 를 따로 만들어 `auth.audit_log_entries` 에 6행을 심었다** + API :3001(`GOTRUE_DATABASE_URL` **있음**) + API :3002(**없음**) + vite :5173. **서버 7 + 화면 17**. 서버 — ①관리자가 아무 회원의 이력을 본다(`available:true` · 4건) ②`token_refreshed` 2행은 빠졌다 ③`최근 30일 2회 · 전체 3회` ④**다른 사용자의 토큰으로는 자기 것만** 나온다(가입 1건 · 전체 로그인 0회) — `/v1/account/logins` 는 **경로에 id 를 받지 않아** 남의 것을 넣을 자리가 없다 ⑤토큰 없이 401 ⑥관리자 경로도 표 없이 401 ⑦**설정이 없는 :3002 도 기동에 성공**하고 경고를 남긴 뒤 `available:false` 를 **HTTP 200** 으로 준다. 화면 — ⑧회원 행의 [이력] 버튼 ⑨처음엔 접혀 있다 ⑩누르면 펼쳐진다 ⑪**누구의 이력인지 밝힌다**(`ok@baro.pro 의 로그인 이력`) ⑫⑬같은 숫자(30일 2회 · 전체 3회) ⑭목록 4건 ⑮`token_refreshed` 행 없음 ⑯**시각이 초까지 + IP**(`2026.08.14 05:04:09 로그아웃 203.0.113.9`) ⑰다시 누르면 접힌다 ⑱아바타 메뉴에 [🕘 로그인 기록] ⑲준비 중 배지 없음 ⑳**비밀번호 변경 아래**(220 < 254) ㉑창이 열린다 ㉒내 이메일과 "모르는 접속이 있으면" 안내 ㉓같은 숫자 ㉔같은 4건. 함정 1가지 — **설정이 없을 때 빈 목록을 주면 안 된다**: "로그인한 적이 없다"로 읽힌다. `available:false` 를 따로 두고 화면이 *왜* 비었는지 말하게 했다(:3002 로 실제로 확인). 확인하지 못한 것 — **감사 로그 표의 실제 모양은 내가 만든 표로만 봤다.** dev 의 진짜 GoTrue DB 는 열어 보지 못했다 | admin-console.md §3.1 |
| e2e143 | **진짜 로그인 이력** — 마지막 로그인 시각 (2026-08-13 사용자 요청) | 구성: PG + API :3001 + **가짜 GoTrue 관리자 :9996**(GET /admin/users 로 last_sign_in_at·banned_until 을 준다) + vite :5173. **서버 4 + 화면 7**. 서버 — ①`loginHistoryAvailable: true` ②두 계정의 마지막 로그인·차단 값이 실려 온다 ③**GoTrue 를 죽이면** `loginHistoryAvailable: false` 가 되고 마지막 로그인 값이 0건이 된다 ④그래도 **회원 목록은 7명 그대로 나온다**(로그인 이력은 덤이지 목록이 열리지 않을 이유가 아니다). 화면 — ⑤콘솔 진입 ⑥⑦**마지막 로그인**과 **마지막 활동(저장)** 칸이 나뉘어 있다 ⑧로그인은 **분까지** 보인다(같은 날 여러 번을 가려야 한다) ⑨차단된 계정에 배지 ⑩안내가 로그인/저장을 구분해 설명한다 ⑪정상일 때는 실패 안내가 뜨지 않는다. 함정 1가지 — **GoTrue 관리자 API 는 마지막 시각만 준다.** 로그인 *횟수*·이력 목록은 감사 로그(`auth.audit_log_entries`)를 읽어야 하고 그 DB 연결이 따로 필요하다 — 만들지 않았고 문서에 그렇게 적었다 | admin-console.md §3.1 |
| e2e142 | **비밀번호 재설정** — 로그인하지 못하는 사람의 길 (2026-08-13 사용자 요청) | 구성: PG + API :3001(supabase) + 가짜 SMTP :2525 + **가짜 GoTrue 관리자 :9997** + vite :5173. **20항목** — ①②로그인 화면의 [비밀번호를 잊으셨나요?]가 **로그인 버튼 아래**(438 < 484) ③④누르면 재설정 화면으로 바뀌고 로그인 폼은 사라진다 ⑤⑥⑦**없는 계정도 같은 화면·같은 문구**(가입된 계정이라면…)로 넘어가되 **메일은 나가지 않는다**(계정 열거 방지) ⑧⑨있는 계정은 실제로 메일이 가고 본문이 `비밀번호 재설정 인증번호` ⑩⑪틀린 번호는 남은 횟수와 함께 거절되고 비밀번호 단계로 못 간다 ⑫맞으면 새 비밀번호 단계 ⑬⑭⑮6자 미만·서로 다름은 잠기고 맞으면 열린다 ⑯⑰로그인 화면으로 돌아가며 새 비밀번호로 로그인하라고 알린다 ⑱⑲**GoTrue 로 `PUT /admin/users/:id` 가 한 번만** 나갔고 `role=service_role` + 새 비밀번호가 실렸다 ⑳콘솔 오류 없음. **되돌려 깨뜨리기**: 표 회수를 넣기 전에는 **같은 재설정표로 두 번 바뀌었다**(HMAC 표는 DB 를 보지 않는다) — 인증번호 줄을 GoTrue 호출 **전에** 지우도록 고쳐 두 번째가 `이미 사용한 인증입니다`로 막히는 것을 확인했다 | auth-session-ui.md §11-B |
| e2e141 | **로그인 라벨 + 비밀번호 변경** (2026-08-13 사용자 지적·요청) | 구성: PG + API :3001(supabase) + 가짜 SMTP :2525 + vite :5173. **22항목** — ①②③일반 로그인에 **아이디 (이메일)·비밀번호** 라벨이 있고 **라벨을 누르면 그 칸으로 포커스가 간다**(`htmlFor` 연결까지 확인) ④⑤⑥⑦관리자 로그인도 같은 라벨 + 👁 토글이 실제로 `type=text` 로 바뀐다 ⑧**메일 제목을 MIME 해독해** `[EasyMindMap] Admin 로그인 인증번호`인지 본다 ⑨본문도 같은 문구 ⑩콘솔 진입 ⑪⑫비밀번호 변경 탭 + 별도 비밀번호가 없다는 안내 ⑬⑭⑮6자 미만/서로 다름은 잠기고 맞으면 열린다 ⑯**GoTrue 로 `PUT /user` 가 실제로 나갔다**(본문의 password 까지 확인) ⑰바꿨다고 알린다 ⑱⑲⑳아바타 메뉴의 항목이 **개인 설정 바로 아래**(186 < 220)이고 준비 중 배지가 없다 ㉑㉒창이 열리고 **일반 앱에서도 실제로 바뀐다**. 함정 2가지 — ⑴**placeholder 만으로는 라벨이 못 된다**: 브라우저 자동완성이 채우는 순간 사라져 그 칸이 무엇인지 알 수 없다 ⑵메일 제목을 **인코딩된 채로** 비교하면 무엇을 넣어도 통과한다 — 해독해서 봐야 한다(처음에 항상 통과하는 단언을 썼다가 고쳤다) | admin-console.md §3.4 |
| e2e140 | **관리자 콘솔** — 2단계 로그인·회원관리·설정관리 (2026-08-13 사용자 요청) | 구성: PG + API :3001(**AUTH_MODE=supabase**, ADMIN_EMAILS=ok@baro.pro) + **가짜 SMTP :2525**(받은 메일을 파일로 떨군다) + vite :5173. **23항목** — ①`/admin` 이 관리자 로그인 화면이고 ②에디터가 열리지 않는다 ③비밀번호 단계 통과 → 인증번호 화면 ④아직 콘솔 밖 ⑤**실제로 발송된 메일에서 인증번호를 읽는다**(심지 않는다 — 심으면 발송 경로가 검증에서 빠진다) ⑥보낸 사람이 `noreply@easymindmap.org` **+ 제목·본문이 `Admin 로그인 인증번호`**(가입 메일은 `가입 인증번호` 그대로 — 두 통을 실제로 보내 비교했다, 2026-08-13 사용자 지적) ⑦틀린 번호는 **남은 횟수**와 함께 거절 ⑧여전히 콘솔 밖 ⑨⑩맞는 번호로 진입 + 누구인지 표시 ⑪⑫회원 목록·요약 ⑬⑭⑮드롭다운으로 요금제 변경 → 화면이 알리고 **DB 에 team·20 GB 로 반영**(트리거 동기화까지) ⑯설정 그룹 11개(서버 7 + 화면 4) ⑰**서버 환경변수의 실제 값**(ADMIN_EMAILS) ⑱**화면 상수의 실제 값**(자동저장 5초) ⑲**비밀번호는 값이 아니라 '설정됨'만** ⑳결제관리는 준비 중 안내 ㉑㉒**일반 계정은 1단계에서 막히고 인증번호 화면도 안 나온다** ㉓콘솔 오류 없음. 함정 3가지 — ⑴인증번호를 **미리 심으면 안 된다**: `login/start` 가 새 번호로 덮어쓴다. 순서는 [다음] → 메일에서 읽기다 ⑵**`AUTH_MODE=dev` 로는 검증할 수 없다**: dev 가드는 토큰을 보지 않아 세션에 이메일이 없고, 그러면 관리자를 가릴 수 없다 ⑶`SMTP_FROM` 처럼 공백·`<>` 가 든 값은 셸 env 파일에서 **따옴표로 감싸야** 한다(감싸지 않아 API 가 기동하지 못했다) | admin-console.md |
| e2e139 | **가입 화면의 돌아가기가 버튼으로 보이는가** (2026-08-11 사용자 지적) | 구성: vite :5173 **만**. ①돌아가기 요소 존재 ②문구가 **'로그인 화면으로 돌아가기'**(상태 설명이 아니라 행동) ③진짜 `<button>` ④**테두리 1px** ⑤**배경이 투명하지 않다**(`rgb(251,248,243)`) ⑥글자가 흐리지 않다(weight 600, 본문색) ⑦**높이 38 ≥ 36**(누르기 좋은 크기) ⑧⑨**주된 행동이 더 강하다** — 가입하기 42 > 돌아가기 38, 배경색도 다르다(`rgb(217,119,6)` vs `rgb(251,248,243)`) ⑩누르면 가입 화면이 닫히고 ⑪로그인 화면이 보인다. **왜 계산된 스타일로 재나** — 요소는 이미 `<button>` 이었는데도 사용자에게 안내문으로 읽혔다. 'button 인가'로는 이 결함을 못 잡는다. **보이는 값**(테두리·배경·굵기·높이)을 재야 잡힌다 | auth-session-ui.md §11.1-1 |
| e2e138 | **회원탈퇴가 로그인 계정까지 지우는가** — e2e137 이 놓친 환경 차이 (2026-08-11) | 구성: PG + API :3001(supabase + `GOTRUE_URL`) + :3002(supabase, **주소 없음**) + :3000(dev) + **가짜 GoTrue :9998**(관리자 API 흉내). ①정상 응답이면 `loginAccountRemoved: true` ②가짜 GoTrue 가 받은 것이 **`DELETE /admin/users/<id>` · `role: service_role` · 서명 유효 · apikey 헤더**임을 로그로 확인 ③GoTrue 가 **500** 이면 `false` + 경고 로그, 그래도 **자료는 지워지고 묘비는 선다** ④⑤⑥`GOTRUE_URL` 이 없는 supabase 배포는 **기동 때 경고**하고 탈퇴 응답도 `false` — 지운 것이 껍데기일 수 있어 **"지웠다"고 답하지 않는다** ⑦⑧자료는 그래도 삭제 ⑨`AUTH_MODE=dev` 는 앱 DB 가 곧 계정이라 `true` ⑩그 경우 기동 경고 없음. 함정 1가지 — **로컬 검증 환경이 운영과 다르면 검증이 통과해도 소용없다**: e2e137 은 GoTrue 없이 shim 하나만 쓰는 DB 에서 돌아 이 결함을 잡지 못했다. 배포 문서(`dev-server-coolify.md`·`gotrue-deploy-walkthrough.md`)에서 **로그인 정보가 어느 DB 에 사는지**를 먼저 읽었어야 했다 추가 실측(2026-08-11 dev 서버) — api·GoTrue 는 같은 `coolify` 네트워크지만 **별칭이 배포 번호가 붙은 전체 이름 하나뿐**이라 짧은 UUID 는 `fetch failed`, 전체 이름은 닿지만 **재배포마다 바뀐다**. 그래서 `GOTRUE_URL` 은 **공개 주소**(`https://auth-dev.mindmap.ai.kr`)를 쓴다 — api 컨테이너 안에서 실제로 닿는 것을 확인했다 | auth-session-ui.md §12.5 |
| e2e137 | **회원탈퇴** — 가입의 반대편 (2026-08-11 사용자 요청) | 구성: PG + API :3000(dev) + API :3001(**AUTH_MODE=supabase**, 직접 서명한 JWT) + vite :5173. **서버 17** — ①미리보기가 맵·첨부·용량을 센다 ②③틀린/빈 확인 문구는 400 이고 **자료가 그대로 남는다** ④탈퇴 200 ⑤~⑦users·auth.users·maps·folders·tags·attachments·ai_jobs·edit_locks 가 **전부 0** ⑧묘비 1 ⑨**남의 맵과 그 히스토리는 남고 `created_by` 만 NULL** ⑩첨부 파일 원본과 업로드 조각이 디스크에서 사라진다 ⑪**탈퇴한 id 의 유효 토큰 → 401** ⑫**되살아나지 않는다**(auth.users 0) ⑬같은 이메일·새 id 는 정상 가입 ⑭**되돌려 깨뜨리기: 묘비를 지우면 되살아난다**(auth.users 1) — 이 장치가 실제로 일한다는 증거 ⑮~⑰표가 없는 서버에서도 탈퇴는 되고, 경고 로그 + `/health` 가 `missingTables` 로 알린다. **화면 15** — 메뉴에 항목 있음 · **로그아웃 아래**(299 < 333) · 다른 색(rgb(220,38,38)) · 대화상자에 맵 3개·첨부 3MB·이메일 표시 · **입력 전 잠김** · 틀린 문구도 잠김 · 정확해야 열림 · 취소하면 계정 유지(maps=3) · 실제 탈퇴 후 **세션이 사라지고** 같은 토큰이 401. 함정 3가지 — ⑴가짜 세션의 `expiresAt` 은 **ms** 다(초로 넣으면 만료로 보고 갱신하러 가서 토큰 없는 세션이 된다 — 화면은 멀쩡한데 API 만 전부 실패한다) ⑵검증 컨테이너는 외부망이 막혀 **폰트 CDN 이 항상 실패**한다(우리 오류가 아니다 — 콘솔 오류 검사에서 걸러야 한다) ⑶dev 모드 가드는 토큰을 보지 않아 **묘비 검사를 타지 않는다**: 되살아남 검사는 반드시 supabase 모드로 | auth-session-ui.md §12 |
| e2e136 | **회원가입** — 이메일 인증·성명·휴대폰·free (2026-08-09 사용자 요청) | 구성: PG + API :3000(AUTH_MODE=dev, **메일 미설정**) + vite :5198(인증 켜짐). **서버 16** — ⓐ인증번호 요청 200 ⓑ메일 미설정+개발이면 코드를 응답에 담는다 ⓒ**"보냈다"고 거짓말하지 않는다**(sent=false) ⓓ60초 재발송 제한 ⓔ틀리면 **남은 횟수**를 알려 준다 ⓕ맞으면 인증표 ⓖ이메일 대소문자를 같은 것으로 본다 ⓗ발송한 적 없는 이메일은 안내 ⓘ성명·휴대폰 저장(하이픈 제거) ⓙ**요금제 free** ⓚ이메일 인증 시각 기록 ⓛ휴대폰 인증은 null(준비 중) ⓜcomplete=true ⓝ**위조 인증표 거절** ⓞ성명 필수 ⓟ너무 짧은 번호 거절. **화면 19** — 가입 화면 전환 · **처음엔 가입 버튼이 잠겨 있다** · 발송 전에는 인증번호 칸이 없다 · 메일 미설정 사실을 화면에 밝힌다 · 틀린 횟수 표시 · 인증 후 이메일 잠금 · 비밀번호 불일치 시 잠김 · 국가번호를 **이름(일본)·번호(82)로** 찾아 고르기 · 한국 번호만 하이픈 · **가입 즉시 로그인되어 앱이 열린다** · **서버에 실제로 저장됐는지**까지 확인. 함정 4가지 — ⑴인증번호 두 호출은 **로그인 전**이라 토큰을 붙이면 안 된다(붙이면 인증 켜진 배포에서 401 → 가입 자체가 막힌다) ⑵`CORS_ORIGIN` 에 :5198 이 없으면 브라우저에서만 조용히 실패 ⑶GoTrue 는 로컬에 없다 — `page.route` 로 가입 응답만 가짜로 돌리고 프로필 저장은 진짜로 나가게 한다 ⑷가입 성공 후 **환영 화면이 사라지는 것이 정상**이다(로그인됨). 로그인 화면 복귀는 확인 메일을 요구하는 서버뿐 | auth-session-ui.md §11 |
| e2e135 | **체크리스트 표시 파리티** — 자리마다 달라 "엉망"이던 것 (2026-08-09 3차 보고) | 구성: vite :5199 **만**. ①**완료해도 취소선이 없다**(인스펙터 탭) ②체크 상자 안에 ✓ 가 그려진다 ③**✓ 가 상자의 60% 이상**(예전 9px 은 초록 칠에 묻혀 '녹색 네모'로만 보였다) ④노트 팝업 제목에 **원문 마커(`- [x]`)가 새지 않는다** ⑤마커를 접어 `☑/☐` 로 ⑥**종류 라벨('체크리스트')이 제목줄에 없다** ⑦여러 줄이 한 줄로 합쳐진다 ⑧팝업 항목에도 취소선 없음 ⑨**내보낸 뷰어 상세 제목**도 같은 규칙 ⑩⑪뷰어에서 체크해도 취소선 없음 + 토글은 여전히 된다. 함정 2가지 — ⑴뷰어 첫 항목은 에디터에서 이미 체크해 둔 것이라 누르면 **꺼진다**: '1 이 된다'가 아니라 **뒤집히는지**를 봐야 한다 ⑵팝업 제목줄은 닫기 버튼을 가진 조상까지 올라가 `innerText` 첫 줄로 읽는다(제목 전용 testid 가 없다). **규칙화**: coding-conventions.md **§5-1-6**(같은 개념은 어디서나 같은 모습) — 체크가 나타나는 네 자리와 합의된 표시 규칙 5개를 적어, 한 자리만 고쳐 규칙이 갈리는 일을 막는다 | 04-node-content.md §18·coding-conventions.md §5-1-6 |
| e2e134 | **저장한 자리** — 히스토리·문서함에 기기·브라우저·IP (2026-08-09 사용자 요청) | 구성: PG + API :3000(AUTH_MODE=dev) + vite :5199. **서버 9** — ⓐ클라이언트가 알려준 값 그대로(**Windows 11** 은 이 경로로만 온다) ⓑclient 없으면 서버가 UA 로 추정(Android 14/Chrome 125) ⓒiOS 17/Safari 17 ⓓ**파생 브라우저를 Chrome 으로 오독하지 않는다**(Edge) ⓔ**IP 는 서버가 정한다** — `X-Forwarded-For` 앞에 끼워 넣은 가짜(9.9.9.9)는 무시하고 프록시가 붙인 마지막 값(203.0.113.77)만 쓴다 ⓕ프록시 없는 요청은 접속 IP 그대로 ⓖ**본문으로 IP 를 보내면 400**(받지 않는 필드) ⓗ`GET /maps` 에도 마지막 저장 자리가 실린다 ⓘ이 기능 전 저장분은 null. **화면 11** — ⓙ브라우저가 스스로 플랫폼·브라우저를 알아낸다(Client Hints) ⓚ히스토리에 🖥 줄 ⓛ그 줄에 셋 다 있다 ⓜ**IP 가 말줄임으로 잘리지 않는다**(좁은 패널에서 접어 보여 준다) ⓝ파일명 호버 0.35초 → 상세 카드 ⓞ카드에 유형·생성·수정·노드·크기·첨부 ⓟ카드에 마지막 저장 자리 ⓠ마우스를 떼면 사라진다 ⓡ**ⓘ 를 누르면 고정**된다 ⓢ고정 카드는 마우스를 떼도 남는다(IP 복사) ⓣ**Esc 는 카드만** 닫는다(문서함 유지). 함정 4가지 — ⑴맵 저장 대화상자의 **이름 칸은 두 번째 input**(첫째는 새 폴더 이름)이고 `fill()` 이 조용히 튕긴다 → 실제 키 입력으로 ⑵제목이 겹치면 409 라 **실행마다 고유 이름** ⑶앞선 실행이 남긴 **복구 배너를 먼저 버려야** 저장 버튼이 눌린다 ⑷UA 만으로는 Windows 10/11 이 갈리지 않는다 — 서버 추정값은 'Windows 10/11' 로 두고 아는 척하지 않는다. **스키마 미적용 서버 실증**: 컬럼 3개를 실제로 지우고 재기동 → 목록·히스토리·저장 모두 **200**(값만 null), 다시 넣으면 **재기동 없이 1분 안에** 값이 쌓인다 | **[3] 2차 9** (실서버 보고 — 2026-08-09) — ⓐ`/health/ip` 진단 창구 ⓑ직접 접속을 그렇다고 알려 준다 ⓒ**프록시 2단계에서 진짜 접속자 IP**(예전 `trust proxy: 1` 은 여기서 nginx 내부 IP 를 집어, 히스토리 IP 가 PC·휴대폰 모두 192.168.x.x 였다) ⓓ**3단계로 늘어도 그대로 맞는다**(숫자로 세지 않고 '사설망은 전부 프록시'로 바꿨다) ⓔ앞에 끼워 넣은 가짜 공인 IP 무시 ⓕ**앞단이 원본 IP 를 버린 경우**(XFF 가 사설 1개)는 "서버 코드로 복구 불가"라고 정확히 말한다 — 예전 문구는 고칠 수 없는 TRUST_PROXY 를 고치라고 안내했다 ⓖ**위장(GREASE) 브랜드 4종**(`Not;A=Brand` `Not)A;Brand` `Not_A Brand` `Not/A(Brand`)에서 진짜 이름을 고른다 — 구두점이 실행마다 달라 나열식 필터가 `=` 조합에서 샜다(안드로이드에 `Not;A=Brand 8` 로 저장됨). 함정 ⑸카드 검사는 **방금 저장한 맵**을 짚어야 한다 — 목록 첫 줄은 이 기능 전 저장분이라 '기록 없음'이 맞다 ⑹`getClientInfo()` 는 세션당 한 번 캐시라 브랜드 조합마다 **새 컨텍스트**로 열어야 한다 | 13-version-history.md·document-library.md §9·rate-limit.md §4.3 |
| e2e133 | **체크박스가 안 눌린다** — 노트 체크 연속 입력 + 노드/노트/뷰어 토글 (2026-08-09 사용자 보고) | 구성: vite :5200 **만**(서버 불필요). **[1] 연속 입력 7** — ⓐ`+체크` 로 항목 1개 ⓑ새 항목에 커서 ⓒ**Enter = 다음 항목** ⓓ커서가 새 항목으로 ⓔ**Shift+Enter 도** ⓕ**빈 항목에서 Enter 는 쌓지 않는다** ⓖ새 항목은 맨 뒤가 아니라 **누른 항목 바로 뒤**. **[2] 에디터 13** — 노트·태그 탭 토글/해제 · 노드의 ✓ 인디케이터 → **노트 뷰어 팝업에서 토글**(예전엔 읽기 전용) · 팝업에서 바꾸면 **좌측 탭에 곧바로 반영**(같은 맵 데이터) · 노드 글자 안 `- [ ]` 줄의 글리프 토글 · **클릭 판정이 글리프보다 넓다** · **글리프 바깥 3px 을 눌러도 체크된다** · 원문 `[ ]`↔`[x]` 가 실제로 바뀐다. **[3] 내보낸 뷰어 12** — 내보내기 메뉴를 **실제로 눌러** 받은 HTML 을 `file://` 로 열어 확인: 원문대로 실린다 · 넓은 판정 · **누르면 켜지고 꺼진다**(예전엔 클릭 핸들러 자체가 없었다) · 상세 패널의 **노트 체크리스트도** 눌린다 · 취소선은 **글자에만**(글리프 제외) · `localStorage` 에 **바뀐 것만** 담는다 · 새로고침해도 남는다 · **다른 파일은 영향 없다**(파일별 키) · **원본 맵 문서는 안 바뀐다**. 함정 4가지 — ⑴노드는 DOM 순서로 고르면 안 된다: 오른쪽 끝 노드를 고르면 **인디케이터가 화면 밖(x=1498)** 이라 클릭이 안 간다 → 화면 안쪽 좌표의 노드를 고른다 ⑵편집 커밋은 **Enter** 다 — Escape 는 `cancelEdit()` 라 입력이 통째로 버려진다 ⑶노드 텍스트를 store 에서 찾을 때 `map.branches` 만 훑으면 **루트를 놓친다** ⑷SVG 글리프에서 '보이는 사각형'은 첫 `rect` 가 아니다 — 넓힌 투명 판정판이 먼저 그려진다. **[4] 2차 6** (2026-08-09 사용자 보고) — ⓐ뷰어 마커에도 **개수 배지**가 있다(예전엔 없어서 체크 5개짜리가 그냥 ✓ 하나로 보였다) ⓑ체크 5 ⓒ**링크 3·첨부 2·미디어 2** 도 함께 ⓓ에디터와 숫자가 같다 ⓔ**뷰어 체크 줄에 세로 스크롤바가 없다** ⓕ에디터 노트 팝업도 마찬가지(같은 원인 — `.mm-note-block` 이 `overflow-x:auto` 라 `overflow-y` 도 auto 로 계산돼, 글리프에 위아래 padding 2px 을 준 순간 줄마다 스크롤바가 생겼다). 함정 ⑸배지 숫자를 견줄 때 **에디터는 한 노드, 뷰어는 맵 전체**를 세면 안 된다 — 앞 절에서 만든 다른 체크 노드가 뷰어 쪽에만 잡혀 어긋난다. **회귀 실증**: 판정 여유(6px/pad)를 0 으로 되돌리면 **6개 FAIL**, 세로 padding 을 되살리면 스크롤바 2항목이 FAIL 한다(sh 25 vs ch 23 / 22 vs 20) | 04-node-content.md §18·20-export.md |
| e2e132 | **문서함 내용 검색** — 이름뿐 아니라 맵 안(노드·노트·태그·링크·첨부명)까지 (2026-08-08 사용자 요청) | 구성: PG + API :3000(AUTH_MODE=dev) + vite :5199. **서버 16** — ①노드 텍스트 ②맞은 **건수**(미리보기 문장이 아니라) ③노트 본문 ④태그 ⑤링크 이름 ⑥링크 주소 ⑦첨부 파일명 ⑧**첨부의 data URL(base64)은 색인에 없다**(넣으면 색인이 문서만큼 커지고 base64 쓰레기에 걸린다) ⑨이름만 맞으면 `matchCount=0` ⑩제목·내용을 함께 훑는다 ⑪여러 조각이 맞으면 건수도 여럿(노드+노트=2건) ⑫`_` 등 ILIKE 와일드카드 무력화 ⑬응답에 **search_text(맵 평문 전체)가 안 샌다** ⑭검색이 아닐 때는 matchCount 를 안 붙인다 ⑮저장하면 **색인이 곧바로 따라온다**(DB 트리거) ⑯지운 내용은 안 걸린다. **화면 9** — ⑰내용으로 좁혀진다 ⑱`맵(N건)` 배지 ⑲미리보기 문장 줄은 없다 ⑳이름이 맞으면 **그 글자가 강조**(`<mark>`) ㉑이름만 맞으면 배지 없음 ㉒건수 그대로(맵(2건)) ㉓빈 안내 ㉔지우면 전체 복귀 ㉕지우면 강조·배지도 사라진다. 함정 5가지 — ⑴맵 제목은 같은 폴더에서 유일(409) → **실행마다 고유 접미사** + 뒷정리 ⑵프런트는 **250ms 디바운스** 뒤 서버 호출 — 입력 직후 세면 0건 ⑶`CORS_ORIGIN` 에 :5199 가 없으면 **브라우저에서만** 조용히 실패 ⑷시드의 **루트 노드에 제목을 넣으면 안 된다** — 제목으로 검색할 때 내용에도 걸려 "이름만 맞음(matchCount 0)"을 검사할 수 없다 ⑸시드 첨부 파일명이 다른 검사어와 겹치면 총계 단언이 어긋난다. 성능은 별도 실측 — document-library.md §8.2 | document-library.md §8 |
| e2e131 | **뷰어 도형 파리티** — 도형 '없음'이 뷰어에서 안 먹던 버그 (2026-08-08 사용자 보고) | 구성: vite :5199 **만**. 내보내기 메뉴를 **실제로 눌러** 받은 HTML 을 **file:// 로 열어** 그려진 도형 요소를 센다. ⓪기준선(도형이 그려진다) ①**노드별** `none` → 뷰어도 도형 없음 ②글자는 남는다 ③에디터·뷰어 일치 ④**검색 강조는 '없음'이어도 노란 박스**(#FFE066/#DC2626)를 되살린다 ⑤회귀: 색 지정(fillColor/borderColor) 노드 불변 ⑥**맵 설정** 레벨 기본 도형(육각)이 뷰어에 반영된다(뷰어는 `levelShapes` 를 모르므로 **실효 도형을 구워** 보내야 한다) ⑦에디터·뷰어 일치 ⑧노드별 지정이 레벨 기본보다 우선 ⑨맵 설정 `none` → 채움색이 있어도 도형 없음 ⑩회귀: 설정을 되돌리면 다시 그려진다 ⑪**도형 12종**이 제 모양으로(타원→ellipse·다이아/육각/평행/화살◀▶/별→polygon·원통→path+ellipse·둥근/사각/캡슐→rect) + 에디터 태그와 일치 ⑫사각형 계열 **모서리 반경**(캡슐 = 높이/2, 사각 = 2). 함정 4가지 — ⑴**문자열 검사로는 이 버그를 못 잡는다**(e2e129 ⑧이 `noShape` 이 있나만 보고 통과시켰다 — 코드가 있는 것과 그 코드가 동작하는 것은 다르다) ⑵뷰어에서 도형을 셀 때 `class="mm-box"` 로 골라야 한다 — **코드·표 패널 배경도 같은 그룹의 rect** 라 안 그러면 '도형이 남았다'로 오독한다 ⑶에디터 도형은 "그룹의 첫 자식"이 아니라 **채움이 있는 첫 도형**이다 — 노드에 따라 `fill="none"` rect(클릭 판정·글로우)가 앞에 붙어 다이아 노드가 rect 로 읽혔다(b1-2) ⑷레벨 기본 도형 검사 대상은 **depth 1 노드**여야 한다 — DOM 순서로 고르면 depth 를 알 수 없어 `levelShapes[1]` 이 안 걸린다. **회귀 실증**: 내보내기에서 `shapeType` 한 줄을 빼면 13항목 중 **8개가 FAIL** 한다 | 05-node-style.md §16.2 |
| e2e130 | **레이트 리밋** (2026-08-08) | 구성: PG(TCP)+API :3000 — **브라우저 없이 API 를 직접 두드린다**(e2e125 [3] 과 같은 방식). ⚠️ API 를 **`RATE_LIMIT_MAX=5` 로 낮춰 기동**해야 한다(기본 600 이면 600번을 두드려야 해 느리고 불안정). ①한도까지 200, 넘으면 **429** ②429 본문이 **사용자에게 할 일을 말한다**(라이브러리 원문 `ThrottlerException` 이 아니라 한국어 안내 + `retryAfterSec`) ③`Retry-After` 헤더 ④`/v1/health` 는 **세지 않는다**(감시 도구가 막히면 정작 장애일 때 상태를 못 본다) ⑤**같은 IP 라도 토큰이 다르면 안 막힌다**(IP 로만 세면 같은 회사 사람들이 한 바구니에 묶여 서로 막는다) ⑥토큰 없으면 IP 로 센다. 함정 — ⑹은 **정확한 통과/차단 수를 세면 안 된다**: IP 바구니는 같은 컴퓨터에서 나간 다른 호출(수동 curl·앞선 실행)과 **공유**되어 창이 겹치면 이미 차 있다. 각 항목은 **매번 새 토큰**을 써서 앞 항목의 사용량이 딸려 오지 않게 한다 | rate-limit.md |
| e2e129 | 도형 **'없음'** — 글자만 (2026-08-08) | 구성: vite :5199 **만**(서버 불필요). ⓪기본 도형에서는 박스를 그린다 ①`shapeType:'none'` → **박스를 안 그린다** ②글자는 그대로 ③**선택 점선은 보인다**(도형 밖 별도 rect 라 영향 없음) ④연결선 그대로 ⑤라이트↔다크에서 **글자색이 바뀐다**(#1F1B16↔#E8E6E3 — 도형이 없으면 글자가 캔버스 배경 위라, 채움색 유도색·팔레트색을 쓰면 다크에서 묻힌다) ⑥**검색 강조는 도형 없음이어도 보인다**(그때만 노란 박스를 되살린다) ⑦회귀: 육각으로 바꾸면 다시 그린다 ⑧뷰어 — `buildStandaloneHtml` 결과에 뷰어 JS 의 `noShape` 분기와 `shapeType:'none'` 데이터가 **둘 다** 실린다. 함정 3가지 — ⑴노드 그룹의 자식을 셀 때 **`<g>`(콘텐츠 묶음)·`<defs>` 를 도형으로 세면 안 된다**(처음에 이걸 세서 '박스가 남았다'로 오독) ⑵글자색은 `text[fill]` 로 골라야 한다 — **아이콘 text 에는 fill 속성이 없어** 그냥 `text` 를 잡으면 계산값 rgb(0,0,0) 이 나온다 ⑶다크 전환은 `editorUiStore.setThemeName('dark')` 다(`setDark`·`toggleDark` 없음) | 05-node-style.md §19 |
| e2e128 | 로그아웃이 **이 브라우저의 로컬 초안을 전부 지운다** + 확인 게이트 (2026-08-07) | 구성: PG(TCP)+API :3000, vite :5199(인증 꺼짐) + vite :5198(인증 켜짐 — 로그아웃 UI 가 `authEnabled && session` 일 때만 뜬다). ①`signOut()` 이 초안을 **전부** 지운다 ①bGuest 종료(`exitGuest`)도 같다 ②회귀: 저장 성공은 **그 맵 초안만** 지우고 나머지는 보존(`clearLocalDraft`) ③초안이 남아 있으면 로그아웃 전에 **경고**(`logout-draft-warning`) ③b경고 단계에서는 아직 로그아웃 안 됨 ③c[취소] = 로그인 유지 + 초안 보존 ③d[그대로 로그아웃] = 로그아웃 + 초안 전멸 ④초안 0건이면 **묻지 않고** 바로 로그아웃 ⑤회귀: 남은 초안에는 복구 배너가 여전히 뜬다. 함정 3가지 — ⑴`evaluate` 안의 `import('/src/stores/authStore.ts')` 는 앱과 **다른 모듈 인스턴스**가 될 수 있다(로그아웃 뒤에도 옛 세션을 들고 있었다). 세션 판정은 persist 가 적는 `localStorage['emm.auth']` + 화면(아바타 메뉴 사라짐)으로 한다 ⑵vite 는 첫 방문에서 의존성 최적화 후 **페이지를 새로고침**한다 — 그때 evaluate 를 걸면 죽는다(한 번 데우고 들어간다) ⑶:5198 은 맵을 안 연 상태라 캔버스가 아니라 **문서함**이 뜬다 — `svg g[data-node-id]` 가 아니라 `user-menu` 로 기다린다. 세션은 `localStorage['emm.auth']` 에 심고 새로고침해 persist 로 넣는다 | 14-save.md §7.7-② |
| e2e127 | 복구 배너가 **사실일 때만** 뜬다 · 시간축 화살표 겹침 (2026-08-07 5차) | 구성: PG(TCP)+API :3000, vite :5199. ①손으로 ☁ 저장하면 **그 맵의 로컬 초안이 사라진다**(예전에는 자동저장 경로만 지워, 저장했는데도 다음에 열면 '저장되지 않은 맵이 있습니다' 가 떴다 — `notifyExplicitSave(mapId)`) ①b**'저장 없이 닫기'는 그 초안도 버린다**(사용자가 버리기로 답한 문서를 두고 다시 묻던 문제) ①c그래서 **다시 열어도 복구 배너가 안 뜬다** ②시간축 화살촉이 **다른 가지의 노드와 겹치지 않는다**(화살촉 사각형 vs 무리 밖 노드 getBoundingClientRect 교차 0건) ②b회귀: 맵 전체 시간배치는 화살표 1개 그대로. 함정: 초안 확인은 `localDraft.listDrafts()` 로 읽는다(IndexedDB — 같은 브라우저 컨텍스트를 써야 보인다) · 초안은 **구조 변경(노드 수 변화)이면 즉시**, 타이핑은 1초 뒤에 적히므로 가지를 하나 넣고 1.4초 기다린다 | 14-save.md §7.5-②·08-layout.md §22.1-1 |
| e2e126 | 시간배치 연결선·축 화살표·＋버튼 — **서브트리에서도** (2026-08-07 4차) | 구성: vite :5199 만. 보고 5건이 **한 뿌리**였다 — 축 위 노드와 그 아래 스택을 **깊이로만** 갈라서, 시간배치를 서브트리에 걸면(축 시작점이 중심 주제가 아니다) 전부 어긋났다. 이제 배치가 `_timelineRole`('axis'|'stack')을 심고 엣지·축·＋버튼이 모두 그것을 본다. ①`timeline` 서브트리 = 연결선이 **시작점 오른쪽 변**에서 나간다(보고: 위/아래에서 나갔다) ①b서브트리에도 **시간축 + 화살표**가 있다(예전엔 0개) ②`timeline-center` 서브트리 = 축 노드가 시작점과 **같은 높이**(SubtreeStrategy 의 '위 넘침 보정'이 축을 무너뜨려 아래로 밀렸다 — 시간배치는 그 보정에서 뺐다) ②b축 시작점→축 노드 엣지는 **안 그린다**(축이 곧 연결선) ②c화살표 있음 ③축 노드에 하위가 붙어도 시간배치 규칙 유지(예전엔 `from.layoutType` 이 비어 **맵 레이아웃(진행트리)** 엣지가 걸렸다) ④하위가 생겨도 **여전히 오른쪽 변**에서 나간다 ⑤축 노드의 [하위 추가] 버튼이 **그 노드의 하위가 붙는 쪽**을 가리킨다(첫째 위·둘째 아래로 부호가 갈린다) + 회귀: 맵 전체 timeline 은 중심 주제 오른쪽 변에서 주제마다 한 줄 + 화살표 1개. 함정: 하위를 붙이면 **시작점 노드 자체가 옮겨 앉는다**(블록이 커진다) — 좌표가 같은지 보면 안 되고 '여전히 오른쪽 변에서 나가는가'를 봐야 한다 · 하위 추가 액션은 `addChildNode(parentId)` 다 | 08-layout.md §22.1-1 |
| e2e125 | 실사용 3건 — 시간배치 ＋버튼 방향·서브트리 허용·**대용량 첨부 재생** (2026-08-07 3차) | 구성: PG(TCP)+API :3000, vite :5199. **[3] 은 브라우저 없이 API 를 직접 두드린다** — ③전체 요청 = 200 + `Accept-Ranges: bytes` + 올바른 Content-Type ③a`Content-Disposition: inline`(다운로드 강제 아님 — 그래야 재생) ③b앞 구간 = **206** + `Content-Range` + 그 구간 바이트 ③c가운데 구간 = 그 자리 바이트(구간 이동) ③d열린 구간(`N-`)·끝 구간(`-N`) ③e파일 밖 = **416** + `bytes */크기` ③f프런트가 **파일을 통째로 받지 않는다**(window.fetch 를 세어 0 인지 확인 + 창에 넘긴 값이 첨부 URL 인지) ①시간배치 3레벨의 ＋버튼 방향과 **실제 생성 위치의 부호가 같다**(child/before/after × 위쪽·아래쪽 주제 6조합) ①b[하위 추가]와 [형제 뒤 추가]가 **다른 자리**에 만든다(예전엔 같은 자리라 구분이 안 됐다) ②하위 노드에서 시간배치 버튼이 **안 잠긴다** ②b그 노드에만 걸리고 맵 전체는 그대로 ②c고른 노드가 축의 시작점(자식이 그 오른쪽). 함정: `before` 는 새 노드가 **선택 노드의 원래 자리**를 차지하고 선택 노드가 밀린다 — 클릭 전 좌표와 견주면 '제자리'로 잘못 읽히므로 **재배치 뒤의 선택 노드**와 견준다 · 이 테스트도 **편집 잠금을 먼저 지운다**(잠기면 읽기 전용으로 열려 서버 링크가 안 붙는다) | attachment-storage.md §13·08-layout.md §22.2 |
| e2e124 | 실사용 6건 — 시간배치 아이콘/중앙노드 재정의·첨부 유실·문서함 트리·열 정렬 (2026-08-07 2차) | 구성: PG(TCP)+API :3000, vite :5199 + **seedtree.mjs**(Guide/기획/2026 폴더 + 맵 7개). ①레이아웃 탭의 시간배치 아이콘 2개 모두 **왼쪽 끝이 중심 주제 상자**(예전엔 선부터 시작해 중심이 없었다) ①b중앙노드 아이콘은 축 위 상자 3개 ②`timeline-center` = 중심 주제가 **맵 제일 왼쪽**(모든 노드보다 왼쪽) ②b2레벨 주제가 **축 위**(루트와 같은 y, ±1.5px) ②c하위만 위/아래로 갈림 ②d시간축은 **노드 사이 토막**(주제 수+1 선분 — 한 줄이면 글자를 가로지른다) ③서버 맵이면 확인 버튼이 **'저장하고 계속'** ③b**닫기 전 편집이 서버에 남는다**(유실 수정 실증 — 마커를 넣고 게이트를 지난 뒤 getDocument 로 재확인) ⑤문서함 기본 **모두 접기** ⑤b폴더 클릭 = **그 자리에서 펼침**(홈 맵도 그대로 보인다 = 들어가지 않았다) ⑤c검색이 **하위 폴더까지**(모두 접힌 상태에서도) ⑤d맞는 것이 든 폴더만 자동 펼침(2026 폴더는 사라짐) ⑤e그 폴더의 **다른 파일은 제외** ⑥숫자 4열 머리글·합계·값이 **같은 오른쪽 선**(±1px, textAlign right). 함정: 글리프는 페이지에서 `react-dom/client` 를 import 할 수 없다 — **레이아웃 탭을 열어 실제 렌더된 svg** 를 읽는다 · 트리라 `browser-crumb-home` 이 없다 | 08-layout.md §22.1·document-library.md §5 |
| e2e122 | API 키 모드도 **웹 AI 와 같은 적용 흐름** (2026-08-06 결정) | 구성: vite :5199 만 (AI 호출은 `page.route` 로 가짜 응답). 사장님 기억 — "두 아이콘이 나란히, 기본 선택 없음, 고르면 새 맵/노드 확장을 묻고, 노드를 안 고르고 노드 쪽을 누르면 먼저 선택하라고 알려 줬다". **그 흐름은 웹 AI 모드에만 있었다**(#196·#197·#198). API 키 모드는 호출이 끝나면 confirm 하나 띄우고 **곧바로 새 맵으로 열어** "노드에 붙일까?"를 고를 기회가 없었다. ①생성 직후 **아무것도 적용되지 않고** 두 버튼이 뜬다 ②노드 없이 [선택 노드에 삽입] → **먼저 고르라고 알려 준다**(팝오버 안 뜸) ③노드를 고르면 **확인 팝오버 → 확인해야** 하위에 붙는다 ④[새 맵 생성] 도 확인을 거친다 ④b확인 문구가 **Ctrl+Z 로 돌아갈 수 있다고 말하지 않는다**(#210 이후 사실이 아님). 함정: **persist 키는 `easymindmap-ai-settings`** · 확인 팝오버는 공용 `AiConfirmPopover` 로 뽑았고 API 쪽 접두사는 `data-aiapi-*` 다(웹 AI 의 `data-webai-*` 선택자는 그대로 살렸다) | 18-ai.md |
| e2e121 | 선택 노드 확장이 **입력창 요청**을 함께 보낸다 (2026-08-06 3차) | 구성: vite :5199 만 (순수 함수). 보고: 노드를 고르고 입력창에 "고흐의 작품을 연도별로"를 적고 [선택 노드 자세히 확장]을 눌렀더니 **맵에 들어 있던 쿠버네티스 내용**이 나왔다. 원인은 `buildExpandContext` 가 **입력창 값을 아예 안 썼던 것** — 확장은 맵 문맥(프로젝트 지침+상위 경로+이 노드)만 봤다. 입력창이 바로 위에 있으니 사용자는 반영된다고 본다. ①요청이 `[요청]` 으로 프롬프트에 들어간다 ②어긋나면 **요청을 따르라**고 못 박는다 ③입력창이 비면 예전 그대로(맵 문맥만). 웹 AI 확장(클립보드 복사)도 같은 규칙으로 맞췄다 | 18-ai.md |
| e2e120 | 실사용 4건 — 진행률·아웃라인 접기·목록 합계·AI 키 안내 (2026-08-06 3차) | 구성: PG(TCP)+API :3000, vite :5198/:5199. ①조각 전송이 **보낸 바이트를 보고한다**(XHR `upload.onprogress` 연결) — 보고 "16%·23%에서 한참 멈춘 것처럼 보인다"의 원인은 진행률을 **조각 수**로만 센 것이었다(8MB 조각이라 몇십 초씩 그대로) ①b**[가드]** 화면 진행률이 그 값으로 갱신된다 — **loopback 에서는 "한 조각 안에서 여러 번 오른다"를 볼 수 없다**(브라우저가 조각을 한 번에 밀어 넣어 onprogress 가 조각당 1회) ②아웃라인 +/− 가 맵과 같은 모두 펼치기/접기 ②b접어도 되돌리기 단계가 안 는다(보기 상태다) ③열 머리글에 노드·크기·첨부·첨부 용량 **합계** ③b[＋ 새 폴더] 가 **홈 바로 오른쪽**(좌표로 확인) ④키가 없을 때 'AI로 맵 생성' 버튼이 **눌리고** AI 설정으로 이동한다 — 예전에는 `disabled` 라 "API 키를 등록하세요"라고 적혀 있어도 눌러도 아무 일이 없었다. **함정: [4] 는 persist 키가 `easymindmap-ai-settings` 다** — 처음에 `emm.ai` 로 잘못 써서 웹 AI 패널이 떠 `[data-ai-generate]` 자체가 없었다 | attachment-storage.md §12.7 |
| e2e119 | 요금제·저장 용량 — Free 10MB · Basic 10GB · Pro 30GB · Team 20GB/사용자 (2026-08-06) | 구성: **PG(TCP) 만** — API·브라우저 불필요. `npm run db:apply` 를 **여러 번** 돌려 두 가지를 지킨다. **A. `plan` 이 단일 기준**: ⓪`plan_quota_bytes()` 가 네 요금제의 바이트를 정확히 준다(숫자의 단일 출처) ①신규 가입 = `free`·10MB(트리거가 INSERT 때 넣는다) ②`plan` 을 basic→pro→team→free 로 바꾸면 `quota_bytes` 가 **매번 따라온다** — 결제 연동이 `UPDATE users SET plan='pro'` 한 줄로 끝나려면 이게 성립해야 한다 ③오타 요금제(`premium`)는 CHECK 가 막는다 ④특별 계약(`quota_bytes` 직접 지정)은 plan 무관 UPDATE 에 지워지지 않는다. **B. 일회성 승격이 재적용에 다시 돌지 않는다**: ⑤기준 시각 이전 가입 = Basic 승격 ⑥이후 가입 = Free 유지 ⑦**재적용 사이에 새로 가입한 사람은 그대로 Free**(핵심 불변식) ⑧중복 승격 없음 ⑨**일부러 Free 로 내린 기존 계정이 되살아나지 않는다**(`NOT EXISTS(plan <> 'free')` 조건) ⑩컬럼 기본값. **함정: 기준 시각을 미래로 잡으면 ⑦이 깨진다** — 처음 13:30 으로 적었을 때 실제 시각이 13:17 이라 새 가입자가 승격돼 이 테스트가 잡아냈다. `public.users` 는 `auth.users` 트리거가 자동 생성하므로 테스트는 `auth.users` 에 INSERT 한 뒤 `created_at`·`plan` 을 고쳐 쓴다 | attachment-storage.md §8.1 |
| e2e118 | 청크 업로드 — 브라우저 (C2·C3, 2026-08-06) | 구성: PG(TCP)+API :3000 **`ATTACHMENT_PART_KB=64`**, vite :5198(가짜 세션). ①**평소처럼 노드에 9MB 파일을 드롭**하면 조각 144개로 올라가고 노드에 서버 첨부 URL 이 붙는다 — 사용자가 경로를 고르지 않는다는 §12.7 확인 ①b내려받은 바이트가 원본과 **같다**(조각 순서·이어붙이기) ②올리는 동안 `upload-progress-bar` 의 `data-ratio` 가 **0에서 1로 오른다**(120ms 폴링으로 표본 수집 — 끝나면 사라지므로) ③조각 PUT 하나를 `page.route` 로 500 을 만들어도 **멱등 재시도**로 완료된다 ④[취소] → 진행률이 사라지고 첨부도 안 붙고 **오류 줄도 안 뜬다**(취소는 오류가 아니다 — `UploadAborted` 로 구분). 함정: **`ATTACHMENT_PART_KB` 를 안 주면** 9MB 가 조각 2개라 다중 조각 경로를 거의 검증하지 못한다 · 청크 경로는 로그인 상태(5198)에서만 탄다 · ④는 조각 PUT 을 `route` 로 250ms 지연시켜야 취소할 틈이 생긴다 | attachment-storage.md §12.6·§12.7 |
| e2e117 | 청크 업로드 — 서버 (C1, 2026-08-06) | 구성: **브라우저 없이** PG(TCP)+API :3000 **`ATTACHMENT_PART_KB=64`**. `fetch` 만으로 5개 엔드포인트를 두드린다. ①세션 시작 = 서버가 `partSize`·`parts` 를 정한다 ②조각을 **거꾸로** 보낸다(판정은 ⑧) ③조각이 비면 **409 + `missing` 목록** ④상태 조회로 빠진 조각을 안다(이어받기) ⑤조각 PUT 은 **멱등** — 재전송해도 받은 개수가 안 는다 ⑥크기가 안 맞는 조각은 **400 이고 저장되지 않는다**(잘린 전송이 쌓이면 complete 가 "다 왔다"고 착각한다) ⑦완료 = 단일 업로드와 **같은 모양**의 첨부 메타 ⑧내려받은 바이트가 원본과 **완전히 같다**(`i*31+7` 패턴이라 순서가 뒤바뀌면 티가 난다) ⑨완료하면 세션 404(tmp 정리) ⑩모르는 `uploadId` 404 ⑪취소 = 204 → 이후 404 ⑫상한 초과는 **시작할 때** 413. 함정: **1GB 파일을 만들지 않는다** — `ATTACHMENT_PART_KB=64` 로 띄우면 328KB 파일이 6조각이 되어 같은 경로를 전부 지난다 | attachment-storage.md §12.4·§12.5 |
| e2e116 | 엑셀 표 붙여넣기 = 표 우선 · 복구 배너 반복 (2026-08-06 실사용 보고) | 구성: vite :5199 만. ①`DataTransfer` 에 **표 HTML + 탭 구분 텍스트 + 표를 찍은 PNG** 를 함께 담아 `paste` 를 쏜다 → 노드 텍스트가 **파이프 MD 표**가 되고 `image`/`images` 는 비어 있다. **원인은 사용자가 보내 준 MD 내보내기에서 드러났다** — `![…](files/img-1.png)` 로 표가 사진으로 들어가 있었다(그래서 편집창에 안 보이고 지울 수 없었다). 스프레드시트는 표의 비트맵을 함께 주는데, 코드가 `hasImgFile ? '' : getData('text/html')` 로 그림이 있으면 HTML 을 통째로 버렸다 ②**[가드]** 배너를 [버리기] 한 뒤 같은 세션에서 새 초안이 생겨도 다시 뜨지 않는다 — **리마운트로 다시 뜨는 경로는 재현하지 못했다**(`setBrowserOpen` 으로는 언마운트되지 않는다) ③**노드에 드롭한 첨부가 실패하면 화면에 알린다** — 업로드 응답을 `page.route` 로 400 으로 바꾸고 3MB 파일(내장 한도 2MB 초과 → 서버 경로)을 노드에 드롭하면 `⚠ '큰영상.mp4' 첨부 실패 — …` 안내가 뜬다. 예전에는 **빈 catch** 로 삼켜 "무반응"이었다. 함정: ③은 **로그인 상태(5198)** 여야 서버 업로드 경로를 탄다 — 5199 는 인증이 꺼져 있어 `URL.createObjectURL` 폴백으로 그냥 성공한다 | 04-node-content.md §14.6-1 |
| e2e115 | 실사용 보고 3건 — 확인 게이트 단순화·표 중복 가드·복구 배너 오탐 (2026-08-06) | 구성: PG(TCP)+API :3000, vite :5198/:5199. ①a서버 맵을 열고 **편집하지 않아도** "닫고 진행할까요?" ①b`detachFromServer()` 로 서버 링크가 없어도(읽기 전용·로컬 파일과 같은 상태) 묻는다 ①c빈 문서면 묻지 않는다 ②**[가드]** 중첩 표(엑셀 재복사형) 변환에 셀 텍스트가 섞이지 않는다 — **이 입력으로는 수정 전에도 통과한다. 사용자의 실제 클립보드 HTML 을 확보하지 못해 "텍스트도 보이고 표도 보인다"를 재현하지 못했다.** 회귀 증명이 아니라 불변식 가드다 ③**간격만 조절**해도 초안은 적히지만 복구 배너는 뜨지 않는다(`isDraftFromThisSession`). 함정: ①의 사양 변경으로 **새 맵 만들기를 쓰는 기존 테스트(e2e99·e2e103)가 확인 단계를 먼저 통과**해야 한다 | 12-history-undo-redo.md §6.6-2 |
| e2e114 | 새 맵 만들기 2건 — 템플릿 경로 되돌리기 누수 · 확인 게이트 (2026-08-06) | 구성: PG(TCP)+API :3000, vite :5198. ①**서버 맵을 열고 편집하지 않은 채** '새 맵 만들기' → **"현재 맵 '…'을(를) 닫고 진행할까요?"** 가 뜬다(PR#209 이후 past=0 이 되어 확인이 사라졌던 회귀) ①a**템플릿으로 새 맵을 만든 직후 `past === 0`** — 수정 전에는 "기본 골격 → 템플릿 골격" 전환이 1건 남았다 ②그 뒤 편집·되돌리기·0확정·Ctrl+Z 10회에도 다른 골격이 새어 나오지 않는다 ③빈 문서('문서 없음')면 묻지 않고 바로 템플릿 선택으로. **함정: ①a 가 없으면 ②는 공허하게 통과한다** — 기본 골격과 '트리-진행트리맵' 템플릿이 둘 다 11노드·제목 '새 맵'이라 노드 비교로는 구분되지 않는다 | 12-history-undo-redo.md §6.6-1·§6.6-2 |
| e2e113 | 되돌리기는 문서 경계를 넘지 않는다 (2026-08-06 실사용 보고) | 구성: PG(TCP)+API :3000, vite :5198. 보고 경로를 그대로 재현한다 — ①**서버 맵 → 편집 → 맵 닫기 → 새 맵 → 편집 3회 → Ctrl+Z 10회**: 이전 맵(`통계C-큼`·`이전맵편집*`)이 **한 번도 나오지 않는다** ②맵 닫기 직후 `past/future = 0` ③새 맵 직후 `past/future = 0` ④**같은 맵 안에서는 되돌리기·다시 실행이 그대로**(회귀) ⑤서버 맵 열기도 경계를 끊는다. 수정 전에는 ②`past=5` ③`past=6` 이었고 ①에서 `통계C-큼`이 튀어나왔다(stash 로 되돌려 실패를 실측). 함정: **vite 를 재시작하지 않으면** HMR 이 스토어를 두 인스턴스로 갈라 놓아 "앱 클릭은 반영 안 되는데 `page.evaluate` 의 `import()` 로는 동작하는" 거짓 실패가 난다 — 이 건에서 e2e103 [4b] 가 그렇게 한 번 거짓 실패했다 | 12-history-undo-redo.md §6.6-1 |
| e2e112 | 초안 쓰기 실패를 알린다 — 마지막 사각지대 (2026-08-06) | 구성: vite :5199 만. ①`putDraft` 가 **성공 true / 실패 false** 를 돌려준다 — 구조화 복제 불가 값(함수)을 넣어 실패를 만든다 ②`draftWriteFailed` 가 서면 화면에 **"브라우저 임시 보관에 실패했습니다 · 지금 편집은 이 브라우저에도 남지 않습니다"**(`draft-write-failed`) ③다시 성공하면 사라진다. 배경: 저장소가 꽉 차면 초안이 하나도 안 적히는데 **첫 열기는 성공**하므로 `draft-storage-off` 검사는 통과했다 → 사용자는 보호받는 줄 알았다. 함정: 쿼터 초과는 `req.onerror` 가 아니라 **트랜잭션 `onabort`** 로 나타나므로 둘 다 봐야 한다 | 14-save.md §7.11 |
| e2e111 | "편집"의 경계 — 접기/펴기는 편집이 아니다 (2026-08-06) | 구성: PG(TCP)+API :3000, vite :5198/:5199. ①노드 접기/펴기 3회 = **되돌리기 단계(past.length)가 늘지 않는다** ②`collapseAll`/`expandAll` 도 마찬가지 ③접은 뒤 `undo()` = 접기가 아니라 **그 앞의 진짜 편집**이 되돌아온다(루트 텍스트로 확인) ④서버 맵에서 접기/펴기 12회 = `pendingEdits` 그대로 + `PUT /document` **0건** ⑤반대로 `updateNodeStyle` 은 `pendingEdits` +1 · 배지 `dirty`(좌우·스타일은 내용 쪽). 함정: 되돌리기 기준점을 만들려면 **진짜 편집을 1회 먼저** 해야 한다(past 가 0이면 [3]이 무의미) · 접기 액션은 스토어 API 로 직접 부르는 편이 안정적이다(토글 아이콘은 호버해야 나오고 선택 노드·그 부모는 숨는다) | 14-save.md §7.10 |
| e2e110 | 저장 모델 개편 — 실시간 저장 제거 (2026-08-06) | 구성: PG(**TCP 리스너 필수** `-h 127.0.0.1`)+API :3000, vite :5198/:5199. ①**디바운스 제거** — 편집 후 4초를 기다려도 `PUT /document` 0건(예전엔 1.5초·최대 5초에 갔다) ②**안전 시점** — 탭 숨김(hidden)에서 즉시 1건 ③**미저장 편집 50개** — Enter 52회면 주기 전이라도 나간다 ④**주기 자동저장** — `setAutosaveIntervalMin(0.05)`(=3초)로 낮추면 틱(5초) 안에 나간다 ⑤**자동저장=버전 없음 · ☁ 저장=버전 +1**(`map_document_versions` 실카운트) ⑥배지 = "미저장 편집 N개" ⑦개인 설정 `autosave-interval` 기본 5 · 3/5/10 ⑧**되돌리기 -3 배지 클릭 = 0으로 확정**(내용 그대로) ⑨**간격(spacingX)만 바꿔도 편집으로 센다**. 함정: ⑨는 **지금 값과 다른 값**을 넣어야 한다 — 앞 실행이 저장해 둔 값과 같으면 아무 일도 안 일어나 ⑨·④가 함께 거짓 실패한다(실제로 겪었다) · 판정은 배지 상태가 아니라 **PUT 발생**으로 해야 흔들리지 않는다 | 14-save.md §7.9 |
| e2e109 | 유실 0 마무리 — R3·R6·"마지막 1초" (2026-08-06) | 구성: PG+API :3000, vite :5198(가짜 세션)/:5199. **브라우저 컨텍스트 공유** 필수(IndexedDB). ①**구조 변경은 즉시 초안** — 노드 추가 뒤 350ms(디바운스 1초보다 짧게)에 이미 적혀 있다. 첫 변경은 기준 노드 수를 잡는 용도라 **Enter 를 두 번** 눌러야 2회차가 구조 변경으로 잡힌다 ②**화면 이탈 즉시** — 텍스트만 바꾼 뒤(노드 수 불변) 120ms 만에 hidden 을 쏴도 초안이 남는다 ③**저장이 끝난 문서는 초안을 남기지 않는다**(탭 전환마다 거짓 배너가 뜨던 문제) ④**hidden 이면 자동저장 즉시 발사**(R6) — 편집 150ms 뒤 PUT 0건 → hidden 후 1건 ⑤초안 2건 = "외 1건" + 한 건씩 차례로 ⑥**복구 후에도 초안이 남고** 배지는 `unsaved` ⑦**편집권 상실**(R3) — SQL 로 `map_edit_locks.session_key` 를 남의 것으로 바꾸면 다음 하트비트(주기 25초)에서 `cloudMapId=null` + `unsaved` + 초안 보관 + 사유 메시지. 함정: `document.visibilityState` 는 읽기 전용이라 `Object.defineProperty` 로 갈아 끼운 뒤 `visibilitychange` 를 직접 dispatch 한다 · [7] 은 하트비트 주기 때문에 최대 ~35초 걸린다 · **PG 는 TCP 리스너(`-h 127.0.0.1`)로 띄워야** 한다(소켓 전용이면 e2e96 등이 5433 연결 거부로 죽는다) | 14-save.md §7.8 |
| e2e108 | 유실 방어 3종 — 로컬 초안·maxWait·주기 버전 (2026-08-05 감사 R1·R2·R4) | 구성: PG+API :3000, vite :5198/:5199. **브라우저 컨텍스트를 공유**해야 IndexedDB 초안이 다음 페이지에서 보인다. ①편집 1초 뒤 초안이 IndexedDB 에 적힌다 ②다음 실행에 **"저장되지 않은 맵이 있습니다 — 복구할까요?"** 배너 → [복구] = 노드 수 그대로 복원 + 배너 사라짐 ③[버리기] = 초안 삭제 ④**maxWait** — 1초 간격으로 계속 편집해도 첫 변경 5초 안에 `PUT /document` 가 나간다(예전에는 무한정 미뤄졌다) ⑤개인 설정 `version-interval` = 기본 5분 · 3/5/10 선택 | 14-save.md §7.7 |
| e2e107 | "저장 안 됨"을 정직하게 알린다 (2026-08-05 유실 감사 후속) | 구성: PG+API :3000, vite :5198. ①빈 문서(첫 화면)는 거짓 경고 없이 `saved` ②**서버 미연결 문서**(새 맵 → 건너뛰기 → 편집)는 배지 `unsaved` + "저장 안 됨" 문구 (예전에는 5회 편집 후에도 '저장됨'이라 말했다 — 실측으로 구멍을 먼저 확인) ③서버 맵은 그대로 자동저장(회귀 — 편집·되돌리기 뒤 `saved`) ④편집 이력이 있는 `unsaved` 에서 `beforeunload` 가 `defaultPrevented` (창 닫기를 브라우저가 막는다). 함정: 합성 `beforeunload` 이벤트는 `cancelable: true` 로 보내야 `defaultPrevented` 가 잡힌다 | 14-save.md §7.6 |
| e2e106 | 저장 경로 전수 감사 + 접기 아이콘 위치 (2026-08-05 10차 보고) | 구성: PG+API :3000, vite :5198/:5199. ①**문서 출처 검사** — 서버 맵을 연 채 `loadMap` 으로 문서만 통째로 바꿔도(링크는 그대로) 자동저장이 그 맵을 덮어쓰지 않는다. 재현으로 먼저 실측했다: '통계B-중간' 문서가 AI 맵으로 바뀌는 것을 확인한 뒤 고쳤다 ②AI 새 맵 생성(AITab)이 `loadMap` 전에 `detachFromServer` 를 부른다(정적 확인 — 웹 AI 경로만 부르고 API 키 경로는 빠져 있었다) ③접기 아이콘 = **연결선이 노드를 떠나는 지점** 실측 5종: 트리·오른쪽/진행트리=아래 변 **왼쪽 스파인**(한가운데 아님) · 트리·아래=아래 변 한가운데 · 계층형/방사형=좌우 변 한가운데. 함정: 펼쳐진 노드의 '−' 토글은 **노드에 마우스를 올려야** 나타나고, **선택 노드와 그 부모**는 토글이 숨는다(초기 선택이 b1-2 라 b1 로 재면 안 된다) · 레이아웃을 바꾸면 노드가 화면 밖으로 나가므로 `requestFit()` 후 잰다 | 14-save.md §7.5·10-canvas.md §21.0-1 |
| e2e105 | 맵이 통째로 비워진 유실 — 3중 방어 (2026-08-05 9차 보고) | 구성: PG+API :3000, vite :5198. **검증용 맵을 새로 만들어 쓰고 끝나면 지운다**(고정물 통계A/B/C 를 건드리지 않는다). ①서버 맵을 연 뒤 `Ctrl+Z` 8번 = 문서가 **'연 시점'보다 뒤로 가지 않는다**(loadMap resetHistory) ②가지를 모두 지우고 ☁ 저장 = **확인창** → 취소하면 DB 그대로(3) ③확인을 누르면 그때는 반영(0) ④서버 마지막 방어선 — API 직접 PUT: `allowEmpty` 없는 빈 문서는 **명시 저장(keepVersion=true)이어도 거부**(unchanged, DB 3 유지), `allowEmpty:true` 면 통과(0). 배경: 재현 실측으로 DB 가지 3→0 을 먼저 확인한 뒤 고쳤다 — 자동저장만 막고 명시 저장은 통과시키던 것이 구멍이었다 | 14-save.md §7.4 |
| e2e104 | 8차 보고 3건 (2026-08-05) | :5199 + **클립보드 권한**(`grantPermissions`). ①패널 머리글 `panel-close` ✕ = 패널 접힘(레일만 + '패널 펼치기' 버튼) — 예전 ⋯ 는 onClick 없는 장식이었다 ②노드 텍스트 **전체 선택 + Del + Enter = 정말 비워진다** ②b`Ctrl+X` 도 같고 그 사이 `Ctrl+Z` 로 원복. 가드가 **두 겹**이었다 — 편집창 `saveEdit` 과 스토어 `updateNodeText` 양쪽에서 빈 문자열을 무시했다 ③**다른 탭의 노드 붙여넣기** — 탭 A 에서 Ctrl+C → `navigator.clipboard.readText()` 로 꺼낸 텍스트를 **모듈 메모리가 다른 탭 B** 에 합성 paste (= 다른 창·다른 맵과 같은 조건) → 서브트리가 붙는다 ④`[--iThinkWise--]` 자리표시 텍스트는 노드로 붙지 않고 `canvas-notice` 안내. **함정: vite HMR** — 소스를 고친 뒤에는 모듈 URL 에 `?t=` 가 붙어, `page.evaluate` 의 `import('/src/stores/documentStore.ts')`가 **앱과 다른 스토어 인스턴스**를 잡는다(검증이 거짓 실패). 고친 직후에는 **vite 를 재시작**하고 돌린다 | 02-node-editing.md §5.2·10-canvas.md §21.0 |
| e2e103 | 화면 정리 4건 (2026-08-05 7차 보고) | 구성: PG+API :3000, vite :5198(가짜 세션)/:5199. ①문서함 폴더·맵 **이름 버튼에 `title` 툴팁 없음**(행 강조가 대신한다) ②에디터 검색 결과 행도 툴팁 없음 + **목록 위 한 줄 안내**("누르면 그 노드로 이동합니다") + `.mm-list-row` 강조는 유지 ③문서함 **기본 정렬 = 이름 ▲**(목록이 실제 가나다순인지 `localeCompare('ko')` 로 대조) ④템플릿 선택 단계 `[data-testid="tpl-cancel"]` ✕ = **새 맵 만들기 취소** — 카드가 떠 있는 동안에도 문서가 그대로고(`during`), 취소 후에도 그대로 + 카드 닫힘 + 아래 메뉴 복귀 ④b`tpl-skip` 건너뛰기는 여전히 기본 골격(11노드)으로 새 맵을 시작. 함정: 이 검증은 **문서 교체 시점을 미룬** 구조 변경에 의존한다 — 예전처럼 '새 맵 만들기'를 누르는 즉시 갈아 끼우면 ④의 `during` 이 깨진다 | 01-map.md §5.1·coding-conventions.md §5-1-1 |
| e2e102 | 목록 행 호버 강조 + 저장 실패 배지 (2026-08-05 6차 보고) | 구성: PG+API :3000, vite :5198(가짜 세션)/:5199. **선행 정리**: 앞 실행이 남긴 `map_edit_locks` 를 SQL 로 지운다 — 잠긴 맵은 읽기 전용으로 열려 cloudMapId 가 붙지 않아 [2] 가 흔들린다(제품 버그 아님). ①문서함 맵 행에 호버 = 배경·테두리 변화(`.mm-list-row` 공용 클래스, 색은 `--row-hover`/`--row-hover-bd` 로 주입) ①b폴더 행도 같은 규칙(DB 에 폴더가 없으면 SKIP) ②편집 중인 맵 행은 `aria-selected="true"` 로 계속 강조 ③에디터 검색 결과 행도 같은 규칙(선택된 행은 이미 강조라 **aria-selected="false" 인 행**으로 호버 검증) ③b선택 중인 노드의 결과 행은 호버 없이도 강조 ④배지 문구가 갈렸다 — `retrying`="재시도 중…" · `error`="☁ 저장을 눌러 주세요" ⑤**되돌리기 중 "저장 실패" 회귀 가드** — 편집·되돌리기 뒤 배지가 `saved` 로 끝나고 `/document` 409 가 0건. 함정: 검색어는 초기 샘플 맵('2026 제품 로드맵')에 실제로 있는 말('노드')이어야 결과가 뜬다 · 맵을 연 직후 cloudMapId 는 비동기로 채워지므로 waitForFunction 으로 기다린다 | 14-save.md §7.3·coding-conventions.md §5-1 |
| e2e101 | 드롭존 "노드 안 = 하위" (2026-08-05 5차 보고) | :5199, 새 맵 기본 골격. ①'주제 3'을 '주제 1' **한가운데**로 실드래그 → 놓으면 실제로 주제1의 자식(브랜치 3→2) ②그 지점의 인디케이터 = **노드 전체를 감싸는 초록 테두리**(stroke-width **2** · fill-opacity 0.12 — 2026-08-05 6차에서 3→2 로 얇게) ③박스 **바깥** 좌·우 = 테두리 없이 변 바(형제/부모). 관찰용 드래그는 **빈 공간에서 손을 떼** 이동이 일어나지 않게 한다(Escape 는 드롭을 막지 못한다). 배경: 자식이 가로로 자라는 노드에서는 아래로 끌어도 하위가 되지 않아 "좌우만 보이고 하위에 못 넣는다"·"존이 2곳만 보인다"가 났다 | 02-node-editing.md §노드 안쪽=하위 |
| e2e100 | 불러오기 실패 안내 (2026-08-05 4차 보고) | :5199. 임시 파일 3종을 만들어 파일 선택기로 실제 업로드: ①**일반 웹페이지 HTML**(맵 메타데이터 없음) → `data-notice-kind="error"` 빨간 안내(rgb(185,28,28)) + 파일명·대안 안내 포함 ②구조 없는 MD → 같은 오류 스타일 ③정상 MD → 오류 없이 맵 표시(회귀). 배경: 메시지 코드는 있었지만 **성공과 같은 10.5px 주황 글씨로 패널 맨 위에 2.5초**만 떠서, 아래쪽 불러오기 버튼을 쓴 사용자에게는 화면 밖이라 "아무 반응 없음"으로 보였다 → 오류 전용 스타일 + 8초 + scrollIntoView + 새 파일 선택 시 이전 안내 제거 | importMapFile.ts·NewMapPanel |
| e2e99 | 실사용 3건 (2026-08-05 3차) | :5199. ①**Ctrl+X 잘라내기** — '하위 주제' 서브트리(3노드)를 잘라 11→8, '주제 3' 선택 후 Ctrl+V 로 11 복귀, Ctrl+Z 두 번으로도 11 (복사·붙여넣기만 있고 잘라내기가 없었다) ②'진행트리-트리맵' 템플릿의 **3레벨('하위 주제') 자식 배치 = tree-right**(진행트리에서 변경 — 모듈을 직접 import 해 layoutType 확인) ③가지 0개 문서 = 자동저장 스킵 조건 성립. 서버 쪽 같은 가드는 별도 **guard5.mjs**(API 4단언: 자동저장 빈 문서 거부·명시 저장 통과·내용 있는 자동저장 정상) | 14-save.md §7.2·libraryTemplates.ts |
| e2e98 | 코드 블록 격자 배치 + 도식 살리기 (2026-08-05) | :5199. 한글·영문·기호가 섞인 상자 도식(6줄·각 16칸)을 웹AI 경로로 붙여넣어 맵 생성 후 **브라우저에서 실측**: ①각 줄 `<text>` 의 `getBBox().x + width`(오른쪽 끝)가 6줄 모두 **동일**(spread 0px) ②같은 칸 수 줄들의 렌더 폭도 동일(105.6px) — 격자 전에는 한글 줄만 짧아 spread 33px·폭 편차 49px 였다 ③기본 템플릿 v4.2 = 'ASCII 아트 도식은 금지' 없음 + '글자 격자에 맞춰' 있음 ④**내보낸 HTML 뷰어**를 file:// 로 열어 같은 실측 통과. 함정: 이 테스트가 처음 잡아낸 진짜 원인은 폰트가 아니라 **`mapSourceCandidates` 의 펜스 제거 폴백**이었다 — 견출이 펜스 밖에 있는 답변에서 ``` 줄을 지워 도식이 평문이 됐다(`hasHeadingOutsideFence` 후보 추가로 수정) | monoGrid.ts·web-ai-clipboard.md |
| e2e97 | task #115 실사용 4건 (2026-08-05 보고) | 구성: e2e96 과 동일(PG+API+vite 5198/5199). ①문서함 **관리 버튼 = SVG 아이콘 3종**(I.Pencil·I.FolderMove·I.Trash, 28×26 테두리 버튼 + aria-label) — ✏📂🗑 이모지는 글꼴에 따라 알아볼 수 없이 작아졌다 · 3버튼 묶음 중심이 '관리' 머리글 중심과 ±6px ②**머리글 정렬 = 값과 같은 쪽**(숫자 4열은 머리글도 오른쪽 끝 일치 ≤4px) ③**AI 바로가기 분리** — window.open 을 가로채 실제 주소 검증: Claude/ChatGPT(일반)는 `?q=`에 **프롬프트 전체**, Copilot GPT는 `?q=`에 **주제만**, Gemini 는 파라미터 없음(자동 입력 미지원) ④**붙여넣기=인식만** — 붙여넣어도 노드 수 불변·팝오버 없음, '③ 새 맵 생성' → 팝오버 **취소 = 변화 없음**, 다시 눌러 **확인해야** 새 맵. 함정: window.open 후킹은 페이지 로드 뒤에 해야 하고, prefill 길이가 PREFILL_MAX_LEN(6000)을 넘으면 `?q=` 가 붙지 않는다 | web-ai-clipboard.md·document-library.md |
| e2e96 | task #114 실사용 3건 (2026-08-04 3차 보고) | 구성: 로컬 PG + API :3000(dev) + vite :5198(가짜 세션), '통계A/B/C' 맵 시드(verify114.mjs — 5/10/25노드, B·C에 data URL 첨부 2개). ①문서함 **상세 컬럼 7종**(이름·생성일·수정일·노드·크기·첨부·첨부 용량) 머리글 + 값 표시(10노드·8.7KB·첨부 2·5.9KB, 날짜 `YYYY.M.D HH:mm` 로캘 무관) + 통계 NULL(구저장분) = '—' ②**머리글 클릭 정렬** — 노드수 desc(큼 먼저)→asc(중간 먼저) 토글, NULL 은 양방향 모두 끝(NULLS LAST) ③**자동저장 유실 가드** — 서버 맵 열고 Space 로 자식 추가(디바운스 예약) 직후 웹AI 붙여넣기로 새 맵 전환(확인 팝오버 수락) → 3.5s 대기 후 이전 맵의 node_count·updated_at·doc 내용 **무변화**(자동저장 자체가 안 감). e2e95 [2] 에 팝오버 위치 검증 추가(left ≥ 패널 right, 세로 중앙 ±40px). 함정: 웹AI 확인은 이제 `[data-webai-confirm-ok]` 클릭 — dialog 핸들러는 NewMapPanel 의 '현재 맵 닫고 계속'(window.confirm)에만 남는다 | 14-save.md §7.1·document-library.md·web-ai-clipboard.md |
| e2e95 | 웹 AI 실사용 3건 (2026-08-04 2차 보고) | :5199, 새 맵 11노드에서 시작. ①**진행트리 드롭존**: 하위 주제에 자식 추가 후 그 자식을 하위 주제 **오른쪽 존**으로 드래그 — 초록 **세로 바**(다음 형제) 표시 + 놓으면 같은 행의 형제로 이동 (이전엔 side만 봐서 아래 바가 "하위"로 읽히고 오른쪽 표시가 없었다 — Canvas childDirOf/findDropZone 방향 인지) ②웹AI 패널 **적용 대상**: 노드 선택 → `하위에 삽입` 토글 → 합성 붙여넣기(자동 실행 on) = 선택 노드 하위 +4 (새 맵 안 열림 — 이전엔 자동 실행이 항상 새 맵) ③새 맵 생성 버튼 🗺 이모지 제거(윈도 우산 글리프) + ② 단계 드래그 복사 경고 문구 + AI 설정 기본 템플릿 = v4.1(**ASCII 아트 도식 금지** — 규칙 6). 함정: 기본 화면은 샘플 맵(새 맵 생성 절차 필요) · 선택 노드 textContent에는 크기조절 툴팁이 붙어 **접두 일치**로 찾는다 · 패널 버튼 클릭 후 keydown 전 activeElement.blur() | 02-node-editing.md·web-ai-clipboard.md |


### 2.12 협업 (실시간 동시 편집)

> **여기 적는 것은 코어가 책임지는 부분뿐이다.** 협업 구현은 유료 모듈
> (`open-core-boundary.md`)이고, **그쪽 검증은 그쪽 카탈로그에 적는다** —
> 공개 카탈로그에 적으면 무엇을 어떻게 만들었는지가 그대로 드러난다.
>
> 코어의 몫은 둘이다. **협업맵에서는 단일 세션 편집 잠금을 비켜 주는 것**
> (27-sync-model §7)과 **`map_members` 기반 접근 판정**(§9). 둘 다
> 유료 모듈이 없어도 코어 혼자 지켜야 하는 약속이다.
>
> 실행 환경도 다르다 — 공개 저장소를 로컬에서 띄우면 협업은 꺼져 있으므로
> (`ProStub`), **dev 서버를 실물로 두고 REST 로** 잰다.

| ID | 대상 | 핵심 검증 | 문서 |
|---|---|---|---|
| e2e178 | **협업맵에서 코어가 물러나는가 — dev 서버 실물** (2026-08-23) | 구성: dev 서버(`api-dev` · `auth-dev`) 실물. 계정 2개를 GoTrue `/signup` 으로 직접 만들어(자체 호스팅 GoTrue 는 `mailer_autoconfirm=true` 라 메일 없이 만들어진다) 코어 REST 만으로 잰다. **공개 코어가 책임지는 두 가지**만 여기 적는다 — 협업 자체(동기화·물질화·게이트웨이)의 검증은 **유료 모듈의 것이라 이 카탈로그에 적지 않는다**(open-core-boundary.md). **편집 잠금 8 (27-sync-model §7)** — solo 맵은 첫 세션 `acquired` · 둘째 세션 `busy` · 둘째 세션 저장 **409**, 협업맵은 **`editLock` 을 아예 주지 않고**(잠근 적 없으면서 `acquired` 라고 말하지 않는다) 둘째 세션 저장 **200** · 하트비트 `held:true` · 해제 `ok:true`. **네 자리(열기·저장·하트비트·해제)를 실물에서 다 확인했다** — 하나만 빠져도 협업맵이 조용히 잠긴다. **접근 판정 3 (`map_members`)** — 참가자가 아닌 계정은 맵 메타·문서 모두 404, "공유받은 맵" 0건. 초대된 뒤에는 "공유받은 맵" 에 뜬다. **검증하지 못한 것** — 브라우저 화면. 개발 앱 주소를 `dev.mindmap.ai.kr` 로 알고 갔는데 404 였다 — **주소가 `pro-dev.mindmap.ai.kr` 로 옮겨 간 것**을 나중에야 알았다(2026-08-19 · vision.md "환경별 도메인"). 서버가 죽은 것이 아니었다. 함정 1가지 — 스냅샷을 `PUT` 할 때 `editSession` 을 주면 **그 저장이 잠금을 쥔다.** 시드를 그렇게 만들어 두고 "첫 세션이 왜 `busy` 냐"를 한참 봤다 — 시드는 세션 없이 저장한다 | 27-sync-model.md §7 · open-core-boundary.md |

---

## 3. 회귀 실행 가이드 (영역별 최소 세트)

새 변경을 낼 때 **건드린 영역**의 아래 테스트를 최소한으로 돌린다.

| 변경 영역 | 함께 돌릴 회귀 테스트 |
|---|---|
| 노드 렌더링·마크·크기 | e2e18, e2e35, e2e37, e2e51 |
| 붙여넣기·사진 | e2e38, e2e39, e2e40, e2e41 |
| 레이아웃·드래그 이동 | e2e19, e2e20, e2e21, e2e22, e2e42 |
| 접기/칩/줌 | e2e45, e2e46, e2e46b, e2e49, e2e49b |
| 내보내기/불러오기 | e2e25, e2e27, e2e29, e2e31, e2e32, e2e47 |
| 뷰어(exportHtml) | e2e33, e2e36, e2e37, e2e48, e2e49b (레이아웃 전수 스윕), e2e53 |
| 검색·아웃라인 | e2e37, e2e48, e2e50, e2e53 |
| AI | e2e43, e2e44, e2e52 |
| 저장·히스토리 | e2e79, e2e80, e2e90, e2e93 |
| 협업 (dev 서버 실물) | e2e178 |

> **뷰어 JS는 TS 검사가 닿지 않는다**: `exportHtml.ts`의 VIEWER_JS는
> 템플릿 문자열이라 타입체크가 미치지 않는다. 뷰어를 고칠 때는
> ① 내보낸 스크립트 `eslint no-undef` 정적 검사 ② e2e49b 레이아웃
> 전수 런타임 스윕을 함께 돌린다 (docs/04-extensions/import-export/
> 20-export.md "뷰어 JS 품질 주의" 참조).

---

## 4. 갱신 이력

- 2026-09-04 (112차): **e2e186 실사용 확인**. 지시문을 고친 뒤 AI 생성으로
  **체크리스트가 실제로 나온다**(사용자 확인). 고치기 전 같은 자리는 표 +
  `□` 글자였다 — **지시문 한 줄이 결과를 정하고 있었다.** 파서·렌더러는
  처음부터 체크리스트를 지원하고 있었고 고칠 것이 없었다.

- 2026-09-04 (111차): **e2e187 — 접힌 채 내보낸 진행트리 맵을 펼치면 맵이
  사라졌다**. 접힌 노드 아래 자식에 `pos` 가 없어 뷰어 재배치가 NaN 을
  만들었다 → 내보내기가 숨은 노드의 크기까지 굽고, 뷰어는 `pos` 없는
  자식을 스스로 잰다. **8/8 PASS**(수정 전 노드 2개만 남음).
- 2026-09-04 (110차): **e2e186 — AI 점검표를 체크리스트로**. AI 가 만든
  '최종 점검표'가 표로만 나와 확인란을 누를 수 없다는 보고에서 시작했다.
  **원인은 파서가 아니라 지시문**이었다 — 규칙 14 가 `최종 점검표(표)`
  라고 지정하고 있었다. 파서로 확인해 보니 **표 셀 안의 `- [ ]` 는
  체크박스가 되지 않고**(체크 정규식이 줄 시작을 요구한다), 반대로
  `- [ ]` 로 쓰면 **노트 배치든 노드 배치든 둘 다 눌린다.** 규칙 8-1 을
  신설하고 규칙 7·14 와 `tech`·`wbs` 유형 지시를 함께 고쳤다.
  **마이그레이션을 빠뜨리지 않았다** — 직전 기본값을 PREVIOUS 에 넣어야
  기본값 사용자가 새 지시문을 받는다.

- 2026-09-04 (107차): **e2e180 브라우저 실사용 확인**. NestJS 11 · Express 5
  이관은 로컬(진짜 PG + 빌드 산출물)과 CI 로만 검증했고 **화면은 못 봤다**고
  적어 두었다. dev 서버에서 맵 저장과 **한글 이름 파일 첨부**를 실제로 해
  파일명이 온전히 나오는 것까지 확인했다 — multer 2.2.0 의 latin1 전제가
  실제 브라우저 경로에서도 유효하다는 뜻이다. 같은 날 헬스체크의 `runtime`
  이 `nestjs 11.2.3 · express 5.2.1` 을 보고해 서버 반영도 확정됐다.
  **이번 이관에서 미검증으로 남아 있던 자리는 이제 없다.**

- 2026-09-04 (110차): **e2e186 — HTML 뷰어 아웃라인 양방향 동기**. 맵에서
  노드를 고르면 아웃라인의 그 행이 선택 표시되고 그 행으로 스크롤된다
  (에디터와 같은 규칙). 선택이 바뀐 때만 스크롤해 접기/펴기로 화면이
  튀지 않는다. **11/11 PASS**.
- 2026-09-04 (109차): **e2e185 — HTML 뷰어 아웃라인이 코드·표·체크를 에디터와
  같이 그린다**. 뷰어 아웃라인 행이 검색 결과용 한 줄 요약(`flattenText`)을
  써서 코드·표가 '⧉코드'·'⊞표' 칩으로만 보였다 → 에디터 `NodeRichText` 와
  같은 규칙의 `richTextEl` 로 블록을 그대로 그린다. 같은 PR 로 `?map=`
  열기 실패 안내가 맵을 열면 저절로 사라지게 했다(로그아웃·재로그인으로도
  안 지워지고 주소의 `?map=` 을 지워야 사라지던 후속 보고). **15/15 PASS**.
- 2026-09-04 (108차): **e2e184 — 토큰 갱신이 열린 문서를 지웠다**.
  로그인 1시간 뒤 첫 API 호출이 `session` 객체를 갈아 끼우고, `EditorPage`
  의 전환 리셋이 그것을 로그인 전환으로 오인해 `clearCurrentMap()` 을
  불렀다 — 저장 대화상자 뒤에서 문서가 '문서 없음' 이 되고 그대로 서버에
  가지 0개 맵이 생겼다(HTML 내보내기도 '문서 없음'). 의존성을
  `session.userId` 로 바꾸고, `saveNewMap` 이 보내기 직전에 자리표시를
  거절하며, 문서 저장 실패 시 방금 만든 맵을 지우고, `?map=` 열기 실패
  때 이유 + 문서함을 연다. 가짜 GoTrue(`expires_in=30`)로 매 호출 갱신을
  만들어 **수정 전 8항목 FAIL → 수정 후 23/23 PASS**.
- 2026-09-03 (107차): **e2e183 — EMM 선언의 레벨별 레이아웃이 그림에 반영된다**.
  `levels:`·`template:` 선언이 설정 패널에만 보이고 **그림에는 전혀 반영되지
  않던** 문제를 고쳤다. 원인은 둘이었다 — ⑴불러오기가 `settings` 만 합치고
  노드 `layoutType` 을 안 박았다(엔진은 `settings` 를 읽지 않는다)
  ⑵`levelLayouts` 색인이 앱(branches depth)과 선언 해석(레벨 번호)에서
  한 칸 어긋나 있었다. 순회 규칙은 `utils/levelLayouts.ts` 한 벌로 모아
  설정 패널과 불러오기가 같은 것을 쓰게 했다. 저장된 맵의 `settings` 는
  **마이그레이션하지 않았다.**

- 2026-09-02 (106차): **e2e181 후속 — `commit` 은 빌드한 저장소의 SHA 다**.
  헬스체크를 넣고 dev 서버에서 처음 읽어 보니 `44d642ca5537` 이 나왔는데
  **공개 저장소에 없는 커밋**이었다. 개발 서버는 유료판 한 벌만 돌고
  그것은 private 저장소에서 빌드되니 당연한 결과다 — 그런데 런북에는
  "`git log --oneline -1` 과 눈으로 대조하라"고 적어 두었다. 그대로
  따르면 **언제나 어긋나서 '배포가 안 왔다'고 잘못 판단**한다.
  런북·api-spec·이 카탈로그의 안내를 사실에 맞게 고쳤다. 일상적으로는
  `runtime`(라이브러리 버전)을 보는 것이 맞고, `commit` 은 **private
  저장소**의 `git log` 와 대조한다.
  같은 응답이 `runtime.nestjs = 11.2.3` 을 보고해, 그동안 확정하지 못했던
  **NestJS 11 이관(#347)의 서버 반영을 처음으로 확인**했다.

- 2026-09-02 (106차): **e2e182 — 에디터 실사용 3건**. 새 노드에 붙던 '•',
  좁은 화면에서 세로로 쌓이던 툴바 글자, 새 맵에서 경고색으로 뜨던
  '저장 안 됨' 문구를 고쳤다. 세 건 모두 **되돌려 깨지는 것까지** 확인해
  수치를 남겼다(배지 높이 14px ↔ 되돌리면 240px, '•' 0개 ↔ 1개,
  중립색 `rgb(107,99,88)` ↔ 경고색 `rgb(202,138,4)`).
  기존 맵에 이미 박힌 '•' 는 **지우지 않았다** — 사용자가 일부러 넣은
  것과 구분할 수 없다.

- 2026-09-02 (105차): **e2e181 — 헬스체크가 배포 반영을 말한다**.
  `/v1/health` 에 `commit`(빌드된 커밋 12자)과 `runtime`(node·nestjs·
  express 버전)을 더했다. 그전에는 옛 코드가 돌고 있어도 응답이 똑같아서,
  **배포가 왔는지 확인할 방법이 Coolify UI 나 서버 SSH 뿐**이었다.
  #347 이관 뒤 헬스체크만 보고 '반영됐다'고 판단한 것이 근거가 못 된다는
  지적에서 나왔다 — `schema:"ok"` 는 DB 이야기지 코드 이야기가 아니다.
  노출되는 것은 공개 저장소의 커밋 SHA 와 오픈소스 라이브러리 버전뿐이라
  새로 드러나는 정보가 없다(커밋만 알면 `package-lock.json` 에 다 있다).
  환경변수가 없으면 `commit` 은 **필드째 생략**한다 — 없는 것과 모르는
  것을 섞지 않는다.

- 2026-09-02 (104차): **e2e180 — NestJS 10 → 11 · Express 4 → 5 이관**.
  `@nestjs/core` 취약점(GHSA-36xv-jgw5-4q75)을 지우는 것이 목적이었다.
  **12 가 아니라 11 에서 멈췄다** — `@nestjs/throttler` 최신(6.5.0)이 아직
  NestJS 12 를 peer 로 선언하지 않아 `npm ci` 가 막힌다. 11.2.3 이면
  취약점이 해소되므로(`<=11.1.17` 이 취약) 목적은 그대로 달성된다.
  **소스 수정은 한 줄도 없었다** — 와일드카드 라우트·`req.query` 대입·
  `req.param()` 이 애초에 없어 Express 5 가 그냥 통과했다.
  **잡은 회귀 하나**: `@nestjs/config@12` 가 ESM 전용이라 CJS 빌드가
  `require()` 로 못 읽는다. **dev 모드 기동도 스모크 33건도 전부 통과했고**,
  supabase 모드 부팅 스모크(`--no-experimental-require-module`)만 잡아냈다 —
  2026-08-01 배포 실패 뒤에 넣어 둔 그 검사가 정확히 제 몫을 했다.
  `^4.0.4`(CJS)로 되돌려 해결.
  결과: `npm audit --omit=dev` **0건**(overrides 도 전부 제거), 스모크 33/33,
  vault 단위 전부 통과, 레이트리밋 429 동작, 한글 첨부·청크 업로드 무손실.

- 2026-08-23 (103차): **e2e179 — vault 미러가 실제로 파일을 만든다**
  (vault 미러 슬라이스 3). 슬라이스 1 이 "어떤 이름", 2 가 "안전하게 쓰는
  법", 3 이 **"언제 · 어느 자리"** 다. 경로 계획 · `vault_files` 표 ·
  디바운스 · 저장 경로 배선까지 이어 붙였다.
  **직렬화기를 꽂아 실물로 돌려 봤다** — 진짜 PostgreSQL, 진짜 파일시스템,
  진짜 EMM 직렬화기(퍼블리시 전이라 타르볼을 `--no-save` 로 넣었다).
  `연구/RAG 증분 인덱싱.md` 가 폴더 트리 그대로 생기고, 제목을 바꾸면 옛
  파일이 휴지통으로 가고, 사용자가 고친 파일은 `.conflict.md` 로 비켜 쓰는
  것까지 봤다.
  **잡은 함정 둘**:
  ⑴ **경로 겹침의 마지막 판정은 `folderId` 가 아니라 디렉터리 경로여야
  한다.** 폴더 이름을 가르는 것만으로는, 부모가 사라져 **둘 다 홈으로
  올라오는** 경우를 못 막는다. 그때 두 맵이 같은 파일이 된다.
  ⑵ **충돌 때 `vault_files` 를 갱신하면 안 된다.** 갱신하면 다음 저장에서
  "우리가 쓴 그대로" 로 보여 **사용자의 편집을 조용히 덮는다.** 전용
  항목으로 박아 두었다.
  **큐는 쓰지 않았다.** 설계 §6 의 전제(`export`·`ai-generate` 큐가 있다)가
  틀렸고, vault 는 셀프호스트 전용이라 API 가 한 벌만 돈다 — 맵 id 별 타이머
  하나면 "5초 뒤 · 같은 맵은 합친다" 가 그대로 된다.
  **남은 것은 명령 하나와 코드 열 줄이다.** `@easymindmap/emm-parser` 가
  아직 레지스트리에 없다(npmjs.org 직접 404 · GitHub Packages 404 ·
  `easymindmap` 검색 0건). 퍼블리시가 끝나야 의존성을 넣을 수 있다.

- 2026-08-23 (104차): **§2.12 를 경계에 맞게 줄인다.** 102차에 적은
  e2e178 은 **유료 모듈의 구현 세부**(게이트웨이 경로 · 프레임 규약 ·
  문서 매핑 · 잡은 결함 목록)를 공개 카탈로그에 그대로 실었다.
  `open-core-boundary.md` 가 갈라 둔 선을 넘은 것이다 — **비공개인 것은
  "무엇이 있는가"가 아니라 "어떻게 동작하는가"** 인데 후자를 적었다.
  공개 코어가 책임지는 둘(**협업맵에서 편집 잠금을 비켜 주는 것** ·
  **`map_members` 접근 판정**)만 남기고, 나머지는 유료 저장소 카탈로그로
  옮겼다. 지운 것이 아니라 **자리를 옮긴 것**이다.
  같은 정정에서 e2e179(vault)가 협업 표에 들어가 있던 것도 §2.11 로
  되돌렸다 — 102차가 새 절을 표 바로 앞에 끼워 넣은 탓이다.
- 2026-08-23 (102차): **e2e178 — 협업맵에서 코어가 물러나는가** (§2.12
  신설). 공개 저장소만으로는 협업을 켤 수 없어(`ProStub`) **dev 서버를
  실물로** 두고 쟀다. 코어의 약속 둘이 실물에서 지켜진다 — 협업맵은
  단독 편집 잠금을 **네 자리(열기·저장·하트비트·해제) 모두**에서 비켜
  가고, 참가자가 아닌 계정은 맵 메타·문서 모두 404 다.
  **브라우저 UI 는 검증하지 못했다** — 개발 앱 주소를 옛 `dev.` 로 알고
  갔는데 404 였다(앱은 `pro-dev.mindmap.ai.kr` 로 옮겨 갔다). REST 만
  검증한 결과다.
  (104차에서 유료 구현 세부를 걷어냈다.)
- 2026-08-21 (101차): **e2e177 — vault 에 안전하게 쓴다** (vault 미러 슬라이스 2).
  "단방향 미러" 라는 말은 **vault 폴더를 우리가 마음대로 덮어쓴다**는 뜻이다.
  사용자가 Obsidian 으로 그 파일을 고쳤다면 그 편집이 사라진다. 그래서
  이 슬라이스는 **막는 것이 전부**다 — 마커 · 경로 · 해시 비교 · 휴지통.
  **설계에 없던 위험 셋을 막았다**:
  ⑴ **심볼릭 링크로 vault 밖에 쓰는 것.** 폴더 이름 자리가 링크면
  `연구/x.md` 가 `/etc/x.md` 가 된다. **`path.resolve` 로는 못 잡는다** —
  이미 있는 조상을 `realpath` 로 풀어야 보인다.
  ⑵ **비어 있지 않은 폴더를 vault 로 받는 것.** 마커를 빈 폴더에만 만든다.
  잘못 지정한 경로는 되돌릴 방법이 없다.
  ⑶ **지난 충돌본을 덮는 것.** 아직 정리 안 했을 수 있는데 덮으면 §7 의
  "사용자가 쓴 것을 우리가 지우지 않는다" 가 그 자리에서 깨진다.
  **원자적 쓰기가 왜 필요한지도 분명해졌다**: 반쯤 쓰인 `.md` 가 남으면
  다음 번 해시 비교가 그것을 "사용자가 고쳤다" 로 읽어 **있지도 않은
  충돌**을 만들어 낸다. 임시 파일에 쓰고 `rename` 한다.
  **이동은 새 자리를 먼저 쓴다.** 거꾸로 하면 중간에 죽었을 때 파일이
  사라진 상태로 남는다. 새 자리가 충돌이면 옛 파일을 건드리지 않는다.
  **`.md` 를 어디서 만드나 — npm 퍼블리시로 정했다.** 준비를 끝내고
  **타르볼을 실제로 만들어 빈 프로젝트에 설치, `require` 로 직렬화·왕복까지
  확인**했다(e2e172 와 같은 방법). 남은 것은 사람이 하는 `npm publish`
  하나다 — 그 전에는 `apps/api` 에 의존성을 넣으면 `npm ci` 가 404 로
  깨져 CI 가 빨간불이 된다.

- 2026-08-21 (100차): **e2e176 — vault 파일 이름 규칙** (vault 미러 슬라이스 1).
  vault 미러는 **사용자 폴더에 우리가 파일을 쓰는** 기능이다. 이름 규칙이
  틀리면 유령 파일이 쌓이거나 **남의 파일을 덮어쓴다** — 둘 다 되돌릴 수
  없다. 그래서 **디스크에 손대는 코드를 한 줄도 넣지 않고** 규칙부터 못 박고
  시험했다(B16 에서 "읽는 쪽 먼저" 를 깐 것과 같은 이유다).
  **설계 문서에 없던 위험을 하나 찾았다**: 폴더 이름이 `.attachments` 나
  `.trash` 가 되면 **vault 가 자기 폴더를 사용자 폴더로 덮어쓴다.** 앞의
  점을 `_` 로 바꿔 막고 §3.1 에 규칙을 더했다.
  **잡은 함정 둘**:
  ⑴ 앞의 점을 바꾸는 것을 **끝점 제거보다 먼저** 돌렸더니 제목 `...` 이
  `___.md` 가 됐다 — 빈 제목으로 떨어지지 않아 `Untitled-<id>` 를 못 만든다.
  순서를 뒤집었다.
  ⑵ 255바이트를 **몸통에만** 적용했다가 시험에서 잡혔다. `.md` 까지 합쳐
  255다 — 확장자를 빼고 재면 리눅스에서 `ENAMETOOLONG` 이 난다.
  **마지막은 파일시스템에 물었다.** 12개 이름을 진짜 ext4 에 파일로 만들어
  전부 성공하는 것을 봤다 — 규칙이 맞는지는 우리가 아니라 파일시스템이
  답한다.
  **설계 문서의 전제 둘이 틀린 것도 함께 적었다**(§6·§11): BullMQ 큐가
  아예 없고(`export`·`ai-generate` 큐도 없다), API 가 EMM 직렬화기를 아직
  쓸 수 없다. 슬라이스 1 은 그 둘 없이 갈 수 있게 잡았다.

- 2026-08-20 (98차): **협업 중 '맵 닫기' 는 저장하지 않고 닫는다** — 그리고
  **내 우려가 틀렸다는 것을 재현으로 확인했다.**
  걱정한 것은 "닫는 쪽의 전체 문서 저장이 **상대 편집을 덮어쓴다**" 였다.
  **아니었다.** 로컬 PostgreSQL 16 + 실제 스키마 + 유료 `CollabRooms` ·
  `PgCollabStore` 를 그대로 돌려 확인했다:
  ```
  ① B 가 CRDT 에서 n1 을 고침(아직 DB 에 없음)
  ② 방이 모르는 변경이 REST 로 들어옴 (n1 은 낡은 값 · n3 추가 · 배치 변경)
  ③ 물질화 후 → n1="원래글자+B가고침" · n3 **사라짐** · 배치는 남음
  ```
  `store.pg.ts` 의 `save()` 가 **DB 문서를 읽어 그 위에 CRDT 노드를 얹기**
  때문에, **방이 아는 노드는 방이 이긴다.** 상대 편집은 안전하다.
  **대신 반대 방향의 손실이 있다** — **방이 모르는 노드는 물질화가 지운다.**
  그래서 협업 중 전체 문서 저장은 **이길 수 없다**: 잘 되면 무의미하고
  (방이 곧 덮어씀), 안 되면 **거짓 안심**이다("저장됨" 표시만 남는다).
  자동저장은 이미 같은 이유로 비켜서 있었는데(`isCollabDriving` 세 곳)
  **저장 경로만 그 규칙에서 빠져 있었다.** 닫기에 같은 검사를 넣었다.
  **끊긴 동안에는 건너뛰지 않는다** — 소켓이 닫히면 유료 클라이언트가 곧바로
  `setCollabDriving(null)` 을 부르므로(`client.ts:164`), 그때는 평소처럼
  저장한다. 여기까지 막으면 **그 편집이 어디에도 남지 않는다.**
  **남은 빈자리(고치지 않았다)**: 위 ②·③ 은 맵 닫기만의 일이 아니다.
  **끊긴 동안 자동저장이 쓴 것도** 방이 살아 있으면 다음 물질화에서 지워진다.
  제대로 고치려면 재접속 때 **끊긴 동안의 편집을 CRDT 에 합쳐 넣는** 길이
  있어야 한다 — 이번 범위 밖이고, 27-sync-model.md §14 에 함께 둘 자리다.
  → **2026-08-21 에 닫혔다.** 재접속할 때 끊긴 동안의 편집을 **방 상태 위에
  다시 얹는다**(구현과 검증은 유료 저장소 — 이 카탈로그에는 적지 않는다,
  `CLAUDE.md` 의 공개/비공개 경계). 코어 쪽에서 달라진 것은 없다:
  자동저장·맵 닫기의 `isCollabDriving` 규칙은 그대로다.
  **재현 절차**(스크립트는 커밋하지 않는다 — 카탈로그가 산출물이다):
  로컬 PG 16 기동 → `dev/00-supabase-shim.sql` · `schema.sql` ·
  `functions/move_node_subtree.sql` · `dev/01-seed-dev-user.sql` 적용 →
  유료 저장소에서 `CollabRooms` 를 직접 띄우고 `insertText` 로 B 의 편집을
  넣은 뒤, `map_documents.doc` 를 직접 UPDATE 해 REST 저장을 흉내내고
  `rooms.flushAll()` 결과를 본다.
  **이 장치를 초안 흘리기(#42) 때 썼어야 했다** — "둘이 같은 노드를 친다"는
  정확히 이 방식으로 잡히는 것이었고, 내보내기 전에 봤을 것이다.
  타입 검사 통과(`tsc -b`).

- 2026-08-20 (99차): **e2e175 — 노트 HTML 속 사진도 문서 밖으로**
  (B16 ② 슬라이스 6). 28번 §3.5 가 "의도된 예외"로 적어 두었던 **셋째 줄을
  없앴다** — 그 줄은 원래 "아직 못 옮긴 곳"이라고 스스로 밝히고 있었다.
  **사진이 노드에 붙는 방식이 둘이라는 것이 이 작업의 전부다.**
  `image`·`images` 는 값이 주소이고, 노트 HTML 은 **주소가 문자열 안**에
  있다. 그래서 "주소를 훑는 곳" 여섯 군데가 전부 앞의 것만 보고 있었다 —
  내보내기 되돌리기 · 화면 토큰 · 메타 주석 · 저장 때 옮기기 · **정리(GC)
  참조 세기** · 쓰는 쪽. 하나라도 남기면 **노트 사진에서만** 깨진다.
  **잡은 함정 셋**:
  ⑴ 정규식 `\bsrc\s*=` 가 **`data-src=` 에도 걸렸다** — `-` 와 `s` 사이가
  단어 경계다. 지연 로딩 자리표시자(1px gif)의 주소를 진짜 사진으로 알았다.
  단위 테스트를 먼저 써서 잡았다.
  ⑵ `<img[^>]*?src=` 는 **속성값 안의 `>` 에서 끊긴다**
  (`<img alt="a>b" src="x">`). 직렬화는 속성값의 `>` 를 이스케이프하지 않아
  기사 alt 텍스트에서 실제로 나온다. ⑴⑵ 둘 다 **"조용히 안 바뀐다"** 로
  끝난다 — 파일은 정상 생성되고 그 사진만 깨지므로 사람이 열어 봐야 안다.
  그래서 정규식을 버리고 **따옴표를 지키는 속성 스캐너**를 썼다.
  ⑶ **GC 참조 세기를 빠뜨렸다면 데이터가 없어졌다.** 노트 HTML 은 주소가
  여럿인데 기존 `take()` 는 첫 하나만 본다 — 그대로 넘겼으면 두 번째
  사진부터 "안 쓰는 것"으로 판정돼 **하루 뒤 화면에 보이는 사진이 지워진다.**
  되돌릴 수 없는 종류라 전용 항목(⑲)으로 박아 두었다.
  배운 것: **"어디를 훑는가"가 규칙보다 중요했다.** 규칙은 처음부터 하나였고
  (우리 저장소 사진은 주소만 남긴다), 틀린 것은 그 규칙을 **적용하지 않은
  자리들**이었다. 그래서 `@emm/note-images` 한 곳에 모으고 여섯 자리가
  그것을 부르게 했다.

- 2026-08-20: **e2e174 — 문서에 사진 바이트가 남지 않는다** (B16 ② D-5·D-6,
  #316 에 이어).
  #316 이 D-1~D-4(들어오는 문)를 끝냈고, 여기서 **남은 두 구멍**을 막았다.
  배운 것 넷.
  ⑴ **눈으로 보는 검사가 통과해도 안 끝난 것이었다.** 메타 주석 자체가
    base64 라, 그 안에 사진 data URL 이 통째로 들어 있어도 `.md` 에는
    `base64` 라는 글자가 보이지 않는다. **디코드해 보고 나서야** D-5 가
    실제로는 손도 안 댄 상태라는 것을 알았다.
  ⑵ **막는 쪽만 시험하면 받는 쪽이 죽은 것을 못 본다.** `from-url` 의 SSRF
    차단 9항목은 전부 통과하는데 **성공 경로는 하나도 안 됐다** —
    `pinnedLookup` 이 Node 의 `lookup` 규약(`{all:true}` → 배열)을 어겨
    `Invalid IP address: undefined` 로 죽고 있었다. 차단은 연결 **전에**
    끝나므로 그 시험에는 영원히 안 걸린다.
  ⑶ **뒷문이 D-6 하나가 아니었다.** `remoteImages.ts` 도 우리 저장소
    사진을 `http` 로 보고 되받아 문서에 도로 넣고 있었다 — 앞문·ZIP 문을
    다 잠가도 **내보냈다 다시 여는 것만으로 원상복귀**된다.
  ⑷ **"기존 판정 함수를 그냥 부른다" 가 답이 아닐 때가 있다.**
    `attachmentUrlForFile` 의 ≤2MB 내장은 **첨부**에 맞는 규칙이라
    스크린샷 대부분이 문서에 남았다. 판정을 두 벌로 만들지 않으려고
    **그 함수에 `preferServer` 옵션**을 넣었다 — 첨부 동작은 그대로임을
    회귀 시험 2항목으로 확인했다.
  못 한 것: **진짜 로그인(GoTrue) 스택이 이 환경에 없어** 가짜 세션을
  주입해 쟀다. 토큰 검증 자체는 이 변경과 무관하다.

- 2026-08-20 (98차): **e2e173 — 붙여넣은 사진이 문서 밖으로 나간다(쓰는 쪽)**
  (B16 ② 슬라이스 5 · 28-sync-prework-plan.md §3.4 D-1~D-4). 읽는 쪽 둘
  (내보내기 되돌리기·화면 토큰)은 2026-08-16 에 깔려 있었다. 이번이 **쓰는
  쪽**이다 — 붙여넣는 그 자리에서 서버로 보낸다.
  **판정을 새로 만들지 않은 것이 이 작업의 핵심이다.** 첨부는 이미
  `attachmentUrlForFile()` 에서 "문서에 넣을까 / 서버에 올릴까" 를 정하는데
  **사진만 그 문을 안 지나고 있었다.** 같은 문으로 보냈다. 그 결과
  **작은 스크린샷은 지금도 data URL 로 남는다**(그 문이 2MB 이하를 문서에
  넣기 때문) — 그건 버그가 아니라 그 문의 규칙이고, 사진용 기준을 따로
  만들면 두 벌이 된다. 문서에서 빠지는 것은 저장 때 서버가 한다(e2e159).
  **잡은 함정 셋**:
  ⑴ **`192.0.0.0/24` 를 두 옥텟만 보고 막았다** — `192.0.0.0/16` 전체가
  막혔고, 하필 이 컨테이너의 IP(192.0.2.2)가 거기 걸려 **검증을 할 수
  없어서** 발견했다. 검증 환경이 아니었으면 멀쩡한 공개 대역을 조용히
  막은 채로 나갔다.
  ⑵ **URL 파서가 `[::ffff:127.0.0.1]` 을 `::ffff:7f00:1` 로 정규화한다.**
  점 표기만 보고 IPv4-mapped 를 벗기면 그 꼴이 IPv4 검사를 **통째로**
  건너뛴다. 처음엔 통과한 줄 알았는데 connect 오류로 우연히 막힌
  것이었다 — **거절됐다는 사실만 보면 안 되고 거절 이유를 봐야 한다.**
  ⑶ **전체 시간 상한을 플래그로만 뒀더니 아무것도 안 끊겼다.** 1바이트씩
  느리게 흘리는 서버에 소켓이 잡힌 채로 남는다. 열린 요청을 들고 있다가
  `destroy` 해야 한다.
  **설계가 검증에서 갈렸다**: 리치 노트 HTML 속 `<img>` 를 서버 주소로
  바꾸려다, 내보내기의 사진 되돌리기(`serverImages.ts`)와 화면의 토큰
  붙이기(`imageSrc.ts`)가 **`image`·`images` 만 훑는다**는 것을 확인했다.
  넣었으면 **내보낸 파일이 서버 없이 안 열렸다.** 그래서 노트는 data URL 로
  두고 **받아오는 일만** 서버에 맡겼다(`from-url` 의 `store: false`).
  배운 것: **"서버를 껐다"고 적는 것과 정말 끄는 것은 다르다.** API 를
  죽이고 외부 요청을 전부 차단한 채 `file://` 로 열어 `naturalWidth > 0`
  까지 봤다 — 여기서 안 봤으면 노트 HTML 건은 못 잡았다.

- 2026-08-20 (97차): **`buildTreeFromFlat` — 평평한 목록을 중첩 트리로**
  (28-sync-prework-plan.md §2.3 A-5, Step B). 협업 CRDT 는 노드를
  `{id,parentId,order}` 로 평평하게 들고 있고 정본은 중첩 트리다. **고아는
  그 변환 지점에서 생긴다** — 물질화는 유료 모듈이지만 **트리 모양 규칙은
  공개 코어에 한 벌만** 두므로 이 함수가 그 경계다.
  **처음 계획은 `documentStore` 에 `repairOrphans` 를 두는 것이었다**(사용자
  정정으로 바뀜). 거기는 **중첩 children 모델이라 고아가 구조적으로 생길 수
  없다** — 부모 없는 노드를 표현할 방법 자체가 없다. 자리를 잘못 잡을 뻔했다.
  **잡은 함정 셋**:
  ⑴ `parentId: null` 은 '뿌리 직속' 인데 `findOrphans` 는 `null` 을 '부모
  없음 → 고아' 로 읽는다. 그대로 넘겼으면 **뿌리 직속 노드가 전부 고아**가
  됐다. `'root'` 로 바꿔 넘긴다(`buildParentIndex` 와 같은 규약).
  ⑵ `findOrphans` 는 **자손까지 함께** 돌려준다(자손도 뿌리에 못 닿으니까).
  그대로 올리면 **가지가 산산조각 난다.** 끊긴 사슬의 **맨 위 하나**만 올리고
  나머지는 딸려 오게 했다. 순환이면 **id 가 가장 작은 것**을 끊어 같은
  입력이 항상 같은 결과를 내게 했다.
  ⑶ **`findOrphans` 의 기본 상한 1000 에 걸렸다.** 그건 무한 반복 방지값이라,
  정상 문서라도 사슬이 더 길면 **멀쩡한 노드를 고아로 본다** — 2만 단계 사슬
  테스트가 잡았다. 상한을 **노드 수**로 준다(한 노드는 사슬에 한 번만 나온다).
  **재귀로 짜지 않았다** — 망가진 입력에 스택이 터진다. 반복문 + 방문 표시.
  **아무것도 버리지 않는다**를 테스트로 고정했다(모든 경우에 입력 수 == 출력 수).
  단위 테스트는 `test/build-tree.test.ts` 에 두고 `npm run test:unit` 로 돈다 —
  `conformance/` 는 EMM 파싱 코퍼스라 끼워 넣지 않았다.
  **21항목 전부 통과**, 기존 conformance 회귀 없음, `tsc --noEmit` 통과.

- 2026-08-20 (96차): **순환 판정을 한 벌로** (28-sync-prework-plan.md §2.3
  Step A). 같은 판정이 세 군데 있었다 — 화면(`isSelfOrDescendant`) ·
  DB(`ltree <@`) · `packages/emm-parser`(`wouldCreateCycle`, 호출부 0곳).
  **셋 중 실제로 도는 것과 앞으로 쓸 것이 달랐다.** 협업 모듈이 emm-parser
  쪽을 부를 예정이라, 그대로 두면 **화면과 협업이 서로 다른 판정**을 하게
  된다.
  화면을 `wouldCreateCycle` 로 바꾸고 `isSelfOrDescendant` 는 지웠다 —
  남겨 두면 다음 사람이 그걸 부른다. DB 의 ltree 판정은 **그대로 둔다**:
  두 벌이지만 **역할이 다르다.** 앱은 "미리 막아 알려 주는 것", DB 는
  "그래도 들어오면 거절하는 것" — 마지막 방벽은 있어야 한다. 주석에 판정
  원본이 어디인지 적었다.
  **계획서의 표기 하나를 고쳐서 넣었다.** §2.3 은 `wouldCreateCycle(idx.get,
  …)` 이라고 적혀 있는데, `Map.prototype.get` 을 **떼어 넘기면 `this` 를
  잃어 터진다.** 화살표로 감쌌다.
  **성능** — `findParentId` 를 사슬마다 부르면 이동 한 번이 O(n²) 다.
  1만 노드에서 실측: 인덱스 방식 **2.03ms** vs 직접 넘김 **21.4ms**(11배).
  인덱스는 반드시 **이동 전에** 만든다 — 뒤에 만들면 이미 옮긴 트리를 보고
  판정해서 순환을 못 막는다.
  **깊이 가드(MAX_DEPTH)는 건드리지 않았다** — 순환과 별개 규칙이고, 빠져도
  화면이 안 깨져 한참 뒤에야 발견된다. `MAX_DEPTH` 등장 횟수 9 → 9 로 확인.
  **판정 확인 9항목 전부 통과**: 자기 자손·자기 자신 밑으로 막힘 · 남의
  가지로는 통과 · 뿌리로 올리기 통과 · 최상위 브랜치의 부모는 `'root'` ·
  뿌리는 `null` · 모르는 노드는 `undefined`(그래야 오판하지 않는다) ·
  모르는 부모에서 끊기면 순환 아님 · 고아 판정.
  `tsc -b` 통과.

- 2026-08-20 (95차): **사이드바 접기가 세 번 만에 됐다 — 부수효과 때문이었다.**
  #306 에서 기본값을 `sidebarCollapsed: true` 로 바꿨는데도 로그인하면 매번
  펼쳐졌다. 원인은 `EditorPage` 의 로그인 효과가 부르는
  `setNavTab('newMap')` — 그 동작이 탭을 고르는 김에
  **`sidebarCollapsed: false` 까지 함께** 한다.
  사용자가 아이콘을 눌렀을 때는 그 부수효과가 맞다. 그런데 여기는
  **아무도 누르지 않았다** — 로그인했을 뿐이다. 정할 것은 **"열면 무엇이
  보이나"** 지 **"열어라"** 가 아니었다.
  `useEditorUiStore.setState({ navTab, activeSection })` 로 **부수효과 없이**
  탭만 정한다. 2026-08-04 의 원래 의도(첫 화면에 '스타일' 대신 '새 맵')는
  그대로 지켜진다.
  **배운 것**: 기본값을 바꿔도 안 바뀌면 **그 값을 나중에 덮는 곳**을 찾는다.
  "한 동작이 두 가지 일을 한다" 는 것이 그 자리를 숨긴다.
  타입 검사 통과(`tsc -b`).

- 2026-08-20 (94차): **재배포해도 옛 화면이 뜨던 진짜 이유 — nginx 캐시.**
  고쳐서 배포했는데 사용자 화면은 그대로였고, 원인을 **코드에서** 찾느라
  몇 번을 헛돌았다. `index.html` 에 `no-store` 를 붙이는 블록이
  `location = /index.html` 에만 있었는데, 주소창에 `/` 로 들어오면
  **`location /` 의 `try_files` 가** index.html 을 내보내므로 그 블록을
  타지 않는다. 브라우저가 낡은 index.html 을 들고 있으면 그것이 **옛 번들
  파일 이름**을 가리키고, 그래서 배포가 아무 효과가 없어 보인다.
  `location /` 자체에 `no-store` 를 걸고, 해시 붙은 `/assets/` 는 반대로
  `immutable` 로 오래 캐시하게 나눴다(이름이 바뀌면 새로 받으므로 낡을 수가
  없다).
  **"고쳤는데 안 된다" 가 반복되면 코드가 아니라 전달 경로를 의심한다** —
  이번에 값을 치르고 배운 것이다.
  함께 낸 것 — 문서함 유형 칸을 감싸는 **유료 자리 `ProMapMembersTip`**
  (사용자 요청: 협업맵에 마우스를 올리면 참여자 목록). 공개판 스텁은
  `children` 을 그대로 돌려준다 — **자리와 규칙은 공개, 알맹이는 비공개**
  라는 경계 규칙 그대로다(open-core-boundary.md §5).
  타입 검사 통과(`tsc -b`).

- 2026-08-20 (93차): **문서함의 작은 거짓말 둘을 걷었다**(사용자 지적).
  ⑴ **열린 맵이 없는데 '문서함 닫기' 가 있었다.** 닫으면 '문서 없음' 빈
  화면만 남는다 — **갈 곳이 없는 문**이다. 사용자는 자기가 뭘 잘못 눌렀다고
  생각한다. 돌아갈 맵이 있을 때만 보여 준다(Esc 는 그대로 동작한다).
  ⑵ **읽기 전용 안내가 열람자에게 '다른 이름으로 저장하세요' 를 권했다.**
  읽기만 권한은 #307 로 **사본 저장도 막혀 있다** — 안 되는 것을 하라고
  안내하면 두 번 실망한다. 그 길이 실제로 열려 있는 경우(다른 세션이 편집
  중이라 읽기 전용)에만 문장을 붙인다.
  `openMapHere` 가 `viewer` 를 함께 돌려주게 해서, 부르는 쪽이 두 갈래를
  구분할 수 있게 했다. 타입 검사 통과(`tsc -b`).

- 2026-08-20 (92차): **열람자에게도 협업 세션 자리를 준다** — 받기만 한다.
  읽기 전용으로 연 맵은 링크(`cloudMapId`)가 없다(저장 경로를 끊는 장치다).
  그런데 `ProCollabSession` 이 그 링크를 보고 붙으므로, **열람자는 협업에
  아예 못 붙었다.** 결과는 **초대받은 시점의 문서**를 보는 것이다 — 주인이
  고쳐도 모르고, 새로고침해야 안다. 그건 **공유가 반쯤 된 것**이고
  사용자에게는 그냥 고장으로 보인다(2026-08-20 사용자 보고).
  `readOnlyInfo` 에 `kind` 를 함께 담고, 세션 자리에는
  `cloudMapId ?? (viewer ? readOnlyInfo.mapId : null)` 을 넘긴다.
  **저장 경로는 그대로 끊긴 채**로 협업만 열린다 — 두 가지를 같은 값
  하나(`cloudMapId`)로 판정하고 있던 것이 문제였다. 뜻이 다른 것은 값도
  달라야 한다.
  보내지 못하게 막는 것은 **화면(#307 잠금)과 서버(유료 게이트웨이)** 두
  곳이다. 화면만 막으면 브라우저 콘솔로 우회된다.
  타입 검사 통과(`tsc -b`).

- 2026-08-20 (91차): **읽기 전용을 '표시'에서 '잠금'으로 바꿨다**(사용자 보고).
  #305 로 배너는 떴는데 **편집도 되고 되돌리기도 되고, 저장하면 자기
  문서함에 같은 이름으로 사본이 생겼다.** 사용자가 정확히 짚었다 —
  **주인은 '읽기만' 을 준 것이지 문서를 가져가도 좋다고 한 적이 없다.**
  세 곳을 막았다. ⑴`documentStore` 의 `set` 한 자리에서 **문서(`map`)를
  건드리는 변경을 거절**한다 — 41개 액션마다 검사를 흩뿌리면 하나는 반드시
  빠뜨린다. 서버가 정한 변경(불러오기·원격 반영·닫기)만 `asServerDriven`
  으로 지난다. ⑵툴바에서 **저장·다른 이름으로 저장 버튼을 없앤다** —
  누를 수 있게 두고 거절하는 것보다 없는 편이 정직하다. ⑶`saveCurrentMap`
  이 `isViewerLocked()` 를 보고 거절한다 — 버튼이 없어도 단축키·자동저장
  경로가 남으므로, **문이 있는 모든 곳**을 막아야 한다.
  `readOnlyInfo.viewer` 로 갈래를 나눈 이유도 분명하다 — **다른 세션 잠금**
  은 내 권한이 있는 상태라 사본 저장을 열어 두는 것이 맞고, **열람자**는
  그 자체가 안 된다. 같은 '읽기 전용' 이라도 할 수 있는 일이 다르다.
  타입 검사 통과(`tsc -b`).

- 2026-08-19 (90차): **왼쪽 사이드바를 접힌 채로 시작한다**(사용자 요청).
  로그인·새로고침 직후에 '새 맵' 패널이 펼쳐져 있어서, 정작 보려던
  문서함을 가렸다. `sidebarCollapsed` 기본값을 `true` 로 바꿨다 — 아이콘
  막대는 그대로 남으므로 한 번 눌러 열 수 있고, `setNavTab` 이 열어 주기도
  한다(동작을 없앤 것이 아니라 **처음 상태만** 바꿨다).
  함께 고친 것 — **문서함 안내가 거짓말을 하고 있었다.** 읽기 전용으로
  열릴 때마다 "다른 세션(브라우저)에서 편집 중" 이라고 단정했는데,
  #305 로 **읽기만 권한**이라는 두 번째 이유가 생겼다. 사용자가 할 일이
  다르다 — 앞은 기다리면 되고, 뒤는 주인에게 권한을 올려 달라고 해야
  한다. `openMapHere` 가 `reason` 을 함께 돌려주고 문서함이 그것을 그대로
  보여 준다.
  타입 검사 통과(`tsc -b`).

- 2026-08-19 (89차): **열람자에게 편집을 허용하는 것처럼 보였다** — 실사용에서
  걸렸다. '읽기만' 으로 초대받은 계정이 맵을 고치고 **되돌리기까지** 했고,
  **저장할 때에야** 403 을 만났다. 그때는 이미 한참 작업한 뒤라 **그 편집이
  갈 곳이 없다.** 맵 닫기도 안 됐다(저장이 실패하니 닫기가 멈춘다).
  서버는 처음부터 알고 있었다 — `requireAccessibleMap` 이 `access_role` 을
  판정한다. **알면서 말하지 않은 것**이 문제였다. `GET /maps/:id/document`
  응답에 `role` 을 실었다.
  화면 쪽은 **새로 만들 것이 없었다.** 단일 세션 잠금(2026-08-04)이 이미
  "읽기 전용으로 여는 길"을 갖고 있다 — 링크를 만들지 않아 저장 경로가
  막히고, 배너가 뜨고, 닫기는 저장 없이 그냥 닫는다. `role='viewer'` 를
  그 길에 얹었다.
  다만 **이유는 갈라 적었다.** `editLock='busy'`(내 권한은 있는데 다른
  세션이 편집 중)와 `role='viewer'`(권한 자체가 없다)는 사용자가 할 일이
  다르다 — 앞은 기다리면 되고 뒤는 주인에게 권한을 올려 달라고 해야 한다.
  `readOnlyInfo.reason` 을 두어 배너가 그 문장을 그대로 보여 준다.
  타입 검사 통과(프런트·API 각각 `tsc -b`).

- 2026-08-19 (88차): **인증 프록시의 CORS 허용 주소가 고정값이라 새 프런트가
  로그인 못 했다**(`dev-server-coolify.md` §5.5 ④ 에 경고 추가).
  유료판 프런트를 `pro-dev` 로 올리자 preflight 가 막혔다 —
  `Access-Control-Allow-Origin` 이 `https://dev…` 로 **박혀 있어서** 요청한
  주소(`pro-dev`)와 달랐다. NPM 의 Advanced 설정에 손으로 적어 둔 값이다.
  **허용 목록을 두고 요청한 주소를 그대로 되돌려 주는 방식**으로 바꿨다.
  고정값은 프런트 도메인이 하나 늘어날 때마다 같은 사고를 낸다 —
  §8(임시 도메인) 처럼 **일시적으로 도메인이 늘어나는 절차가 있는 이상**
  구조적으로 다시 걸린다.
  **한 번에 하나씩 바꾼 것이 이번을 갈랐다.** preflight 만 고치고 실제
  응답용 헤더는 일부러 미뤘는데, 그것만으로 로그인이 됐다 → GoTrue 가
  스스로 붙여 준다는 것이 확인됐다. 둘 다 넣었으면 헤더가 **두 개**가 되어
  또 막혔을 것이고, 그때는 원인이 preflight 인지 중복인지 갈리지 않았다.
  문서에도 "실제 응답에는 넣지 말 것"을 근거와 함께 남겼다.

- 2026-08-19 (87차): **dev 서버 로그인이 안 되고 있었다 — `VITE_SUPABASE_AUTH_PREFIX`
  누락**(`dev-server-coolify.md` §5.5 에 경고 추가). 유료판 프런트를 올리려고
  `easymindmap-frontend` 의 환경변수를 복사하다가 이 변수가 **아예 없다**는
  것을 발견했다. 코드는 없으면 기본값 `/auth/v1` 을 쓰는데
  (`supabaseAuth.ts` — `!== undefined` 로 판정하므로 "없음"과 "빈 값"이
  다르다), dev 의 GoTrue 는 **단독 배포라 루트로 서비스한다.**
  실측으로 확인했다 — `/health` 는 `{"version":"v2.194.0","name":"GoTrue",…}`,
  `/auth/v1/health` 는 **`404 page not found`**.
  **내가 만든 문제가 아니라 원래 있던 문제다.** §5.5 표에는 처음부터 올바른
  값이 적혀 있었고, 배포에 반영되지 않았을 뿐이다. 시험으로 잡히지 않은
  이유도 분명하다 — e2e 는 GoTrue 없이 shim 으로 돌아서 **프런트가 어느
  주소를 부르는지는 아예 검증 범위 밖**이다(e2e138 이 같은 이유로 반쪽이었던
  것과 같은 자리다).
  문서에 세 가지를 못박았다. ⑴증상이 **"화면은 뜨는데 로그인만 안 된다"**
  이고 기동 로그에 아무것도 안 남는다는 것 ⑵권장 값을 빈 값이 아니라
  **`/`** 로 바꿨다 — 결과는 같은데(`'/'.replace(/\/$/,'')===''`) UI 가
  빈 값을 저장하지 않는 경우를 피하고, **"넣었는데 비었다"와 "안 넣었다"를
  눈으로 구별**할 수 있다 ⑶`VITE_*` 는 빌드 시점에 박히므로 **고친 뒤
  반드시 재배포**해야 한다는 것.

- 2026-08-18 (86차): **e2e172 — 유료판 배포 준비**(파서 패키징 · 종료 훅).
  유료 모듈을 서버에 올리려고 **배포와 같은 방식**(npm 타르볼 설치)으로
  심어 보다가 둘을 찾았다.
  ⑴ **파서를 밖에서 쓸 수 없었다.** `main` 이 `src/index.ts` 라 Node 가
  읽지 못한다 — EMM 의 **레퍼런스 구현**이라면서 정작 제3자가 설치해 쓸 수
  없는 모양이었다. CJS·ESM·타입을 함께 내도록 고쳤다. 프런트엔드는 그대로
  소스를 본다(`@emm` 별칭) — 고치는 즉시 반영돼야 하기 때문이다.
  ⑵ **재배포 때마다 마지막 편집이 조용히 사라질 뻔했다.** `main.ts` 에
  `enableShutdownHooks()` 가 없어 종료 훅이 한 번도 불리지 않았고, 훅을
  켠 뒤에도 **열린 WebSocket 이 HTTP 서버 close 를 붙잡아** 저장이 20초가
  지나도 시작되지 않았다. 저장을 `beforeApplicationShutdown` 단계로
  올리자 **0.6초**에 저장되고 프로세스는 2.1초에 끝난다(유료 저장소 #15).
  배운 것: `--no-save` 설치는 다음 `npm install` 이 지운다 — 이미지를
  만들 때 **유료 모듈과 파서를 한 번에** 설치해야 한다.
- 2026-08-18 (85차): **e2e171 — 초대 화면과 "공유받은 맵" 목록**(협업 6단계).
  **SQL 없이 협업을 시작할 수 있게 된 날**이다. 그전까지 참가자는 DB 에
  직접 넣어야 했다.
  나눈 자리: **읽기는 코어, 쓰기는 유료.** 코어는 `map_members` 를 읽어
  권한을 판정하고 "공유받은 맵" 목록을 보여 주고, **행을 넣고 빼는 초대
  API 는 유료 모듈**이 가져온다.
  설계에서 갈린 지점 둘. ⑴**공유 목록을 `GET /maps` 에 합치지 않는다** —
  공유받은 맵의 폴더는 **소유자의 폴더**라, 내 트리에 섞으면 있지도 않은
  폴더 안에 파일이 있는 것처럼 보인다. ⑵**초대하면 맵을 협업맵으로
  올린다** — 안 그러면 초대는 됐는데 단일 세션 잠금 때문에 **둘이 열면
  한쪽이 읽기 전용**이 된다.
  **검증이 코드를 고쳤다.** 공유 목록이 늘 비어 있었는데, 원인은
  `u.email`(이메일은 `auth.users` 에 있다)이었다. 그런데 그 오류를
  **catch 가 통째로 삼켜** "공유 없음"으로 보여 주고 있었다 — 컬럼 이름
  하나 틀린 것을 아무도 못 찾는 모양이다. `to_regclass` 로 **표만
  확인**하고 나머지는 드러나게 바꿨다(코어 map-access 와 같은 규칙).
  함께 살린 것: 툴바의 **공유 버튼**. 예전에는 `onClick` 이 없어 **눌러도
  아무 일도 일어나지 않았다** — 가짜 빨간 점을 지웠던 것과 같은 이유다.
- 2026-08-18 (84차): **e2e169 — 협업 프런트엔드 자리**(협업 5단계, 공개 쪽).
  협업 프런트엔드의 첫 문제는 커서도 아바타도 아니었다 — **누가 저장을
  책임지는가**였다. 코어의 자동저장은 문서 **전체 스냅샷**을 올리고 협업은
  글자 단위로 합친다. 둘을 함께 켜면 합쳐 놓은 결과 위에 내 화면을 통째로
  얹어 **같은 순간 남이 고친 것을 지운다.**
  그래서 스위치를 하나 뒀다(`setCollabDriving`). 판정 기준은 **"협업맵이면"
  이 아니라 "협업이 실제로 붙어 있으면"** 이다 — 유료 모듈이 없거나 소켓이
  끊긴 서버에서도 협업맵은 열리는데, 그때까지 저장을 멈추면 **편집이
  어디에도 남지 않는다.** 공개판 검증 13항목이 그 자리를 지킨다.
  **검증이 코드를 고쳤다.** 배지 값을 `saveState` 한 칸에 얹었더니 **맵
  여는 경로가 뒤늦게 `'saved'` 로 덮어써서**, 통째 저장을 안 하고 있는데
  "저장됨"이라고 말하는 화면이 됐다. 몰고 있는 맵 id 를 따로 두고 배지가
  그것을 우선으로 파생하게 고쳤다.
  **검증도 고쳤다.** 원격 변경을 한 번만 넣어서는 "남의 편집이 되돌리기에
  안 쌓인다"를 증명하지 못한다(한 칸 되돌려도 결과가 같아 보인다). 세 번
  넣고 **Ctrl+Z 한 번**으로 내 편집이 돌아오는지 보게 바꾼 뒤에야 가드를
  뺐을 때 실제로 깨졌다.
  함께 막은 것: `packages/pro-ui/` 를 **.gitignore 에 넣었다.** 유료 UI 를
  이 작업 사본에서 빌드하면 `git add -A` 한 번으로 **유료 화면이 통째로
  공개**된다.
- 2026-08-18 (83차): **e2e168 — 참가자 표 델타 스크립트**(런북 §1.5-0-G).
  **4항목**. ①`bash -n` 문법 통과 ②**가짜 `docker` 를 PATH 에 심어** 이
  서버 상황(rbooster + easymindmap)을 재현 → `rb-api-…` 를 건너뛰고
  **우리 표가 있는 DB** 를 고른다 ③진짜 PostgreSQL 에 **두 번 실행해도**
  같은 결과(멱등) ④검증 출력이 표 이름과 참가자 수를 준다.
  e2e166 에서 정한 방식(이름·포트가 아니라 **우리 표로 확인**)을 새 델타에도
  그대로 썼다 — 같은 실수를 세 번째로 하지 않기 위해서다.
- 2026-08-18 (82차): **e2e167 — 공유받은 사람이 맵을 연다** (협업 4단계).
  협업의 첫 장애물은 CRDT 도 소켓도 아니었다. **공유받은 사람이 맵을 아예
  열 수 없다**는 것이었다 — 코어의 모든 맵 조회가 `owner_id = $2` 였다.
  `map_members` 를 **코어에** 두기로 했다. 협업 자체는 유료지만, "이 사람이
  이 맵을 열어도 되는가"는 **데이터를 가진 쪽**이 답해야 한다. 권한 판정이
  플러그인에 흩어지면 **플러그인이 틀렸을 때 남의 문서가 열린다.**
  표가 비어 있으면 코어는 예전과 똑같이 소유자 전용으로 돈다(행을 넣는
  초대 흐름이 유료다).
  **첨부를 함께 열었다.** 이게 없으면 공유받은 맵이 **이미지 자리마다 깨진
  채로** 열린다 — 맵은 보이는데 그림이 안 보이는 것은 공유가 반쯤 된
  것이고, 사용자에게는 그냥 고장으로 보인다. 지우기는 소유자 전용 그대로다.
  **검증이 코드를 고쳤다.** 델타 미적용 서버를 흉내 내려고 표를 지웠더니
  **맵 열기가 통째로 503** 이었다. 코어의 `DatabaseService` 가 `42P01` 을
  **503 예외로 바꿔서** 올려 주기 때문에, 오류 코드를 보고 물러나려던 첫
  구현은 아무것도 잡지 못했다. `to_regclass` 로 **존재 여부를 직접 묻도록**
  바꿨다. "없다"는 답은 60초 뒤 다시 묻는다 — 실측에서 델타를 적용하자
  **재기동 없이 50초 만에** 공유가 살아났다.
  하지 않은 것을 분명히 적어 둔다: **"나에게 공유된 맵" 목록이 없고**
  (`GET /maps` 는 여전히 소유한 맵만 준다 — 맵 id 를 알아야 들어간다),
  **초대 API 도 없다**(참가자 행은 아직 SQL 로만 들어간다).
- 2026-08-18 (81차): **e2e166 — 런북이 남의 앱 DB 를 고르지 않는다**.
  같은 실수가 **두 번째**다. 8월 14일에는 "이미지 이름으로 postgres 를
  찾다가 다른 앱 DB 가 잡혔다"였고(e2e151), 이번엔 **"API 컨테이너를
  이름·포트로 고르다가 rbooster 가 잡혔다"** 였다. 사용자가 델타 SQL 을
  **남의 DB 에 실행**했고, 표가 없어 끊겨서 아무것도 안 바뀌었을 뿐이다.
  **고친 방향이 지난번과 다르다.** 지난번엔 "`DATABASE_URL` 을 읽어라"로
  고쳤는데, 그것만으로는 **어느 컨테이너의** `DATABASE_URL` 인지를 여전히
  추측한다. 이번엔 **우리 표(`map_documents`)가 있는지 직접 확인**하도록
  바꿨다 — 추측이 한 겹도 남지 않는다. 런북의 8곳을 모두 바꿨다.
  **가짜 `docker` 를 PATH 에 심어** rbooster + easymindmap 이 같은 호스트에
  있는 상황을 재현해 확인했다. 서버 스크립트도 검증할 수 있다.

- 2026-08-18 (80차): **e2e165 — 구독 요금을 관리자 화면에서** (사용자 요청).
  용량처럼 요금도 콘솔에서 정한다. 결제가 **청구액을 이 값에서 읽는다** —
  화면이 보낸 금액을 쓰는 길은 아예 만들지 않았다(그러면 1,000원 결제로
  상위 요금제를 받아갈 수 있다).
  **1~999 를 막는다.** PG 최소 결제 금액이 1,000원이라, 통과시키면
  **저장은 되는데 결제창에서 거절**되는 값이 생긴다. `0` 은 "결제하지 않는
  요금제"라는 뜻으로 따로 허용한다.
  **검증에서 잡은 것 둘.** ⑴처음엔 요금을 용량과 **같은 쿼리**로 읽었는데,
  컬럼이 없는 서버(델타 미적용)에서 쿼리 전체가 실패해 **용량 편집까지
  사라졌다.** 새로 더한 것 때문에 이미 되던 것이 사라지면 안 된다 — 쿼리를
  나눴다. ⑵저장 실패 메시지가 "`/v1/health` 의 missingColumns 를 보라"고
  안내하는데 **그 목록에 이 컬럼이 없었다.** 아무것도 알려주지 않는 곳을
  가리키는 안내라 `REQUIRED_COLUMNS` 에 넣었다.
  요금 변경은 **회원 행을 건드리지 않는다** — 이미 결제한 원장은 그때의
  청구액을 들고 있어 과거 결제가 흔들리지 않는다.

- 2026-08-18 (79차): **e2e164 — 결제 통지 경로를 레이트 리밋에서 뺀다** (PG 준비).
  사이다페이 연동 설계에서 **코어를 먼저 고쳐야 할 것 2개**를 짚었는데,
  **되돌려 깨뜨리기가 그중 하나를 뒤집었다.**
  ⑴ **레이트 리밋 예외는 진짜였다** — 빼면 통지 경로가 429 로 막힌다.
  PG 는 429 를 실패로 보고 재전송을 소진한다 → 돈은 빠져나갔는데 원장은
  미결제. 헬스체크와 같은 이유로 비켜 준다.
  ⑵ **폼 파서 추가는 필요 없었다.** `main.ts` 에 JSON 파서만 보여서
  "폼 인코딩 본문이 빈 객체로 들어온다"고 적었는데, **파서를 빼고 다시
  재 보니 그대로 들어왔다** — Nest 가 기본으로 등록한다.
  `useBodyParser('json', …)` 은 JSON 설정만 바꾼다. **코드를 읽고 내린
  결론이 틀렸고, 되돌려 깨뜨리는 검증이 그것을 잡았다.** 근거가 틀린
  변경은 넣지 않는 쪽이 맞아서 `main.ts` 는 원복했다.
  기본 한도 100kb 는 통지 본문(수백 바이트~2KB)에 충분하다.

- 2026-08-18 (78차): **e2e163 — 트리 모양 규칙을 한 벌로** (협업 준비).
  `27-sync-model.md` §4 가 "규칙을 두 벌로 만들지 않는다"고 적어 둔 것을
  실제로 지킬 수 있게 했다. 순환 판정과 고아 찾기를 **순수 함수로
  `packages/emm-parser`(공개)** 에 두었다 — 유료 협업 모듈도 이것을
  가져다 쓴다. 트리 모양은 **포맷의 일부**라 공개가 맞다(경계 §3.1 ②).
  갈린 지점 둘. ⑴**판정할 수 없으면 막는다** — 사슬을 훑는 상한을 두고,
  넘으면 `true`(이동 거절). 이미 순환이 들어간 문서를 만나도 **무한
  반복에 빠지지 않는다.** ⑵**고아는 지우지 않고 뿌리로 올린다.**
  순환에 갇혀 뿌리에 못 닿는 노드도 고아로 센다 — **화면에서 사라지는
  것은 부모가 없는 것과 똑같기** 때문이다.
  적합성 12/12 무회귀 확인.

- 2026-08-18 (77차): **e2e162 — Yjs 실측** (협업 착수 전).
  설계 문서에 **"구현 전에 재 봐야 한다"** 고 적어 둔 것을 건너뛰지 않고
  쟀다. 결과는 설계를 **바꾸지 않아도 된다**는 쪽이었다 — 인코딩이 우리
  JSON 의 1.28배, 한 글자 전파 24바이트, 물질화 15ms.
  **§3 에서 겁냈던 묘비 문제는 구조 편집에서는 없었다** — 1만 번 편집 뒤
  1.07배, **노드를 절반 지우면 오히려 0.56배로 줄었다.** 무서운 것은
  구조가 아니라 **바이트를 CRDT 에 넣었을 때**다(그래서 사진을 뺀 것이
  맞았다).
  **대신 새 제약이 드러났다.** 서버 메모리가 노드당 약 2.1KB — 사람 수가
  아니라 **동시에 열려 있는 큰 맵 수**에 비례한다. 1만 노드 맵 100개면
  약 2GB 다. 설계의 "아무도 없으면 내린다"가 **선택에서 필수로** 바뀌었고,
  인스턴스당 상한과 **거절 이유를 말하는 것**을 구현 항목에 넣었다.
  **재지 않은 것을 재지 않았다고 적었다** — awareness 오버헤드, 브라우저
  메모리, 접속자 20명 상한의 근거. 마지막 것은 이번 측정이 답하지 않는다:
  대역폭이 아니라 **화면에 아바타 20개가 의미 있는가**가 진짜 질문이다.

- 2026-08-18 (76차): **e2e161 — 루트 노드 사진이 MD 로 나간다** (B17).
  e2e159 검증 중 발견한 유실 버그다. 루트에 붙인 사진이 MD 내보내기에서
  **사라졌다**(HTML 은 정상이었다).
  **직렬화만 고치면 안 됐다.** 파서는 첫 견출 앞의 문단을 루트 노트로
  담으므로, 내보내기만 고치면 돌아올 때 **사진 문법이 그대로 적힌 루트
  노트**가 된다 — 유실이 오염으로 바뀔 뿐이다. 파서에 "첫 견출 전의
  사진만 있는 문단 → 루트 사진" 갈래를 함께 넣었다.
  **기존 적합성 12/12 무회귀**를 먼저 확인하고 손댔다 — 파서는 왕복
  규칙이 얽혀 있어 한 갈래를 넣으면 다른 갈래가 조용히 바뀐다.
  배운 것: `files/` 상대 경로는 **본문이 아니라 메타데이터·ZIP 이
  복원한다**(가지 노드도 같은 규칙). 이걸 모르고 본문 왕복을 기대해서
  **멀쩡한 동작을 두 번 실패로 읽었다.**

- 2026-08-18 (75차): **e2e160 — 안 쓰는 사진만 지운다** (B16 ② 슬라이스 4 · 마지막).
  **사용자 파일을 지우는 코드**라 안전장치를 셋 두고 각각을 검증했다.
  ⑴**우리가 옮긴 사진만**(`img-<해시>.<확장자>` 이름) — 잘못 돌아도 사람이
  올린 파일은 안전하다. ⑵참조는 **현재 문서 + 모든 히스토리 버전**에서
  모으고, 사진만이 아니라 **사람이 붙인 첨부까지** 센다 — 하나라도
  빠뜨리면 쓰는 파일을 지운다. ⑶**하루가 지난 것만** — 사진을 지우고
  자동저장이 돈 직후 Ctrl+Z 를 누르면 다시 필요해진다.
  **설계에서 놓쳤다가 검증에서 잡은 것**: 처음엔 바뀐 저장에서만 정리했다.
  그런데 사진을 지운 저장은 **한 번뿐이고 그때는 유예기간 안**이라 못
  지운다 — 그 뒤로는 문서가 그대로라 **정리할 기회가 영영 오지 않는다.**
  무변경 저장 경로에도 정리를 넣었다.
  **내 오해 둘.** ⑴`keepVersion:true` 픽스처 때문에 히스토리 버전이 사진을
  붙들고 있었는데 GC 버그로 의심했다 — **코드가 옳았다.** ⑵옛 첨부 id 를
  물고 있는 픽스처를 재사용해 GC 가 "안 쓰는 사진"으로 옳게 판정한 것을
  실패로 읽었다. **검증 문서는 매번 현재 DB 에서 떠야 한다.**
  알려진 한계로 남긴 것: 사용자가 올린 파일 이름이 우연히 우리 꼴이고
  어디서도 참조되지 않으면 지워진다.

- 2026-08-16 (74차): **e2e159 — 저장하면 사진이 저장소로 옮겨간다**
  (B16 ② 슬라이스 3). **65,941 바이트 → 741 바이트.** 여기서 문서가
  실제로 가벼워졌다.
  설계에서 갈린 지점 넷. ⑴**주소는 상대 경로** — 서버는 브라우저가 어느
  주소로 접속했는지 모르고, **도메인이 바뀌면 절대 URL 이 전부 깨진다.**
  사람이 붙인 첨부는 절대 URL 이라 프런트가 둘 다 알아보게 넓혔다.
  ⑵**내용 해시로 같은 사진을 알아본다** — 이게 없으면 조용히 망가진다:
  화면은 저장 뒤에도 **메모리에 data URL 을 들고 있어** 자동저장마다 같은
  것을 또 보내고, 그러면 **저장할 때마다 사진이 하나씩 늘어** 사용자
  용량을 갉아먹는다. ⑶**쿼터를 재기 전에 옮긴다** — 순서를 바꾸면 같은
  바이트를 문서와 첨부에서 두 번 센다. ⑷**옮기다 실패하면 그 사진만
  원래대로 둔다** — 저장을 실패시키면 사용자가 사진 한 장 때문에 편집분
  전체를 잃는다. 다만 **쿼터 초과(413)는 그대로 올린다.**
  되돌려 깨뜨리기를 두 방향으로 했다 — 이관을 끄면 옛 동작 그대로,
  다시 켜면 **옛 문서를 읽어 주고 다음 저장에서 옮긴다**(일괄
  마이그레이션 없이). 이게 역호환의 전부다.
  배운 것: `POST /maps` 응답 키는 `id` 가 아니라 **`mapId`** 다.

- 2026-08-16 (73차): **e2e158 — 사진 이관의 읽는 쪽** (B16 ② 슬라이스 1·2).
  아직 사진을 옮기지 않았지만 **받는 쪽을 먼저 깔았다.** 순서를 바꾸면
  옮기기 시작한 그 순간부터 내보내기가 깨진 파일을 만들어 낸다.
  **아직 없는 것을 검증하는 방법** — 진짜 1×1 PNG 를 첨부로 올리고, 그
  URL 을 `image.src` 에 넣은 문서를 손으로 만들어 돌렸다. "쓰는 쪽이
  없으니 검증도 못 한다"가 아니다.
  설계에서 갈린 지점 셋. ⑴**우리 것만 손댄다** — 남의 사이트 사진은 원래
  부터 인터넷이 있어야 보이는 것이라 그대로 둔다. ⑵**못 받은 사진을
  지우지 않는다** — 원래 src 를 남기고 몇 장인지 `external` 로 알린다.
  조용히 넘기면 사용자가 반쪽 파일을 온전한 줄 안다. ⑶**토큰을 문서에
  저장하지 않는다** — 만료되므로 저장하면 어느 날 사진이 전부 깨진다.
  그릴 때마다 붙인다.
  **검증에서 잡은 것 둘.** HTML 내보내기에 **첨부가 없으면 일찍 반환하는
  길**이 있어서, 사진 되돌리기를 그 위에 두지 않으면 **사진만 있는 맵이
  서버 URL 로 나갔다**. 그리고 직렬화가 담을 수 있는 형식이 넷뿐이라,
  그 밖의 형식을 data URL 로 바꾸면 **사진이 통째로 빠진다** — 담을 수
  없으면 손대지 않는 쪽이 맞다.
  **별건 발견**: MD 내보내기가 **루트 노드 사진을 빠뜨린다**(B17). 검증
  중 기댓값이 3인데 2가 나와 알았다 — 내 변경 때문이 아니라 원래 그렇다.

- 2026-08-16 (72차): **사진 이관 설계 확정** (B16 ②) — e2e 없음(설계).
  조사에서 **계획을 바꾼 것 하나, 막는 것 둘**이 나왔다.
  **바꾼 것** — 참조 형식을 새로 만들려다 그만뒀다. 첨부가 이미 같은
  문제를 풀어 뒀다(`{API}/v1/attachments/<id>` + `attachmentFetchUrl()`
  이 쓸 때 토큰을 붙인다). 사진도 그 규약을 그대로 쓰면 **EMM 포맷이
  안 바뀐다** — 새 필드는 파서·직렬화·불러오기를 다 건드리고, 포맷
  변경은 **공개 표준의 문제**가 된다.
  **막는 것 ①** 직렬화가 `http(s)` 사진 src 를 URL 그대로 내보낸다.
  사진이 우리 서버 URL 이 되면 내보낸 ZIP·HTML 이 **서버가 살아 있어야
  열리는 파일**이 된다 — `content-permanence.md` 가 §1 에서 약속한
  "파일 하나로 온전히"가 깨진다.
  **막는 것 ②** 지운 사진이 저장소에 남아 **용량이 안 줄어든다.** 정리
  하려면 **현재 문서 + 그 맵의 모든 히스토리 버전**에서 참조를 모아야
  한다 — 현재 문서만 보고 지우면 **과거 버전이 깨진다.**
  그래서 슬라이스를 4단계로 나누고 **읽는 쪽을 쓰는 쪽보다 먼저** 두었다.
  순서를 바꾸면 그 순간부터 내보내기가 깨진 파일을 만든다.

- 2026-08-16 (71.5차): **작업 사본이 또 되돌아갔다** — 이번엔 **31커밋**.
  `claude/doc-images` 브랜치를 만들 때 `git fetch` 없이
  `git checkout -B <브랜치> origin/main` 을 써서, **낡은 remote-tracking
  ref** 를 기준으로 잡았다. 그 위에 커밋하고 **푸시까지 했다** — 병합
  했으면 #257~#287 이 통째로 되돌아갈 뻔했다.
  CLAUDE.md §2.3 이 `git fetch origin` 을 **먼저** 하라고 적어 둔 이유가
  이것이다. 이번엔 `origin/main` 이라고 썼으니 최신일 것이라 여겼는데,
  **remote-tracking ref 는 fetch 하기 전까지 낡은 채로 있다.** 브랜치를
  만들기 전이 아니라 **커밋하기 전에도** 한 번 더 봐야 한다는 것을
  이번에 배웠다(푸시 뒤에 알아챘다). 고친 방법: 변경분을 패치로 떠 두고
  최신 `origin/main` 에서 브랜치를 다시 잡아 재적용했다.

- 2026-08-16 (71차): **e2e157 — 협업맵이 편집 잠금을 비켜 간다** (B16 ①).
  `27-sync-model.md` 가 "협업의 첫 장애물은 CRDT 가 아니라 `map_edit_locks`"
  라고 짚은 그 자리를 고쳤다. `maps.kind` 를 보지 않아 두 사람이 열면
  **둘째가 읽기 전용**이 되던 것이다.
  **네 군데를 다 비켜 줘야 했다** — 저장 · 열기 · 하트비트 · 해제. 판정을
  `isCollabMap()` 하나로 모은 이유가 그것이다. 특히 **하트비트**가 조용하다:
  표를 그냥 두면 갱신 행이 0이라 `held:false` 가 나가고, 프런트는 그것을
  편집권 상실로 읽어 **맵과의 연결을 끊는다.** 25초마다 편집이 로컬 초안으로
  빠지는데 화면에는 "저장 안 됨"만 뜬다.
  열기에서는 `editLock` 을 **아예 주지 않는다** — `'acquired'` 로 답하면
  **잠근 적이 없는데 잠갔다고 말하는 것**이다.
  **검증에서 내 실수 둘을 잡았다.** ⑴`PUT /document` 는 `editSession` 을
  **본문**으로 받는데 쿼리에 넣어, 잠금 주인의 저장까지 409 가 났다 —
  코드가 아니라 검증이 틀렸다. ⑵두 맵에 같은 제목을 줘서 **제목 중복 409**
  가 났는데, 잠금 409 와 **상태 코드가 같아** 잠금이 깨진 것처럼 보였다.
  **409 는 코드가 아니라 문장으로 판정해야 한다** — 그러지 않으면 다른
  이유로 나는 409 를 "고쳐야 할 버그"로 착각하거나, 반대로 진짜 잠금 실패를
  놓친다.
  되돌려 깨뜨리기도 했다 — `kind` 를 `solo` 로 바꾸면 다시 잠긴다.

- 2026-08-16 (70차): **협업 동기화 모델 설계** (`27-sync-model.md`) —
  e2e 없음(설계 문서). 구현 전에 **무엇으로 합칠지**를 정했다.
  **먼저 확인한 것이 설계를 바꿨다.** 기존 `25-map-collaboration.md` 는
  Supabase Realtime · Redis · WebSocket Gateway 를 전제하는데 **셋 다
  코드에 없다**(Supabase 는 Auth 전용, Redis 의존성 없음,
  `@nestjs/websockets` 미사용). "이미 있는 것을 쓰면 공수가 준다"가 그
  설계의 근거였는데 그 '이미 있는 것'이 없다 — 없는 것을 전제로 세운
  계획은 착수하는 날 무너진다. 그 문서 머리에 경고를 붙이고 로드맵의
  같은 문장도 정정했다.
  결정 여섯. ⑴**LWW 대신 CRDT(Yjs)** — LWW 는 텍스트 동시 편집에서
  **한쪽 문장을 통째로 버리고**, 트리 이동·삭제가 얽히면 적용 순서에
  따라 결과가 달라진다. ⑵**CRDT 에는 구조와 텍스트만** — 이미지 바이트를
  넣으면 업데이트 로그에 영원히 남는다(CRDT 는 지운 것도 묘비로 든다).
  ⑶**트리 규칙은 우리가 지킨다** — CRDT 는 "모두 같은 결과"만 보장하지
  그 결과가 옳은지는 모른다. ⑷전송은 우리 WebSocket 하나(Redis 는 지금
  넣지 않는다 — 없어도 되는 부품을 넣으면 그것이 죽었을 때의 장애를
  공짜로 얻는다). ⑸**정본은 여전히 `map_documents.doc`** — 형식을 갈면
  내보내기·히스토리·문서함이 두 벌이 되고, **유료 모듈을 떼면 문서가
  남지 않는다.** ⑹Undo 는 자기 origin 한정(안 그러면 Ctrl+Z 가 남이 방금
  한 일을 되돌린다).
  **가장 값어치 있는 발견** — 협업의 첫 장애물은 CRDT 가 아니라
  `map_edit_locks` 다. 지금 `maps.kind` 를 보지 않아 **두 사람이 열면
  둘째가 읽기 전용**이 된다. 이것과 "이미지를 문서 밖으로"를 **B16(공개
  코어)** 으로 갈라냈다 — 둘 다 협업이 없어도 값어치가 있어, 협업을 안
  하기로 해도 버리는 일이 되지 않는다.
  **재지 않은 것을 재지 않았다고 적었다** — 이 선택들은 구조상의 이유로
  고른 것이지 벤치마크 결과가 아니다(§12).

- 2026-08-16 (69차): **e2e156 — 협업 자리가 서버에 물어본다** (B15).
  프런트 확장점(`@pro` 별칭 + 스텁)과 메뉴 자리를 붙였다. **`/v1/features`
  를 부르는 첫 소비자**가 생겼다 — 그전까지 서버는 답할 준비만 되어 있고
  아무도 듣지 않았다.
  화면이 **지어내지 않는 것 셋**이 이번의 핵심이다. ⑴이유를 우리가 고쳐
  쓰지 않는다 — 바꿔 쓰면 만료·미계약·서버 설정 누락이 화면에서 같은 말이
  되어, 서버에서 갈래를 나눈 이유가 사라진다. ⑵**서버에 못 물었을 때
  "꺼졌다"고 단정하지 않는다** — 백엔드가 잠깐 죽은 것을 "유료 기능입니다"로
  보여 주면 사용자는 **있지도 않은 결제를 의심한다.** ⑶서버는 켜졌다는데
  유료 UI 가 없으면 **그 사실을 말한다** — 조용히 빈 화면을 주면 배포에서
  UI 를 빠뜨린 것을 아무도 못 찾는다.
  **함께 지운 것** — 레일 아래에 읽지 않은 메시지를 뜻하는 **빨간 점**이
  붙은 "협업 채팅" 버튼이 있었다. 누르면 아무 일도 없었고, 점은 오지도
  않은 메시지를 왔다고 말하고 있었다. 자리를 살리면서 뺐다.
  배운 것: 콘솔의 `Failed to load resource` 는 **주소를 담지 않는다.**
  문자열로 걸러 통과시키면 **무엇이 실패했는지 모른 채** 초록불이 된다.
  `requestfailed` 로 URL 을 모아 판정하도록 고쳤고, 그 결과 남은 실패가
  외부 폰트 CDN(이 샌드박스의 프록시가 막는 것) 하나뿐임을 확인했다.

- 2026-08-16 (68차): **/v1/features 가 코어 카탈로그를 뼈대로 삼는다** (B15).
  e2e155 에 4항목 추가(→ 28항목). 유료 모듈이 목록을 **통째로** 답하게 두면,
  모듈이 코어보다 오래된 판일 때 **새 기능이 목록에서 통째로 사라진다.**
  화면은 빈 자리를 "그런 기능이 원래 없다"로 읽으므로 **사용자는 살 수 있는
  기능이 있는 줄도 모르게 된다** — 돈이 걸린 조용한 버그다. 뼈대를 코어가
  들고, 유료 모듈은 그 위에 상태만 얹도록 고쳤다.
  반대 방향도 막았다 — 모듈이 카탈로그에 없는 id 를 주면 **감추지 않고 뒤에
  붙인다.** 코어보다 새 모듈이 먼저 나올 수 있고, 그때 감추면 같은 문제가
  반대로 생긴다. 이름은 코어 것을 쓴다(화면 문구의 주인은 코어다).
  검증은 **dist 를 손으로 고쳐** 오래된 모듈을 흉내내 확인했다 — 코드를
  읽어서 "그럴 것이다"로 넘기면 이런 것은 안 잡힌다.

- 2026-08-16 (67차): **병합 정책 축소** — e2e 없음(규칙 문서).
  사용자 요청으로 §2.0 의 예외를 **스키마 파괴 하나만** 남겼다.
  가른 기준은 "되돌릴 수 있나" 하나다 — **코드는 revert 로 끝나지만
  지워진 데이터는 돌아오지 않는다.** 표·컬럼을 **더하는 것**은 파괴가
  아니므로 멈추지 않는다(지금까지의 멱등 델타 SQL 그대로).
  없앤 예외 셋은 **버린 것이 아니라 내 일로 옮겼다** — CI 빨간불은
  "멈춤"이 아니라 "고치고 병합", 검증 실패는 "못 했다고 적고 병합",
  설계 갈림길은 "내가 고르고 이유를 적기". 예외를 지우면서 그것들을
  그냥 없던 일로 하면 **빨간불을 병합해도 규칙상 문제없는** 상태가
  된다 — 그게 이번 변경의 유일한 함정이었다.

- 2026-08-16 (66차): **e2e155 — 유료 확장점을 하나의 이음매로 일반화** (B15).
  #279 의 협업 전용 확장점(`collab/`)을 `pro/` 로 바꾸고, 경로를
  `/v1/collab/*` 두 개에서 **`GET /v1/features` 하나**로 줄였다.
  **왜 지금인가** — 유료 범위가 MVP 이외 전부가 되면서, 기능마다 구멍을
  뚫으면 코어에 거의 똑같은 모듈이 8개 생기고 **기능이 하나 늘 때마다
  공개 코어를 고쳐야** 한다. 그러면 확장점을 만든 의미가 없다. 그리고
  `/v1/collab/*` 를 **쓰는 곳이 아직 하나도 없어서** 지금이 가장 쌌다.
  검증은 지난번처럼 **양방향**이다 — 스텁 10 + 가짜 유료 패키지 9 +
  **지우면 되돌아오는지** 5. 이번엔 유료 모듈이 **자기 경로를 가져오는
  것**(`/v1/collab/ping`)까지 확인했다. 이음매의 핵심이 "구현이 자기
  것을 통째로 들고 온다"이므로 그게 안 되면 일반화가 실패한 것이다.
  갈린 지점 셋. ⑴**주입 토큰을 문자열로** — 클래스·심볼이면 유료 패키지가
  코어의 값을 import 해야 같은 토큰이 되는데, 문자열은 값이 같으면 같은
  토큰이다. **두 저장소가 서로의 타입을 몰라도 DI 가 이어진다.**
  ⑵**기능 목록은 코어가 안다** — 로드맵에 이미 공개된 정보이고, 화면이
  꺼진 기능의 **자리도 그려야** 하기 때문이다. 비공개인 것은 무엇이
  있는가가 아니라 **어떻게 동작하는가**다. ⑶**스텁이 빈 목록을 주지
  않는다** — 비면 화면이 "그런 기능이 원래 없다"로 읽는다.
  배운 것: 가짜 패키지를 **순수 JS** 로 쓰면 데코레이터를 못 쓰는데,
  `@Module` 을 그냥 쓰면 `require` 가 문법 오류로 던지고 **try/catch 가
  그걸 삼켜 "패키지가 없다"로 조용히 오인**된다. 확장점의 try/catch 는
  이렇게 **실수를 감추는 성질**이 있어, 검증에서 로그를 반드시 본다.

- 2026-08-16 (65차): **private 저장소 이름 변경 마무리** — e2e 없음(문서만).
  `easymindmap-collab` → **`easymindmap-pro`**(사용자). 담는 것이 협업만이
  아니게 되면서 이름이 좁아졌다. **배포가 붙기 전이라 값이 거의 안 들었다** —
  고칠 것은 문서의 이름 언급뿐이었다. GitHub 이 옛 URL 을 리다이렉트로 살려
  두지만 **누가 옛 이름으로 저장소를 새로 만들면 끊기므로** 리다이렉트에
  기대지 않고 링크를 다 고쳤다. 남겨 둔 교훈: 이름을 바꿔야 할 것 같으면
  **배포가 붙기 전에** 한다 — 붙은 뒤에는 Coolify Source·배포 키·CI 까지
  따라 움직인다.

- 2026-08-16 (64차): **오픈코어 경계 재정의** (B15) — e2e 없음(문서만).
  유료 범위가 협업만이 아니라 **MVP 이외 제품 기능 전부**로 정해졌다
  (사용자 결정). 코드는 건드리지 않았고 다음 PR 로 미뤘다.
  **확인부터 했다** — 로드맵의 V1~V3 기능(WBS·Redmine·Obsidian·번역·
  대시보드·Workflow)이 공개 저장소에 **구현돼 있는지** 파일 목록으로
  훑었다. 전부 설계 문서만 있고 코드가 없다. 협업 관련 코드는
  `CollabAvatars`·`CollabCursor` **105줄**뿐이고 `COLLAB_PRESENCE_UI=false`
  로 꺼진 목데이터 껍데기라 경계상 올바른 쪽(자리는 공개)에 있다.
  **그래서 소급해서 지울 것이 없다** — 있었다면 지워도 회수되지 않으므로
  이 확인이 먼저였다.
  설계에서 갈린 지점 셋. ⑴**이음매를 하나로** — 유료 기능이 8개면 코어에
  똑같은 모듈이 8개 생기고 기능이 늘 때마다 코어를 고쳐야 한다. 코어는
  `@easymindmap/pro` 하나만 찾는다. ⑵**"MVP 에 없으면 유료"의 예외 셋을
  미리 박았다** — 운영 도구(관리자 콘솔·헬스체크·백업)는 파는 게 아니라
  돌리는 데 필요한 것이라 공개, **EMM 포맷·파서는 공개**(표준의 레퍼런스
  구현이라 비공개로 돌리면 표준이 아니게 되고 **파일 호환이 깨지면 사용자가
  자기 문서를 잃는다**), 화면의 자리는 공개. ⑶라이선스 판정은 비공개 쪽 —
  공개 코어에 두면 지우면 그만이다.
  **§7 을 정정했다** — 전에 "코어를 npm 의존성으로 당겨 태그로 고정한다"고
  적었는데, 확인해 보니 **코어에 태그가 하나도 없고**(0개) npm workspaces
  모노레포도 아니다(루트에 package.json 이 없다). 성립하지 않는 방식을
  적어 둔 것이라 "코어를 clone 해서 얹는다 · 커밋 SHA 로 고정"으로 고쳤다.

- 2026-08-15 (63차): **e2e154 추가 — 협업 확장점을 실제로 뚫었다** (B15).
  62차의 설계대로 서버 쪽 자리를 만들었다. 계약·스텁·모듈·컨트롤러.
  **가장 중요한 검증은 양방향이다.** 스텁이 404 를 주는 것만 보면 절반이다 —
  `node_modules` 에 가짜 유료 패키지를 만들어 **구현이 정말로 스텁을
  밀어내는지**(로그가 `설치됨` 으로 바뀌고, join 이 200 으로 성공하고,
  구현이 만든 세션이 그대로 오는지) 보고, **패키지를 지우면 다시 스텁으로
  돌아가는지**까지 확인했다. 확장점은 "구현이 꽂힌다"가 핵심인데 그걸
  검증하지 않으면 꽂히지 않는 확장점을 만들 수 있다.
  갈린 지점 셋. ⑴**경로를 공개 코어에 둔다** — 유료 모듈이 경로까지
  만들면 404 가 "원래 없는 기능"인지 "유료라서 없는 것"인지 구분되지 않는다.
  ⑵**`status` 는 로그인 없이 답한다** — 화면이 메뉴를 그릴지 정하는 데
  쓰므로 로그인 전에도 알아야 한다. ⑶**헬스체크에 협업 표를 넣지 않는다** —
  넣으면 공개판만 배포했을 때 영원히 `degraded` 가 된다.
  **화면 쪽은 일부러 미뤘다.** 협업 메뉴 자리는 에디터 탭 레지스트리를
  함께 건드려야 하고, 아무도 쓰지 않는 스텁은 죽은 코드가 된다. 서버가
  이미 "켜졌는지 + 왜 꺼졌는지"를 답하므로 화면은 그때 물어보면 된다.
  배운 것: **NestJS 의 POST 기본 응답은 201 이다.** 이 저장소 관례대로
  `@HttpCode(200)` 을 붙였다(검증에서 201 이 나와 알았다).

- 2026-08-15 (62차): **오픈코어 경계 설계** (B15) — e2e 없음(설계 문서).
  "Apache 2.0 으로 공개했는데 유료 협업 기능 소스는 어떻게 관리하나"에
  답한 결과다. 결정: **저장소는 공개 그대로, 구현만 private. 설계 문서는
  공개에 남긴다.**
  확인한 사실 셋.
  ⑴**법적으로는 나눌 필요가 없다.** Apache-2.0 은 허용적 라이선스라
  **독점 파생을 명시적으로 허용**하고, SaaS 공개 의무도 없다. 나누는
  이유는 **git 저장소가 통째로 public/private 이라 실수로 커밋되면
  되돌릴 수 없기 때문**이다 — 법이 아니라 사고 방지다.
  ⑵**이미 쓰고 있는 패턴이 있다.** 관리자 콘솔의 결제관리 탭이
  `준비 중` 자리만 차지하는 것(`TABS[].soon`)이 정확히 확장점이다.
  프런트 별칭도 `@emm` → `packages/emm-parser` 로 선례가 있다.
  ⑶**설계 문서는 이미 공개돼 있었다**(COLLAB-01~17, collaboration/).
  되돌릴 수 없으므로 "그대로 둔다"를 결정으로 못 박았다.
  갈린 지점: **코어는 유료 모듈의 타입을 import 하지 않는다** — 계약만
  보고 구현은 런타임에 꽂힌다. 그래야 공개 저장소만으로 타입체크가 통과한다.
  그리고 **스텁이 기본이다** — 유료 모듈이 없는 것이 오류가 아니라
  공개판의 정상 동작이다.
  적어 둔 원칙: **판단이 서지 않으면 private 에 둔다.** 공개로 옮기는 것은
  언제든 되지만 그 반대는 불가능하다.

- 2026-08-15 (61차): **e2e153 추가 — 헬스체크 감시** (B14 ⑤).
  "무언가 잘못돼도 사용자가 말해 줘야 안다"를 고쳤다.
  갈린 지점 셋. ⑴**API 안이 아니라 호스트에서 돈다** — API 안에 넣으면
  **API 가 죽었을 때 아무도 알리지 못한다**(배포 직후 기동 실패가 정확히
  그 경우다). ⑵**새로 넣을 설정이 없다** — 받는 사람은 `ADMIN_EMAILS`,
  메일 서버는 `SMTP_*` 를 **`docker inspect`** 로 읽는다. 컨테이너가 멈춰
  있어도 읽히기 때문에 API 가 죽은 상황에서도 보낼 수 있다.
  ⑶**알림 피로를 막는다** — 상태가 바뀔 때만, 계속 나쁘면 하루 한 번만.
  **검증에서 진짜 버그를 잡았다**: 첫 실행에서 정상인데도 `복구됨` 메일을
  보냈다(이전 상태가 없으니 "바뀐 것"으로 본 것이다). 설치하자마자 뜬금없는
  복구 메일이 온다 — 첫 실행 + 정상이면 조용하도록 고쳤다.
  배운 것: 메일 헤더는 **CRLF** 다. `base64 -d` 앞에서 `\r` 을 빼지 않으면
  제목 해독이 빈 문자열이 되고 **단언이 조용히 통과**한다.
  **작업 사본이 또 되돌아갔다** — 이번 턴에만 두 번(하루 세 번째). §2.3
  규칙대로 커밋 전에 확인해 두 번 다 잡았다.

- 2026-08-15 (60차): **DB 백업 — 확인해 보니 하나도 없었다** (B14 ④).
  Coolify → `easymindmap-db` → Backups 가
  `No scheduled backups configured.` 였다. **지금 서버가 죽으면 전부
  잃는다.** 런북 §2.1 에 즉시 1회 백업·자동 백업(cron)·복원 절차를 적었다.
  **가장 중요한 함정: Coolify 기본 백업으로는 로그인 계정이 빠진다.**
  GoTrue 는 별도 데이터베이스(`gotrue`)를 쓰는데 Coolify 는 기본적으로
  `POSTGRES_DB` **하나만** 담는다(Import 화면의
  `Backup includes all databases` 체크박스가 그 증거다). 그대로 복원하면
  **맵은 살아나는데 아무도 로그인할 수 없다.** 그래서 `pg_dumpall` 로
  인스턴스 전체를 담게 했고, 스크립트가 담긴 데이터베이스 목록을 먼저
  출력해 `gotrue` 가 있는지 눈으로 확인하게 했다.
  적어 둔 원칙: **백업은 "잡아 뒀다"가 아니라 "되돌려 봤다"여야 믿을 수
  있다.** 운영 인스턴스를 세울 때 dev 백업을 복원해 보는 것이 가장 싼
  리허설이다.
  **또 사고.** 이번 턴에도 컨테이너가 재시작되며 작업 사본이 `4ffdca1`
  로 되돌아가 있었다(58차와 같은 일, 하루에 두 번). 이번에는 §2.3 규칙대로
  **커밋 전에 확인해서** 잡았다 — 낡은 파일 위에 쓴 편집을 버리고
  `origin/main` 위에서 다시 했다. 규칙이 실제로 값을 했다.

- 2026-08-15 (59차): **개인정보 처리방침 초안** (B14 ⑥).
  법률 문서라 e2e 가 없다. 대신 **수집 항목을 기억이 아니라 코드에서**
  뽑았다 — `schema.sql` 의 컬럼과 `account.service.ts` 의 탈퇴 처리를
  읽어 항목·보유기간·파기 방식을 적었고, 각 절에 근거 파일을 달았다.
  확인하면서 나온 것 셋.
  ⑴**AI 는 우리 서버를 거치지 않는다.** 브라우저가 사용자 키로 AI
  제공자를 직접 부른다(`aiProviders.ts`) — 서버에는 AI 모듈이 없다.
  "맵 내용을 AI 에 보낸다"고 잘못 적을 뻔했고, 실제로는 **적을 수 있는
  가장 강한 문장**("회사 서버로 전송되지 않습니다")이 사실이었다.
  ⑵**휴대전화번호를 받아 두고 쓰지 않는다**(`phone_verified_at` 은 항상
  NULL). 쓰임새가 없으면 **수집 항목에서 빼는 것**이 맞다 — 안 쓰는
  개인정보는 위험만 남는다. 방침에 그렇게 적었다.
  ⑶**저장 버전 이력은 보유 기간이 없다.** 처리방침에 "무제한"이라고 적을
  수는 있지만 좋은 답이 아니다 — 기간·개수 상한을 정해야 한다.
  못 한 것: **법률 검토.** 나는 변호사가 아니고, 사업자 정보·보호책임자·
  시행일은 사용자만 채울 수 있다. 문서 맨 위에 그렇게 적었다.

- 2026-08-15 (58차): **백로그 현행화 + 안 하기로 한 것 세 가지.**
  "그럼 PG만 남았건가?"에 답하려고 백로그를 읽어 보니 **표가 낡아 있었다.**
  ⑴`B9 첨부` 는 "설계 완료·구현 대기"였지만 **P1 과 청크 업로드는 이미
  끝났다**(2026-08-02·08-06). 남은 것은 P2~P4 뿐이다.
  ⑵`B14 외부 공개 준비` 는 "구멍은 레이트 리밋과 가입 통제 둘"이라고
  적혀 있었지만 **둘 다 이미 막혔다** — 레이트 리밋(08-08)·가입 이메일
  인증(08-09). 남은 것은 **③별도 인스턴스 ④DB 백업 ⑤모니터링
  ⑥개인정보 처리방침** 넷이고, **오픈을 막고 있는 것은 결제(PG)가 아니라
  이쪽이다.**
  안 하기로 한 셋(C5 이어받기 · 캔버스 전역 드롭 · 되돌리기 영구 보관)은
  **이유까지** 백로그와 각 기능 문서에 적었다 — 적어 두지 않으면 다음에
  또 올라와 같은 논의를 반복한다.
  **작업 중 사고 하나.** 컨테이너가 재시작되면서 작업 사본이 **18커밋 뒤로**
  돌아가 있었는데(HEAD=4ffdca1) 그것을 모르고 브랜치를 만들어 커밋·푸시했다.
  병합했다면 #256~#274 를 통째로 되돌릴 뻔했다. `origin/main` 위로 다시
  만들어 바로잡았다. **규칙: 컨테이너 재시작 뒤에는 브랜치를 만들기 전에
  `git fetch && git log --oneline origin/main -1` 로 기준을 확인한다.**

- 2026-08-15 (57차): **e2e152 추가 — 칸은 늘 두고, 중복 창을 줄였다.**
  사용자 지적 둘.
  ⑴**"IP 값은 없더라도 IP 컬럼은 있어야 되지 않나?"** 맞다. 값이 없으면
  칸을 숨기던 것은 GoTrue 가 IP 를 **영원히** 안 주던 때의 판단이었다
  (그때는 칸이 통째로 `—` 라 "고장 났다"로 읽혔다). 이제 값이 채워지므로
  숨기면 **회원마다 표의 칸 수가 달라진다** — 관리자가 두 회원을 나란히
  볼 때 같은 화면을 다른 것으로 읽게 된다. 빈 값은 `—` 로 두고 이유는
  목록 아래 한 줄이 말한다.
  ⑵**스크린샷에서 잡은 것: 중복 방지 창이 실제 로그인을 삼켰다.**
  04:27:41 앱 로그인 → 04:28:20 관리자 콘솔 로그인(39초 차)에서 뒤엣것의
  IP 가 비어 있었다. 60초 창에 걸린 것이다. 그런데 **긴 창은 애초에 필요가
  없었다** — 기록은 `authStore` 의 로그인·가입에서만 부르고, 새로고침·탭
  복원은 로그인을 다시 하지 않아 부르지 않는다. 남는 위험은 같은 요청의
  중복 전송뿐이라 **5초**면 충분하다. 2초/6초 간격으로 눌러 두 경우가
  각각 막히고 기록되는 것을 확인했다.
  배운 것: **막는 규칙은 무엇을 막는지 정확히 알고 정해야 한다.** "새로고침
  때문에"라고 적어 놓고 실제로는 새로고침이 그 경로를 타지도 않았다 —
  그 사이 진짜 로그인만 잃고 있었다.

- 2026-08-14 (56차): **런북의 DB 찾기와 사설 IP 안내를 고쳤다.**
  둘 다 **실사용에서 깨진 것**을 고친 것이다.
  ⑴**델타 SQL 이 DB 를 못 찾았다.** 스크립트가 이미지 이름에 `postgres` 가
  든 컨테이너를 찾는데, 이 서버의 앱 DB 는 **이미지가 ID(`33f923b05f64`)로만**
  보여 걸리지 않았다. 대신 다른 앱의 DB 와 `coolify-db` 가 잡혀
  `role "postgres" does not exist` 로 끊겼다 — 계정 이름도 컨테이너마다
  다르다. **API 의 `DATABASE_URL` 을 그대로 따라가도록** 바꿨다(§1.5-0-A).
  추측이 없고 엉뚱한 DB 를 건드릴 위험도 없다.
  ⑵**`/v1/health/ip` 가 고칠 수 없는 자리를 고치라고 안내했다.** 사슬이
  `192.168.0.1, 192.168.0.74` 처럼 **둘 다 사설**일 때, 기존 판정은 사슬이
  하나일 때만 원인을 짚어서 "TRUST_PROXY 조정이 필요합니다"라고만 말했다.
  그런데 사슬에 공인 IP 가 없으면 그 설정으로는 아무것도 달라지지 않는다.
  이제 그 경우를 알아보고 **가르는 법**(휴대폰 LTE 로 다시 열어 보기)을
  알려 준다.
  배운 것: **"고칠 수 없다"와 "여기를 고쳐라"를 섞지 않는다.** 안내가
  틀리면 사용자가 엉뚱한 곳을 붙들고 시간을 쓴다.

- 2026-08-14 (55차): **e2e150 추가 — 안 보이는 이유를 갈라 말한다.**
  "이젠 이력화면에서 IP가 빠졌다"(사용자). 54차에서 IP 를 채우고도 화면은
  **왜 안 보이는지 한 가지로만** 말하고 있었다 — 원인이 둘인데.
  ⑴`login_events` 표가 없다(델타 SQL 미적용) ⑵표는 있는데 그 사람이
  **기록 시작 뒤로 아직 로그인하지 않았다.** 앞은 관리자가 SQL 을 넣어야
  하고, 뒤는 사용자가 다시 로그인하면 된다 — **할 일이 정반대**다.
  서버가 `ipSource` 로 갈라서 주고, 화면이 각각 무엇을 하면 되는지 짚는다.
  **표 유무는 캐시하지 않는다.** 델타 SQL 은 서버가 도는 중에 적용되므로,
  캐시하면 적용하고도 재기동 전까지 계속 "표가 없다"고 답한다.
  안내가 **거짓말이 아닌지**까지 봤다 — "다시 로그인하면 보인다"고 적어
  놓고 실제로 로그인 폼을 눌러 IP 가 나오는 것을 확인했다.

- 2026-08-14 (54차): **e2e149 추가 — 로그인 접속 IP 를 우리가 남긴다.**
  53차에서 "GoTrue 가 IP 를 기록하지 않아 보여 줄 수 없다"고 밝혔던 것을,
  **우리가 직접 기록해서** 채웠다. 로그인 성공 직후 `authStore` 가
  `POST /v1/account/login-event` 를 한 번 부르고 서버가 그때의 IP·기기를
  남긴다.
  갈린 지점 넷.
  ⑴**IP 는 본문으로 받지 않는다.** 클라이언트가 보낸 IP 는 위조할 수 있고,
  그러면 남의 접속인 척 심을 수 있다. 서버가 요청에서 직접 본다 — 본문에
  `ip` 를 실으면 400 이다.
  ⑵**우리 기록이 로그인 목록의 기준은 아니다.** 목록·횟수는 여전히 GoTrue
  감사 로그가 기준이고, 우리 것은 IP·기기를 **덧붙일** 뿐이다. 우리 것만
  보면 이 기능 이전의 로그인이 통째로 빠진다.
  ⑶**시각으로 맞추되 ±60초·일대일.** 두 기록은 같은 로그인인데 1~2초
  다르다. 창을 넓히면 1분 간격 로그인 둘의 IP 가 서로 바뀌고(실제로 그런
  기록이 있다), 일대일이 아니면 한 기록이 여러 줄에 번져 **실제보다 많은
  곳에서 들어온 것처럼** 읽힌다.
  ⑷**못 맺은 줄은 비워 둔다.** 8월 14일 이전 로그인이 그렇다. 화면이 왜
  비었는지 밝힌다 — 없는 접속을 지어내지 않는다.
  검증에서 배운 것: **`INET` 컬럼은 `::text` 가 `/32` 를 붙인다.** 화면에
  `203.0.113.9/32` 로 새어 나가는 것을 보고 기존 컬럼과 같은
  `VARCHAR(45)` 로 맞췄다. 한 저장소 안에서 IP 를 두 가지로 담을 이유가 없다.
  검증 방식: **진짜 로그인 폼을 눌러** 확인했다(가짜 GoTrue 로그인 서버).
  API 만 직접 부르면 "화면이 정말 부르는가"가 검증에서 빠진다.

- 2026-08-14 (53차): **e2e148 추가 — 가로 스크롤 · 건수 규칙 · IP.**
  사용자 지적 셋을 한 번에 다뤘다.
  ⑴**가로 스크롤.** 회원관리는 칸이 11개다. 곁들이 정보(`문서 33KB ·
  첨부 0`, `Windows 11 · Chrome 136 · IP`)를 같은 줄에 붙이면 표의 최소
  너비가 **1424px** 이 되어 그릇(1242px)을 넘는다. 아래 줄로 내리니
  들어왔다. 콘솔 폭도 1280 → 1560 으로 넓혔지만 **그건 고친 대목이
  아니다** — 되돌려 깨뜨려 구분했다.
  ⑵**"일부만 나온다"의 규칙.** 서버가 최근 50건까지 준다. 그런데 화면은
  "최근 N건입니다"라고만 써서 **그것이 전부인지 잘린 것인지 알 수 없었다.**
  서버가 `limit` 을 함께 주고, 화면이 `최근 50건까지만 (전체 141회)` 와
  `기록 전체 8건` 을 갈라 쓴다.
  ⑶**IP 가 비어 있다.** 우리 문제가 아니었다 — supabase/auth v2.194.0 이
  `token.go`·`logout.go` 에서 `NewAuditLogEntry(…, "" , …)` 로 **빈
  문자열**을 넣는다(소스에서 확인). payload 에도 IP 키가 없다. 그래서
  **칸을 아예 만들지 않고** 왜 없는지 한 줄로 말한다 — 줄줄이 `—` 는
  "기록이 없다"가 아니라 "고장 났다"로 읽힌다. 값이 있는 경우에는 칸이
  나오도록 읽기는 남겼다.
  검증에서 배운 것: **측정 대상을 잘못 잡으면 단언이 늘 통과한다.**
  처음에 `admin-members > div` 의 마지막 자식을 쟀는데 그건 표가 아니라
  **표 아래 안내 문구**였다(그릇=내용이라 항상 "안 넘침"). 손잡이를 붙여
  다시 쟀다.
  못 한 것: **진짜 접속 IP.** 우리가 직접 기록해야 한다(로그인 직후 우리
  API 를 부르는 방식) — 별도 작업으로 남긴다.

- 2026-08-14 (52차): **e2e147 추가 — 관리자 콘솔 스크롤.**
  "설정관리 화면이 스크롤이 안되어 하단의 내용이 짤린다" (사용자 지적).
  원인은 관리자 코드가 아니라 **에디터를 위한 전역 규칙**이었다 —
  `global.css` 의 `html, body { overflow: hidden }`. 캔버스가 페이지째
  구르면 안 되어 둔 것인데, `minHeight:100vh` 짜리 긴 문서인 관리자 화면이
  `#root` 밖으로 넘치자 body 가 그대로 잘라냈다.
  **전역 규칙을 풀지 않았다** — 풀면 에디터가 깨진다. 관리자 화면만
  `height:100vh + overflow-y:auto` 로 **자기 스크롤 그릇**이 되게 했고,
  탭 머리글은 `sticky` 로 붙여 긴 목록 아래에서 되올라오지 않게 했다.
  로그인 카드도 같이 고쳤다. 낮은 창에서 **`align-items: center` 는 넘칠 때
  위쪽을 잘라먹는다** — `margin: auto 0` 으로 바꿔야 위가 살아 있다.
  검증에서 배운 것: **`scrollTop` 을 프로그램으로 넣으면 이 버그가
  재현되지 않는다.** `overflow:hidden` 인 body 도 스크립트로는 움직이기
  때문이다. 사용자가 겪은 것은 "휠이 안 먹는다"이므로 **실제 휠 이벤트**로
  재현해야 했다 — 처음에 scrollTop 으로 재현하려다 통과해 버려 고쳤다.

- 2026-08-14 (51차): **e2e146 추가 — 설정관리 편집(DB 값만).**
  "DB 값만 편집 열고 나머지는 조회로" (사용자 결정). 열린 것은
  **요금제 한도 하나**다.
  **환경변수를 열지 않은 이유**: 관리자 콘솔이 서버를 멈출 수 있는 도구가
  된다. `CORS_ORIGIN` 하나만 잘못 넣어도 전원이 로그인하지 못하고, 콘솔도
  같은 API 를 쓰므로 **그 상태를 화면에서 되돌릴 수 없다.**
  갈린 지점 셋.
  ⑴**함수에서 표로 옮겼다.** 용량 숫자가 `plan_quota_bytes()` 본문에 박혀
  있어 바꾸려면 배포가 필요했다. 씨앗 INSERT 는 **`ON CONFLICT DO
  NOTHING`** 이다 — `DO UPDATE` 로 넣어 보니 `schema.sql` 재적용에
  **관리자 수정이 그대로 되돌아갔다.**
  ⑵**기존 회원을 어떻게 할 것인가.** `users.quota_bytes` 는 요금제가 바뀔
  때만 트리거가 따라오므로 표만 고치면 아무 일도 안 일어난다. 그렇다고
  전원을 덮으면 **특별 계약이 조용히 깎인다** — 실제로 덮어 50 GB 가
  20 GB 로 사라지는 것을 봤다. 그래서 **옛 한도와 정확히 같은 사람만**
  옮기고, 손대지 않은 인원수를 화면이 함께 밝힌다.
  ⑶**편집 가능 여부는 서버가 정한다.** 화면이 `source === 'db'` 로
  짐작하면, 표가 아직 없는 서버(델타 적용 전)에서 **입력칸이 먼저 생기고
  저장에서 터진다.** 그 상태를 재현해 입력칸이 0개인 것까지 확인했다.
  못 한 것: **편집 이력이 남지 않는다.** `plan_quotas.updated_by` 에
  마지막으로 바꾼 사람만 남는다 — 언제 누가 얼마로 바꿨는지의 목록은 없다.

- 2026-08-14 (50차): **e2e145 추가 — 관리자 도메인 분기와 환경 배지.**
  **개발은 경로(`pro-dev.mindmap.ai.kr/admin`), 운영은 도메인
  (`admin.easymindmap.org`)** 으로 간다(사용자 결정). 운영만 나누는
  이유는 하나다 — 그래야 프록시에서 **IP 제한**을 걸 수 있다. 경로로만
  갈려 있으면 에디터까지 같이 막힌다.
  갈린 지점 셋. ⑴**관리자 도메인에서는 경로와 무관하게 콘솔만 뜬다.**
  `?map=` 로도 에디터가 열리면 IP 제한을 우회하는 통로가 된다.
  ⑵**앞자리만 같은 주소를 관리자로 착각하지 않는다** — `/administrator`,
  `web.` 호스트를 실제로 열어 확인했다. ⑶**개발 콘솔과 운영 콘솔은 화면이
  똑같다.** 개발인 줄 알고 운영 회원의 요금제를 바꾸는 실수가 가능해져
  `로컬/개발/운영` 배지를 붙였다 — 판정은 **프런트 주소가 아니라 API
  주소**로 하고(실제로 건드리는 데이터가 거기 있다), **모르면 '운영'으로
  본다**(개발을 운영으로 잘못 부르면 조심할 뿐이지만 반대는 사고다).
  검증에서 배운 것: **호스트명 분기는 `localhost` 로 확인할 수 없다.**
  `/etc/hosts` 에 `admin.localhost` 를 심어 진짜 이름으로 열어야
  `startsWith('admin.')` 가 실제로 검증된다.
  못 한 것: **운영 콘솔은 실제로 세우지 못했다.** 운영 VM 과
  `easymindmap-prod` 프로젝트가 없다 — 코드와 순서(admin-console.md §7)만
  준비했고, 서버가 생긴 뒤 확인해야 한다.

- 2026-08-14 (49차): **e2e144 추가 — 로그인 이력 전체(횟수·시각·IP).**
  48차에서 못 한 것을 했다. GoTrue 관리자 API 는 마지막 시각만 주므로
  **감사 로그(`auth.audit_log_entries`) 를 직접 읽는다** — 앱 DB 가 아닌
  GoTrue DB 라 연결이 하나 더 필요하다(`GOTRUE_DATABASE_URL`).
  갈린 지점 셋. ⑴**설정이 없으면 빈 목록이 아니라 "볼 수 없다"** 고
  말한다 — 빈 목록은 "로그인한 적이 없다"로 읽힌다. 설정을 뺀 API 를
  따로 띄워 기동이 되는지, 200 에 `available:false` 가 실리는지 봤다.
  ⑵**목록에 함께 싣지 않는다** — 회원 200명이면 감사 로그를 200번
  뒤진다. 누른 사람 것만 그때 가져온다. ⑶**자동 토큰 갱신은 뺐다** —
  하루 수십 건이 섞이면 사람이 한 일이 묻힌다.
  사용자 쪽은 **경로에 id 를 두지 않았다**(`/v1/account/logins`).
  받을 자리가 없으면 남의 것을 넣어 볼 수도 없다 — 다른 사용자의
  토큰으로 자기 것만 나오는지 확인했다.
  못 한 것: **감사 로그 표의 실제 모양은 내가 만든 표로만 확인했다.**
  dev 의 진짜 GoTrue DB 를 열어 보지 못했으므로, 배포 후 관리자 콘솔에서
  실제 이력이 뜨는지 한 번 봐야 한다.
  덤으로 48차 때 **e2e143·142·141 세 줄이 리터럴 `\n` 으로 한 줄에
  뭉쳐 있던 것**을 풀었다(표가 한 칸으로 뭉개져 있었다).

- 2026-08-13 (48차): **e2e143 추가 — 진짜 로그인 이력.**
  그동안 회원관리의 '마지막 활동'은 **저장 기록**이었다(로그인 기록이
  우리 DB 에 없어서). GoTrue 관리자 API 로 **진짜 로그인 시각**을
  가져와 칸을 나눴다 — 회원탈퇴·비밀번호 재설정이 쓰는 것과 같은 길이라
  새 환경변수가 필요 없다.
  갈린 지점 둘. ⑴**로그인과 저장을 합치지 않는다** — 로그인만 하고
  저장하지 않을 수도, 오래 열어 둔 탭이 저장만 계속할 수도 있다.
  ⑵**못 가져와도 목록은 열린다** — 이력은 덤이다. 대신 응답의
  `loginHistoryAvailable` 로 화면이 왜 비었는지 밝힌다. GoTrue 를 실제로
  죽여서 이 두 가지를 확인했다.
  못 한 것: 로그인 **횟수**와 이력 목록. GoTrue 관리자 API 가 마지막
  시각만 준다 — 감사 로그를 읽으려면 그 DB 연결이 따로 필요하다.

- 2026-08-13 (47차): **e2e142 추가 — 비밀번호 재설정.**
  런북의 서버 명령(§1.5-0-D)으로만 가능하던 것을 화면으로 옮겼다.
  설계에서 갈린 지점 셋.
  ⑴**계정이 있는지 알려 주지 않는다** — 응답이 계정 유무에 따라
  달라지면 아무나 가입된 주소를 알아낼 수 있다. 없는 계정에도 같은
  모양으로 답하되 메일은 보내지 않고, 화면 문구도 "가입된 계정이라면"
  으로 단정하지 않는다.
  ⑵**표가 한 번만 쓰이게** — 재설정표는 HMAC 이라 DB 를 보지 않는다.
  검증에서 **같은 표로 두 번 바뀌는 것을 실제로 확인**하고, 인증번호
  줄을 GoTrue 호출 전에 지워 회수하도록 고쳤다(동시 요청까지 막으려면
  호출 뒤가 아니라 앞이어야 한다).
  ⑶**비밀번호의 주인은 GoTrue** — 우리 DB 는 손대지 않고 관리자 API 로
  바꾼다. 회원탈퇴가 계정을 지울 때와 같은 길이다.

- 2026-08-13 (46차): **e2e141 추가 — 로그인 라벨과 비밀번호 변경.**
  관리자 로그인의 두 번째 칸이 비밀번호인데 **라벨이 없었다**
  (사용자: 아이디 밑의 란은 무엇인가?). placeholder 만 두면 브라우저
  자동완성이 채우는 순간 사라져, 그 칸이 무엇인지 알 방법이 없어진다.
  두 로그인 화면 모두 `아이디 (이메일)`·`비밀번호` 라벨을 붙였다.
  이어서 **비밀번호 변경**을 붙였다. 관리자 콘솔에 초기 비밀번호가 따로
  없다는 점(1단계는 그 사람의 앱 계정으로 로그인한다)을 화면과 문서에
  분명히 적고, 폼은 **한 벌로** 두어 앱 아바타 메뉴와 관리자 콘솔이 같은
  것을 쓴다 — 최소 길이·확인 입력·오류 문구가 갈리지 않게.
  검증에서 배운 것: **메일 제목을 인코딩된 채로 비교하면 무엇을 넣어도
  통과한다.** 처음 쓴 단언이 그랬고, MIME 해독 후 비교하도록 고쳐서야
  진짜 검증이 됐다.

- 2026-08-13 (45차): **e2e140 추가 — 관리자 콘솔.**
  "admin 페이지를 만들자 — 2단계 로그인, 기본 계정 ok@baro.pro,
  회원관리·결제관리·설정관리." 설계에서 갈린 지점 넷.
  ⑴**비밀번호는 우리 서버를 지나가지 않는다** — 1단계는 프런트가 GoTrue
  로 직접 로그인하고, 우리는 그 토큰만 검증한다.
  ⑵**관리자 명단은 환경변수**다. DB 컬럼이면 DB 를 건드릴 수 있는 사람이
  스스로를 관리자로 올릴 수 있다. 명단에서 빼면 이미 발급된 표도 즉시
  무효가 되도록, 표 검증이 서명·만료를 통과한 뒤 명단을 다시 본다.
  ⑶**설정값을 베껴 적지 않는다** — env·DB·코드에서 살아 있는 값을
  가져온다. 베껴 적으면 원본이 바뀔 때 관리 화면이 조용히 거짓말을 한다.
  레이아웃 픽셀은 일부러 뺐다(정책 값이 묻힌다).
  ⑷**없는 데이터를 있는 척하지 않는다** — 로그인 이력이 없으므로
  '로그인 정보' 대신 '마지막 활동·최근 30일 저장'으로 적었다.
  검증에서는 **가짜 SMTP** 를 세워 실제로 발송된 메일에서 인증번호를
  읽었다 — 심어 넣으면 발송 경로가 검증에서 빠진다.

- 2026-08-11 (44차): **e2e139 추가 — 가입 화면의 돌아가기 버튼.**
  "가입하기 버튼 밑에 '이미 계정이 있습니다 — 로그인'이 **안내 메시지처럼**
  보인다. '로그인 화면으로 돌아가기'라고 하고 버튼으로 만들어 달라."
  요소는 처음부터 `<button>` 이었다 — 테두리·배경이 없어 그렇게 보이지
  않았을 뿐이다. **그래서 'button 인가'를 묻는 시험으로는 못 잡는다.**
  계산된 스타일(테두리·배경·굵기·높이)을 재고, **주된 행동보다 약한지**
  까지 함께 본다. 둘이 같아 보이면 무엇을 눌러야 할지 고민하게 된다.
  규칙으로 남긴 것: **누를 수 있는 것은 눌러 보이게, 문구는 상태가
  아니라 행동으로**(auth-session-ui.md §11.1-1).

- 2026-08-11 (43차): **e2e138 추가 — 회원탈퇴가 로그인 계정까지 지우는가.**
  e2e137 이 32항목을 통과했는데도 **운영에서는 반쪽**이었다. dev 서버는
  GoTrue 가 **별도 DB(`gotrue`)** 에 살고 앱 DB 의 `auth.users` 는
  껍데기(shim)인데, 검증 환경은 GoTrue 없이 shim 하나만 쓴다. 그래서
  껍데기를 지우고 "지웠다"고 답해도 시험은 통과했다 — 실제로는 자료만
  사라지고 **같은 이메일로 재가입이 막힌다.**
  **교훈: 검증 환경과 운영 환경이 다른 지점을 먼저 적어 두고 시작한다.**
  이번에는 배포 문서에 이미 적혀 있었는데 읽지 않았다.
  고친 방향 셋. ⑴GoTrue 관리자 API 로 **양쪽**을 지운다. ⑵관리자 토큰은
  이미 가진 JWT 비밀키로 **직접 서명한다** — 서비스 키를 따로 보관하지
  않는다. ⑶**모르면 모른다고 답한다**: 주소가 없는 supabase 배포는
  `loginAccountRemoved: false` 를 내고 화면도 그대로 알린다. 예전 이름
  `authRemoved` 는 껍데기만 보고 true 를 내서 오해를 줬다.

- 2026-08-11 (42차): **e2e137 추가 — 회원탈퇴.**
  "가입 후 회원탈퇴도 만들어 달라." 지우는 기능은 **지워지는지보다
  안 지워져야 할 것이 남는지**가 어렵다. 갈린 지점 셋.
  ⑴**남의 맵에 남긴 히스토리는 그 맵의 것**이다 — `created_by` 만
  비우고 행은 남긴다. 함께 지우면 남의 계정 히스토리에 구멍이 난다.
  ⑵**만료 전 토큰이 계정을 되살린다** — AuthGuard 의 JIT 프로비저닝이
  없는 사용자를 만들어 주기 때문이다. 되살아나면 그 이메일이 다시 잡혀
  **같은 주소로 재가입할 수 없게 된다.** 그래서 `deleted_accounts`
  묘비를 두고 가드가 먼저 본다. 이 검증은 **묘비를 지웠을 때 실제로
  되살아나는 것까지** 확인해 장치가 일한다는 것을 증명했다(⑭).
  ⑶**첨부는 메타와 파일이 따로 산다** — CASCADE 는 DB 행만 지운다.
  저장소 접두사(`u/<id>`·`tmp/<id>`)를 통째로 지우지 않으면 디스크에
  주인 없는 파일이 남는다.
  표가 없는 서버에서도 탈퇴가 되도록 낮춰 두고(경고 로그 + `/health`),
  그 상태까지 시험에 넣었다 — 배포와 델타 SQL 사이에는 반드시 틈이 있다.

- 2026-08-09 (41차): **e2e136 추가 — 회원가입.**
  "가입 버튼을 누르면 바로 가입되는 것 같은데, 이메일 인증·성명·휴대폰을
  받아 free 로 가입시켜 달라." 설계에서 갈린 지점 셋.
  ⑴**메일은 우리 API 가 보낸다** — 앞으로 나갈 메일이 여럿이라(재설정·
  초대·용량 알림) 문구·발신자·제한을 한 규칙으로 묶어야 한다. 통로는
  환경변수라 Workspace → SES 로 옮겨도 코드가 안 바뀐다.
  ⑵**인증표(HMAC)로 ②와 ③을 잇는다** — 가입은 GoTrue 에서 일어나 그 사이에
  세션이 새로 생긴다. 표가 "어느 이메일을 확인했는지" 스스로 말하게 만들어,
  세션에 이메일이 없는 dev 스텁에서도 가입이 끝난다.
  ⑶**설정이 없어도 앱은 죽지 않는다** — 개발 모드에서는 코드를 화면에
  보여 주고 그 사실을 밝힌다(운영에서는 절대 안 나간다).
  만들다 잡은 실수 하나: 인증번호 두 호출에 **토큰을 붙이고 있었다**.
  로그인 전에 부르는 요청이라, 인증이 켜진 배포에서는 401 이 나 **가입
  자체가 막힐** 뻔했다 — apiClient.req 에 anon 표시를 더해 고쳤다.

- 2026-08-09 (40차): **e2e135 추가 — 체크리스트 표시 파리티.**
  "지난번에는 문제 없던 것이 오류가 나는 일이 없게 해 달라"는 지적과 함께
  받은 3건. 정직하게 나누면 **내가 만든 것 하나, 원래 있던 것 셋**이다.
  ⑴뷰어 체크의 **취소선은 내가 넣었다**(#243) — 인스펙터를 보고 따라
  했는데, 정작 맵 노드의 체크는 취소선을 긋지 않아 규칙이 갈렸다.
  ⑵인스펙터의 9px ✓, ⑶머리글의 상시 ☑ 아이콘, ⑷팝업 제목의 원문 마커는
  **전부터 있던 것**이다. 다만 **뷰어의 체크를 누를 수 있게 만든 순간**
  아무도 누르지 않아 드러나지 않던 차이가 한꺼번에 보였다.
  그래서 개별 수정에 더해 **§5-1-6 을 규칙으로 세웠다** — 체크가 나타나는
  네 자리(맵 노드·아웃라인/칸반·인스펙터·팝업/뷰어)를 적고, 표시 규칙
  5개를 못 박고, "새 자리에 같은 개념을 그릴 때는 기존 자리를 먼저 보고
  맞춘다. 다르게 하려면 기존 자리도 함께 바꾼다"를 작업 규칙으로 남겼다.
  e2e135 는 **네 자리를 한 번에** 훑어, 한 곳만 바뀌면 실패한다.

- 2026-08-09 (39차): **e2e134 확장 — 내부 IP · 위장 브랜드.**
  실서버에 올린 뒤 두 가지가 드러났다. ⑴**IP 가 전부 내부 주소**(PC 도
  휴대폰도 192.168.x.x) — `trust proxy` 를 1단계로 뒀는데 실제 배포는
  nginx → Traefik **두 단계**라 nginx 의 IP 에서 멈췄다. 단계 수를 사람이
  세는 방식은 프록시가 늘거나 줄면 조용히 틀리므로 **"사설망 주소는 전부
  우리 프록시"**로 바꿨다(TRUST_PROXY 로 덮어쓸 수 있다). 레이트 리밋도
  같은 값을 쓰므로 **모든 사용자가 한 바구니에 담기던 것**까지 같이
  고쳐졌다. ⑵**브라우저가 `Not;A=Brand 8`** — 크로미움이 일부러 끼워 넣는
  위장(GREASE) 항목이 그대로 저장됐다. 구두점이 실행마다 달라
  (`Not)A;Brand`/`Not;A=Brand`/`Not_A Brand`…) 나열식 필터가 `=` 조합에서
  샜다 → **글자·숫자만 남겨** 비교한다.
  추측으로 고치지 않으려고 **`GET /v1/health/ip`** 를 함께 넣었다 —
  서버가 내 IP 를 무엇으로 보는지(사슬·헤더·trustProxy)와 무엇을 고쳐야
  하는지(`hint`)를 그대로 돌려준다.

- 2026-08-09 (38차): **e2e134 추가 — 저장한 자리(기기·브라우저·IP).**
  사용자 요청: "히스토리에 실행한 Platform(Windows 11··, Android V14,
  iOS…)·브라우저 종류·IP 도 같이 기록하고 보여 달라." 이어서 "내 문서에서도
  파일명에 호버링하면 상세하게 — 아니면 오른쪽에 상세정보 아이콘".
  설계에서 갈린 지점 둘. ⑴**IP 는 서버만 정한다** — 클라이언트가 보낸 값을
  쓰면 위조된다. `trust proxy: 1` 이 이미 걸려 있어(레이트 리밋과 같은 근거)
  프록시가 붙인 마지막 값만 믿는다. 몸통으로 `client.ip` 를 보내면 전역
  화이트리스트에 걸려 400 이다. ⑵**플랫폼은 클라이언트가 더 정확하다** —
  User-Agent 문자열로는 **Windows 10 과 11 이 갈리지 않는다**(둘 다
  'Windows NT 10.0'). Client Hints 의 platformVersion 이 13 이상이면
  Windows 11 이라는 규칙은 스크립트에서만 물어볼 수 있어, 프런트가 알아내
  보내고 서버는 못 받았을 때만 UA 로 추정한다.
  검색 기능에서 겪은 사고(새 컬럼을 무조건 SELECT → 스키마 미적용 서버
  503)를 되풀이하지 않으려고 `information_schema` 로 컬럼 유무를 보고
  **있을 때만** 넣는다. 캐시는 1분 **양방향** — 컬럼을 실제로 지워 보고
  (목록·저장·히스토리 200), 다시 넣어 재기동 없이 붙는 것까지 확인했다.

- 2026-08-09 (37차): **e2e133 확장 — 마커 개수 배지 · 줄마다 생긴 스크롤바.**
  #243 을 쓰신 뒤 두 가지 보고. ⑴**뷰어에 개수 숫자가 없다** — 에디터는
  인디케이터가 2개 이상이면 작은 숫자를 붙이는데 뷰어에는 그 코드가 아예
  없었다. 노트 종류별·링크·첨부·멀티미디어 **모든 마커**에 같은 자리·같은
  크기로 붙였고, 색은 다크에서도 따라 바뀌도록 `SKIN.accent` 로 뒀다.
  ⑵**체크 오른쪽에 상하 화살표(뷰어)·회색 바(에디터)가 생긴다** — 내가
  #243 에서 넣은 것이었다. 클릭 판정을 넓히려고 글리프에 위아래 padding
  2px 을 줬는데, 그 블록이 `overflow-x: auto` 라 CSS 가 `overflow-y` 도
  auto 로 계산해 **줄마다 세로 스크롤바**가 생겼다. 좌우 padding 만
  남겼다(높이는 이미 줄높이라 세로 판정은 그대로다). 규칙으로 적어 뒀다
  — coding-conventions.md §5-1-5 의 경고 문단.

- 2026-08-09 (36차): **e2e133 추가 — 체크박스 토글.**
  사용자 보고: "노드의 체크, 첨부의 체크 들이 모두 체크 및 체크해제가
  안 된다, HTML 뷰어에서도 마찬가지다." 확인한 사실은 셋으로 갈렸다.
  ⑴**뷰어는 정말 읽기 전용이었다** — 체크 글리프에 클릭 핸들러가 아예
  없었다(주석에도 "읽기 전용"이라 적혀 있었다). 이제 눌린다.
  ⑵**노트 뷰어 팝업(노드의 ✓ 인디케이터)도 읽기 전용이었다** — 체크는
  좌측 노트·태그 탭에서만 바꿀 수 있었다. 팝업이 화면에 크게 떠 있는데
  거기서 안 눌리니 "안 된다"로 보이는 게 당연하다. 이제 팝업에서도
  토글한다. ⑶**맵 노드 글자 안의 체크는 코드상 동작했다** — 다만 판정이
  13px 사각형뿐이라 조금만 빗나가면 노드가 선택돼 "안 눌린다"로 보인다.
  사방 6px 로 넓혔다(coding-conventions.md §5-1-5 로 규칙화).
  **뷰어 체크가 어디에 남는가**는 사용자 질문이기도 했다 — 내보낸 HTML
  은 정적 파일이라 스스로를 못 고친다. 파일별 localStorage 키에
  **원본과 다른 것만** 담아, 같은 브라우저에서 다시 열면 살아난다.
  맵 파일 자체는 바뀌지 않는다(그러려면 에디터에서 체크하고 다시 내보낸다).
  덤으로 체크 항목 **연속 입력**(Enter/Shift+Enter → 다음 항목)을 넣었다.

- 2026-08-08 (35차): **e2e132 추가 — 문서함 내용 검색.**
  사용자 질문: "폴더명·파일명만 검색하는데 파일 안(맵의 노드, 노트)
  내용에서도 검색 가능하나?" — 불가능했다. 프런트 판정이
  `name.includes(q)` 한 줄이었고 서버 `GET /maps` 에 검색 파라미터 자체가
  없었다. 저장 시 DB 트리거가 만드는 `map_documents.search_text` +
  pg_trgm 로 서버 검색을 넣었다. 결과 표시는 처음에 맞은 줄 미리보기
  문장이었으나, 같은 날 2차에 사용자 결정으로 **이름 글자 강조 +
  `맵(N건)` 배지**로 바꿨다 — 맵 하나에서 여러 노드·노트가 걸리면 대표
  문장을 고르는 문제가 끝없이 복잡해진다.
  실측으로 걸러낸 것 셋 — ⑴질의 형태(맵 3,000개×200노드·색인 42MB,
  3글자): 두 테이블에 걸친 OR 337ms → 하위 질의 276ms → **MATERIALIZED
  CTE 1.8ms**. 앞의 둘은 trigram 인덱스를 못 쓴다(OR 안의 hashed SubPlan).
  ⑵`search_text` 를 늘 SELECT 하면 **스키마 미적용 서버에서 문서함
  목록이 통째로 503** 이 된다 — 검색할 때만 고르도록 고쳤고, 그 상태의
  폴백(이름 검색 + 배너)도 실제로 컬럼을 지워 확인했다. ⑶첨부의 data URL
  을 색인에 넣으면 색인이 문서만큼 커진다 — 첨부는 **파일명만** 넣는다.
  2차에서 검색 범위에 **링크 이름·주소·첨부 파일명**도 더했다 (25항목).

- 2026-08-08 (34차): **e2e131 추가 — 뷰어 도형 파리티.**
  사용자 보고: "에디터에서는 도형 없음이 되는데 HTML 뷰어에서는 도형이
  있다." 원인은 내보내기가 노드 `style` 을 화이트리스트로 추리면서
  **`shapeType` 을 빠뜨린 것**이었다(PR #232). 뷰어에 `noShape` 분기
  코드는 있었지만 값이 온 적이 없어 늘 false 였다. 맵 설정(레벨별 기본
  도형)도 뷰어가 알 방법이 없어 `textAlign` 처럼 **실효 도형을 구워**
  보내도록 고쳤다. 값이 안 오니 분기할 일도 없어 가려져 있었을 뿐,
  **뷰어는 12종을 전부 사각형으로 그리고 있었다** — 에디터와 같은 좌표
  공식으로 이식했다(PR #235).
  교훈: **e2e129 ⑧은 문자열만 봐서 이 버그를 통과시켰다.** 내보내기
  테스트는 결과 HTML 을 실제로 **열어서** 확인해야 한다. e2e131 은
  내보내기 메뉴를 실제로 눌러 받은 파일을 file:// 로 열고, 회귀 실증도
  해 뒀다(수정 한 줄을 되돌리면 13항목 중 8개 FAIL).

- 2026-08-08 (33차): **e2e130 추가 — 레이트 리밋.**
  사용자 결정: B14 를 쪼개 **레이트 리밋만 먼저** 넣었다(가입 통제는
  MVP 시점). 넓게 공개하기 전에 넣어야 하는데, 공개 직전에 넣으면
  **한도가 적절한지 검증할 시간이 없다** — 지금 넣으면 몇 주 실사용으로
  저절로 검증된다.
  구현 중 두 가지를 실측으로 잡았다:
  ⑴ 기본 429 메시지가 `ThrottlerException: Too Many Requests` 라 화면에
    그대로 뜨면 "알 수 없는 오류"와 다를 게 없었다 → 한국어 안내 +
    `retryAfterSec` 로 교체(프런트가 서버 `message` 를 그대로 띄운다).
  ⑵ `Retry-After` 헤더는 **이미** 붙고 있었다 — 문서에 "향후 개선"으로
    적었다가 실측으로 정정.
  또 `app.set('trust proxy', 1)` 을 넣었다. 없으면 프록시 뒤에서 **모든
  사용자가 프록시 IP 하나로 묶여** 한 사람 때문에 전원이 막힌다.
  초보자용 설명은 [rate-limit.md](rate-limit.md) 에 따로 썼다.

- 2026-08-08 (32차): **e2e129 추가 — 도형 '없음'.**
  사용자 요청: *"스타일-도형에 도형 없음을 추가해줘, 노드에 도형없이
  텍스트만 입력하는 경우도 있다"*. 박스만 안 그리고 노드는 그대로 둔다.
  구현에서 걸린 곳 둘 다 **"도형을 안 그린다"의 부작용**이었다 —
  ⑴글자가 캔버스 배경 위에 놓여 **다크 모드에서 묻힌다**(테마 본문색으로
  전환) ⑵검색 강조가 노란 채움이라 **통째로 안 보인다**(그때만 박스를
  되살린다). 뷰어는 원래 도형을 통째로 무시하지만(기존 갭), **'없음'만은
  따르게** 했다 — "도형을 없앴는데 내보내면 박스가 생긴다"가 가장 눈에
  띄기 때문.
  ※ 이 회차도 컨테이너 재활용으로 스택이 내려가 있었다. 이 테스트는
  **서버가 필요 없어** vite 만 띄워 돌렸다 — 프런트 전용 검증은 그렇게
  하는 편이 빠르다.

- 2026-08-07 (31차): **e2e128 추가 — 로그아웃 시 로컬 초안 삭제.**
  사용자 결정: *"로그아웃할 때도 로컬 초안(IndexedDB)을 지워줘"*. 초안은
  계정이 아니라 **브라우저**에 붙어 있어서, 안 지우면 공용 PC 에서 다음
  사람이 로그인했을 때 앞사람 문서가 복구 배너로 뜬다.
  대가(실수 로그아웃 = 되돌릴 수 없음)는 **잃을 것이 있을 때만 묻는**
  확인 게이트로 갚았다 — '맵 닫기' 와 같은 규칙.
  구현 중 결함 하나를 e2e128 [1] 이 잡았다: 초안을 지운 **직후 화면
  리셋이 문서 변경으로 잡혀 곧바로 다시 적혔다**(지운 의미가 없어진다).
  시간으로 막으면 경계가 흐리므로 상태로 막았다 — `pauseLocalDrafts`.
  회귀: e2e127·126·125·124·123·122·121 전부 통과.
  ※ 이 회차에 컨테이너가 재활용되어 **로컬 DB 가 비어 있었다.** e2e124·
  125·127 이 처음엔 실패했는데 전부 데이터 부재였다 — dev 사용자 1행과
  문서함 고정 데이터(Guide·기획·2026 폴더 + 맵 6개)를 다시 심으니 통과.
  **회귀 실패를 보면 DB 상태부터 확인할 것.**

- 2026-08-07 (30차): **e2e127 추가 — 실사용 2건.**
  *"저장하지 않고 닫고 로그아웃한 뒤 다시 로그인하면 **항상** 복구 배너가
  뜬다"* — 초안을 지우는 자리가 **두 곳 비어 있었다**. 손으로 ☁ 저장해도
  (자동저장 경로만 지웠다), "저장 없이 닫기" 로 버려도 초안이 남았다.
  이제 **저장했으면 지우고, 버렸으면 지운다** — 로그아웃에서는 지우지
  않는다(실수 로그아웃 복구는 남긴다).
  시간축 화살촉이 **다른 가지의 노드 위에 얹히던** 것도 고쳤다 — 축 끝을
  "이 무리의 최대 x + 46" 으로만 잡던 것을, 축 높이와 겹치는 **가장 가까운
  남의 노드 앞**에서 멈추게 했다(자리가 없으면 아예 안 그린다).
- 2026-08-07 (29차): **e2e126 추가 — 시간배치 서브트리 5건.** 보고 5건이
  **한 뿌리**였다: 시간배치가 "축 위 노드 / 그 아래 스택"을 **깊이로만**
  갈라서, 서브트리에 걸면(축 시작점이 중심 주제가 아니다) 엣지·축·＋버튼이
  모두 어긋났다. 배치가 `_timelineRole` 을 심고 셋 다 그것을 보게 했다.
  덤으로 두 가지가 더 드러나 함께 고쳤다 — 축 노드의 **자식**은
  `from.layoutType` 이 비어 맵 레이아웃 엣지가 걸렸고(역할이 있으면 시간배치
  엣지를 쓰도록), `SubtreeStrategy` 의 '위 넘침 보정'이 **축을 무너뜨렸다**
  (시간배치는 그 보정에서 제외). 뷰어도 같은 규칙으로 맞췄다.
- 2026-08-07 (28차): **e2e125 추가 — 실사용 3건 (3차 보고).**
  **대용량 첨부가 재생되지 않던 진짜 원인**을 찾았다 — 서버가 HTTP Range
  를 지원하지 않았고(항상 200 전체), 프런트는 `fetch → blob()` 으로 **파일
  전체를 메모리에 받은 뒤** 열었다. 884MB 를 다 받을 때까지 창은
  `about:blank` 였고, 받아도 통짜 Blob 이라 구간 이동이 안 됐다. 이제
  서버는 `Accept-Ranges` + 206/416 을 제대로 주고, 프런트는 브라우저가 열
  수 있는 형식이면 **URL 을 그대로 넘겨** 스트리밍시킨다.
  시간배치 ＋버튼은 **depth 로 방향 규칙을 가르도록** 고쳤다(3레벨+ 는
  아웃라인과 같은 모양이라 자식=들여쓰기, 형제=쌓이는 축). 시간배치 2종의
  **중심 주제 전용 제한도 풀었다**(SubtreeStrategy).
  기존 테스트 2건은 **전제 조건을 보강**했다(제품 변경 아님) —
  e2e120 은 시작할 때 요금제를 basic 으로 세우고(9MB 업로드가 e2e119 가
  되돌린 free 10MB 에 막혔다), e2e124 는 **편집 잠금을 먼저 지운다**
  (잠긴 맵은 읽기 전용으로 열려 확인 버튼이 '저장하고 계속' 이 아니다).
- 2026-08-07 (27차): **e2e124 추가 — 실사용 6건 (2차 보고).**
  `timeline-center` 를 사용자 정의대로 다시 만들었다 — **중심 주제가 축
  왼쪽 끝, 2레벨 주제가 축 위에 일렬**(앞서 만든 '좌·우 분할'은 폐기).
  축은 노드 사이 토막으로 긋고 루트→주제 엣지는 그리지 않는다.
  레이아웃 아이콘 2개도 실제 배치에 맞춰 다시 그렸다.
  **첨부 유실의 진짜 원인**을 찾았다 — `새 맵 만들기 > 계속` 이 저장 없이
  문서를 갈아 끼웠고, 자동저장은 주기·50건·탭전환에만 올린다. 이제 서버
  맵이면 **먼저 저장하고** 닫는다(실패하면 안 닫는다).
  문서함은 **트리**가 됐다(폴더 클릭 = 그 자리에서 펼치기, 전체 검색).
  기존 테스트 4건은 **사양 변경을 따라가도록** 고쳤다 —
  e2e91[1b] 삭제된 'AI로 초안 만들기' 진입 → AI 탭 진입으로,
  e2e103[1] 폴더 토글 툴팁 규칙 유지 확인, e2e120[3b] 새 폴더 위치 기준을
  '홈 오른쪽' → '툴바 왼쪽 묶음'(홈 버튼이 없어짐), e2e120[2] 아웃라인
  접기 대상 맵을 **하위가 있는 샘플**로 고정(목록 첫 맵이 2레벨이면
  접을 것이 없어 0 이 나왔다 — 기능이 아니라 데이터 문제),
  e2e96 은 통계A 를 **테스트 안에서 NULL 로 되돌리고**(다른 e2e 가 첨부를
  올려 값을 채운다) 정렬 판정을 **통계 3개끼리의 상대 순서**로 완화했다.
- 2026-08-07 (26차): **e2e123 추가 — 실사용 9건.** 좌하단 상태바에서
  **하드코딩된 가짜 값**(`20 노드 · depth 2`, `x: 742, y: 358`,
  `Supabase 동기화됨`)을 걷어내고 **이 맵의 무게**(문서·첨부)로 바꿨다 —
  "정보가 반영되지 않는다"는 보고의 원인이 가짜 값이었다. 문서함 머리글을
  1줄로 정리(`이름`→`폴더/파일명`, 수량은 오른쪽, 합계는 열 이름 위)하고
  **이름 검색창**을 넣었다. 템플릿 이름을 **배치 모양**으로 통일하고
  (`브레인스토밍`→`방사형 양쪽`, `제품 로드맵`→`시간배치 (타임라인)`+
  레이아웃도 `timeline`, `WBS 프로젝트`→`계층형 오른쪽`), 시간축이 중심
  주제를 관통하는 **`timeline-center`** 레이아웃을 새로 넣었다.
  기존 테스트 2건은 **사양 변경을 따라가도록 기대값만** 고쳤다 —
  e2e103[3] 열 이름(`이름`→`폴더/파일명`), e2e106[2] `loadMap(map…)` →
  `loadMap(result.map…)` 정규식(호출 순서·옵션은 그대로 본다).
- 2026-08-06 (25차): **e2e122 추가 — API 키 모드의 적용 흐름을 웹 AI 와
  통일**(사장님 결정). AI 호출 결과를 **바로 열지 않고** 미리보기로 잡아
  두고, [② 새 맵 생성] / [선택 노드에 삽입] 중 하나를 눌러야 반영된다.
  확인 팝오버는 `AiConfirmPopover` 로 **공용화**해 두 모드가 같은 모양·
  같은 자리에 띄운다. "이전에는 이랬다"는 기억이 실재했음을 코드에서
  확인한 뒤(웹 AI 패널) 그쪽 규칙을 API 쪽으로 옮긴 것이다.
- 2026-08-06 (24차): **e2e120·e2e121 추가 — 실사용 3차 보고.** 업로드
  진행률을 **조각 수 → 보낸 바이트**로 바꿨다(`fetch` 에는 업로드
  진행률이 없어 XHR 로 교체) + 속도·남은 시간 + 멈춘 듯 보일 때
  "느립니다" 안내와 흐르는 줄무늬. 아웃라인에 모두 접기/펼치기,
  문서 목록 열 머리글 합계, [＋ 새 폴더] 위치 이동. **AI 두 건**:
  키가 없을 때 버튼이 AI 설정으로 데려가고, **선택 노드 확장이
  입력창 요청을 함께 보낸다**(예전에는 무시했다). 또 AI 새 맵 확인
  대화의 "Ctrl+Z 로 되돌릴 수 있습니다"가 #210 이후 **사실이 아니게
  됐는데 문구만 남아 있어** 사실대로 고쳤다.
  **e2e91 갱신**: 새 맵 확인 게이트(#211)를 먼저 통과하도록 — 사양
  변경에 따라가지 못한 낡은 테스트였다(e2e99·e2e103 과 같은 계열).
- 2026-08-06 (23차): **요금제 확정 — Free 10MB · Basic 10GB · Pro 30GB ·
  Team 20GB/사용자**(가격 미정). `users.plan` 컬럼을 신설하고 **용량 숫자는
  DB 함수 `plan_quota_bytes()` 한 곳**에만 두었다 — `users_sync_quota`
  트리거가 `plan` → `quota_bytes` 를 맞추므로 **결제 연동은 `plan` 만
  바꾸면 된다.** e2e119 를 그 모델에 맞춰 10항목으로 다시 썼다.
- 2026-08-06 (22차): e2e119 추가 — 저장 용량 정책(당시 Free 1GB).
  `schema.sql` 은 배포마다 다시 적용되므로 일회성 데이터 변경에는
  **고정 시각 조건**이 필수다 — 없으면 재적용할 때마다 그 사이 가입한
  무료 사용자까지 올라간다. 기준 시각을 실수로 **미래**로 잡았을 때 이
  테스트가 실제로 잡아냈다.
- 2026-08-06 (21차): **e2e117·e2e118 추가 — 대용량 첨부 청크 업로드
  구현**(C1~C4). 8MB 초과 파일은 조각(기본 8MB)으로 나뉘어 올라가고,
  서버는 조각을 **스트림 그대로** 디스크에 흘린다 — 서버 메모리가 파일
  크기와 무관해져 상한이 **1GB** 로 열렸다. 조각 PUT 은 **멱등**이라
  재시도가 안전하고, 쿼터는 **시작 시점에 예약**한다(다 올린 뒤 거절하지
  않는다). 사용자에게는 경로를 나누지 않았다 — 같은 드롭·같은 메뉴로
  올리고 큰 파일에만 진행률·[취소]가 붙는다. **두 테스트 모두 API 를
  `ATTACHMENT_PART_KB=64` 로 띄워야** 다중 조각 경로를 실제로 지난다
  (1GB 파일을 만들지 않기 위한 장치).
- 2026-08-06 (20차): e2e116 추가 — **엑셀 표가 "그림"으로 들어가던
  원인 확정**(사용자 MD 내보내기가 결정적 단서). 표가 있으면 그림보다
  표를 우선한다. 복구 배너 세션 1회 가드, 첨부 오류를 그 오류가 난
  섹션에 표시, 첨부 1개 상한 20MB → 200MB(1GB 는 스트리밍 업로드 필요).
- 2026-08-06 (19차): e2e115 추가 — 확인 게이트를 **"화면에 맵이 있으면
  묻는다"** 하나로 단순화(읽기 전용·로컬 파일까지 포함), 복구 배너가
  **이번 세션에 쓴 초안**을 되묻지 않게, 표 변환에 텍스트가 섞이지 않도록
  가드. 사양 변경에 맞춰 e2e99·e2e103 이 확인 단계를 통과하도록 갱신.
  **엑셀 표 중복 보고는 재현하지 못했다** — 실제 클립보드 HTML 필요.
- 2026-08-06 (18차): e2e114 추가 — PR#209 후속 2건. **새 문서를 만드는
  `loadMap` 은 전부 `resetHistory`**(템플릿 새 맵·AI 새 맵 2종)로 통일했고,
  새 맵 확인 게이트를 "편집했는가"에서 **"닫을 맵이 열려 있는가"** 로
  바꿨다. e2e106 [2] 의 정적 검사도 문자열 일치 → **호출 순서 + 정규식**
  으로 견고하게 고쳤다(주석 한 줄에 거짓 실패했다).
- 2026-08-06 (17차): e2e113 추가 — **되돌리기가 문서 경계를 넘던 버그**.
  `closeMap`/`newMap` 이 past/future 를 안 비웠고, 비우더라도 zustand
  구독이 전환 자체를 도로 쌓았다. `asDocumentSwap` 한 곳으로 모아 해결.
  맵 닫기는 이제 Ctrl+Z 로 되살아나지 않는다(그 편의가 증상의 통로였다).
- 2026-08-06 (16차): e2e112 추가 — **"사용자가 인식 못하는 유실"의 마지막
  사각지대**. 초안 쓰기 실패(쿼터 초과)를 조용히 삼키고 있었다.
  `withStore` 가 성공/실패를 구분하고(`onabort` 포함) `putDraft` 가
  반환하며, 실패는 화면에 알린다(R8 신설).
- 2026-08-06 (15차): e2e111 추가 — **"편집"의 경계**를 확정했다.
  접기/펴기(`collapsed`)는 되돌리기·미저장 편집 카운트·로컬 초안에서
  모두 제외(`documentStore.asViewOnly`). 좌우(`side`)·스타일·아이콘·
  태그·정렬·크기는 **내용 쪽**이라 포함. 기준: "다시 만들어야 하는 것"은
  포함, "다시 보면 되는 것"은 제외.
- 2026-08-06 (14차): e2e110 추가 — **저장 모델 개편**. 실시간 저장
  (디바운스 1.5초 + maxWait 5초)을 없애고 주기(3·5·10분) + 미저장 편집
  50개 + 안전 3시점으로 바꿨다. 히스토리는 명시 저장에서만. 배지는
  "미저장 편집 N개". 되돌리기 배지 클릭 = 최신 확정.
  **낡은 기대값 정리**: e2e102 [5]·e2e107 [3] 은 편집 직후 `saved` 를
  기대했는데 새 모델에서는 `dirty` 가 정상이라 "탭 숨김 → saved" 로
  바꿨고, e2e108 [4](maxWait)·[5](버전 간격)는 e2e110 으로 이관했다.
- 2026-08-06 (13차): e2e109 추가 — 남은 유실 위험을 **끝까지** 막았다.
  구조 변경·화면 이탈 시 **즉시** 초안(마지막 1초 제거) · 백그라운드
  진입 시 자동저장 즉시 발사(R6) · 오프라인 무기한 재시도 + `online`
  즉시 재개(R2) · 편집권 상실 시 연결 해제 + 초안 보관 + 새 이름 저장
  유도(R3) · 초안 여러 건 순차 처리 · 복구 후 재보관 · IndexedDB 사용
  불가 안내(R7 신설).
- 2026-08-05 (12차): e2e108 추가 — 로컬 초안 백업(IndexedDB 1초) ·
  자동저장 **최대 대기 한도 5초**(디바운스가 계속 리셋돼 무한정 미뤄지던
  것을 새로 발견) · 자동저장 주기 버전(개인 설정 3·5·10분).
- 2026-08-05 (11차): e2e107 추가 — "저장 안 됨" 정직 표시. 같은 문서에
  **남은 유실 위험 목록(14-save.md §7.6)** 을 함께 적었다 — 로컬 초안
  백업(R1·R2)이 없는 한 "100% 안전"이라고 말하지 않는다.
- 2026-08-05 (10차): e2e106 추가 — **저장 경로 전수 감사**에서 나온 두
  번째 유실(엉뚱한 맵에 다른 문서가 저장됨)의 재현·구조적 차단, 그리고
  접기 아이콘을 연결선 시작점으로 옮긴 검증. 같은 변경으로 **e2e45
  [A][C] 의 기대값을 갱신**했다 — 토글이 '아래 변 한가운데'에서 '아래 변
  **왼쪽 스파인**'으로 바뀌었다(사양 변경). e2e46 [A] 는 접힘 +N 칩이
  노드를 파고드는 것을 잡아냈다 → 밀어내는 거리를 **칩 반지름+1** 로.
- 2026-08-05 (9차): e2e105 추가 — **세 번째 유실**(맵이 통째로 비워짐)의
  재현·3중 방어. 같은 날 ThinkWise 표시 제거를 e2e104 [4b] 로 확장했다
  (표시만 온 경우 = 그림 복사 → 안내 / 표시 + 내용 = 표시만 지우고 붙기).
  `guard5.mjs` ② 의 기대값도 바뀌었다 — 확인 없는 명시 저장의 빈 문서는
  이제 **거부**되고, allowEmpty 를 실은 ②b 만 반영된다.
- 2026-08-05 (8차): e2e104 추가 — 패널 ✕ 닫기·빈 텍스트 저장·다른 탭
  노드 붙여넣기·ThinkWise 자리표시 안내. **고정물 주의**: 기본 정렬이
  이름순이 되면서 문서함 첫 행이 `통계A-작음`(= e2e96 [1] 의 "구저장분
  통계 NULL" 고정물)이 됐고, e2e102 [5] 가 그 맵을 편집·자동저장해
  통계를 채워 버렸다 → e2e102 [5] 는 **`통계C-큼`을 열도록** 바꾸고
  통계A 는 `reseedA.mjs` + SQL(NULL 통계·원래 타임스탬프)로 복원했다.
- 2026-08-05 (7차): e2e103 추가 — 목록 툴팁 제거·기본 정렬(이름 ▲)·
  템플릿 선택 단계 ✕ 취소. **새 맵은 템플릿을 고른 뒤에 만들어진다**로
  흐름이 바뀌면서 e2e85 [2] 의 문구 기대값을 '새 맵을 시작했습니다' →
  '새 맵을 시작합니다'(시제) 로 갱신했다. 참고: e2e62 는 뒷부분이
  **2026-08-02 부터 이미 낡아** 있다(없어진 '새 맵 제목' 입력창을
  기다린다) — 앞 4항목(새 맵 메뉴 구조)만 유효하다.
- 2026-08-05 (6차): e2e102 추가 — 목록 행 호버 강조(`.mm-list-row`)와
  저장 배지 문구, 그리고 **되돌리기 중 "저장 실패"의 진짜 원인**
  (자동저장이 편집 세션 키를 싣지 않아 자기 잠금에 막혀 409) 회귀 가드.
  드롭존 테두리를 3px → **2px** 로 얇게 하면서 e2e101 [2] 의
  stroke-width 기대값도 갱신했다.
- 2026-08-05 (5차): e2e101 추가 — 드롭존을 "축"이 아니라 "영역"으로
  판정하도록 바꾼 검증. 같은 변경으로 **e2e95 [1] 의 드롭 지점을 노드
  안쪽(w*0.9) → 바깥 마진(+15px)** 으로 옮겼다(안쪽은 이제 하위다).
  인디케이터가 <g pointer-events="none"> 로 감싸이면서 rect 자체에는
  그 속성이 없어 바 셀렉터도 rx 만 보도록 갱신했다.
- 2026-08-05 (4차): e2e100 추가 — 불러오기 실패 안내(일반 HTML·구조 없는
  MD). 첨부 있는 맵의 내보내기 포장은 zipcheck.cjs 로 실측 확인했다 —
  MD·HTML **둘 다 ZIP**(맵 파일 + files/)으로 나간다.
- 2026-08-05 (3차): e2e99 추가 — 잘라내기·템플릿 3레벨 레이아웃·자동저장
  가드. e2e97 [3] 의 전용 GPT 기대값을 '?q= 주제만' → '파라미터 없음
  (클립보드 경로)'으로 갱신했다 — 전용 GPT 페이지가 ?q= 를 무시하는 것을
  실사용에서 확인했다. 함정: 이 회귀 세트는 같은 DB 를 쓰므로 검증용
  임시 맵(가드테스트 등)을 남기면 e2e96 [2] 정렬 기대값이 흔들린다.
- 2026-08-05 (2차): e2e98 추가 — 코드 블록 격자 배치(폰트 무관 정렬) +
  도식 평문화 버그 수정 + 템플릿 v4.2(AI 제한 해제). e2e95 [3] 의
  템플릿 기대값을 'ASCII 금지' → '도식 허용'으로 갱신했다.
- 2026-08-05: e2e97 추가 — 문서함 관리 아이콘·머리글 정렬, AI 바로가기
  일반/전용 GPT 분리 + ?q= 자동 입력, 붙여넣기 자동 실행 제거. 같은
  변경으로 **e2e91 [3]~[3d] 는 '새 맵 생성' 클릭 + 확인**으로,
  **e2e95 [2] 는 [2a](미선택 삽입 안내)/[2b](선택 후 팝오버 삽입)** 로
  갈라 갱신했다 — 적용 대상 토글(`data-webai-target-opt`)과 자동 실행
  체크(`data-webai-autogen`)는 코드에서 사라졌다.
- 2026-08-04 (6차): e2e96 추가 — 문서함 상세 컬럼·정렬, 자동저장 유실
  가드, 웹AI 확인 팝오버. e2e91·e2e95 의 확인 흐름을 native dialog →
  `[data-webai-confirm-ok]` 클릭으로 갱신 (회귀 e2e91·e2e94·e2e95 통과).
- 2026-08-04 (5차): e2e95 추가 — 진행트리 드롭존 방향 인지·웹AI 적용
  대상 토글·템플릿 v4.1(ASCII 도식 금지). 회귀 e2e91·e2e94 통과.
- 2026-08-04 (4차): 문서 정리 — e2e79~e2e94 를 §2.11 "서버 저장·
  문서함·세션" 절로 분리 신설하고, §3 회귀 가이드에 "저장·히스토리"
  행을 추가했다 (테스트 내용 변경 없음).
- 2026-08-04 (3차): 기본 기능 5건으로 **e2e94 추가** — 골격 축소가 걸릴
  수 있는 회귀 e2e73(칸반 상호작용)·89·90·91·92·93 전부 green (기존
  테스트는 노드 수를 상대값으로 재서 골격 축소에 안 깨졌다).
- 2026-08-04 (2차): 단일 세션 편집 잠금으로 **e2e93 추가** + e2e92 [7]
  현행화(Guest 메뉴 = 로그아웃, 설정 항목 숨김). 회귀 e2e85/89/90 green.
- 2026-08-04: 로그인 UX 4건 + Guest 체험으로 **e2e92 추가** — 회귀
  e2e81/84/89/90/91 green (로그인 UI·세션 전환·새맵 패널을 건드리는
  변경이라 로그인 계열 전부 재실행).
- 2026-08-03 (3차): 웹 AI 클립보드 왕복(방법 A)으로 **e2e91 추가** —
  클립보드 실측(grantPermissions) + 합성 paste 이벤트 패턴 확립.
- 2026-08-03 (2차): dev 보고 2건 + 첨부 상세로 **e2e90 추가** — 템플릿
  선택 중 메뉴 숨김 · 무변경 저장/닫기 스킵(jsonb 등가 + 스냅샷 정규화) ·
  히스토리 첨부 개수·총 용량. 스냅샷 정규화(buildSnapshot)가 모든 저장
  경로에 걸리므로 e2e79~89 전 회귀 재실행 green.
- 2026-08-03: UI·히스토리 5건으로 **e2e89 추가**. 새 맵 패널 평탄화로
  기존 테스트의 `'+ 새 맵 만들기'` 내부 버튼 셀렉터를 menuHeader
  텍스트 포함 검색으로, 로그인(:5198) 흐름의 '서버 맵 불러오기' 클릭을
  `crumb-docs`(상단 '내 문서')로 일괄 갱신했다 (e2e82/84/85/86/87/88).
- 2026-08-03: 맵당 내장 첨부 합계 상한 10MB(초과분 서버 우회)와 단수
  첨부 클릭 토큰 통일로 e2e88 에 ⑥⑦ 추가.
- 2026-08-02: B9 첨부 저장소(방식 A)로 **e2e88 추가**. e2e87 ③④의 큰
  파일 시나리오는 로그인 안 한 :5199 로 이동 — 로그인 상태에서는 2MB
  초과도 서버 업로드로 유지되기 때문.
- 2026-08-02: 첨부 원본 생존성(≤2MB data URL 내장 + 내보내기 안내)으로
  **e2e87 추가**. 구식 스크립트(e2e32·e2e49b)는 `SP=$PWD` 환경변수를
  줘야 돌아간다 (미지정 시 file://상대경로 오류 — 회귀 아님).
- 2026-08-02: UI 4건 + 뷰어 파리티로 **e2e86 추가**. e2e85 ③의 레이아웃
  변경 경로를 't' 패널 → 레일 '레이아웃' 탭으로 갱신했다 (Tweaks 시안
  패널은 초기 개발 잔재 정리로 코드째 삭제 — App.tsx/EditorCanvas/
  InlineNodeEditor/LayoutPicker/FreeformStrategy/meta·kanban·outline
  샘플, documentStore.kanban 필드 포함).
- 2026-07-25: 최초 작성 — e2e7~e2e50 카탈로그화 (PR #1~#98 범위).
  이후 새 E2E 추가/변경 시 이 문서의 해당 영역 표와 회귀 세트를 갱신.
- 2026-08-02: dev 실사용 보고 4건 수정으로 **e2e85 추가**. e2e82 는 유형
  선택 제거(배지 표시 전용)로, e2e81 [12] 는 "편집 중 맵 클릭 = 편집
  화면 복귀"로 기대값을 갱신했다.
- 2026-08-02: 로그인 후 빈 화면·폴더 선택창으로 **e2e84 추가**.
- 2026-08-02: 스키마 미적용 진단(503 + 안내 배너)으로 **e2e83 추가**.
- 2026-08-02: 문서함(폴더·저장 규칙·문서 브라우저) 도입으로 **e2e82 추가**.
  같은 변경으로 e2e79·e2e80·e2e81·e2e-cloud2 를 문서함 셀렉터로 갱신했다 —
  첫 저장은 `save-map-dialog`(폴더·이름), 목록은 `map-browser`/
  `browser-map-open`, 미저장 맵 닫기는 `unsaved-warning` 이다.
- 2026-08-02: 로그인 UI 정리로 e2e81 추가. 같은 변경으로 **e2e79·e2e80·
  e2e-cloud2 의 셀렉터·기대값을 갱신**했다 — 저장은 `map-save`,
  닫기는 `map-close`, 열기는 좌측 '서버 맵 불러오기'(옛 `cloud-menu`/
  `cloud-save`/`cloud-open` 은 사라졌다).
