# 07. AI로 맵 생성

질문을 입력하면 AI가 답을 만들어 **그대로 마인드맵으로** 변환해 줍니다.
방법은 두 가지입니다 — 왼쪽 속성의 `AI` 탭 상단에서 전환합니다:

| 모드 | 필요한 것 | 어울리는 분 |
|---|---|---|
| **🌐 웹 AI (키 불필요)** | Claude·ChatGPT·Gemini 등 **웹 구독**(또는 무료 웹) | API 키 발급이 부담스러운 대부분의 사용자 — **기본 모드** |
| **🔑 API 키** | 각 AI 회사의 API 키 (별도 과금) | 앱 안에서 버튼 한 번으로 끝내고 싶은 분 |

> API 키를 등록하지 않았다면 **웹 AI가 기본**으로 열립니다(키를
> 등록하면 API 키 모드가 기본). **키를 등록하는 순간 화면이 API 키
> 모드로 바뀝니다** — 웹 AI 화면으로 돌아가려면 패널 위
> `🌐 웹 AI (키 불필요)` 를 누르세요(선택은 저장됩니다).

---

## 먼저 — 버튼이 두 개입니다 ★

AI 패널에는 성격이 다른 버튼이 **두 개** 있고, 이걸 헷갈리면 "엉뚱한
답이 나온다"고 느끼게 됩니다.

| | **① AI에게 물어보기** (맵 만들기) | **선택 노드 자세히 확장** |
|---|---|---|
| **입력창 질문** | **꼭 적어야 합니다** — 비면 버튼이 눌리지 않습니다 | **안 적어도 됩니다** (적으면 함께 보냅니다) |
| **무엇을 보고 답하나** | 내가 적은 질문 | **맵의 내용** — 중심 주제 + `@소스` + 상위 경로 + 고른 노드 |
| **노드 선택** | 필요 없음 | **꼭 골라야 합니다** |
| **결과가 어디로** | 새 맵 / 선택 노드 — **내가 고름** | **항상 고른 노드의 하위** |
| **Ctrl+Z 로 되돌리기** | 새 맵은 ✗ · 노드 삽입은 ✅ | ✅ |

**어떤 걸 쓰나:**

- **새로운 주제로 맵을 만들고 싶다** → ① AI에게 물어보기
- **지금 맵의 어떤 가지를 더 깊게 파고 싶다** → 선택 노드 자세히 확장
  (질문을 안 적어도 맵 흐름을 이어서 채웁니다)
- **같은 가지를 특정 각도로 받고 싶다** → 노드를 고르고 **입력창에 그
  관점을 적은 뒤** 확장 (예: "보안 취약점 위주로")
- **맵과 아무 상관 없는 주제** → ① 물어보기 → **② 새 맵 생성**
  (남의 노드 밑에 붙이면 자리가 이상해집니다)

---

## 🌐 웹 AI 모드 — API 키 없이 (기본)

쓰고 있는 AI 웹 채팅(Claude, ChatGPT, Gemini, 무엇이든)으로 맵을
만듭니다. **복사 2번**이면 됩니다:

1. `AI` 탭 → 🌐 웹 AI → **① 무엇을 만들까요?** 에 주제 입력
   (예: "신제품 출시 계획을 마인드맵으로 정리해줘")
2. **`📋 ① 프롬프트 복사`** — 맵 변환 규칙(EMM 템플릿)과 주제가 함께
   클립보드에 담깁니다. 아래 [Claude] [ChatGPT] [Gemini] 버튼을 누르면
   AI 창이 새 탭으로 열리면서 **질문까지 자동 입력**됩니다(주제를 먼저
   적어 두세요). Gemini만 자동 입력을 지원하지 않아 Ctrl+V로 붙여넣으면
   됩니다 — 어느 쪽이든 클립보드에도 복사해 둡니다.
3. AI 채팅창에 **붙여넣고(Ctrl+V) 실행** → 답변이 코드블록 하나로
   나옵니다 → 블록 우상단 **복사 버튼** 클릭.
4. EasyMindMap의 **③ 답변 붙여넣기** 칸에 Ctrl+V — 붙여넣기만으로는
   **아무 것도 바뀌지 않습니다.** "'제목' · N노드 인식됨" 안내가 뜨면,
   아래에서 반영할 방법을 **직접 고르세요**:
   - **`③ 새 맵 생성`** — 답변을 새 맵으로 엽니다.
   - **`선택 노드에 삽입`** — 맵에서 고른 노드의 하위로 추가합니다.
     (노드를 고르지 않고 누르면 먼저 노드를 선택하라고 알려 줍니다.)

   어느 쪽을 누르든 **패널 오른쪽 옆(화면 세로 중앙)에 확인 창**이
   떠서 [확인]을 눌러야 진행됩니다. 되돌리려면 Ctrl+Z.

- 답변을 복사할 때는 **답변 끝의 ⧉ 복사 버튼**을 쓰세요 — 화면을 드래그해 복사하면 코드·도식 블록이 깨집니다. 답변은 **`~~~`(물결 3개) 코드블록** 하나로 오도록 요청됩니다 —
  백틱(```)으로 나오면 `⧉ 재요청 문구 복사`를 쓰세요.
- 답변이 코드블록 없이 나오거나 형식이 어긋나면 빨간 안내와 함께
  **`⧉ 재요청 문구 복사`** 버튼이 나타납니다 — AI 창에 붙여넣으면
  올바른 형식으로 다시 출력해 줍니다.
- 입력한 내용과 답변은 **EasyMindMap 서버로 전송되지 않습니다.**
- Guest 체험 중에는 이 **웹 AI 모드만** 열립니다(API 키 등록·호출은
  가입 후).

### 더 편하게 ① — ChatGPT 사용자: 'EasyMindMap ChatGPT 앱'

바로가기 줄 아래의 **`⚡ EasyMindMap ChatGPT 앱`** 버튼은 [ChatGPT]
(일반 채팅)와 **다른 것**입니다 — 우리가 공개한 전용 GPT로, 맵 변환
규칙이 GPT 안에 이미 들어 있습니다. 그래서 **① 프롬프트 없이 주제만**
자동 입력되고, 그대로 코드블록 답변이 나옵니다 (ChatGPT 로그인 필요).
(답변 복사 → ③칸 붙여넣기만 하면 끝 — 복사 1번)

### 더 편하게 ② — Claude 사용자: '프로젝트'에 지침 넣기

Claude에는 커스텀 GPT 대신 **프로젝트(Projects)** 가 있습니다.
프로젝트마다 **지침(Instructions)을 미리 심어둘 수 있어서**, 그 안에서
대화하면 Claude가 매번 지침을 자동으로 읽고 답합니다 — 맵 변환 규칙을
한 번만 넣어두면 이후로는 **주제만 입력**하면 됩니다.

**만들기 (한 번만, 약 2분 — Claude Pro 권장):**

1. claude.ai → 왼쪽 메뉴 **프로젝트(Projects)** → **+ 새 프로젝트**
2. 이름: `EasyMindMap Copilot` → 만들기
3. 프로젝트 화면의 **지침 설정(Set project instructions)** 클릭
4. 개발 문서 `docs/04-extensions/ai/easymindmap-copilot-gpt.md` §2의
   **'지침' 블록 전문**을 붙여넣고 저장

**사용하기 (매번):**

1. **그 프로젝트 안에서** 새 대화 시작 — 프로젝트 밖 일반 대화에는
   지침이 적용되지 않습니다 (가장 자주 헷갈리는 부분!)
2. 주제만 입력 → 코드블록 답변 → 복사 → EasyMindMap ③칸에 붙여넣기

**Claude 프로젝트만의 활용법:**

- **프로젝트 지식(Knowledge)** 에 PDF·문서를 올려두면 그 내용을
  참고해 맵을 만듭니다 — 예: 사업계획서를 올리고 "이 문서 기반으로
  출시 로드맵 마인드맵 만들어줘".
- 맵 생성 대화가 프로젝트 폴더에 모여서 "지난번 그 맵 이어서
  확장해줘"처럼 이어가기 좋습니다.
- "3장을 더 자세히 늘려줘" 같은 후속 요청에도 수정된 전체가 다시
  코드블록으로 나옵니다 — 복사해 다시 붙여넣으면 됩니다.

**세 가지 방법 비교:**

| | 일반 AI + 프롬프트 복사 | EasyMindMap ChatGPT 앱 | Claude 프로젝트 |
|---|---|---|---|
| 매번 할 일 | 복사 2번 | **복사 1번** | **복사 1번** |
| 쓸 수 있는 AI | 전부 | ChatGPT 전용 | Claude 전용 |
| 사전 준비 | 없음 | 없음 (공개 GPT) | 내 계정에 1회 생성 |
| 규칙 갱신 시 | 자동 (앱이 최신 복사) | 자동 (우리가 교체) | 지침 재붙여넣기 |

### 웹 AI로 선택 노드 확장

1. 맵에서 노드 클릭 → 웹 AI 패널 아래 **`📋 확장 프롬프트 복사`**
2. AI 창에 붙여넣고 실행 → 답변 복사
3. **③칸에 붙여넣고** → **`선택 노드에 삽입`** — 그 노드의 하위로
   채워집니다 (`Ctrl+Z`로 되돌리기 가능)

> **①칸(주제)에 적어 둔 요청도 확장 프롬프트에 함께 담깁니다**
> (2026-08-06 — API 키 모드와 같은 규칙). 맵 문맥만으로 확장하려면
> ①칸을 비우고 복사하세요.

## 🔑 API 키 모드

앱 안에서 버튼 한 번으로 생성까지 끝나는 방식입니다.

### 준비 — API 키 등록 (처음 한 번)

> **중요**: AI 사용료는 각 회사의 **API 요금**으로 별도 과금됩니다.
> ChatGPT Plus·Claude Pro·Google AI Pro 같은 **구독 요금제와는 별개**입니다
> (Gemini만 결제 없이 쓸 수 있는 무료 사용량 제공). 구독만 있다면
> 위의 **웹 AI 모드**를 쓰세요.

1. `AI` 탭 → **`AI 설정`**.
2. 사용할 회사의 칸에 API 키를 붙여넣습니다.
3. 각 회사 칸의 **"키 발급 방법 (처음이신가요?)"** 을 펼치면 발급
   페이지 주소와 단계별 안내가 나옵니다.

`TODO(스크린샷): AI 설정 — 키 입력 + 도움말`

#### 모델·우선순위 설정

- **모델**: 회사별로 기본 모델이 정해져 있고, 목록에서 바꾸거나 직접
  입력할 수 있습니다.
  - Gemini는 구글이 모델을 자주 바꾸므로, 오류가 나면 **"↻ 지금 키로
    사용 가능한 모델 불러오기"** 를 눌러 현재 쓸 수 있는 모델을 고릅니다.
- **우선순위**: 키를 여러 개 등록했을 때 어떤 AI를 먼저 쓸지 순서를
  `▲▼`로 정합니다. 생성 화면의 **'자동'** 은 이 순서대로 씁니다.

### 맵 만들기 (2026-08-06 — 웹 AI와 같은 흐름으로 통일)

1. `AI` 탭 → 🔑 API 키 모드.
2. 질문을 입력합니다 (예: "우분투 22.04에 워드프레스 설치 방법을 초보자
   수준으로 알려줘").
3. **`① AI에게 물어보기`** → 잠시 후 답변이 옵니다.
   **이 시점에는 맵이 아직 바뀌지 않습니다.**

   ```
   ✨ 답변을 받았습니다 — 152개 노드. 아래에서 어디에 넣을지 골라 주세요.
   [② 새 맵 생성]   [선택 노드에 삽입]
   이 답변 버리기
   ```

4. 두 버튼 중 하나를 **직접 고릅니다** — 어느 쪽도 미리 선택돼 있지
   않습니다.
   - **`② 새 맵 생성`** — 답변을 새 맵으로 엽니다.
   - **`선택 노드에 삽입`** — 맵에서 고른 노드의 **하위로** 추가합니다.
     (노드를 고르지 않고 누르면 *"먼저 맵에서 삽입할 노드를 클릭해
     선택하세요"* 라고 알려 줍니다 — 버튼이 사라지지는 않습니다.)
5. 어느 쪽을 누르든 **패널 오른쪽 옆(화면 세로 중앙)에 확인 창**이 떠서
   [확인]을 눌러야 반영됩니다.

> **⚠️ `새 맵 생성`은 되돌리기(Ctrl+Z)로 돌아올 수 없습니다.** 새 문서로
> 열리기 때문입니다. 지금 맵에 저장하지 않은 편집이 있으면 **먼저 ☁
> 저장**하세요. 확인 창도 그렇게 안내합니다.
> (`선택 노드에 삽입`은 지금 맵을 고치는 것이라 Ctrl+Z로 되돌아갑니다.)

`TODO(스크린샷): AI 생성 결과 — 두 버튼과 확인 창`

> AI는 웹 채팅과 같은 상세함으로 답하도록 설정되어 있습니다. 답이 너무
> 짧으면 설정에서 더 큰 모델을 고르거나, 프롬프트 템플릿을 확인하세요.

> **API 키를 아직 등록하지 않았다면** 버튼이 `🔑 API 키를 등록하세요 —
> 눌러서 AI 설정으로` 로 보입니다. **눌러 보세요** — `AI 설정` 탭으로
> 데려갑니다. (키가 없으면 위의 **🌐 웹 AI** 를 쓰시면 됩니다.)

### 선택 노드 자세히 확장

이미 만든 맵(AI 생성이든 직접 만든 것이든)에서 **특정 노드를 더 깊게**
파고들 때 씁니다.

1. 맵에서 확장할 **노드를 클릭**합니다.
2. `AI` 탭 아래 **"선택 노드 자세히 확장"** 에 대상이 표시됩니다.
3. **`선택 노드 자세히 확장`** 버튼을 누릅니다.
4. 세부 내용이 **그 노드의 하위 노드로** 채워집니다 (확인 후 추가,
   `Ctrl+Z`로 되돌리기 가능).

#### 무엇을 보고 답을 만드나 — **질문을 안 적어도 됩니다**

확장은 **맵의 내용 자체를 질문으로 씁니다.** 그래서 위 입력창을 비워
두어도 동작합니다 (생성 버튼과 다른 점입니다).

AI에게 보내는 것:

| 무엇 | 어디서 오나 |
|---|---|
| **프로젝트 지침** | 중심 주제(루트 노드)의 내용 |
| **프로젝트 소스** | 이름에 **`@소스`** 가 들어간 2레벨 노드의 서브트리 |
| **상위 경로** | 루트 → … → 고른 노드까지의 조상 노드들 |
| **확장할 노드** | 고른 노드의 내용 |

즉 *"이 프로젝트에서, 이 경로 아래에 있는 이 노드를 더 자세히 풀어 줘"*
라고 묻는 것입니다.

#### 입력창에 적으면 **그 요청도 함께** 갑니다 (2026-08-06)

입력창이 비어 있지 않으면 그 문장이 `[요청]` 으로 함께 전달되고,
**맵 문맥과 어긋나면 요청을 따르라**고 AI에게 지시합니다.

```
[상위 경로]
↳ Q1 · 기반 구축

[확장할 노드]
인증 시스템 (Supabase Auth)

[요청]
보안 취약점 위주로 정리해줘

(위 요청이 맵 문맥과 다르면 요청을 따른다.)
```

> 예전에는 이 값이 **전달되지 않았습니다** — 입력창에 적어도 맵 문맥
> 대로만 나와서 "엉뚱한 답"으로 보였습니다. 지금은 함께 갑니다.
>
> 다만 **"요청이 이긴다"는 보장이 아니라 지시**입니다. 최종 판단은 AI가
> 합니다. 주제가 완전히 다르다면 확장이 아니라 **[① AI에게 물어보기] →
> [② 새 맵 생성]** 이 맞습니다(엉뚱한 주제를 남의 노드 밑에 붙이면
> 자리가 이상해집니다).

`TODO(스크린샷): 선택 노드 확장 — 하위에 채워진 세부 노드`

### 최근 기록 (API 키 모드)

최근 생성·확장 기록 10건이 남습니다. 클릭하면 그 질문을 다시 넣습니다.

### 도식(상자 그림)도 그대로 들어옵니다 (2026-08-05)

AI가 코드블록 안에 상자·화살표로 그린 계통도·흐름도를 넣어 주면
**그대로 코드 블록으로 표시**됩니다. 한글이 섞여도 칸이 어긋나지
않도록 EasyMindMap이 글자를 격자에 맞춰 그리기 때문에, 도식이 깨질
걱정 없이 AI에게 마음껏 그려 달라고 하셔도 됩니다.

> 예전에는 "도식을 그리지 말라"는 지침이 AI 프롬프트에 들어 있었지만,
> 그러면 정말 도식이 필요한 자리에도 나오지 않아 **지침을 없앴습니다.**

### 결과는 **내가 고른 뒤에만** 반영됩니다 (두 모드 공통)

예전에는 붙여넣는 순간 미리 골라진 대상으로 바로 반영됐지만, "고른
적이 없는데 반영됐다"는 의견이 많아 **자동 실행과 대상 미리 선택을
없앴습니다**(2026-08-05, 웹 AI 모드).

**2026-08-06 부터 API 키 모드도 같은 규칙입니다.** 예전에는 API 키
모드만 호출이 끝나면 곧바로 새 맵으로 열려서, "이 답을 노드에 붙일까?"를
고를 기회가 없었습니다.

| | 두 모드 공통 규칙 |
|---|---|
| 답이 오면 | **아무것도 바뀌지 않고** 인식 결과만 보여 준다 |
| 반영하려면 | `새 맵 생성` 또는 `선택 노드에 삽입` 중 **직접 고른다** |
| 미리 선택 | **없다** — 어느 쪽도 기본값이 아니다 |
| 마지막 관문 | **확인 창에서 [확인]** (패널 오른쪽·화면 세로 중앙) |
| 노드 미선택 | 버튼은 그대로 보이고, 누르면 **먼저 고르라고 알려 준다** |
